# Git 계정 규칙

이 프로젝트는 **terajh** 계정으로 작업한다. kakao-carter-p(회사 계정)로 커밋하지 말 것.

## 로컬 Git Config (설정 완료)

```
user.name  = terajh
user.email = terajoohyun@ajou.ac.kr
```

## 주의사항

- `git config user.name` / `user.email`을 변경하지 말 것
- 커밋 시 author가 `terajh <terajoohyun@ajou.ac.kr>`인지 확인
- global config(`carter.p@kakaocorp.com`)가 적용되지 않도록 local config가 우선함
