<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>dev RSS - Level Up Coding</title>
    <link>https://levelup.gitconnected.com/</link>
    <description>Level Up Coding RSS 피드</description>
    <lastBuildDate>Wed, 17 Jun 2026 18:40:42 GMT</lastBuildDate>
    <item>
      <title>10 Ways to Optimize LangChain Apps Every Developer Should Know</title>
      <link>https://levelup.gitconnected.com/10-ways-to-optimize-langchain-apps-every-developer-should-know-359b59e3ab35?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/10-ways-to-optimize-langchain-apps-every-developer-should-know-359b59e3ab35?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Wed, 17 Jun 2026 16:22:58 GMT</pubDate>
      <content:encoded>&lt;div class=&quot;medium-feed-item&quot;&gt;&lt;p class=&quot;medium-feed-image&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/10-ways-to-optimize-langchain-apps-every-developer-should-know-359b59e3ab35?source=rss----5517fd7b58a6---4&quot;&gt;&lt;img src=&quot;https://cdn-images-1.medium.com/max/2080/1*RLpU51v6lGKXB2pTbL2-2w.png&quot; width=&quot;2080&quot;&gt;&lt;/a&gt;&lt;/p&gt;&lt;p class=&quot;medium-feed-snippet&quot;&gt;In this article, we&amp;#x2019;ll walk through 10 practical ways to optimize LangChain-based applications, so they run faster, scale better, and&amp;#x2026;&lt;/p&gt;&lt;p class=&quot;medium-feed-link&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/10-ways-to-optimize-langchain-apps-every-developer-should-know-359b59e3ab35?source=rss----5517fd7b58a6---4&quot;&gt;Continue reading on Level Up Coding »&lt;/a&gt;&lt;/p&gt;&lt;/div&gt;</content:encoded>
    </item>
    <item>
      <title>I’ve Rebuilt the Same Rule Engine in Four Languages. It Broke the Same Way Every Time</title>
      <link>https://levelup.gitconnected.com/ive-rebuilt-the-same-rule-engine-in-four-languages-it-broke-the-same-way-every-time-f16644becd58?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/ive-rebuilt-the-same-rule-engine-in-four-languages-it-broke-the-same-way-every-time-f16644becd58?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Wed, 17 Jun 2026 16:22:42 GMT</pubDate>
      <content:encoded>&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/0*NyoI_xaGXnxZc3Vx&quot; /&gt;&lt;figcaption&gt;Photo by &lt;a href=&quot;https://unsplash.com/@afgprogrammer?utm_source=medium&amp;amp;utm_medium=referral&quot;&gt;Mohammad Rahmani&lt;/a&gt; on &lt;a href=&quot;https://unsplash.com?utm_source=medium&amp;amp;utm_medium=referral&quot;&gt;Unsplash&lt;/a&gt;&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;Three years ago, a discount rule that had been quietly running in production for almost two years started giving every customer free shipping. Nobody touched the discount logic. Someone updated an unrelated currency formatting function, and that was apparently enough.&lt;/p&gt;&lt;p&gt;It took us six hours to find it, because the rule wasn’t sitting anywhere obvious. It was buried four files deep, inside a function that had grown over time, written by three engineers who didn’t work there anymore. That’s usually how it goes.&lt;/p&gt;&lt;p&gt;I’ve seen this exact shape of failure more times than I’d like to admit, across companies, across stacks. After rebuilding rule-driven logic in Java, Python, Node, and a fair amount of raw SQL, I stopped blaming the language at some point. The language was never really the problem.&lt;/p&gt;&lt;p&gt;A &lt;a href=&quot;https://www.nected.ai/blog/top-10-business-rules-engine&quot;&gt;rule engine&lt;/a&gt;, at its core, is just a system that lets you define “if this, then that” logic somewhere outside your main application code, so business rules can change without a deploy. Pricing tiers, fraud checks, loan approvals, eligibility rules — anything that changes more often than your codebase should, technically, live in one. In practice, it rarely does. Most of it ends up scattered across if-else blocks that nobody really wants to touch.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;The Java version&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;My first real exposure to this was a Java-based insurance platform. Eligibility rules — age limits, region restrictions, exclusions — lived inside a service class that had ballooned past 2,000 lines. Every new exclusion meant a new conditional. Every new conditional meant a new deployment, a new round of QA, a new chance to break something three teams away from the one actually making the change.&lt;/p&gt;&lt;p&gt;We eventually moved some of it into a &lt;a href=&quot;https://www.nected.ai/blog/java-rule-engines&quot;&gt;Java rule engine&lt;/a&gt; setup, the kind a lot of teams reach for once they hit this wall. It helped, for a while. Decisions got faster to change. But the engine itself slowly became its own maintenance burden — XML configs nobody fully understood anymore, rule files only the original author could safely edit. We’d traded one bottleneck for a smaller, quieter one.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Then Python, then Node&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;A few years later, different company, same shape of problem, different stack. This time it was a fraud-scoring system built with a &lt;a href=&quot;https://www.nected.ai/blog/python-rule-engines-automate-and-enforce-with-python&quot;&gt;Python rule engine&lt;/a&gt; running on a pile of nested conditionals that had started simple and stopped being simple about a year in. Someone suggested we keep adding rules as functions and chain them. We did. For a while it worked fine, mostly because the team was small and everyone knew where the bodies were buried.&lt;/p&gt;&lt;p&gt;Then two people left within a month of each other, and whoever inherited it had no idea why a transaction over a certain amount from a certain region got auto-flagged. The logic worked. Nobody could explain why anymore. That’s actually a worse position than something being broken outright — broken things get fixed, things nobody understands just get left alone, quietly, until they cause a real incident.&lt;/p&gt;&lt;p&gt;The &lt;a href=&quot;https://www.nected.ai/blog/rule-engine-in-node-js-javascript&quot;&gt;Node.js&lt;/a&gt; version of this story is almost identical, just with promises instead of nested ifs. We were building rule-based routing for support tickets — priority, team assignment, SLA tiers — directly inside an Express middleware layer. Fast to build. Fast to break, too. A teammate touched the SLA logic to add a new tier and accidentally changed how an unrelated routing rule evaluated, because both shared a helper function nobody remembered was shared.&lt;/p&gt;&lt;p&gt;And then there’s the SQL stretch, which I think gets overlooked the most. At one company, half our “business rules” lived as CASE WHEN chains inside stored procedures — a &lt;a href=&quot;https://www.nected.ai/blog/sql-rule-engine&quot;&gt;SQL rule engine&lt;/a&gt; by accident more than design. Honestly not a bad approach for certain things. Fast, transparent if you know SQL, easy to audit with a query. But nobody outside the data team could read it, and the moment a rule needed conditional logic spanning multiple tables, the query started looking like abstract art.&lt;/p&gt;&lt;p&gt;I haven’t personally shipped a production &lt;a href=&quot;https://www.nected.ai/blog/c-sharp-business-rule-engine&quot;&gt;C# rule engine&lt;/a&gt;, but I reviewed enough of them at a former client’s request to notice the same pattern playing out there too. Teams reach for a structured rules engine because raw conditionals in the codebase got unmanageable. Six months later, the rules engine itself becomes the unmanageable part.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;The actual pattern, if there is one&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;Here’s what all four of these had in common, once I stopped looking at the language and started looking at the decision sitting underneath it: every single one treated rules as code. Not data. Code that needs a developer, a deploy pipeline, and a code review just to change a number or a threshold.&lt;/p&gt;&lt;p&gt;That’s the real reason rule engines fail in production, more than scaling issues or bad architecture, honestly. The failures I’ve actually run into weren’t about throughput or load. They were about ownership. A rule that only an engineer can touch is a rule that takes two days to update when the business needed it changed an hour ago.&lt;/p&gt;&lt;p&gt;Every &lt;a href=&quot;https://www.nected.ai/blog/rules-engine-design-pattern&quot;&gt;rule engine design pattern&lt;/a&gt; I’ve come across — chain of responsibility, decision tables, rule trees, whatever name people give it — eventually circles back to that same question. Is the rule data, or is it code dressed up to look flexible?&lt;/p&gt;&lt;p&gt;There’s a smaller, separate mistake buried in a couple of these stories too — picking the wrong chaining direction for the problem. The fraud system would have been a much better fit for &lt;a href=&quot;https://www.nected.ai/blog/forward-chaining-vs-backward-chaining&quot;&gt;backward chaining&lt;/a&gt;, starting from “is this fraudulent” and working backward through which conditions support that conclusion, instead of forward chaining through every rule on every transaction regardless of relevance. We didn’t think about this distinction at the time. We just wrote rules in whatever order they occurred to us, which is how most rule logic actually gets written, if I’m being honest.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;What eventually worked, for us&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;The fix that stuck, across more than one of these situations, was pulling rules out of the codebase entirely and into something structured but not tied to any particular language. A &lt;a href=&quot;https://www.nected.ai/blog/json-rules-engine&quot;&gt;JSON rule engine&lt;/a&gt; setup, basically — a rule expressed as readable, structured data instead of a function buried somewhere. Doesn’t matter if the application underneath is Java, Python, or Node; the rule sits outside all of it, and someone who isn’t a backend engineer can open it and actually understand what it’s doing.&lt;/p&gt;&lt;p&gt;Written down like that, it sounds obvious. It wasn’t, while we were three sprints deep into a Python conditional chain that “worked fine, mostly.”&lt;/p&gt;&lt;p&gt;I ended up spending a chunk of the last couple of years around exactly this problem at Nected, working with teams trying to pull rule logic out of hardcoded conditionals and into something a non-engineer can own and version without filing a ticket every time a threshold changes. It’s the same fix, every time, regardless of which language the rules used to live in. The interesting part was never really the engine. It’s that almost nobody decides to fix this until something breaks badly enough to make the fix worth the effort.&lt;/p&gt;&lt;p&gt;If there’s one thing I’d tell anyone starting a rule engine from scratch, it’s this — decide on day one whether a rule is data or code. Everything else, which language, which design pattern, forward or backward chaining, matters a lot less than getting that one decision right early. Undoing it later is expensive in a way that’s hard to see coming, right up until you’re the one explaining a free-shipping bug to your CTO at 11pm.&lt;/p&gt;&lt;img src=&quot;https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=f16644becd58&quot; width=&quot;1&quot; height=&quot;1&quot; alt=&quot;&quot;&gt;&lt;hr&gt;&lt;p&gt;&lt;a href=&quot;https://levelup.gitconnected.com/ive-rebuilt-the-same-rule-engine-in-four-languages-it-broke-the-same-way-every-time-f16644becd58&quot;&gt;I’ve Rebuilt the Same Rule Engine in Four Languages. It Broke the Same Way Every Time&lt;/a&gt; was originally published in &lt;a href=&quot;https://levelup.gitconnected.com&quot;&gt;Level Up Coding&lt;/a&gt; on Medium, where people are continuing the conversation by highlighting and responding to this story.&lt;/p&gt;</content:encoded>
    </item>
    <item>
      <title>The Generalist trap: Why your first hire in 2026 should not be T-Shaped</title>
      <link>https://levelup.gitconnected.com/the-generalist-trap-why-your-first-hire-in-2026-should-not-be-t-shaped-95dc6606c331?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/the-generalist-trap-why-your-first-hire-in-2026-should-not-be-t-shaped-95dc6606c331?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Wed, 17 Jun 2026 16:22:23 GMT</pubDate>
      <content:encoded>&lt;div class=&quot;medium-feed-item&quot;&gt;&lt;p class=&quot;medium-feed-image&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/the-generalist-trap-why-your-first-hire-in-2026-should-not-be-t-shaped-95dc6606c331?source=rss----5517fd7b58a6---4&quot;&gt;&lt;img src=&quot;https://cdn-images-1.medium.com/max/2600/1*NNCVrszUG5iSRjfPd5yGhw.jpeg&quot; width=&quot;4499&quot;&gt;&lt;/a&gt;&lt;/p&gt;&lt;p class=&quot;medium-feed-snippet&quot;&gt;For a decade founders swore by the versatile generalist. Generative AI just made that playbook obsolete &amp;#x2014; and the startups figuring this&amp;#x2026;&lt;/p&gt;&lt;p class=&quot;medium-feed-link&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/the-generalist-trap-why-your-first-hire-in-2026-should-not-be-t-shaped-95dc6606c331?source=rss----5517fd7b58a6---4&quot;&gt;Continue reading on Level Up Coding »&lt;/a&gt;&lt;/p&gt;&lt;/div&gt;</content:encoded>
    </item>
    <item>
      <title>Building an IoC Container from Scratch: How NestJS and Spring Actually Work</title>
      <link>https://levelup.gitconnected.com/building-an-ioc-container-from-scratch-how-nestjs-and-spring-actually-work-413ada598a1e?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/building-an-ioc-container-from-scratch-how-nestjs-and-spring-actually-work-413ada598a1e?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Wed, 17 Jun 2026 16:22:14 GMT</pubDate>
      <content:encoded>&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*_uXVnabLvGr_UaxmrYAr1g.png&quot; /&gt;&lt;/figure&gt;&lt;p&gt;&lt;em&gt;You’ve written &lt;/em&gt;&lt;em&gt;@Injectable() dozens of times. But if someone asked you what it actually does, could you explain it without saying &amp;quot;it enables dependency injection&amp;quot;?&lt;/em&gt;&lt;/p&gt;&lt;p&gt;I used to treat NestJS like a magic box.&lt;/p&gt;&lt;p&gt;Write a decorator, add it to a module, and somehow the service appeared with all its dependencies already wired. A controller got its service. The service got its repository. The repository got a database connection. I had no idea what was happening underneath, and for a while, I didn’t care.&lt;/p&gt;&lt;p&gt;Then I hit a circular dependency error in production. The stack trace was useless. The NestJS docs told me what a circular dependency &lt;em&gt;was&lt;/em&gt;, but not why the container couldn’t resolve it, or what the container was even doing when it tried. I had no mental model. I was just guessing.&lt;/p&gt;&lt;p&gt;So I sat down and built a container from scratch.&lt;/p&gt;&lt;p&gt;This article is everything I learned. By the end, decorators like @Injectable(), @Inject(), and @Module() should stop feeling like rituals you copy from tutorials and start feeling like obvious answers to specific problems. We&amp;#39;re going from zero — a class that creates its own dependencies — all the way to a working container with factory registration, singleton scope, reflection-based injection, circular dependency detection, and provider definitions.&lt;/p&gt;&lt;p&gt;Let’s start with the problem.&lt;/p&gt;&lt;h3&gt;1. Why the Naive Approach Breaks&lt;/h3&gt;&lt;p&gt;Start with something simple. You’re building a user module. You need a database connection, a repository to fetch users, and a service with business logic:&lt;/p&gt;&lt;pre&gt;class Database {}&lt;br&gt;&lt;br&gt;class UserRepository {&lt;br&gt;  private db = new Database();&lt;br&gt;}&lt;br&gt;class UserService {&lt;br&gt;  private repo = new UserRepository();&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;This runs. But look at what’s actually happening: every class creates its own dependencies internally. UserRepository decides how Database gets constructed. UserService decides how UserRepository gets constructed.&lt;/p&gt;&lt;p&gt;Try to test UserService in isolation. You want to replace UserRepository with a mock. You can&amp;#39;t — it&amp;#39;s hardcoded inside. Try to swap Database for a different implementation. You&amp;#39;d have to dig into every class that references it and change the new Database() call.&lt;/p&gt;&lt;p&gt;The business logic and the object construction logic are tangled together. That’s the real problem. Not the classes themselves — the way they’re organized.&lt;/p&gt;&lt;h3&gt;The restaurant that doesn’t scale&lt;/h3&gt;&lt;p&gt;Imagine a restaurant where every waiter grows vegetables, bakes bread, and cooks food before serving a customer. One table? Fine. Five customers at once? The whole thing collapses.&lt;/p&gt;&lt;p&gt;A real restaurant separates concerns: suppliers bring ingredients, chefs prepare the food, waiters serve it. The waiter doesn’t know how the vegetables were grown. The chef doesn’t drive to the farm. Each role has a clear boundary.&lt;/p&gt;&lt;p&gt;Our code has the same structural problem. Each class is acting as the waiter, the chef, and the farmer simultaneously.&lt;/p&gt;&lt;h3&gt;2. Inversion of Control&lt;/h3&gt;&lt;p&gt;The fix is called Inversion of Control (IoC). The idea is simple: instead of a class creating what it needs, something external creates it and hands it over.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Before IoC — class controls its own dependencies:&lt;/strong&gt;&lt;/p&gt;&lt;pre&gt;UserRepository&lt;br&gt;  └── creates Database internally&lt;/pre&gt;&lt;p&gt;&lt;strong&gt;After IoC — external container controls everything:&lt;/strong&gt;&lt;/p&gt;&lt;pre&gt;Container&lt;br&gt;  ├── creates Database&lt;br&gt;  └── creates UserRepository(Database)&lt;/pre&gt;&lt;p&gt;The responsibility for object creation moves outward. Classes no longer manage their own dependencies. A container does. That’s the inversion.&lt;/p&gt;&lt;h3&gt;3. Dependency Injection: How the Container Delivers&lt;/h3&gt;&lt;p&gt;Once the container controls creation, it needs a way to give dependencies to the classes that need them. That’s Dependency Injection.&lt;/p&gt;&lt;p&gt;Instead of a class creating its dependency:&lt;/p&gt;&lt;pre&gt;class UserRepository {&lt;br&gt;  private db = new Database(); // internal creation&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;The class declares what it needs through the constructor:&lt;/p&gt;&lt;pre&gt;class UserRepository {&lt;br&gt;  constructor(private db: Database) {} // receives it from outside&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;The constructor becomes a contract. The class is saying: “I need a Database. I don’t care where it comes from.” The container answers: “I’ll handle that.”&lt;/p&gt;&lt;p&gt;That’s DI. Not a design pattern, not a framework feature — just a way of expressing dependencies through the constructor so something external can satisfy them.&lt;/p&gt;&lt;h3&gt;4. Manual DI: Better, But It Doesn’t Scale&lt;/h3&gt;&lt;p&gt;Before building a container, do this manually. Wire everything up in one place:&lt;/p&gt;&lt;pre&gt;const db = new Database();&lt;br&gt;const repo = new UserRepository(db);&lt;br&gt;const service = new UserService(repo);&lt;/pre&gt;&lt;p&gt;Already better. Object creation is centralized. You can swap implementations for testing. But now imagine a real system:&lt;/p&gt;&lt;pre&gt;const db = new Database();&lt;br&gt;const logger = new Logger();&lt;br&gt;const config = new ConfigService();&lt;br&gt;const email = new EmailService(config, logger);&lt;br&gt;const userRepo = new UserRepository(db, logger);&lt;br&gt;const userService = new UserService(userRepo, email, logger);&lt;br&gt;const userController = new UserController(userService);&lt;br&gt;const paymentService = new PaymentService(config, logger);&lt;br&gt;// ...keeps going&lt;/pre&gt;&lt;p&gt;The startup file becomes a maintenance nightmare. Add a new dependency to any class and you have to come back here and update the wiring by hand. Tests need to replicate all this setup. Bugs get introduced in bootstrap code that nobody wants to touch.&lt;/p&gt;&lt;p&gt;This is exactly why containers exist.&lt;/p&gt;&lt;h3&gt;5. What a Container Actually Is&lt;/h3&gt;&lt;p&gt;Here’s the thing nobody explains clearly: &lt;strong&gt;a container is not magic. It’s a registry with logic.&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;At its core, a container is just a map:&lt;/p&gt;&lt;pre&gt;Map&amp;lt;Token, ProviderDefinition&amp;gt;&lt;/pre&gt;&lt;p&gt;That’s it. A token (the identifier) maps to a provider definition (the instructions for how to create the thing). Everything else — reflection, singleton caching, circular detection — is logic that sits on top of that map.&lt;/p&gt;&lt;p&gt;Think of it as three layers:&lt;/p&gt;&lt;pre&gt;Smart Registry&lt;br&gt;      +&lt;br&gt;Object Factory&lt;br&gt;      +&lt;br&gt;Lifecycle Manager&lt;/pre&gt;&lt;p&gt;&lt;strong&gt;Registry:&lt;/strong&gt; Stores the mapping between tokens and provider definitions.&lt;br&gt; &lt;strong&gt;Object Factory:&lt;/strong&gt; Reads the provider definition and creates the object, including resolving sub-dependencies recursively.&lt;br&gt; &lt;strong&gt;Lifecycle Manager:&lt;/strong&gt; Decides whether to return a cached instance (singleton) or create a fresh one (transient).&lt;/p&gt;&lt;p&gt;That’s the full picture. When you call container.resolve(UserService), those three layers work together. The registry finds the definition, the factory builds the object graph, the lifecycle manager decides whether to cache the result.&lt;/p&gt;&lt;p&gt;Here’s a minimal but real implementation of that idea:&lt;/p&gt;&lt;pre&gt;class Container {&lt;br&gt;  private registry = new Map&amp;lt;any, any&amp;gt;();    // Token → ProviderDefinition&lt;br&gt;  private singletons = new Map&amp;lt;any, any&amp;gt;();  // Token → Cached instance&lt;br&gt;&lt;br&gt;register(definition: ProviderDefinition) {&lt;br&gt;    this.registry.set(definition.token, definition);&lt;br&gt;  }&lt;br&gt;  resolve&amp;lt;T&amp;gt;(token: any): T {&lt;br&gt;    // Layer 3: Lifecycle check first&lt;br&gt;    if (this.singletons.has(token)) {&lt;br&gt;      return this.singletons.get(token);&lt;br&gt;    }&lt;br&gt;    // Layer 1: Registry lookup&lt;br&gt;    const definition = this.registry.get(token);&lt;br&gt;&lt;br&gt;    if (!definition) throw new Error(`No provider registered for token`);&lt;br&gt;&lt;br&gt;    // Layer 2: Object factory&lt;br&gt;    const instance = this.createFromDefinition(definition);&lt;br&gt;&lt;br&gt;    // Layer 3: Cache the result (singleton scope)&lt;br&gt;    this.singletons.set(token, instance);&lt;br&gt;    return instance;&lt;br&gt;  }&lt;br&gt;&lt;br&gt;  private createFromDefinition(def: ProviderDefinition): any {&lt;br&gt;    if (&amp;quot;useValue&amp;quot; in def) return def.useValue;&lt;br&gt;    if (&amp;quot;useFactory&amp;quot; in def) return def.useFactory(this);&lt;br&gt;    if (&amp;quot;useClass&amp;quot; in def) {&lt;br&gt;      const deps = this.getDependencies(def.useClass);&lt;br&gt;      return new def.useClass(...deps);&lt;br&gt;    }&lt;br&gt;  }&lt;br&gt;  &lt;br&gt;  private getDependencies(cls: any): any[] {&lt;br&gt;    const paramTypes = Reflect.getMetadata(&amp;quot;design:paramtypes&amp;quot;, cls) || [];&lt;br&gt;    const customTokens = Reflect.getMetadata(&amp;quot;custom:inject&amp;quot;, cls) || [];&lt;br&gt;    return paramTypes.map((type: any, i: number) =&amp;gt;&lt;br&gt;      this.resolve(customTokens[i] ?? type)&lt;br&gt;    );&lt;br&gt;  }&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;This is roughly 40 lines. Every DI framework in existence — NestJS, Spring, Angular, TSyringe, InversifyJS — is a more sophisticated version of this same structure. The sophistication comes from edge cases, performance optimization, module scoping, and error handling. But the core is always: registry + factory + lifecycle.&lt;/p&gt;&lt;h3&gt;6. Why Framework Authors Actually Built This&lt;/h3&gt;&lt;p&gt;Here’s the context most articles skip.&lt;/p&gt;&lt;p&gt;Without a container, a large application might have 200 service classes. Each one creates its own logger. That’s 200 logger instances, all doing the same thing, consuming memory separately.&lt;/p&gt;&lt;p&gt;Each class creates its own database client. Now you have 200 database connections open simultaneously. Your database crashes.&lt;/p&gt;&lt;p&gt;Each class creates its own config service. That’s 200 objects reading the same environment variables and parsing the same JSON files at startup.&lt;/p&gt;&lt;p&gt;The container centralizes ownership. One logger, one database client, one config service — shared everywhere. The container creates each thing once and hands it out to whoever needs it.&lt;/p&gt;&lt;p&gt;That’s why Spring was invented. Java enterprise applications in the early 2000s had hundreds of beans (their word for injectable services), and manually wiring all of them was becoming impossible. Spring’s IoC container solved that problem. Two decades later, NestJS brought the same solution to Node.js with TypeScript syntax.&lt;/p&gt;&lt;h3&gt;7. Building the First Real Container&lt;/h3&gt;&lt;p&gt;The first version: a map that stores factories (not instances).&lt;/p&gt;&lt;pre&gt;type Token&amp;lt;T&amp;gt; = new (...args: any[]) =&amp;gt; T;&lt;br&gt;type Factory&amp;lt;T&amp;gt; = () =&amp;gt; T;&lt;br&gt;&lt;br&gt;class Container {&lt;br&gt;  private factories = new Map&amp;lt;Token&amp;lt;any&amp;gt;, Factory&amp;lt;any&amp;gt;&amp;gt;();&lt;br&gt;  register&amp;lt;T&amp;gt;(token: Token&amp;lt;T&amp;gt;, factory: Factory&amp;lt;T&amp;gt;) {&lt;br&gt;    this.factories.set(token, factory);&lt;br&gt;  }&lt;br&gt;  resolve&amp;lt;T&amp;gt;(token: Token&amp;lt;T&amp;gt;): T {&lt;br&gt;    const factory = this.factories.get(token);&lt;br&gt;    if (!factory) throw new Error(`No provider found for ${token.name}`);&lt;br&gt;    return factory();&lt;br&gt;  }&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;Why store a factory instead of an instance?&lt;/p&gt;&lt;pre&gt;// Storing the instance — application controls creation&lt;br&gt;container.register(Database, new Database());&lt;br&gt;&lt;br&gt;// Storing the factory - container controls creation&lt;br&gt;container.register(Database, () =&amp;gt; new Database());&lt;/pre&gt;&lt;p&gt;The factory is a recipe. The container decides when to cook, how many times, and whether to reuse the result. Storing an instance gives up that control.&lt;/p&gt;&lt;h3&gt;8. Singleton Scope (One Object, Shared Everywhere)&lt;/h3&gt;&lt;p&gt;Call resolve(Database) twice without caching:&lt;/p&gt;&lt;pre&gt;const db1 = container.resolve(Database);&lt;br&gt;const db2 = container.resolve(Database);&lt;br&gt;&lt;br&gt;console.log(db1 === db2); // false - two separate objectsty&lt;/pre&gt;&lt;p&gt;For a database client, that’s a problem. You don’t want a new connection every time something needs the database. Add a cache:&lt;/p&gt;&lt;pre&gt;class Container {&lt;br&gt;  private factories = new Map&amp;lt;Token&amp;lt;any&amp;gt;, Factory&amp;lt;any&amp;gt;&amp;gt;();&lt;br&gt;  private instances = new Map&amp;lt;Token&amp;lt;any&amp;gt;, any&amp;gt;(); // cache&lt;br&gt;&lt;br&gt;register&amp;lt;T&amp;gt;(token: Token&amp;lt;T&amp;gt;, factory: Factory&amp;lt;T&amp;gt;) {&lt;br&gt;    this.factories.set(token, factory);&lt;br&gt;  }&lt;br&gt;  resolve&amp;lt;T&amp;gt;(token: Token&amp;lt;T&amp;gt;): T {&lt;br&gt;    if (this.instances.has(token)) {&lt;br&gt;      return this.instances.get(token); // return cached&lt;br&gt;    }&lt;br&gt;    const factory = this.factories.get(token);&lt;br&gt;    if (!factory) throw new Error(`No provider found for ${token.name}`);&lt;br&gt;    const instance = factory();&lt;br&gt;    this.instances.set(token, instance); // cache it&lt;br&gt;    return instance;&lt;br&gt;  }&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;Now:&lt;/p&gt;&lt;pre&gt;const db1 = container.resolve(Database);&lt;br&gt;const db2 = container.resolve(Database);&lt;br&gt;&lt;br&gt;console.log(db1 === db2); // true&lt;/pre&gt;&lt;p&gt;This is singleton scope. Create once, reuse forever.&lt;/p&gt;&lt;p&gt;Think of a building’s water tank — one tank, shared by everyone. Nobody builds a new tank for each person who wants water.&lt;/p&gt;&lt;p&gt;Singleton is the right default for stateless services: database clients, loggers, config services, cache clients. They’re expensive to create, they don’t hold per-user state, and there’s no reason to duplicate them.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;But singleton has a real danger.&lt;/strong&gt; Never use it for objects that carry per-request or per-user state:&lt;/p&gt;&lt;pre&gt;class ShoppingCart {&lt;br&gt;  items = [];&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;If this is singleton, User A adds an item and User B sees it. This bug is invisible in development when you’re the only user, and catastrophic in production when you’re not. The container will do exactly what you tell it to. Make sure you’re telling it the right thing.&lt;/p&gt;&lt;p&gt;That’s why NestJS defaults to singleton but also offers Scope.TRANSIENT (new instance every time) and Scope.REQUEST (one instance per HTTP request) for cases where shared state is dangerous.&lt;/p&gt;&lt;p&gt;The mental model that matters:&lt;/p&gt;&lt;pre&gt;Creation strategy    ≠    Lifetime strategy&lt;br&gt;(useClass / useValue / useFactory)    (singleton / transient / request)&lt;/pre&gt;&lt;p&gt;These are two independent decisions. useClass: UserService tells the container &lt;em&gt;how&lt;/em&gt; to build the object. Scope.DEFAULT tells the container &lt;em&gt;how long&lt;/em&gt; to keep it. You configure them separately, and frameworks that collapse them into one thing make both harder to reason about.&lt;/p&gt;&lt;h3&gt;9. Constructor Injection and the Dependency Graph&lt;/h3&gt;&lt;p&gt;Now the real challenge. The container can create Database. But what about this?&lt;/p&gt;&lt;pre&gt;class UserRepository {&lt;br&gt;  constructor(private db: Database) {}&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;To create UserRepository, the container needs Database first. And for UserService:&lt;/p&gt;&lt;pre&gt;class UserService {&lt;br&gt;  constructor(private repo: UserRepository) {}&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;The full dependency graph looks like this:&lt;/p&gt;&lt;pre&gt;UserService&lt;br&gt;    └── UserRepository&lt;br&gt;            └── Database&lt;/pre&gt;&lt;p&gt;Without automatic resolution, you’d write:&lt;/p&gt;&lt;pre&gt;container.register(UserRepository, () =&amp;gt;&lt;br&gt;  new UserRepository(container.resolve(Database))&lt;br&gt;);&lt;/pre&gt;&lt;p&gt;That works, but you’re manually spelling out every dependency. The whole point of a container is to figure this out automatically. Which means the container needs to &lt;em&gt;read&lt;/em&gt; a class’s constructor and discover what it needs.&lt;/p&gt;&lt;h3&gt;10. Why Interfaces Disappear at Runtime&lt;/h3&gt;&lt;p&gt;This is the exact moment most developers get confused about @Inject(), so let&amp;#39;s be precise about it.&lt;/p&gt;&lt;p&gt;You write this in TypeScript:&lt;/p&gt;&lt;pre&gt;interface UserRepository {&lt;br&gt;  save(user: User): Promise&amp;lt;void&amp;gt;;&lt;br&gt;  findById(id: string): Promise&amp;lt;User&amp;gt;;&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;Now compile it. Open the JavaScript output. The interface is gone. Completely. Not compiled differently — just absent. JavaScript has no concept of interfaces. TypeScript’s type system exists only at compile time.&lt;/p&gt;&lt;p&gt;The same thing happens with constructor parameter types:&lt;/p&gt;&lt;pre&gt;// TypeScript&lt;br&gt;class UserService {&lt;br&gt;  constructor(private repo: UserRepository) {}&lt;br&gt;}&lt;br&gt;&lt;br&gt;// Compiled JavaScript&lt;br&gt;class UserService {&lt;br&gt;  constructor(repo) {} // ← UserRepository is gone&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;The parameter name (repo) survives. The type (UserRepository) doesn&amp;#39;t.&lt;/p&gt;&lt;p&gt;So when the container looks at the compiled JavaScript and asks “what does UserService need?&amp;quot;, it sees repo with no type information. It can&amp;#39;t resolve what to inject.&lt;/p&gt;&lt;p&gt;This is why @Inject() exists — and we&amp;#39;ll get to it after setting up reflection.&lt;/p&gt;&lt;h3&gt;11. Reflection Metadata&lt;/h3&gt;&lt;p&gt;TypeScript has a way to preserve type information at runtime, but you have to opt into it.&lt;/p&gt;&lt;p&gt;Two compiler options:&lt;/p&gt;&lt;pre&gt;{&lt;br&gt;  &amp;quot;experimentalDecorators&amp;quot;: true,&lt;br&gt;  &amp;quot;emitDecoratorMetadata&amp;quot;: true&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;Install:&lt;/p&gt;&lt;pre&gt;npm install reflect-metadata&lt;/pre&gt;&lt;p&gt;Import once at startup:&lt;/p&gt;&lt;pre&gt;import &amp;quot;reflect-metadata&amp;quot;;&lt;/pre&gt;&lt;p&gt;With emitDecoratorMetadata on, TypeScript embeds constructor parameter types into the compiled output. You read them back like this:&lt;/p&gt;&lt;pre&gt;Reflect.getMetadata(&amp;quot;design:paramtypes&amp;quot;, UserRepository);&lt;br&gt;// Returns: [Database]&lt;/pre&gt;&lt;p&gt;Now the container can see that UserRepository needs Database. It doesn&amp;#39;t have to guess.&lt;/p&gt;&lt;p&gt;Think of it as a blueprint. The class itself is the house. The metadata is the blueprint that says “this house needs these materials.” The builder (the container) reads the blueprint and sources what’s needed.&lt;/p&gt;&lt;p&gt;One important note: this only works when the class has at least one decorator on it. That’s actually one of the real reasons @Injectable() exists — not just to mark the class, but to trigger metadata emission on it. Without a decorator, TypeScript doesn&amp;#39;t emit the metadata even if emitDecoratorMetadata is on.&lt;/p&gt;&lt;h3&gt;12. Recursive Resolution (Walking the Graph)&lt;/h3&gt;&lt;p&gt;With reflection, the container can now automatically discover and build dependency chains.&lt;/p&gt;&lt;p&gt;Here’s the resolution flow for UserService:&lt;/p&gt;&lt;pre&gt;resolve(UserService)&lt;br&gt;  → read metadata → needs [UserRepository]&lt;br&gt;  → resolve(UserRepository)&lt;br&gt;      → read metadata → needs [Database]&lt;br&gt;      → resolve(Database)&lt;br&gt;          → read metadata → needs nothing&lt;br&gt;          → create Database ✓&lt;br&gt;      → create UserRepository(Database) ✓&lt;br&gt;  → create UserService(UserRepository) ✓&lt;/pre&gt;&lt;p&gt;The container walks the graph bottom-up. It creates the lowest-level dependency first, assembles upward, and ultimately hands you back a fully-constructed UserService.&lt;/p&gt;&lt;p&gt;You call one line:&lt;/p&gt;&lt;pre&gt;const service = container.resolve(UserService);&lt;/pre&gt;&lt;p&gt;And the container builds the entire tree. This is the core mechanism behind both NestJS and Spring.&lt;/p&gt;&lt;h3&gt;13. @Injectable() — The Badge, Not the Magic&lt;/h3&gt;&lt;p&gt;Most developers think @Injectable() enables dependency injection. It doesn&amp;#39;t.&lt;/p&gt;&lt;p&gt;The real question it answers is: &lt;em&gt;should this class participate in the dependency graph at all?&lt;/em&gt;&lt;/p&gt;&lt;p&gt;A real codebase has hundreds of classes — entities, DTOs, utilities, helpers, event handlers, value objects. The container shouldn’t try to manage all of them. It needs a way to know which classes have opted in.&lt;/p&gt;&lt;p&gt;@Injectable() is that opt-in marker.&lt;/p&gt;&lt;p&gt;Think of a hotel. Not everyone in the building is staff. Guests, delivery people, contractors, employees — they all walk the same hallways. The employee badge doesn’t create the employee. It tells the system who belongs to the operational structure.&lt;/p&gt;&lt;p&gt;Here’s the full implementation:&lt;/p&gt;&lt;pre&gt;const INJECTABLE_KEY = Symbol(&amp;quot;injectable&amp;quot;);&lt;br&gt;&lt;br&gt;function Injectable(): ClassDecorator {&lt;br&gt;  return (target) =&amp;gt; {&lt;br&gt;    Reflect.defineMetadata(INJECTABLE_KEY, true, target);&lt;br&gt;  };&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;The decorator runs, stores one boolean on the class, and does nothing else. When the container resolves a class, it checks:&lt;/p&gt;&lt;pre&gt;const isInjectable = Reflect.getMetadata(INJECTABLE_KEY, token);&lt;br&gt;if (!isInjectable) {&lt;br&gt;  throw new Error(`${token.name} is not marked @Injectable()`);&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;That’s the whole thing. No magic. A metadata flag that the container checks before proceeding.&lt;/p&gt;&lt;h3&gt;14. @Inject() — Solving the Interface Problem&lt;/h3&gt;&lt;p&gt;Remember how TypeScript interfaces disappear at runtime? Here’s when that actually causes a problem.&lt;/p&gt;&lt;p&gt;Your service depends on a UserRepository interface:&lt;/p&gt;&lt;pre&gt;constructor(private repo: UserRepository) {}&lt;/pre&gt;&lt;p&gt;After compilation, the type is gone. The metadata shows Object — useless to the container. Same issue with primitive values:&lt;/p&gt;&lt;pre&gt;constructor(private url: string) {}&lt;/pre&gt;&lt;p&gt;Metadata shows String. But which string? The database URL? The Redis host? The container has no way to know.&lt;/p&gt;&lt;p&gt;@Inject() solves this by attaching an explicit token to a parameter:&lt;/p&gt;&lt;pre&gt;const DATABASE_URL = Symbol(&amp;quot;DATABASE_URL&amp;quot;);&lt;br&gt;&lt;br&gt;class MysqlUserRepository {&lt;br&gt;  constructor(@Inject(DATABASE_URL) private url: string) {}&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;Now the container doesn’t guess. You’re telling it directly: “For this parameter, look up what’s registered under DATABASE_URL.&amp;quot;&lt;/p&gt;&lt;p&gt;The warehouse analogy: two boxes on a shelf, both the same size and brown. One has USB cables, one has Ethernet cables. No label — the worker guesses and gets it wrong half the time. @Inject() is the label.&lt;/p&gt;&lt;p&gt;Here’s what @Inject() actually stores:&lt;/p&gt;&lt;pre&gt;function Inject(token: any): ParameterDecorator {&lt;br&gt;  return (target, _, parameterIndex) =&amp;gt; {&lt;br&gt;    const existing = Reflect.getMetadata(&amp;quot;custom:inject&amp;quot;, target) || [];&lt;br&gt;    existing[parameterIndex] = token;&lt;br&gt;    Reflect.defineMetadata(&amp;quot;custom:inject&amp;quot;, existing, target);&lt;br&gt;  };&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;And when resolving, the container checks this metadata first, falling back to reflection only when no override exists:&lt;/p&gt;&lt;pre&gt;const customTokens = Reflect.getMetadata(&amp;quot;custom:inject&amp;quot;, target) || [];&lt;br&gt;const paramTypes = Reflect.getMetadata(&amp;quot;design:paramtypes&amp;quot;, target) || [];&lt;br&gt;&lt;br&gt;const tokens = paramTypes.map((type, i) =&amp;gt; customTokens[i] ?? type);&lt;/pre&gt;&lt;p&gt;So @Inject() doesn&amp;#39;t mean &amp;quot;this object is this interface.&amp;quot; It means &amp;quot;for this parameter, use this token.&amp;quot; That&amp;#39;s a crucial distinction.&lt;/p&gt;&lt;h3&gt;15. Provider Definitions: useClass, useValue, useFactory&lt;/h3&gt;&lt;p&gt;Once you have custom tokens, you need a way to tell the container what each token should resolve to. Provider definitions answer that question.&lt;/p&gt;&lt;p&gt;A provider definition is one record in the registry:&lt;/p&gt;&lt;pre&gt;type ProviderDefinition =&lt;br&gt;  | { token: any; useClass: new (...args: any[]) =&amp;gt; any }&lt;br&gt;  | { token: any; useValue: any }&lt;br&gt;  | { token: any; useFactory: (container: Container) =&amp;gt; any };&lt;/pre&gt;&lt;p&gt;There are three forms.&lt;/p&gt;&lt;h3&gt;useClass — map a token to an implementation&lt;/h3&gt;&lt;pre&gt;{&lt;br&gt;  token: USER_REPOSITORY,&lt;br&gt;  useClass: MysqlUserRepository&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;When something asks for USER_REPOSITORY, the container creates a MysqlUserRepository. Your business code depends on the abstract token. The concrete implementation is registered in one place.&lt;/p&gt;&lt;p&gt;Want to switch to Postgres? Change MysqlUserRepository to PostgresUserRepository in that one registration. Zero changes to business code. This is dependency inversion from SOLID — the D — in practice.&lt;/p&gt;&lt;h3&gt;useValue — return a value directly&lt;/h3&gt;&lt;pre&gt;{&lt;br&gt;  token: DATABASE_URL,&lt;br&gt;  useValue: &amp;#39;postgres://localhost:5432/mydb&amp;#39;&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;No construction. No dependency graph. The container returns this value as-is. Perfect for configuration, API keys, feature flags, environment variables — data, not services.&lt;/p&gt;&lt;h3&gt;useFactory — run a function to produce the value&lt;/h3&gt;&lt;pre&gt;{&lt;br&gt;  token: DATABASE,&lt;br&gt;  useFactory: (container) =&amp;gt; {&lt;br&gt;    const url = container.resolve(DATABASE_URL);&lt;br&gt;    return createConnection(url);&lt;br&gt;  }&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;Some things can’t be created with new. Database connections need a URL. Redis clients need a host and port. HTTP clients need base URLs and auth headers. A factory handles that setup logic and can itself resolve other dependencies from the container.&lt;/p&gt;&lt;h3&gt;16. Circular Dependency Detection&lt;/h3&gt;&lt;p&gt;Now for the failure case the container must catch.&lt;/p&gt;&lt;pre&gt;class UserService {&lt;br&gt;  constructor(private orderService: OrderService) {}&lt;br&gt;}&lt;br&gt;&lt;br&gt;class OrderService {&lt;br&gt;  constructor(private userService: UserService) {}&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;The container tries to resolve UserService, which needs OrderService, which needs UserService, which needs OrderService... infinite recursion. Stack overflow.&lt;/p&gt;&lt;p&gt;Two coworkers: Alice says she’ll start after Bob finishes. Bob says he’ll start after Alice finishes. Nothing ever happens. That’s exactly what this looks like inside the container.&lt;/p&gt;&lt;p&gt;The fix is a “currently resolving” set:&lt;/p&gt;&lt;pre&gt;private resolving = new Set&amp;lt;any&amp;gt;();&lt;br&gt;&lt;br&gt;resolve&amp;lt;T&amp;gt;(token: Token&amp;lt;T&amp;gt;): T {&lt;br&gt;  if (this.resolving.has(token)) {&lt;br&gt;    throw new Error(&lt;br&gt;      `Circular dependency detected while resolving ${token.name}`&lt;br&gt;    );&lt;br&gt;  }&lt;br&gt;  this.resolving.add(token);&lt;br&gt;  // ... resolve dependencies and create instance ...&lt;br&gt;  this.resolving.delete(token);&lt;br&gt;  return instance;&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;When the container starts resolving UserService, it adds it to the set. When it then tries to resolve OrderService and &lt;em&gt;that&lt;/em&gt; tries to resolve UserService again — it&amp;#39;s already in the set. Error thrown immediately. Clear message.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;But here’s what actually matters:&lt;/strong&gt; circular dependencies are almost never a container problem. They’re a design problem. The container is just the thing that catches it first.&lt;/p&gt;&lt;p&gt;When UserService and OrderService need each other, ask why. Usually it means both services share some responsibility that belongs in a third, more focused service. Extract that logic, and both services depend on the shared abstraction instead of each other. The cycle disappears, and the design gets cleaner.&lt;/p&gt;&lt;pre&gt;Before (circular):               After (clean):&lt;br&gt;&lt;br&gt;UserService ←→ OrderService      UserService    OrderService&lt;br&gt;                                       ↓              ↓&lt;br&gt;                                   SharedDomainService&lt;/pre&gt;&lt;p&gt;Treat the circular dependency error as design feedback. Something in the service boundary is wrong. The container is just the first thing to tell you.&lt;/p&gt;&lt;h3&gt;17. @Module() — Organization, Not Injection&lt;/h3&gt;&lt;p&gt;@Module() confuses people because it doesn&amp;#39;t create objects or inject anything. So what does it actually do?&lt;/p&gt;&lt;p&gt;It organizes providers into feature boundaries.&lt;/p&gt;&lt;pre&gt;@Module({&lt;br&gt;  providers: [UserService, UserRepository],&lt;br&gt;  imports: [DatabaseModule],&lt;br&gt;  exports: [UserService],&lt;br&gt;})&lt;br&gt;class UserModule {}&lt;/pre&gt;&lt;p&gt;Think of a university department. The Computer Science department doesn’t teach classes or grade assignments. It organizes the people who do. The department is the structure, not the actor.&lt;/p&gt;&lt;p&gt;@Module() is the same. It&amp;#39;s a folder for your dependency graph.&lt;/p&gt;&lt;p&gt;Under the hood, it stores metadata:&lt;/p&gt;&lt;pre&gt;function Module(metadata: { providers?: any[], imports?: any[], exports?: any[] }): ClassDecorator {&lt;br&gt;  return (target) =&amp;gt; {&lt;br&gt;    Reflect.defineMetadata(&amp;quot;module:providers&amp;quot;, metadata.providers ?? [], target);&lt;br&gt;    Reflect.defineMetadata(&amp;quot;module:imports&amp;quot;, metadata.imports ?? [], target);&lt;br&gt;    Reflect.defineMetadata(&amp;quot;module:exports&amp;quot;, metadata.exports ?? [], target);&lt;br&gt;  };&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;During bootstrap, the container reads that metadata and registers each provider. The imports field lets you compose modules — the UserModule can pull in everything from DatabaseModule without re-declaring each dependency. The exports field controls what&amp;#39;s visible to other modules that import this one.&lt;/p&gt;&lt;p&gt;That module system is how NestJS keeps large applications organized. Instead of one global registry with 500 providers, you have isolated feature modules that explicitly declare their public surface.&lt;/p&gt;&lt;h3&gt;18. NestJS and Spring: Same Architecture, Different Syntax&lt;/h3&gt;&lt;p&gt;Many Node developers don’t realize NestJS didn’t invent any of this. Spring solved the same problem in Java 20 years earlier. The architecture is nearly identical — only the syntax differs.&lt;/p&gt;&lt;pre&gt;Spring                          NestJS&lt;br&gt;──────────────────────────────────────────────&lt;br&gt;@Component                      @Injectable()&lt;br&gt;@Autowired                      @Inject()&lt;br&gt;@Configuration + @Bean          Provider definition (useFactory)&lt;br&gt;ApplicationContext               Container / ModuleRef&lt;br&gt;singleton (default scope)       Scope.DEFAULT (singleton)&lt;br&gt;prototype scope                 Scope.TRANSIENT&lt;br&gt;request scope                   Scope.REQUEST&lt;br&gt;@SpringBootApplication          bootstrapModule(AppModule)&lt;/pre&gt;&lt;p&gt;NestJS also drew heavily from Angular’s DI system for its decorator syntax and module structure. So it’s Spring’s IoC ideas, expressed with Angular’s decorator approach, running on Node.js. Once you see that lineage, a lot of NestJS design decisions make immediate sense.&lt;/p&gt;&lt;h3&gt;19. The Complete Flow: From Bootstrap to Resolution&lt;/h3&gt;&lt;p&gt;Here’s the full picture of what happens when your application starts and when you resolve a service.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;At application bootstrap:&lt;/strong&gt;&lt;/p&gt;&lt;pre&gt;bootstrapModule(AppModule)&lt;br&gt;        │&lt;br&gt;        ▼&lt;br&gt;Read @Module metadata&lt;br&gt;        │&lt;br&gt;        ▼&lt;br&gt;Find all providers (including imported modules)&lt;br&gt;        │&lt;br&gt;        ▼&lt;br&gt;Register each provider definition in the registry&lt;br&gt;        │&lt;br&gt;        ▼&lt;br&gt;Container is ready — nothing created yet&lt;/pre&gt;&lt;p&gt;&lt;strong&gt;At resolution (lazy creation):&lt;/strong&gt;&lt;/p&gt;&lt;pre&gt;container.resolve(UserService)&lt;br&gt;        │&lt;br&gt;        ▼&lt;br&gt;Is it in the singleton cache? → Yes → return cached instance&lt;br&gt;        │ No&lt;br&gt;        ▼&lt;br&gt;Is it currently being resolved? → Yes → Circular dependency error&lt;br&gt;        │ No&lt;br&gt;        ▼&lt;br&gt;Find provider definition in registry&lt;br&gt;        │&lt;br&gt;        ▼&lt;br&gt;Is the class @Injectable()? → No → Error&lt;br&gt;        │ Yes&lt;br&gt;        ▼&lt;br&gt;Read constructor metadata (reflection + @Inject overrides)&lt;br&gt;        │&lt;br&gt;        ▼&lt;br&gt;Recursively resolve each dependency&lt;br&gt;        │&lt;br&gt;        ▼&lt;br&gt;Create instance with resolved dependencies&lt;br&gt;        │&lt;br&gt;        ▼&lt;br&gt;Store in singleton cache&lt;br&gt;        │&lt;br&gt;        ▼&lt;br&gt;Remove from &amp;quot;currently resolving&amp;quot; set&lt;br&gt;        │&lt;br&gt;        ▼&lt;br&gt;Return instance&lt;/pre&gt;&lt;p&gt;That’s the entire machine. Every NestJS application runs through some version of this flow at startup, and again each time a new dependency is requested.&lt;/p&gt;&lt;h3&gt;20. Putting It All Together&lt;/h3&gt;&lt;p&gt;Here’s a full example with every concept in play:&lt;/p&gt;&lt;pre&gt;import &amp;quot;reflect-metadata&amp;quot;;&lt;br&gt;&lt;br&gt;const USER_REPOSITORY = Symbol(&amp;quot;USER_REPOSITORY&amp;quot;);&lt;br&gt;const DATABASE_URL = Symbol(&amp;quot;DATABASE_URL&amp;quot;);&lt;br&gt;@Injectable()&lt;br&gt;class MysqlUserRepository {&lt;br&gt;  constructor(@Inject(DATABASE_URL) private url: string) {&lt;br&gt;    console.log(`Connecting to: ${url}`);&lt;br&gt;  }&lt;br&gt;  &lt;br&gt;  findById(id: string) {&lt;br&gt;    return { id, name: &amp;quot;Tanzim&amp;quot; };&lt;br&gt;  }&lt;br&gt;}&lt;br&gt;&lt;br&gt;@Injectable()&lt;br&gt;class UserService {&lt;br&gt;  constructor(@Inject(USER_REPOSITORY) private repo: MysqlUserRepository) {}&lt;br&gt;  &lt;br&gt;  getUser(id: string) {&lt;br&gt;    return this.repo.findById(id);&lt;br&gt;  }&lt;br&gt;}&lt;br&gt;@Module({&lt;br&gt;  providers: [&lt;br&gt;    { token: DATABASE_URL, useValue: &amp;quot;postgres://localhost:5432/mydb&amp;quot; },&lt;br&gt;    { token: USER_REPOSITORY, useClass: MysqlUserRepository },&lt;br&gt;    UserService,&lt;br&gt;  ],&lt;br&gt;})&lt;br&gt;class UserModule {}&lt;br&gt;// Bootstrap and resolve&lt;br&gt;const container = new Container();&lt;br&gt;container.loadModule(UserModule);&lt;br&gt;const service = container.resolve(UserService);&lt;br&gt;console.log(service.getUser(&amp;quot;123&amp;quot;)); // { id: &amp;quot;123&amp;quot;, name: &amp;quot;Tanzim&amp;quot; }&lt;/pre&gt;&lt;p&gt;Resolution path:&lt;/p&gt;&lt;pre&gt;UserService&lt;br&gt;    ↓&lt;br&gt;@Inject(USER_REPOSITORY)&lt;br&gt;    ↓&lt;br&gt;useClass → MysqlUserRepository&lt;br&gt;    ↓&lt;br&gt;@Inject(DATABASE_URL)&lt;br&gt;    ↓&lt;br&gt;useValue → &amp;quot;postgres://localhost:5432/mydb&amp;quot;&lt;/pre&gt;&lt;p&gt;One call to container.resolve(UserService) and the entire chain builds itself.&lt;/p&gt;&lt;h3&gt;What This Means for Debugging Real Problems&lt;/h3&gt;&lt;p&gt;Before building a container, when something went wrong with dependency injection I’d read the error message and start guessing. “Maybe I forgot @Injectable(). Maybe there&amp;#39;s a circular dependency. Maybe the module isn&amp;#39;t imported.&amp;quot;&lt;/p&gt;&lt;p&gt;After? I read the error and know exactly what the container was doing when it failed.&lt;/p&gt;&lt;p&gt;&amp;quot;Nest can&amp;#39;t resolve dependencies of UserService (?)&amp;quot; — The ? means the container inspected the constructor, found a parameter, and the metadata was either missing or unresolvable. The fix is almost always a missing @Inject() on an interface-typed parameter, or a missing emitDecoratorMetadata flag.&lt;/p&gt;&lt;p&gt;&amp;quot;A circular dependency has been detected (UserService -&amp;gt; OrderService -&amp;gt; UserService)&amp;quot; — The container added UserService to its resolving set, then tried to resolve OrderService, which tried to re-resolve UserService, and found it already in the set. The error trace shows the exact cycle. Don&amp;#39;t use forwardRef() as the default fix — first ask whether the services should depend on a shared abstraction instead.&lt;/p&gt;&lt;p&gt;&amp;quot;Cannot determine the module associated with a component in the current context&amp;quot; — The container resolved a class that wasn&amp;#39;t registered in any module the current module can see. Either the provider is missing from providers, or the module that owns it isn&amp;#39;t listed in imports, or the owning module doesn&amp;#39;t export the provider.&lt;/p&gt;&lt;p&gt;These aren’t random errors anymore. Each one maps to a specific point in the resolution flow described above.&lt;/p&gt;&lt;h3&gt;What Changes After You Build This&lt;/h3&gt;&lt;p&gt;Before going through this exercise, I read NestJS internals and saw unfamiliar words:&lt;/p&gt;&lt;pre&gt;InstanceWrapper&lt;br&gt;Injector&lt;br&gt;Provider&lt;br&gt;Module&lt;br&gt;NestContainer&lt;br&gt;Scope&lt;/pre&gt;&lt;p&gt;After? Each one maps directly to something I built. InstanceWrapper is the container&amp;#39;s internal representation of a provider definition plus its singleton cache slot. Injector is the recursive resolver. NestContainer is the registry. Module is the metadata grouping with import/export resolution.&lt;/p&gt;&lt;p&gt;Spring works the same way under different names. BeanDefinition is the provider definition. BeanFactory is the container. ApplicationContext extends it with module-level features. @Component is @Injectable(). @Autowired does what @Inject() does.&lt;/p&gt;&lt;p&gt;The names differ. The architecture doesn’t.&lt;/p&gt;&lt;h3&gt;The Complete Picture&lt;/h3&gt;&lt;p&gt;At this point, we’ve discussed a lot of concepts:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;@Module()&lt;/li&gt;&lt;li&gt;@Injectable()&lt;/li&gt;&lt;li&gt;@Inject()&lt;/li&gt;&lt;li&gt;Reflection Metadata&lt;/li&gt;&lt;li&gt;Provider Definitions&lt;/li&gt;&lt;li&gt;useClass&lt;/li&gt;&lt;li&gt;useValue&lt;/li&gt;&lt;li&gt;useFactory&lt;/li&gt;&lt;li&gt;Singleton Scope&lt;/li&gt;&lt;li&gt;Circular Dependency Detection&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;Individually, each concept is simple.&lt;/p&gt;&lt;p&gt;The challenge is understanding how they work together.&lt;/p&gt;&lt;p&gt;The diagram below shows the complete lifecycle of a dependency inside a modern IoC container.&lt;/p&gt;&lt;pre&gt;Application Start&lt;br&gt;        │&lt;br&gt;        ▼@Module()&lt;br&gt;        │&lt;br&gt;        ▼&lt;br&gt;Container Reads Metadata&lt;br&gt;        │&lt;br&gt;        ▼&lt;br&gt;Provider Registration&lt;br&gt;        │&lt;br&gt;        ▼&lt;br&gt;Container Registry&lt;br&gt;Map&amp;lt;Token, Provider&amp;gt;&lt;br&gt;        │&lt;br&gt;        ▼&lt;br&gt;resolve(UserService)&lt;br&gt;        │&lt;br&gt;        ▼&lt;br&gt;Already Cached?&lt;br&gt;        │&lt;br&gt;   ┌────┴────┐&lt;br&gt;   │         │&lt;br&gt;  Yes        No&lt;br&gt;   │         │&lt;br&gt;   ▼         ▼&lt;br&gt;Return     Inspect Constructor&lt;br&gt;Cached     Using Reflection&lt;br&gt;              │&lt;br&gt;              ▼&lt;br&gt;       Dependency Graph&lt;br&gt;              │&lt;br&gt;              ▼&lt;br&gt;      Resolve Dependencies&lt;br&gt;              │&lt;br&gt;              ▼&lt;br&gt;       Create Instance&lt;br&gt;              │&lt;br&gt;              ▼&lt;br&gt;      Apply Provider Strategy&lt;br&gt;      ┌────────┼────────┐&lt;br&gt;      │        │        │&lt;br&gt;      ▼        ▼        ▼&lt;br&gt; useClass  useValue  useFactory&lt;br&gt;      │&lt;br&gt;      ▼&lt;br&gt;   Apply Scope&lt;br&gt;      │&lt;br&gt; ┌────┴────┐&lt;br&gt; │         │&lt;br&gt; ▼         ▼&lt;br&gt;Singleton  Transient&lt;br&gt; │&lt;br&gt; ▼&lt;br&gt;Cache Instance&lt;br&gt; │&lt;br&gt; ▼&lt;br&gt;Return Dependency&lt;br&gt;&lt;/pre&gt;&lt;p&gt;The important thing to notice is that every feature we’ve discussed solves a different problem:&lt;/p&gt;&lt;pre&gt;@Module()&lt;br&gt;      ↓&lt;br&gt;Organization&lt;br&gt;      ↓&lt;br&gt;@Injectable()&lt;br&gt;      ↓&lt;br&gt;Container-managed class&lt;br&gt;@Inject()&lt;br&gt;      ↓&lt;br&gt;Token override&lt;br&gt;Reflection&lt;br&gt;      ↓&lt;br&gt;Dependency discovery&lt;br&gt;Provider Definitions&lt;br&gt;      ↓&lt;br&gt;Creation strategy&lt;br&gt;Scopes&lt;br&gt;      ↓&lt;br&gt;Lifetime management&lt;br&gt;Container&lt;br&gt;      ↓&lt;br&gt;Orchestrates everything&lt;/pre&gt;&lt;p&gt;Once you see the system as a whole, frameworks like NestJS and Spring stop feeling magical.&lt;/p&gt;&lt;p&gt;They’re simply well-designed runtime systems for managing object creation, dependency graphs, and object lifetimes.&lt;/p&gt;&lt;h3&gt;The Real Lesson&lt;/h3&gt;&lt;p&gt;Frameworks aren’t magic. They’re well-engineered solutions to a problem that every large application eventually faces:&lt;/p&gt;&lt;blockquote&gt;How do you create, connect, and manage hundreds of objects without turning your startup code into an unmaintainable mess?&lt;/blockquote&gt;&lt;p&gt;The answer evolved into what we now call an IoC container, and the key ideas are:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;&lt;strong&gt;Tokens&lt;/strong&gt; identify dependencies without coupling to concrete implementations&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Provider definitions&lt;/strong&gt; describe how to create each dependency (useClass, useValue, useFactory)&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Reflection&lt;/strong&gt; lets the container discover dependency relationships automatically&lt;/li&gt;&lt;li&gt;&lt;strong&gt;@Inject()&lt;/strong&gt; overrides reflection when types disappear at runtime (interfaces, primitives)&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Scopes&lt;/strong&gt; control object lifetime independently from object creation&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Modules&lt;/strong&gt; organize providers into feature boundaries with explicit public interfaces&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Circular detection&lt;/strong&gt; catches bad design before it causes a stack overflow&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;Two mental models worth keeping:&lt;/p&gt;&lt;pre&gt;Creation  ≠  Lifetime&lt;br&gt;Token     ≠  Implementation&lt;/pre&gt;&lt;p&gt;Creation is &lt;em&gt;how&lt;/em&gt; something is built (useClass, useFactory). Lifetime is &lt;em&gt;how long&lt;/em&gt; it lives (singleton, transient). They&amp;#39;re independent decisions that frameworks often conflate.&lt;/p&gt;&lt;p&gt;A token is an identifier. An implementation is what gets created when you ask for it. Separating them is what makes it possible to swap MysqlUserRepository for PostgresUserRepository without touching a single line of business code.&lt;/p&gt;&lt;p&gt;Once these ideas click, you stop reading NestJS or Spring as a collection of decorators. You start reading it as a runtime that solves object lifecycle management at scale. And that’s when you go from someone who uses the framework to someone who can reason about it, debug it, and extend it.&lt;/p&gt;&lt;p&gt;The best way to actually get there: build it yourself. Start with a map. Add factory registration. Add singleton caching. Add reflection-based injection. Add circular detection. Each step takes 30 minutes, and each step teaches you something the framework documentation will never explain.&lt;/p&gt;&lt;p&gt;&lt;em&gt;Published by a developer who got confused by a circular dependency error in production and decided to understand the system instead of just fixing the immediate bug.&lt;/em&gt;&lt;/p&gt;&lt;img src=&quot;https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=413ada598a1e&quot; width=&quot;1&quot; height=&quot;1&quot; alt=&quot;&quot;&gt;&lt;hr&gt;&lt;p&gt;&lt;a href=&quot;https://levelup.gitconnected.com/building-an-ioc-container-from-scratch-how-nestjs-and-spring-actually-work-413ada598a1e&quot;&gt;Building an IoC Container from Scratch: How NestJS and Spring Actually Work&lt;/a&gt; was originally published in &lt;a href=&quot;https://levelup.gitconnected.com&quot;&gt;Level Up Coding&lt;/a&gt; on Medium, where people are continuing the conversation by highlighting and responding to this story.&lt;/p&gt;</content:encoded>
    </item>
    <item>
      <title>5 Database Design Patterns Every Python Backend Needs in Production</title>
      <link>https://levelup.gitconnected.com/5-database-design-patterns-every-python-backend-needs-in-production-68749ba4991c?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/5-database-design-patterns-every-python-backend-needs-in-production-68749ba4991c?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Wed, 17 Jun 2026 16:22:06 GMT</pubDate>
      <content:encoded>&lt;div class=&quot;medium-feed-item&quot;&gt;&lt;p class=&quot;medium-feed-image&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/5-database-design-patterns-every-python-backend-needs-in-production-68749ba4991c?source=rss----5517fd7b58a6---4&quot;&gt;&lt;img src=&quot;https://cdn-images-1.medium.com/max/1672/1*KbXOZzEF6ijfI2Nq_bP6rA.png&quot; width=&quot;1672&quot;&gt;&lt;/a&gt;&lt;/p&gt;&lt;p class=&quot;medium-feed-snippet&quot;&gt;Your models define your data. Your patterns define whether you can trust it.&lt;/p&gt;&lt;p class=&quot;medium-feed-link&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/5-database-design-patterns-every-python-backend-needs-in-production-68749ba4991c?source=rss----5517fd7b58a6---4&quot;&gt;Continue reading on Level Up Coding »&lt;/a&gt;&lt;/p&gt;&lt;/div&gt;</content:encoded>
    </item>
    <item>
      <title>Apple Cut Siri AI From 450 Million EU iPhones Rather Than Open It to Rivals: The Siri Extensions…</title>
      <link>https://levelup.gitconnected.com/apple-cut-siri-ai-from-450-million-eu-iphones-rather-than-open-it-to-rivals-the-siri-extensions-59d41c734282?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/apple-cut-siri-ai-from-450-million-eu-iphones-rather-than-open-it-to-rivals-the-siri-extensions-59d41c734282?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Wed, 17 Jun 2026 16:21:58 GMT</pubDate>
      <content:encoded>&lt;div class=&quot;medium-feed-item&quot;&gt;&lt;p class=&quot;medium-feed-image&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/apple-cut-siri-ai-from-450-million-eu-iphones-rather-than-open-it-to-rivals-the-siri-extensions-59d41c734282?source=rss----5517fd7b58a6---4&quot;&gt;&lt;img src=&quot;https://cdn-images-1.medium.com/max/1376/1*Gaea1_47pvglU7Zf4kMtaw.png&quot; width=&quot;1376&quot;&gt;&lt;/a&gt;&lt;/p&gt;&lt;p class=&quot;medium-feed-snippet&quot;&gt;The leaked iOS 27 framework that turns Siri into a router for Gemini and Claude, the Bing-on-Edge math that explains it, and the standoff&amp;#x2026;&lt;/p&gt;&lt;p class=&quot;medium-feed-link&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/apple-cut-siri-ai-from-450-million-eu-iphones-rather-than-open-it-to-rivals-the-siri-extensions-59d41c734282?source=rss----5517fd7b58a6---4&quot;&gt;Continue reading on Level Up Coding »&lt;/a&gt;&lt;/p&gt;&lt;/div&gt;</content:encoded>
    </item>
    <item>
      <title>My App Returned 403 But My Code Never Did</title>
      <link>https://levelup.gitconnected.com/my-app-returned-403-but-my-code-never-did-543538d1882d?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/my-app-returned-403-but-my-code-never-did-543538d1882d?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Wed, 17 Jun 2026 16:21:51 GMT</pubDate>
      <content:encoded>&lt;div class=&quot;medium-feed-item&quot;&gt;&lt;p class=&quot;medium-feed-image&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/my-app-returned-403-but-my-code-never-did-543538d1882d?source=rss----5517fd7b58a6---4&quot;&gt;&lt;img src=&quot;https://cdn-images-1.medium.com/max/1200/0*agudliJyuUWzBnUr.jpg&quot; width=&quot;1200&quot;&gt;&lt;/a&gt;&lt;/p&gt;&lt;p class=&quot;medium-feed-snippet&quot;&gt;An edge WAF blocked my API on raw HTML in the JSON body. The fix was 40 lines.&lt;/p&gt;&lt;p class=&quot;medium-feed-link&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/my-app-returned-403-but-my-code-never-did-543538d1882d?source=rss----5517fd7b58a6---4&quot;&gt;Continue reading on Level Up Coding »&lt;/a&gt;&lt;/p&gt;&lt;/div&gt;</content:encoded>
    </item>
    <item>
      <title>Spring Data JPA with Kotlin: Entities, Repositories, and Pitfalls</title>
      <link>https://levelup.gitconnected.com/spring-data-jpa-with-kotlin-entities-repositories-and-pitfalls-5804593a28f0?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/spring-data-jpa-with-kotlin-entities-repositories-and-pitfalls-5804593a28f0?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Wed, 17 Jun 2026 16:21:45 GMT</pubDate>
      <content:encoded>&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/641/1*ZZGXyMdn2UUrsUHitfLlwg@2x.png&quot; /&gt;&lt;/figure&gt;&lt;p&gt;This guide covers Spring Data JPA with Kotlin for Spring Boot 4.x projects -from JPA in Spring Boot setup and entity modeling to Spring Data JPA repositories, projections, pagination, and dynamic filtering. Every Spring Boot JPA example uses idiomatic Kotlin.&lt;/p&gt;&lt;blockquote&gt;&lt;em&gt;You can enjoy the original version of this article on my website, &lt;/em&gt;&lt;a href=&quot;https://protsenko.dev/spring-data-jpa-with-kotlin-best-practices-for-spring-boot/&quot;&gt;&lt;em&gt;protsenko.dev&lt;/em&gt;&lt;/a&gt;&lt;em&gt;.&lt;/em&gt;&lt;/blockquote&gt;&lt;p&gt;Spring Boot is the framework layer that lets you build production-ready apps quickly, with auto-configuration, an embedded server, sensible defaults, and fewer manual setup steps.&lt;/p&gt;&lt;p&gt;Spring Data JPA is the persistence layer that sits on top of JPA/Hibernate and provides repository APIs for common database tasks (CRUD, pagination, query methods) without requiring you to write boilerplate DAO code.&lt;/p&gt;&lt;p&gt;Kotlin improves this workflow by making backend code shorter and safer:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;Null-safety reduces a large class of runtime null errors.&lt;/li&gt;&lt;li&gt;Data/DTO modeling is concise and readable.&lt;/li&gt;&lt;li&gt;Extension functions and expressive syntax keep service/repository code easier to maintain.&lt;/li&gt;&lt;/ul&gt;&lt;h3&gt;Start your Kotlin project with Spring Initializr&lt;/h3&gt;&lt;p&gt;For beginners, Spring Initializr is the fastest and safest way to create a correct project skeleton.&lt;/p&gt;&lt;ol&gt;&lt;li&gt;Open &lt;a href=&quot;https://start.spring.io/&quot;&gt;start.spring.io&lt;/a&gt;.&lt;/li&gt;&lt;li&gt;Choose:&lt;/li&gt;&lt;/ol&gt;&lt;ul&gt;&lt;li&gt;Project: Gradle (Kotlin)&lt;/li&gt;&lt;li&gt;Language: Kotlin&lt;/li&gt;&lt;li&gt;Spring Boot: latest stable 4.x&lt;/li&gt;&lt;li&gt;Group: for example dev.protsenko&lt;/li&gt;&lt;li&gt;Artifact: for example demo&lt;/li&gt;&lt;/ul&gt;&lt;ol&gt;&lt;li&gt;Add dependencies:&lt;/li&gt;&lt;/ol&gt;&lt;ul&gt;&lt;li&gt;Spring Web&lt;/li&gt;&lt;li&gt;Spring Data JPA&lt;/li&gt;&lt;li&gt;one database driver (H2 for local learning, or PostgreSQL Driver for real DB usage)&lt;/li&gt;&lt;/ul&gt;&lt;ol&gt;&lt;li&gt;Generate and unzip the project.&lt;/li&gt;&lt;li&gt;Open it in IntelliJ IDEA and run the main application class.&lt;/li&gt;&lt;/ol&gt;&lt;p&gt;Recommendation: use Spring Initializr for your first projects and most new services. It prevents common setup mistakes and gives you a clean baseline to start coding features immediately.&lt;/p&gt;&lt;p&gt;At this point, your project should be running, so let’s move to the core Spring Data JPA concept: entities.&lt;/p&gt;&lt;h3&gt;JPA Entity Basics in Kotlin: @Entity, @Id, @Column&lt;/h3&gt;&lt;p&gt;A &lt;strong&gt;JPA entity&lt;/strong&gt; is a Kotlin class that JPA maps to a database table. One entity instance usually represents one row. Fields map to columns, and associations (@OneToMany, @ManyToOne, etc.) map to relationships.&lt;/p&gt;&lt;p&gt;At minimum, an entity needs @Entity and a primary key definition (@Id or @EmbeddedId). In practice, JPA annotations split into three layers: class-level mapping (@Entity), key mapping (@Id or @EmbeddedId), and optional field-level tuning (@Column when defaults are not enough). The next subchapters explain each layer.&lt;/p&gt;&lt;h3&gt;@Entity&lt;/h3&gt;&lt;ul&gt;&lt;li&gt;For what: marks a Kotlin class as a JPA entity (persistent model).&lt;/li&gt;&lt;li&gt;Arguments: main argument is name, used by JPQL (@Entity(name = &amp;quot;...&amp;quot;)).&lt;/li&gt;&lt;li&gt;When to use: always on persistent classes; set name explicitly when you use JPQL strings and want safer class refactors.&lt;/li&gt;&lt;/ul&gt;&lt;h3&gt;@Id&lt;/h3&gt;&lt;ul&gt;&lt;li&gt;For what: defines a single-column primary key.&lt;/li&gt;&lt;li&gt;Arguments: @Id itself has no arguments; it is commonly paired with @GeneratedValue(strategy = ...).&lt;/li&gt;&lt;li&gt;When to use: when your table key is one column (for example id UUID, Long, Integer).&lt;/li&gt;&lt;/ul&gt;&lt;h3&gt;@EmbeddedId&lt;/h3&gt;&lt;ul&gt;&lt;li&gt;For what: defines a composite primary key via an embeddable key object.&lt;/li&gt;&lt;li&gt;Arguments: @EmbeddedId has no arguments; key fields are defined in the embeddable class.&lt;/li&gt;&lt;li&gt;When to use: when the table primary key is naturally multi-column.&lt;/li&gt;&lt;/ul&gt;&lt;h3&gt;@Column(...) (optional)&lt;/h3&gt;&lt;ul&gt;&lt;li&gt;For what: customizes field-to-column mapping.&lt;/li&gt;&lt;li&gt;Arguments and how they work:&lt;/li&gt;&lt;li&gt;name: explicit DB column name. Use when DB naming differs from your Kotlin property name.&lt;/li&gt;&lt;li&gt;nullable: whether the column can store NULL. Keep this aligned with Kotlin type nullability. When Hibernate generates DDL it uses this value to emit NOT NULL; when you manage schema with a migration tool (Liquibase, Flyway), the constraint lives in the migration script – @Column(nullable = false) still communicates intent to the ORM, but the actual NOT NULL in the database comes from your migration.&lt;/li&gt;&lt;li&gt;length: max length for string columns (for example VARCHAR). Use for bounded text fields.&lt;/li&gt;&lt;li&gt;unique: shorthand single-column unique constraint. Use sparingly; prefer DB migrations for complex unique indexes.&lt;/li&gt;&lt;li&gt;precision: total number of digits for decimal columns (usually with BigDecimal).&lt;/li&gt;&lt;li&gt;scale: number of digits after decimal point (usually with BigDecimal).&lt;/li&gt;&lt;li&gt;When to use: when default mapping is not enough. For minimal entities, you can omit @Column.&lt;/li&gt;&lt;li&gt;Kotlin nullability rule:&lt;/li&gt;&lt;li&gt;Prefer non-null Kotlin types by default (String, Int, UUID) for required data.&lt;/li&gt;&lt;li&gt;Use nullable Kotlin types (String?, Int?, UUID?) only when the value is truly optional in business logic.&lt;/li&gt;&lt;li&gt;Keep type and column settings consistent: non-null type with nullable = false, nullable type with nullable = true.&lt;/li&gt;&lt;li&gt;Important: a non-null Kotlin type alone does &lt;strong&gt;not&lt;/strong&gt; create a NOT NULL constraint in the database. Add @Column(nullable = false) on required fields to communicate nullability intent to the ORM. The actual constraint in the database depends on how you manage schema: Hibernate-generated DDL will use this annotation, but in production environments schema is typically managed by a migration tool (Liquibase, Flyway) – define NOT NULL in the migration script and keep it in sync with the entity annotation.&lt;/li&gt;&lt;/ul&gt;&lt;h3&gt;No-arg constructor requirement&lt;/h3&gt;&lt;ul&gt;&lt;li&gt;For what: JPA needs a no-arg constructor for entity instantiation.&lt;/li&gt;&lt;li&gt;Arguments: not an annotation itself in this context.&lt;/li&gt;&lt;li&gt;When to use: always required; in Kotlin projects this is typically handled by the &lt;strong&gt;Kotlin JPA plugin&lt;/strong&gt; (kotlin(&amp;quot;plugin.jpa&amp;quot;)), which generates synthetic no-arg constructors for @Entity-annotated classes at compile time.&lt;/li&gt;&lt;li&gt;Non-final requirement: Jakarta Persistence also requires entity classes and their persistent members to be non-final. Kotlin classes are final by default, so Kotlin projects add kotlin(&amp;quot;plugin.allopen&amp;quot;) (or the Spring Boot equivalent kotlin(&amp;quot;plugin.spring&amp;quot;) combined with allOpen JPA annotation configuration) to make JPA-annotated classes open automatically.&lt;/li&gt;&lt;/ul&gt;&lt;h3&gt;Mapping Domain States with Enums&lt;/h3&gt;&lt;p&gt;Enums are the safest way to represent different states (status, type, role, mode) in Spring Data JPA.&lt;/p&gt;&lt;ul&gt;&lt;li&gt;For what: map business states to a closed set of values instead of magic numbers or free-text strings.&lt;/li&gt;&lt;li&gt;How it works: use @Enumerated(EnumType.STRING) on the enum field.&lt;/li&gt;&lt;li&gt;When to use: default choice for state columns. Avoid EnumType.ORDINAL because reordering enum constants can silently change data meaning.&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;If your existing schema stores numeric codes (for example 1, 2, 3), use an AttributeConverter instead of EnumType.ORDINAL.&lt;/p&gt;&lt;pre&gt;@Converter&lt;br&gt;class CompanyStatusCodeConverter : AttributeConverter&amp;lt;CompanyStatus, Int&amp;gt; {&lt;br&gt;    override fun convertToDatabaseColumn(attribute: CompanyStatus) = when (attribute) {&lt;br&gt;        CompanyStatus.ACTIVE -&amp;gt; 1&lt;br&gt;        CompanyStatus.SUSPENDED -&amp;gt; 2&lt;br&gt;        CompanyStatus.DISSOLVED -&amp;gt; 3&lt;br&gt;    }&lt;/pre&gt;&lt;pre&gt;    override fun convertToEntityAttribute(dbData: Int?) = when (dbData) {&lt;br&gt;        1 -&amp;gt; CompanyStatus.ACTIVE&lt;br&gt;        2 -&amp;gt; CompanyStatus.SUSPENDED&lt;br&gt;        3 -&amp;gt; CompanyStatus.DISSOLVED&lt;br&gt;        else -&amp;gt; throw IllegalArgumentException(&amp;quot;Unknown status code: $dbData&amp;quot;)&lt;br&gt;    }&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;Two rules for writing a safe converter:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;convertToDatabaseColumn receives the Kotlin type – match its nullability to the entity field. If the field is non-nullable (CompanyStatus), the parameter is non-nullable too – no null branch needed.&lt;/li&gt;&lt;li&gt;convertToEntityAttribute must never silently return null for unknown DB values when the entity field is non-nullable. Throw instead – a corrupt database value is a programming error, not a recoverable state.&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;Use it on the entity field:&lt;/p&gt;&lt;pre&gt;@Convert(converter = CompanyStatusCodeConverter::class)&lt;br&gt;var status: CompanyStatus = CompanyStatus.ACTIVE&lt;/pre&gt;&lt;p&gt;Example: let’s map a company table to an entity with id (UUID primary key), name, countryCode, status (enum), and optional description.&lt;/p&gt;&lt;pre&gt;enum class CompanyStatus {&lt;br&gt;    ACTIVE,&lt;br&gt;    SUSPENDED,&lt;br&gt;    DISSOLVED&lt;br&gt;}&lt;/pre&gt;&lt;pre&gt;@Entity(name = &amp;quot;Company&amp;quot;)&lt;br&gt;@Table(name = &amp;quot;company&amp;quot;)&lt;br&gt;class Company(&lt;br&gt;    val name: String,&lt;br&gt;    val countryCode: String,&lt;br&gt;    @Enumerated(EnumType.STRING)&lt;br&gt;    var status: CompanyStatus = CompanyStatus.ACTIVE,&lt;br&gt;    var description: String? = null&lt;br&gt;) {&lt;br&gt;    @Id&lt;br&gt;    @GeneratedValue(strategy = GenerationType.UUID)&lt;br&gt;    var id: UUID? = null&lt;br&gt;}&lt;/pre&gt;&lt;h3&gt;Overriding equals and hashCode in JPA Entities&lt;/h3&gt;&lt;p&gt;Overriding equals and hashCode in JPA entities is context-dependent. Many applications work fine with defaults from Any/Object.&lt;/p&gt;&lt;p&gt;Context details:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;In a typical Spring Boot spring-boot-starter-data-jpa setup, Hibernate is the JPA provider (ORM library) brought in by the starter.&lt;/li&gt;&lt;li&gt;A &lt;strong&gt;persistence context&lt;/strong&gt; is Hibernate’s first-level cache bound to the current transaction/session. Inside that scope, one DB row is represented by one managed object instance.&lt;/li&gt;&lt;li&gt;Because of that identity map, reference equality is often enough while you stay in one transaction/session.&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;Why dynamic/mutable equals/hashCode is dangerous:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;If equality depends on mutable fields, object hash can change after insertion into Set/Map, which breaks lookup/remove behavior.&lt;/li&gt;&lt;li&gt;If equality touches lazy associations, equals/hashCode/toString can trigger extra SQL queries unexpectedly (N+1 side effects).&lt;/li&gt;&lt;li&gt;If equality traverses relationships, it can recurse deeply, increase CPU/memory usage, and even cause stack overflows in cyclic graphs.&lt;/li&gt;&lt;li&gt;In practice, these patterns create correctness bugs first, and performance problems second.&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;Custom equality is usually needed only when:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;entities are keys in Map,&lt;/li&gt;&lt;li&gt;entities are stored in Set,&lt;/li&gt;&lt;li&gt;entities are compared across persistence contexts.&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;Main strategies:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;&lt;strong&gt;Default implementation from &lt;/strong&gt;Any&lt;strong&gt;/&lt;/strong&gt;Object&lt;strong&gt; (recommended baseline).&lt;/strong&gt; This is reference-based equality, which works well when entities stay inside one persistence context and are not used as keys in hashed collections.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Business-key equality&lt;/strong&gt; (recommended only if the key is unique and immutable).&lt;/li&gt;&lt;li&gt;&lt;strong&gt;ID-based equality&lt;/strong&gt; (common with generated IDs, but requires careful handling of transient entities with id == null).&lt;/li&gt;&lt;li&gt;&lt;strong&gt;All-fields equality&lt;/strong&gt; is unsafe for JPA entities (mutable fields, lazy associations, recursion risk, and unstable hashes).&lt;/li&gt;&lt;/ul&gt;&lt;h3&gt;Best Practices for Designing JPA Entities&lt;/h3&gt;&lt;p&gt;Field typing practices for entities:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;Match Kotlin nullability to DB nullability: String for required values, String? only for truly optional data. Annotate required fields with @Column(nullable = false) to communicate nullability intent to the ORM. In production, the actual NOT NULLconstraint is typically defined in a migration tool (Liquibase, Flyway) – keep the entity annotation and migration script in sync.&lt;/li&gt;&lt;li&gt;Prefer domain-safe types over primitives where meaning matters: BigDecimal for money, Instant/OffsetDateTime for timestamps, enums for finite states.&lt;/li&gt;&lt;li&gt;Reuse embeddable value objects (@Embeddable) for repeated concepts (for example, address, amount+currency) instead of duplicating scalar fields across entities.&lt;/li&gt;&lt;li&gt;Avoid fake placeholder values (empty string, 0, -1) for “missing” data. When a value is truly optional, represent it explicitly as a nullable DB column + nullable Kotlin type (String?, Int?) or model it as an enum state like UNKNOWN/NOT_PROVIDED.&lt;/li&gt;&lt;li&gt;Avoid putting heavy business logic in entity constructors; keep constructor requirements minimal for ORM lifecycle.&lt;/li&gt;&lt;li&gt;Avoid data class for entities because generated equals/hashCode/toString/copy conflict with JPA lifecycle and lazy associations.&lt;/li&gt;&lt;/ul&gt;&lt;h3&gt;Accessing Data with JpaRepository and CrudRepository&lt;/h3&gt;&lt;p&gt;To access the data you write only a repository &lt;strong&gt;interface&lt;/strong&gt;, and Spring Data JPA creates the real implementation class automatically when the app starts. So the parent interface you choose (CrudRepository, JpaRepository, etc.) directly decides what methods your repository has.&lt;/p&gt;&lt;p&gt;Typical flow:&lt;/p&gt;&lt;ol&gt;&lt;li&gt;Define CompanyRepository interface.&lt;/li&gt;&lt;li&gt;Inject it into a service.&lt;/li&gt;&lt;li&gt;Call methods like save, findById, or custom query methods.&lt;/li&gt;&lt;/ol&gt;&lt;ul&gt;&lt;li&gt;Repository&amp;lt;T, ID&amp;gt;: marker-only base; you expose only methods you declare.&lt;/li&gt;&lt;li&gt;CrudRepository&amp;lt;T, ID&amp;gt;: adds standard CRUD methods.&lt;/li&gt;&lt;li&gt;ListCrudRepository&amp;lt;T, ID&amp;gt;: same as CrudRepository, but list-returning methods use List.&lt;/li&gt;&lt;li&gt;PagingAndSortingRepository&amp;lt;T, ID&amp;gt;: adds pagination and sorting.&lt;/li&gt;&lt;li&gt;JpaRepository&amp;lt;T, ID&amp;gt;: most common default; includes CRUD + pagination + sorting + JPA-specific helpers (flush, batch operations).&lt;/li&gt;&lt;li&gt;JpaSpecificationExecutor&amp;lt;T&amp;gt; (optional companion interface): dynamic predicate queries via Specifications.&lt;/li&gt;&lt;li&gt;QueryByExampleExecutor&amp;lt;T&amp;gt; (optional companion interface): probe-based querying without explicit JPQL.&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;Typical Kotlin repository:&lt;/p&gt;&lt;pre&gt;interface CompanyRepository : JpaRepository&amp;lt;Company, UUID&amp;gt;&lt;/pre&gt;&lt;p&gt;When to choose what:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;If you need strict API boundaries, extend Repository (or a slim shared base) and expose only allowed operations. You can also use @NoRepositoryBean on abstract base repository interfaces.&lt;/li&gt;&lt;li&gt;If you need a convenient full-CRUD repository, extend JpaRepository.&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;Small note: @Repository is not required on Spring Data repository interfaces. When an interface extends Spring Data repository contracts (Repository, CrudRepository, JpaRepository, etc.), Spring auto-detects it and registers a proxy bean automatically.&lt;/p&gt;&lt;h3&gt;JPQL and @Query: Querying Data with Spring Data JPA&lt;/h3&gt;&lt;p&gt;If you need data access beyond default CRUD methods, Spring Data JPA provides three mainstream query styles:&lt;/p&gt;&lt;ol&gt;&lt;li&gt;&lt;strong&gt;Derived query methods&lt;/strong&gt; (query generated from method name).&lt;/li&gt;&lt;li&gt;&lt;strong&gt;JPQL queries&lt;/strong&gt; via @Query.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Native SQL queries&lt;/strong&gt; via @Query(nativeQuery = true) or @NativeQuery(introduced in newer Spring Data versions).&lt;/li&gt;&lt;/ol&gt;&lt;p&gt;What “derived” means:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;Spring parses method names like findByCountryCodeAndStatus and builds JPQL automatically.&lt;/li&gt;&lt;li&gt;This is best for straightforward filters and keeps repository code concise.&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;Derived method example with Company:&lt;/p&gt;&lt;pre&gt;fun findByCountryCodeAndStatus(countryCode: String, status: CompanyStatus): List&amp;lt;Company&amp;gt;&lt;/pre&gt;&lt;p&gt;Explicit JPQL query with Company:&lt;/p&gt;&lt;pre&gt;@Query(&lt;br&gt;    &amp;quot;select c from Company c where c.countryCode = :countryCode and c.status = :status&amp;quot;&lt;br&gt;)&lt;br&gt;fun findActiveInCountry(&lt;br&gt;    @Param(&amp;quot;countryCode&amp;quot;) countryCode: String,&lt;br&gt;    @Param(&amp;quot;status&amp;quot;) status: CompanyStatus&lt;br&gt;): List&amp;lt;Company&amp;gt;&lt;/pre&gt;&lt;h3&gt;Updating data in repositories&lt;/h3&gt;&lt;p&gt;For update/delete JPQL queries, both annotations are important:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;@Modifying tells Spring Data this query changes data (not a select).&lt;/li&gt;&lt;li&gt;@Transactional provides transaction boundaries so the change is committed atomically.&lt;/li&gt;&lt;/ul&gt;&lt;pre&gt;@Modifying(clearAutomatically = true, flushAutomatically = true)&lt;br&gt;@Transactional&lt;br&gt;@Query(&amp;quot;update Company c set c.status = :status where c.id = :id&amp;quot;)&lt;br&gt;fun updateStatus(&lt;br&gt;    @Param(&amp;quot;id&amp;quot;) id: UUID,&lt;br&gt;    @Param(&amp;quot;status&amp;quot;) status: CompanyStatus&lt;br&gt;): Int&lt;/pre&gt;&lt;p&gt;Bulk JPQL update/delete queries run directly in the database and bypass the normal entity lifecycle in the current persistence context. In practice, this means managed entities that were already loaded (for example, a Company fetched earlier in the same transaction) are not automatically synchronized with the bulk result, so your code can keep reading stale in-memory values. That stale state can cause incorrect business decisions, confusing test behavior, and accidental overwrite patterns later in the same transaction. The safe default is to flush pending changes before the bulk query and clear the persistence context after it; in Spring Data JPA, @Modifying(clearAutomatically = true, flushAutomatically = true) is a practical way to enforce that for repository bulk methods.&lt;/p&gt;&lt;h3&gt;Kotlin-Idiomatic Entity Fetching with Spring Data JPA&lt;/h3&gt;&lt;p&gt;Spring Data’s Java APIs commonly use Optional&amp;lt;T&amp;gt; for missing individual CRUD results, while query methods can also model absence through null or wrapper return types. In Kotlin, Optional&amp;lt;T&amp;gt; is unnecessary: the type system already expresses presence and absence through nullable types (T vs T?). Spring Data provides native ways to work with this directly.&lt;/p&gt;&lt;pre&gt;// You could specify nullable values in repositories&lt;br&gt;fun findByName(name: String): Company?&lt;br&gt;@Query(&amp;quot;select c from Company c where c.name = :name&amp;quot;)&lt;br&gt;fun findOneByName(@Param(&amp;quot;name&amp;quot;) name: String): Company?&lt;/pre&gt;&lt;pre&gt;// Remember: non-nullable return values will raise exception in runtime&lt;br&gt;fun getByName(name: String): Company&lt;br&gt;@Query(&amp;quot;select c from Company c where c.name = :name&amp;quot;)&lt;br&gt;fun getOneByName(@Param(&amp;quot;name&amp;quot;) name: String): Company&lt;/pre&gt;&lt;p&gt;Prefer nullable return types (Company?) as the default. A non-nullable return type (Company) throws EmptyResultDataAccessException at runtime when no result is found – this is a runtime error the compiler cannot warn you about, making it easy to miss.&lt;/p&gt;&lt;p&gt;Another idiomatic option is findByIdOrNull, a Spring Data extension function that returns T? directly:&lt;/p&gt;&lt;pre&gt;val company: Company? = companyRepository.findByIdOrNull(id)&lt;/pre&gt;&lt;p&gt;The import is required — findByIdOrNull is a top-level extension function on CrudRepository and will not resolve without it.&lt;/p&gt;&lt;h3&gt;Pagination in Spring Boot: Pageable, Page, and Slice&lt;/h3&gt;&lt;p&gt;Most applications need to return data in pages rather than all at once. Spring Data JPA provides three result types for paginated queries — choose based on what the caller actually needs.&lt;/p&gt;&lt;h3&gt;Page vs Slice vs List&lt;/h3&gt;&lt;p&gt;Page&amp;lt;T&amp;gt;Slice&amp;lt;T&amp;gt;List&amp;lt;T&amp;gt;COUNT queryYesNoNoKnows total recordsYesNoNohasNext()mechanismnumber + 1 &amp;lt; totalPagesfetches N+1 rowsn/aUse caseUI with page numbers, total count in APIinfinite scroll, “load more”all results, caller controls&lt;/p&gt;&lt;p&gt;Page&amp;lt;T&amp;gt; fires a SELECT count(*) query when necessary so the caller knows the total number of records. Use it when you need to render “Page 3 of 47” or expose totalElements in an API response.&lt;/p&gt;&lt;p&gt;Slice&amp;lt;T&amp;gt; skips the count query entirely. Spring Data fetches pageSize + 1 rows – if more than pageSize rows come back, hasNext() returns true and the extra row is discarded. Use it for infinite scroll or “load more” UIs where the total is irrelevant.&lt;/p&gt;&lt;p&gt;List&amp;lt;T&amp;gt;&lt;strong&gt; with &lt;/strong&gt;Pageable returns only the content with no metadata at all. Use it when you need a window of results but don’t need navigation information.&lt;/p&gt;&lt;h3&gt;Pageable and PageRequest&lt;/h3&gt;&lt;p&gt;Pageable is the abstraction that carries which page to retrieve and how to sort it. For paged requests, construct it via PageRequest:&lt;/p&gt;&lt;pre&gt;// Page 0, 20 items per page, no sort&lt;br&gt;val page = PageRequest.of(0, 20)&lt;/pre&gt;&lt;pre&gt;// With sort&lt;br&gt;val sorted = PageRequest.of(0, 20, Sort.by(&amp;quot;name&amp;quot;))&lt;br&gt;val desc   = PageRequest.of(0, 20, Sort.Direction.DESC, &amp;quot;status&amp;quot;)&lt;/pre&gt;&lt;pre&gt;// Shortcut for page 0&lt;br&gt;val first10 = PageRequest.ofSize(10)&lt;/pre&gt;&lt;p&gt;Page numbers are &lt;strong&gt;zero-based&lt;/strong&gt;: page 0 is the first page.&lt;/p&gt;&lt;p&gt;PageRequest is immutable. Use the wither methods to derive a new instance:&lt;/p&gt;&lt;pre&gt;val next     = page.next()               // page=1, same size and sort&lt;br&gt;val specific = page.withPage(5)          // page=5, same size and sort&lt;br&gt;val resort   = page.withSort(Sort.by(&amp;quot;status&amp;quot;))&lt;/pre&gt;&lt;p&gt;To fetch all records without pagination, use Pageable.unpaged(). A repository method that accepts Pageable and returns Page&amp;lt;T&amp;gt; will return all results with totalElements == content.size when passed an unpaged request:&lt;/p&gt;&lt;pre&gt;val all = companyRepository.findAll(Pageable.unpaged())&lt;/pre&gt;&lt;h3&gt;Sort&lt;/h3&gt;&lt;p&gt;Sort is immutable and composed of one or more Sort.Order instances. The simplest form:&lt;/p&gt;&lt;pre&gt;Sort.by(&amp;quot;name&amp;quot;)                                        // ASC by default&lt;br&gt;Sort.by(Sort.Direction.DESC, &amp;quot;name&amp;quot;)&lt;br&gt;Sort.by(Sort.Order.desc(&amp;quot;status&amp;quot;), Sort.Order.asc(&amp;quot;name&amp;quot;))&lt;/pre&gt;&lt;p&gt;Sort.Order provides fine-grained control:&lt;/p&gt;&lt;pre&gt;Sort.Order.asc(&amp;quot;name&amp;quot;).ignoreCase()                    // case-insensitive sort&lt;br&gt;Sort.Order.asc(&amp;quot;description&amp;quot;).nullsLast()              // NULLs sorted after non-nulls&lt;br&gt;Sort.Order.desc(&amp;quot;description&amp;quot;).nullsFirst()            // NULLs sorted before non-nulls&lt;/pre&gt;&lt;p&gt;Multiple fields:&lt;/p&gt;&lt;pre&gt;val sort = Sort.by(&lt;br&gt;    Sort.Order.desc(&amp;quot;status&amp;quot;),&lt;br&gt;    Sort.Order.asc(&amp;quot;name&amp;quot;).ignoreCase(),&lt;br&gt;    Sort.Order.asc(&amp;quot;description&amp;quot;).nullsLast()&lt;br&gt;)&lt;/pre&gt;&lt;p&gt;Combine existing Sort instances:&lt;/p&gt;&lt;pre&gt;val combined = Sort.by(&amp;quot;name&amp;quot;).and(Sort.by(Sort.Direction.DESC, &amp;quot;status&amp;quot;))&lt;br&gt;val reversed = Sort.by(&amp;quot;name&amp;quot;).reverse()&lt;/pre&gt;&lt;h3&gt;Type-safe sort with JpaSort and TypedSort&lt;/h3&gt;&lt;p&gt;Plain Sort.by(&amp;quot;name&amp;quot;) is a string – a typo compiles but throws at runtime. Two type-safe alternatives exist.&lt;/p&gt;&lt;p&gt;JpaSort uses the JPA static metamodel attributes (Company_) generated by Hibernate:&lt;/p&gt;&lt;pre&gt;val sort = JpaSort.of(Sort.Direction.ASC, Company_.name)&lt;br&gt;val multi = JpaSort.of(Company_.name).and(JpaSort.of(Sort.Direction.DESC, Company_.status))&lt;/pre&gt;&lt;p&gt;If Company_ doesn’t have the field, the code doesn’t compile.&lt;/p&gt;&lt;p&gt;Sort.TypedSort&amp;lt;T&amp;gt; uses method references on the entity class:&lt;/p&gt;&lt;pre&gt;val typedSort = Sort.sort(Company::class.java)&lt;br&gt;val byName    = typedSort.by(Company::name).ascending()&lt;br&gt;val byStatus  = typedSort.by(Company::status).descending()&lt;/pre&gt;&lt;p&gt;TypedSort works only on open classes – your allOpen plugin configuration already handles this for @Entity classes.&lt;/p&gt;&lt;h3&gt;Repository method signatures&lt;/h3&gt;&lt;p&gt;Declare pagination support by adding a Pageable parameter and choosing the return type:&lt;/p&gt;&lt;pre&gt;interface CompanyRepository : JpaRepository&amp;lt;Company, UUID&amp;gt; {&lt;/pre&gt;&lt;pre&gt;    // Page&amp;lt;T&amp;gt; - fires count query, returns total metadata&lt;br&gt;    fun findByCountryCode(countryCode: String, pageable: Pageable): Page&amp;lt;Company&amp;gt;&lt;/pre&gt;&lt;pre&gt;    // Slice&amp;lt;T&amp;gt; - no count query, only hasNext&lt;br&gt;    // Different method name required - Kotlin cannot overload by return type alone&lt;br&gt;    fun findAllByCountryCode(countryCode: String, pageable: Pageable): Slice&amp;lt;Company&amp;gt;&lt;/pre&gt;&lt;pre&gt;    // Custom countQuery - required when JPQL has joins or GROUP BY&lt;br&gt;    @Query(&lt;br&gt;        value = &amp;quot;select c from Company c where c.countryCode = :code and c.status = :status&amp;quot;,&lt;br&gt;        countQuery = &amp;quot;select count(c) from Company c where c.countryCode = :code and c.status = :status&amp;quot;&lt;br&gt;    )&lt;br&gt;    fun findActiveInCountryPaged(&lt;br&gt;        @Param(&amp;quot;code&amp;quot;) countryCode: String,&lt;br&gt;        @Param(&amp;quot;status&amp;quot;) status: CompanyStatus,&lt;br&gt;        pageable: Pageable&lt;br&gt;    ): Page&amp;lt;Company&amp;gt;&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;For queries involving JOIN, DISTINCT, or GROUP BY, auto-derived count queries frequently produce wrong results – provide an explicit countQuery. Native SQL queries always require one since Spring Data’s built-in enhancer handles only simple queries for count derivation.&lt;/p&gt;&lt;h3&gt;Count query optimization&lt;/h3&gt;&lt;p&gt;Spring Data will skip the COUNT query automatically when the result is a partial page (fewer rows than the requested page size). If the current page is not full, the total can be inferred without hitting the database:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;First page, partial result → totalElements = content.size&lt;/li&gt;&lt;li&gt;Any other page, partial result → totalElements = offset + content.size&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;The count query only fires when the page is full and the total is genuinely unknown.&lt;/p&gt;&lt;h3&gt;Common pitfalls wigh Spring Data JPA pagination and sorting&lt;/h3&gt;&lt;p&gt;join fetch&lt;strong&gt; with &lt;/strong&gt;Page&amp;lt;T&amp;gt; – never combine JOIN FETCH on a collection association with a paginated query returning Page&amp;lt;T&amp;gt;. Hibernate cannot apply LIMIT/OFFSET at the SQL level when a collection is eagerly fetched in the same query, so it loads all matching rows into memory first and then paginates in Java. On large datasets, this causes out-of-memory errors. Hibernate logs a warning: HHH90003004: firstResult/maxResults specified with collection fetch; applying in memory.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Sorting by non-persistent fields&lt;/strong&gt; — Sort.by(&amp;quot;computedField&amp;quot;) throws PropertyReferenceException at runtime if the field does not exist on the entity. Use JpaSort with metamodel attributes to catch this at compile time.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Returning &lt;/strong&gt;PageImpl&lt;strong&gt; directly from controllers&lt;/strong&gt; – PageImpl is not a stable serialization contract and its JSON shape can change between Spring Data versions. Map to an explicit DTO or configure page serialization mode explicitly:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;DIRECT (default): serializes PageImpl directly; the JSON shape includes Spring Data internals and is not guaranteed stable between versions.&lt;/li&gt;&lt;li&gt;VIA_DTO: wraps the result in a documented, stable DTO shape – the recommended choice for API responses.&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;The default is DIRECT for backward compatibility. Set VIA_DTO explicitly:&lt;/p&gt;&lt;pre&gt;spring:&lt;br&gt;  data:&lt;br&gt;    web:&lt;br&gt;      pageable:&lt;br&gt;        serialization-mode: via-dto&lt;/pre&gt;&lt;h3&gt;Dynamic Filtering with Spring Data JPA Specifications&lt;/h3&gt;&lt;p&gt;Derived query methods and @Query work well for fixed filters. When filters are optional or the combination changes at runtime – for example, a search endpoint where the user can filter by status, country, or name in any combination – you need a different approach.&lt;/p&gt;&lt;h3&gt;JpaSpecificationExecutor&lt;/h3&gt;&lt;p&gt;JpaSpecificationExecutor&amp;lt;T&amp;gt; is Spring Data JPA’s built-in option for dynamic filtering. Add it alongside JpaRepository in your repository interface – no extra dependencies needed:&lt;/p&gt;&lt;pre&gt;interface CompanyRepository :&lt;br&gt;    JpaRepository&amp;lt;Company, UUID&amp;gt;,&lt;br&gt;    JpaSpecificationExecutor&amp;lt;Company&amp;gt;&lt;/pre&gt;&lt;p&gt;This adds findAll(spec), findAll(spec, pageable), count(spec), exists(spec), delete(spec), and update(spec).&lt;/p&gt;&lt;p&gt;A Specification is a lambda that receives a JPA Criteria API Root, CriteriaQuery, and CriteriaBuilder and returns a Predicate. Without the type-safe metamodel, field access uses string literals:&lt;/p&gt;&lt;pre&gt;val spec = Specification&amp;lt;Company&amp;gt; { root, _, cb -&amp;gt;&lt;br&gt;    cb.equal(root.get&amp;lt;CompanyStatus&amp;gt;(&amp;quot;status&amp;quot;), CompanyStatus.ACTIVE) // fragile string&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;A typo in &amp;quot;status&amp;quot; compiles fine but throws at runtime. The solution is the &lt;strong&gt;JPA static metamodel&lt;/strong&gt;.&lt;/p&gt;&lt;h3&gt;JPA static metamodel&lt;/h3&gt;&lt;p&gt;Hibernate’s annotation processor generates a Company_ class from your @Entity at build time. Each field becomes a typed SingularAttribute instead of a string:&lt;/p&gt;&lt;pre&gt;// Generated: Company_.java&lt;br&gt;@StaticMetamodel(Company.class)&lt;br&gt;@Generated(&amp;quot;org.hibernate.processor.HibernateProcessor&amp;quot;)&lt;br&gt;public abstract class Company_ {&lt;/pre&gt;&lt;pre&gt;    public static final String NAME = &amp;quot;name&amp;quot;;&lt;br&gt;    public static final String COUNTRY_CODE = &amp;quot;countryCode&amp;quot;;&lt;br&gt;    public static final String STATUS = &amp;quot;status&amp;quot;;&lt;br&gt;    public static final String DESCRIPTION = &amp;quot;description&amp;quot;;&lt;br&gt;    public static final String ID = &amp;quot;id&amp;quot;;&lt;/pre&gt;&lt;pre&gt;    public static volatile EntityType&amp;lt;Company&amp;gt; class_;&lt;br&gt;    public static volatile SingularAttribute&amp;lt;Company, String&amp;gt; name;&lt;br&gt;    public static volatile SingularAttribute&amp;lt;Company, String&amp;gt; countryCode;&lt;br&gt;    public static volatile SingularAttribute&amp;lt;Company, CompanyStatus&amp;gt; status;&lt;br&gt;    public static volatile SingularAttribute&amp;lt;Company, String&amp;gt; description;&lt;br&gt;    public static volatile SingularAttribute&amp;lt;Company, UUID&amp;gt; id;&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;With the metamodel, field access is type-checked at compile time:&lt;/p&gt;&lt;pre&gt;val spec = Specification&amp;lt;Company&amp;gt; { root, _, cb -&amp;gt;&lt;br&gt;    cb.equal(root.get(Company_.status), CompanyStatus.ACTIVE) // compile-time safe&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;Rename status in Company and the compiler immediately flags Company_.status.&lt;/p&gt;&lt;p&gt;Gradle setup (kapt — hibernate-processor version is managed by the Spring Boot BOM):&lt;/p&gt;&lt;pre&gt;plugins {&lt;br&gt;    kotlin(&amp;quot;kapt&amp;quot;)&lt;br&gt;}&lt;/pre&gt;&lt;pre&gt;dependencies {&lt;br&gt;    kapt(&amp;quot;org.hibernate.orm:hibernate-processor&amp;quot;)&lt;br&gt;}&lt;/pre&gt;&lt;pre&gt;kapt {&lt;br&gt;    correctErrorTypes = true&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;After building, kapt generates Company_.java in build/generated/source/kapt/main/.&lt;/p&gt;&lt;p&gt;Specifications compose with .and() and .or(). For negation, use the static Specification.not(...) helper:&lt;/p&gt;&lt;pre&gt;object CompanySpecs {&lt;br&gt;    fun hasStatus(status: CompanyStatus) = Specification&amp;lt;Company&amp;gt; { root, _, cb -&amp;gt;&lt;br&gt;        cb.equal(root.get(Company_.status), status)&lt;br&gt;    }&lt;/pre&gt;&lt;pre&gt;    fun hasCountryCode(code: String) = Specification&amp;lt;Company&amp;gt; { root, _, cb -&amp;gt;&lt;br&gt;        cb.equal(root.get(Company_.countryCode), code)&lt;br&gt;    }&lt;br&gt;}&lt;/pre&gt;&lt;pre&gt;// Usage&lt;br&gt;val result = companyRepository.findAll(&lt;br&gt;    CompanySpecs.hasCountryCode(&amp;quot;DE&amp;quot;).and(CompanySpecs.hasStatus(CompanyStatus.ACTIVE))&lt;br&gt;)&lt;/pre&gt;&lt;pre&gt;val notSuspended = companyRepository.findAll(&lt;br&gt;    Specification.not(CompanySpecs.hasStatus(CompanyStatus.SUSPENDED))&lt;br&gt;)&lt;/pre&gt;&lt;p&gt;For optional filters, returning null from a Specification lambda skips the predicate entirely – JPA treats a null predicate as no restriction:&lt;/p&gt;&lt;pre&gt;fun hasCountryCodeOptional(code: String?) = Specification&amp;lt;Company&amp;gt; { root, _, cb -&amp;gt;&lt;br&gt;    if (code != null) cb.equal(root.get(Company_.countryCode), code) else null&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;When to use Specifications:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;You want zero extra runtime dependencies — everything is built into Spring Data JPA and Hibernate.&lt;/li&gt;&lt;li&gt;You need bulk delete or update via DeleteSpecification / UpdateSpecification. Spring Data JPA 4.0 also introduced PredicateSpecification – a query-type-agnostic variant that works across select, update, and delete queries without being tied to a single operation type.&lt;/li&gt;&lt;li&gt;You prefer a standardised JPA approach over a third-party query DSL.&lt;/li&gt;&lt;/ul&gt;&lt;h3&gt;QueryDSL with Spring Data JPA&lt;/h3&gt;&lt;p&gt;QueryDSL generates type-safe Q-classes from your JPA entities at build time. Instead of writing query conditions as string literals (which break silently on refactoring), you reference real Kotlin properties — QCompany.company.countryCode – that fail to compile if the field is renamed or removed.&lt;/p&gt;&lt;p&gt;QueryDSL solves the same problem as Specifications but with a more readable fluent API: one method, any filter combination, all type-checked at compile time.&lt;/p&gt;&lt;p&gt;Gradle setup (kapt for Q-class generation):&lt;/p&gt;&lt;pre&gt;plugins {&lt;br&gt;    kotlin(&amp;quot;kapt&amp;quot;)&lt;br&gt;}&lt;/pre&gt;&lt;pre&gt;dependencies {&lt;br&gt;    implementation(&amp;quot;com.querydsl:querydsl-jpa::jakarta&amp;quot;)&lt;br&gt;    kapt(&amp;quot;com.querydsl:querydsl-apt::jakarta&amp;quot;)&lt;br&gt;    kapt(&amp;quot;jakarta.persistence:jakarta.persistence-api&amp;quot;)&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;The ::jakarta notation omits the version – it is managed by the Spring Boot BOM. The jakarta classifier selects the Jakarta EE 9+ variant required for Spring Boot 3.x/4.x. After building, kapt generates a QCompany class alongside your entity.&lt;/p&gt;&lt;p&gt;Add QuerydslPredicateExecutor&amp;lt;T&amp;gt; to your repository:&lt;/p&gt;&lt;pre&gt;interface CompanyRepository :&lt;br&gt;    JpaRepository&amp;lt;Company, UUID&amp;gt;,&lt;br&gt;    QuerydslPredicateExecutor&amp;lt;Company&amp;gt;&lt;/pre&gt;&lt;p&gt;This adds findAll(predicate), findAll(predicate, pageable), count(predicate), and exists(predicate).&lt;/p&gt;&lt;p&gt;After building, kapt generates QCompany. Use BooleanBuilder for optional filters:&lt;/p&gt;&lt;pre&gt;fun searchCompanies(&lt;br&gt;    countryCode: String?,&lt;br&gt;    status: CompanyStatus?,&lt;br&gt;    pageable: Pageable&lt;br&gt;): Page&amp;lt;Company&amp;gt; {&lt;br&gt;    val q = QCompany.company&lt;br&gt;    val predicate = BooleanBuilder()&lt;br&gt;    countryCode?.let { predicate.and(q.countryCode.eq(it)) }&lt;br&gt;    status?.let { predicate.and(q.status.eq(it)) }&lt;br&gt;    return companyRepository.findAll(predicate, pageable)&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;An empty BooleanBuilder matches all rows – document this on filter methods so callers know no filter means no restriction.&lt;/p&gt;&lt;h3&gt;Spring Data JPA Projections in Kotlin&lt;/h3&gt;&lt;p&gt;Projections are specialized read models (DTOs) for data exchange with clients. Returning raw entities directly is usually a bad practice for public APIs because it leaks persistence details and often exposes more fields than needed.&lt;/p&gt;&lt;p&gt;Where projections fit in:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;A projection is a read-focused DTO/view shape requested directly from the database query.&lt;/li&gt;&lt;li&gt;In practice, projections are often the most direct DTO path for read APIs when the query selects only required columns.&lt;/li&gt;&lt;li&gt;Important nuance: DB load is reduced only if the query selects fewer columns. Fetching full entities and mapping afterward does &lt;strong&gt;not&lt;/strong&gt; reduce selected columns.&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;A bit more about trade-offs: more DTO/projection types improve API clarity and performance, but they increase maintenance cost, so define them per use case (summary/detail/list item) instead of one mega-DTO.&lt;/p&gt;&lt;h3&gt;Interface-based projection (read-only view)&lt;/h3&gt;&lt;p&gt;An interface-based projection is a plain Kotlin interface where each property getter matches a field name in the entity. Spring Data JPA generates a proxy at runtime that implements the interface and maps query results onto it automatically — no constructor, no mapper, no extra class needed.&lt;/p&gt;&lt;p&gt;When all accessor methods map directly to entity properties, this is called a &lt;strong&gt;closed projection&lt;/strong&gt; in Spring Data JPA terminology. Because Spring Data knows exactly which fields are needed, it can optimize the query and select only the projected fields — skipping any columns not declared in the interface.&lt;/p&gt;&lt;p&gt;Column reduction depends on how the query is written:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;&lt;strong&gt;Derived query methods&lt;/strong&gt; — Spring Data automatically generates a SELECT that fetches only the fields declared in the interface.&lt;/li&gt;&lt;li&gt;@Query(&amp;quot;select c from Company c ...&amp;quot;) – loads all columns and wraps the full entity in a proxy. No column reduction happens.&lt;/li&gt;&lt;li&gt;@Query&lt;strong&gt; with explicit field selection&lt;/strong&gt; – fetches only the listed fields. This is the correct way to get column reduction with a custom query.&lt;/li&gt;&lt;/ul&gt;&lt;pre&gt;interface CompanySummaryView {&lt;br&gt;    val id: UUID&lt;br&gt;    val name: String&lt;br&gt;    val countryCode: String&lt;br&gt;    val status: CompanyStatus&lt;br&gt;}&lt;/pre&gt;&lt;pre&gt;// Derived - Spring Data selects only the four declared fields&lt;br&gt;fun findByCountryCode(countryCode: String): List&amp;lt;CompanySummaryView&amp;gt;&lt;/pre&gt;&lt;pre&gt;// @Query with explicit fields - also reduces columns&lt;br&gt;@Query(&amp;quot;&amp;quot;&amp;quot;&lt;br&gt;    select c.id as id, c.name as name, c.countryCode as countryCode, c.status as status&lt;br&gt;    from Company c where c.countryCode = :countryCode&lt;br&gt;&amp;quot;&amp;quot;&amp;quot;)&lt;br&gt;fun findSummaryByCountryCode(@Param(&amp;quot;countryCode&amp;quot;) countryCode: String): List&amp;lt;CompanySummaryView&amp;gt;&lt;/pre&gt;&lt;p&gt;Kotlin note: you cannot declare two methods with the same name and same parameters differing only by return type. If findByCountryCode(countryCode: String): List&amp;lt;Company&amp;gt; already exists, you need a distinct method name for the projection overload – as shown with findSummaryByCountryCode above.&lt;/p&gt;&lt;p&gt;You can also transform or combine columns in the query and map the result to the interface by matching the alias name:&lt;/p&gt;&lt;pre&gt;interface CompanyListView {&lt;br&gt;    val id: UUID&lt;br&gt;    val displayName: String&lt;br&gt;}&lt;/pre&gt;&lt;pre&gt;@Query(&amp;quot;select c.id as id, concat(c.name, &amp;#39; (&amp;#39;, c.countryCode, &amp;#39;)&amp;#39;) as displayName from Company c&amp;quot;)&lt;br&gt;fun findAllForList(): List&amp;lt;CompanyListView&amp;gt;&lt;/pre&gt;&lt;p&gt;When to use:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;Simple read-only list and summary API responses where all fields map directly to entity properties or returned from database.&lt;/li&gt;&lt;li&gt;When you want minimal boilerplate — no DTO class, no constructor, no mapper required.&lt;/li&gt;&lt;/ul&gt;&lt;h3&gt;Class/DTO projection with Spring Data JPA&lt;/h3&gt;&lt;p&gt;A class-based projection is a regular Kotlin data class used as the query result type directly. Instead of returning entities and mapping them afterward, you instruct JPQL to construct the DTO inline using the new keyword with a fully qualified class name. Class-based projections use JPA’s constructor expression mechanism to create DTO instances – no interface proxy is created. The response model is completely decoupled from the entity.&lt;/p&gt;&lt;pre&gt;data class CompanySummaryDto(&lt;br&gt;    val id: UUID,&lt;br&gt;    val name: String,&lt;br&gt;    val countryCode: String,&lt;br&gt;    val status: CompanyStatus&lt;br&gt;)&lt;/pre&gt;&lt;pre&gt;@Query(&lt;br&gt;    &amp;quot;&amp;quot;&amp;quot;&lt;br&gt;    select new dev.protsenko.jpa.CompanySummaryDto(c.id, c.name, c.countryCode, c.status)&lt;br&gt;    from Company c&lt;br&gt;    where c.countryCode = :countryCode&lt;br&gt;    &amp;quot;&amp;quot;&amp;quot;&lt;br&gt;)&lt;br&gt;fun findCompanySummaries(@Param(&amp;quot;countryCode&amp;quot;) countryCode: String): List&amp;lt;CompanySummaryDto&amp;gt;&lt;/pre&gt;&lt;p&gt;Trade-offs:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;The fully qualified class name in the JPQL string (new dev.protsenko.jpa.CompanySummaryDto(...)) is a plain string – the compiler does not validate it. With the default eager repository bootstrap, a wrong class name or mismatched constructor fails at application startup during query validation. Only under BootstrapMode.LAZY is this deferred to first interaction. IDEs like IntelliJ IDEA can resolve the reference and warn you, but it is not a compile-time guarantee.&lt;/li&gt;&lt;li&gt;Constructor argument order is not validated by the compiler. The new expression maps arguments positionally – a same-type positional swap (for example, name and countryCode both being String) passes all validation but silently assigns wrong values to wrong fields. Always keep the JPQL argument list and the constructor parameter list in sync, and cover the mapping with a test that asserts specific field values.&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;When to use:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;Public API contracts where you need an explicit, stable response model that can evolve independently from the entity (for example, introducing CompanySummaryDtoV2 for a new API version without touching the original).&lt;/li&gt;&lt;li&gt;Cases where you want the mapping to be explicit and visible in code — for example, when field names don’t match or you need to transform values (like combining firstName and lastName into fullName).&lt;/li&gt;&lt;/ul&gt;&lt;h3&gt;Dynamic projection (choose shape at call site)&lt;/h3&gt;&lt;p&gt;A dynamic projection lets you reuse one query method and decide the result shape at the call site by passing a Class&amp;lt;T&amp;gt; parameter. Spring Data JPA detects the Class&amp;lt;T&amp;gt;argument at startup, and at each invocation reads the actual class value to choose how to transform the result.&lt;/p&gt;&lt;pre&gt;interface CompanyRepository : JpaRepository&amp;lt;Company, UUID&amp;gt; {&lt;br&gt;    fun &amp;lt;T&amp;gt; findByCountryCode(countryCode: String, type: Class&amp;lt;T&amp;gt;): List&amp;lt;T&amp;gt;&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;All three shapes work from the same method:&lt;/p&gt;&lt;pre&gt;// Interface-based projection - SQL fetches only declared columns&lt;br&gt;val summaries = repo.findByCountryCode(&amp;quot;DE&amp;quot;, CompanySummaryView::class.java)&lt;/pre&gt;&lt;pre&gt;// DTO (data class) - selects only DTO constructor properties; no interface proxy&lt;br&gt;val items = repo.findByCountryCode(&amp;quot;DE&amp;quot;, CompanySummaryDto::class.java)&lt;/pre&gt;&lt;pre&gt;// Full entity - all columns fetched&lt;br&gt;val entities = repo.findByCountryCode(&amp;quot;DE&amp;quot;, Company::class.java)&lt;/pre&gt;&lt;p&gt;Important distinction: both interface and DTO projections reduce SQL columns in derived queries — only the entity shape selects all entity columns. If column reduction matters and the derived query shape is insufficient, use an explicit field-selecting @Query with an interface or DTO projection.&lt;/p&gt;&lt;p&gt;The ::class.java syntax is a Java idiom. In Kotlin you can hide it with a reified inline extension:&lt;/p&gt;&lt;pre&gt;inline fun &amp;lt;reified T&amp;gt; CompanyRepository.findByCountryCode(countryCode: String): List&amp;lt;T&amp;gt; =&lt;br&gt;    findByCountryCode(countryCode, T::class.java)&lt;/pre&gt;&lt;p&gt;Kotlin note: member functions shadow extension functions when the signatures match. If the repository already declares findByCountryCode(String): List&amp;lt;Company&amp;gt;, calling repo.findByCountryCode(&amp;quot;DE&amp;quot;) without a type argument resolves to the member. Provide an explicit type argument so the compiler picks the reified extension:&lt;/p&gt;&lt;pre&gt;// Explicit type argument required - member would otherwise shadow the extension&lt;br&gt;val summaries = repo.findByCountryCode&amp;lt;CompanySummaryView&amp;gt;(&amp;quot;DE&amp;quot;)&lt;br&gt;val entities  = repo.findByCountryCode&amp;lt;Company&amp;gt;(&amp;quot;DE&amp;quot;)&lt;/pre&gt;&lt;p&gt;When to use:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;Same filter logic needed by multiple callers that each require a different result shape — for example, a list API (summary projection), a detail API (full entity), and an event publisher (specific fields).&lt;/li&gt;&lt;li&gt;Internal service utilities or generic base services where the caller decides the shape.&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;When not to use:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;Public API controllers — prefer explicit return types for stable, documentable contracts.&lt;/li&gt;&lt;li&gt;Queries with joins, aggregations, or complex logic where different result shapes actually require different SQL — one query cannot serve all shapes efficiently.&lt;/li&gt;&lt;li&gt;Native queries — dynamic Class&amp;lt;T&amp;gt; works when native result columns match DTO constructor arguments in order and type. For mismatched or transformed results, use @NativeQuery(resultSetMapping = …) with @SqlResultSetMapping.&lt;/li&gt;&lt;/ul&gt;&lt;h3&gt;DTO mapping approaches beyond projections&lt;/h3&gt;&lt;p&gt;When projections are not enough — for example, when you need to map write requests to entities, combine data from multiple sources, or produce complex nested response shapes — you need a mapper.&lt;/p&gt;&lt;h4&gt;Handwritten extension functions&lt;/h4&gt;&lt;p&gt;The idiomatic Kotlin approach is a plain extension function on the entity. No dependencies, no configuration, full null safety enforced by the compiler, and trivial to debug. This should be your default choice for most Kotlin projects.&lt;/p&gt;&lt;pre&gt;fun Company.toSummaryDto() = CompanySummaryDto(&lt;br&gt;    id = requireNotNull(id),&lt;br&gt;    name = name,&lt;br&gt;    countryCode = countryCode,&lt;br&gt;    status = status&lt;br&gt;)&lt;/pre&gt;&lt;p&gt;For write-path mapping (request → entity), an extension function works the same way:&lt;/p&gt;&lt;pre&gt;fun CreateCompanyRequest.toEntity() = Company(&lt;br&gt;    name = name,&lt;br&gt;    countryCode = countryCode&lt;br&gt;)&lt;/pre&gt;&lt;p&gt;If you need Spring injection (for example, when a mapper depends on another service), group mappings in a @Component class instead:&lt;/p&gt;&lt;pre&gt;@Component&lt;br&gt;class CompanyMapper {&lt;br&gt;    fun toSummaryDto(company: Company) = CompanySummaryDto(&lt;br&gt;        id = requireNotNull(company.id),&lt;br&gt;        name = company.name,&lt;br&gt;        countryCode = company.countryCode,&lt;br&gt;        status = company.status&lt;br&gt;    )&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;When to use:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;Recommended default for most Kotlin projects regardless of size.&lt;/li&gt;&lt;li&gt;When mappings contain non-trivial logic that a code generator cannot express cleanly.&lt;/li&gt;&lt;li&gt;When you want zero extra dependencies and full compiler-enforced null safety.&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;If extension functions produce too much boilerplate in a large codebase, Konvert is the next step.&lt;/p&gt;&lt;h4&gt;Konvert&lt;/h4&gt;&lt;p&gt;Konvert is a compile-time code generator built on KSP (Kotlin Symbol Processing). It generates actual Kotlin extension functions and fully respects Kotlin null safety.&lt;/p&gt;&lt;pre&gt;plugins {&lt;br&gt;    id(&amp;quot;com.google.devtools.ksp&amp;quot;) version &amp;quot;2.2.21-2.0.5&amp;quot; // match your Kotlin version&lt;br&gt;}&lt;/pre&gt;&lt;pre&gt;dependencies {&lt;br&gt;    implementation(&amp;quot;io.mcarle:konvert-api:4.4.0&amp;quot;)&lt;br&gt;    ksp(&amp;quot;io.mcarle:konvert:4.4.0&amp;quot;)&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;Annotate the entity with @KonvertTo and Konvert generates the extension function automatically:&lt;/p&gt;&lt;pre&gt;@KonvertTo(CompanySummaryDto::class)&lt;br&gt;class Company(...)&lt;/pre&gt;&lt;pre&gt;// Generated: Company.toCompanySummaryDto()&lt;br&gt;val dto = company.toCompanySummaryDto()&lt;/pre&gt;&lt;p&gt;When to use:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;When extension functions produce too much boilerplate across many entities and DTOs.&lt;/li&gt;&lt;li&gt;When starting a new Kotlin project and want a code generator that generates idiomatic Kotlin output.&lt;/li&gt;&lt;/ul&gt;&lt;h4&gt;Worth to know: MapStruct&lt;/h4&gt;&lt;p&gt;MapStruct is a well-known mapping library in the Java/Spring ecosystem and you will see it in many Spring Boot projects. It generates mapper implementations at compile time from annotated interfaces and has strong IDE support in IntelliJ IDEA.&lt;/p&gt;&lt;p&gt;However, for Kotlin projects there are two reasons to think twice before adopting it. First, MapStruct is a Java annotation processor, so Kotlin projects commonly run it through kapt. kapt is in maintenance mode, while KSP is the Kotlin-first processing API and the better default for new Kotlin code generators. Second, MapStruct 1.6.x (current stable) processes Kotlin projects by generating Java source files, so Kotlin null safety and data class semantics have historically been weaker in the generated output. MapStruct 1.7.0.Beta1 significantly improves Kotlin data class support via kotlin-metadata-jvm, but KSP support is not yet available in any stable release.&lt;/p&gt;&lt;p&gt;If you are maintaining a large Java codebase that is gradually migrating to Kotlin, MapStruct is a reasonable tool to keep. For new Kotlin projects, prefer handwritten extension functions or Konvert.&lt;/p&gt;&lt;h3&gt;At the end&lt;/h3&gt;&lt;p&gt;I hope this article will be helpful to you; it’s one of the largest materials I’ve ever prepared. If you like it, follow me on &lt;a href=&quot;https://www.linkedin.com/in/protsenkodev/&quot;&gt;LinkedIn&lt;/a&gt;.&lt;/p&gt;&lt;p&gt;Other articles in the series:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;&lt;a href=&quot;https://protsenko.dev/spring-data-jpa-best-practices-entity-design-guide/&quot;&gt;Spring Data JPA Best Practices: Entity Design Guide&lt;/a&gt;&lt;/li&gt;&lt;li&gt;&lt;a href=&quot;https://protsenko.dev/spring-data-jpa-best-practices-repositories-design-guide/&quot;&gt;Spring Data JPA Best Practices: Repositories Design Guide&lt;/a&gt;&lt;/li&gt;&lt;li&gt;&lt;a href=&quot;https://protsenko.dev/spring-data-jpa-best-practices-transactions-and-manual-queries/&quot;&gt;Spring Data JPA Transactions and Queries&lt;/a&gt;&lt;/li&gt;&lt;/ul&gt;&lt;img src=&quot;https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=5804593a28f0&quot; width=&quot;1&quot; height=&quot;1&quot; alt=&quot;&quot;&gt;&lt;hr&gt;&lt;p&gt;&lt;a href=&quot;https://levelup.gitconnected.com/spring-data-jpa-with-kotlin-entities-repositories-and-pitfalls-5804593a28f0&quot;&gt;Spring Data JPA with Kotlin: Entities, Repositories, and Pitfalls&lt;/a&gt; was originally published in &lt;a href=&quot;https://levelup.gitconnected.com&quot;&gt;Level Up Coding&lt;/a&gt; on Medium, where people are continuing the conversation by highlighting and responding to this story.&lt;/p&gt;</content:encoded>
    </item>
    <item>
      <title>Migrate to RubyLLM: The Exact Path Off Provider Lock-In</title>
      <link>https://levelup.gitconnected.com/migrate-to-rubyllm-the-exact-path-off-provider-lock-in-58b47c1cc2b7?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/migrate-to-rubyllm-the-exact-path-off-provider-lock-in-58b47c1cc2b7?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Wed, 17 Jun 2026 16:21:37 GMT</pubDate>
      <content:encoded>&lt;div class=&quot;medium-feed-item&quot;&gt;&lt;p class=&quot;medium-feed-image&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/migrate-to-rubyllm-the-exact-path-off-provider-lock-in-58b47c1cc2b7?source=rss----5517fd7b58a6---4&quot;&gt;&lt;img src=&quot;https://cdn-images-1.medium.com/max/1536/1*1F9CNx3i5SW8Cstt0wvPgg.png&quot; width=&quot;1536&quot;&gt;&lt;/a&gt;&lt;/p&gt;&lt;p class=&quot;medium-feed-snippet&quot;&gt;The single-provider trap most Rails apps shipped into in 2024, and the swap to one API across GPT, Claude, Gemini, and local models&lt;/p&gt;&lt;p class=&quot;medium-feed-link&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/migrate-to-rubyllm-the-exact-path-off-provider-lock-in-58b47c1cc2b7?source=rss----5517fd7b58a6---4&quot;&gt;Continue reading on Level Up Coding »&lt;/a&gt;&lt;/p&gt;&lt;/div&gt;</content:encoded>
    </item>
    <item>
      <title>What is Loop Engineering? How it is different than Harness Engineering?</title>
      <link>https://levelup.gitconnected.com/what-is-loop-engineering-how-it-is-different-than-harness-engineering-0e764f373fb1?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/what-is-loop-engineering-how-it-is-different-than-harness-engineering-0e764f373fb1?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Wed, 17 Jun 2026 16:21:26 GMT</pubDate>
      <content:encoded>&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*ZDAvYJULsH_SMunyFS4q8Q.png&quot; /&gt;&lt;figcaption&gt;Image by author&lt;/figcaption&gt;&lt;/figure&gt;&lt;h4&gt;&lt;em&gt;The AI industry loves renaming old patterns. But this time, something genuinely shifted. &lt;/em&gt;A Forward Deployed Engineer’s perspective on what enterprises actually need before they start building loops.&lt;/h4&gt;&lt;p&gt;You’ve probably read something like this recently:&lt;/p&gt;&lt;blockquote&gt;&lt;em&gt;“You shouldn’t be prompting coding agents anymore. You should be designing loops that prompt your agents.” — Peter Steinberger&lt;/em&gt;&lt;/blockquote&gt;&lt;p&gt;And your first reaction was probably: wait, isn’t this just a cron job that calls an LLM?&lt;/p&gt;&lt;p&gt;That’s a fair reaction. And it’s also where the conversation usually stops, which is the problem.&lt;/p&gt;&lt;p&gt;In my previous post, I argued that agent harness engineering is 90% systems design we’ve always known, applied to a new substrate. The remaining 10% is genuinely novel because you’re now wrapping a non-deterministic core. That framing still holds. But loop engineering sits one level above the harness, and the difference is worth unpacking carefully before you dismiss it as marketing vocabulary.&lt;/p&gt;&lt;p&gt;Let me explain what it actually is, where the skepticism is justified, and where something real is happening.&lt;/p&gt;&lt;h3&gt;What is loop engineering?&lt;/h3&gt;&lt;p&gt;Here’s the cleanest definition, from &lt;a href=&quot;https://x.com/steipete&quot;&gt;Peter Steinberger&lt;/a&gt;:&lt;/p&gt;&lt;blockquote&gt;&lt;em&gt;“Loop engineering is replacing yourself as the person who prompts the agent. You design the system that does it instead.”&lt;/em&gt;&lt;/blockquote&gt;&lt;p&gt;Boris Cherny, who leads Claude Code at Anthropic, said something almost identical: “I don’t prompt Claude anymore. I have loops running that prompt Claude and figuring out what to do. My job is to write loops.”&lt;/p&gt;&lt;p&gt;When I first read this, my mind went to the same place yours probably did: reactive workflows. Event-driven pipelines. Cron jobs with smarter jobs inside. The shape is familiar.&lt;/p&gt;&lt;p&gt;The difference is what’s inside the loop.&lt;/p&gt;&lt;p&gt;A traditional cron job executes fixed steps. It runs a SQL query, writes to a file, sends an email. The steps are deterministic and defined at write time. You know exactly what it will do.&lt;/p&gt;&lt;p&gt;A loop in the sense Steinberger and Cherny are describing executes steps that are determined at runtime by the model. The loop says “triage yesterday’s CI failures” and the model decides what counts as a failure worth acting on, how to prioritize, what fix to attempt, and when to stop. The loop’s behavior is shaped by context the author never explicitly programmed.&lt;/p&gt;&lt;p&gt;That’s a real difference. Not revolutionary. But real.&lt;/p&gt;&lt;h3&gt;Where the skepticism lands correctly&lt;/h3&gt;&lt;p&gt;Here’s what is fair to be skeptical about.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;The terminology is inflated.&lt;/strong&gt; Every three months there’s a new compound noun for a pattern you already know. Context engineering, agent harness, agentic workflows, loop engineering. Some of this is genuine vocabulary evolution. Some of it is SEO and conference talk titles. The reasonable default is to distrust the word until you understand the concept underneath it.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;The cost question is serious.&lt;/strong&gt; A loop that runs on a schedule, spawns parallel sub-agents, and iterates until a goal is met will burn tokens at a rate that varies wildly depending on how you’ve designed it. For most teams, writing a simple Python script that checks deployment status is better than a loop that pays a frontier model to think about it every 15 minutes. The capability does not justify the spend unless the problem actually requires dynamic judgment at runtime.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;The demos are carefully chosen.&lt;/strong&gt; The examples used to illustrate loop engineering, daily CI triage, commit briefings, bug hunting from last week’s commits, are exactly the cases where the dynamic judgment of the model adds value. The impression you’re left with is that loops are universally better than scripts. They’re not. They’re situationally better, when the work requires reasoning that can’t be pre-specified.&lt;/p&gt;&lt;h3&gt;How harness engineering and loop engineering actually relate&lt;/h3&gt;&lt;p&gt;This is the part that most explanations gloss over.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Harness engineering&lt;/strong&gt; is about the execution environment one agent runs inside. It handles context management, tool permissions, retries, logging, state persistence across calls. The harness is what keeps a single agent from hallucinating a tool call, forgetting its goal 40 turns in, or silently producing wrong output with high confidence. If you’ve built real backend systems, you already know 80% of what a harness does.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/878/0*9wambhgpl7EwiBmX.png&quot; /&gt;&lt;figcaption&gt;Image : &lt;a href=&quot;https://medium.com/gitconnected/agent-harness-is-just-system-design-with-a-new-name-d91be4a648c5&quot;&gt;https://medium.com/gitconnected/agent-harness-is-just-system-design-with-a-new-name-d91be4a648c5&lt;/a&gt;&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;&lt;strong&gt;Loop engineering&lt;/strong&gt; is about orchestrating multiple agents and runs over time. It answers questions like: Who finds the work? Who does the work? Who checks the work? How does tomorrow’s run know what today’s run finished? The loop is the control plane above the harness.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*1dapCK4UWAeiZGmsHZVAcQ.png&quot; /&gt;&lt;figcaption&gt;Image by author&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;Think of it this way. A harness is what a single agent lives inside. A loop is what decides when to spin up an agent, what to give it, and what to do with what it returns.&lt;/p&gt;&lt;p&gt;You need both, and they solve different problems.&lt;/p&gt;&lt;p&gt;The five structural pieces of a loop from Addy’s &lt;a href=&quot;https://addyosmani.com/blog/loop-engineering/&quot;&gt;blog&lt;/a&gt; are:&lt;/p&gt;&lt;ol&gt;&lt;li&gt;&lt;strong&gt;Automations&lt;/strong&gt;: scheduled or event-triggered prompts that surface work. In Claude Code, that’s /loop, /goal, and scheduled tasks. In the Codex app, it&amp;#39;s the Automations tab with a Triage inbox.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Worktrees&lt;/strong&gt;: isolation for parallel agents. Two agents writing the same file at the same time is the same failure mode as two engineers committing to the same lines with no coordination.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Skills&lt;/strong&gt;: project knowledge written down once, outside the conversation, so the agent doesn’t re-derive your conventions from scratch every run. I wrote about this separately as SKILL.md.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Connectors&lt;/strong&gt;: Model Context Protocol (MCP) integrations that let the loop act inside your real environment, opening PRs, updating tickets, pinging channels.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Sub-agents&lt;/strong&gt;: the maker/checker split. The agent that wrote the code is not the one who grades it.&lt;/li&gt;&lt;/ol&gt;&lt;p&gt;The sixth thing, which is the spine of the whole structure, is state. A markdown file. A Linear board. Anything that lives outside the conversation and holds what’s been tried, what passed, what’s still open. The model forgets between runs. The repo doesn’t.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/562/1*WL7wJqZN7PBi81gSMlsTtw@2x.png&quot; /&gt;&lt;figcaption&gt;Image showing how &lt;a href=&quot;https://code.claude.com/docs/en/scheduled-tasks&quot;&gt;/loop &lt;/a&gt;works in Claude Code&lt;/figcaption&gt;&lt;/figure&gt;&lt;h3&gt;The /goal primitive is the most interesting part&lt;/h3&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*ZUIWo5f9mSgiy3mQliL6Gw.png&quot; /&gt;&lt;figcaption&gt;Image by Author&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;Most of the loop machinery is familiar enough. But there’s one primitive that doesn’t have a clean prior-art analogy, and it’s worth paying attention to.&lt;/p&gt;&lt;p&gt;Both Claude Code and Codex expose a /goal command. You give it a verifiable stopping condition: &amp;quot;all tests in test/auth pass and lint is clean.&amp;quot; The loop keeps running until that condition is true. After every turn, a separate smaller model evaluates whether the goal is met, so the agent that did the work isn&amp;#39;t the one grading it.&lt;/p&gt;&lt;p&gt;That maker/checker split applied to the stop condition itself is genuinely new design territory. It’s not a cron job. A cron job stops when the script exits. This stops when an independent judge confirms the result meets the spec.&lt;/p&gt;&lt;p&gt;Whether that’s worth the token cost depends entirely on the problem. For “does CI pass,” write a bash check. For “is this PR safe to merge given our security conventions,” the model judgment might be worth paying for.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/570/1*umpEicH1ENnRvr1JSNuxYA@2x.png&quot; /&gt;&lt;figcaption&gt;Image showing how &lt;a href=&quot;https://code.claude.com/docs/en/goal&quot;&gt;/goal &lt;/a&gt;works in Claude Code&lt;/figcaption&gt;&lt;/figure&gt;&lt;h3&gt;The honest verdict&lt;/h3&gt;&lt;p&gt;Loop engineering is not a fundamentally new paradigm. It’s an orchestration pattern built on top of agent harness primitives, applied to the problem of autonomous multi-agent work over time.&lt;/p&gt;&lt;p&gt;What’s new is the /goal primitive and the maker/checker split on stopping conditions. That&amp;#39;s a real design contribution that doesn&amp;#39;t map cleanly to prior reactive workflow patterns.&lt;/p&gt;&lt;h4&gt;What’s inflated is the framing that you should stop prompting agents and start designing loops as a general principle. It’s the right move for a specific class of problems.&lt;strong&gt; It’s wasteful for most of the things people will immediately try to apply it to.&lt;/strong&gt;&lt;/h4&gt;&lt;p&gt;The analogy that holds best: you wouldn’t replace every function call with a microservice just because microservices exist. You use them where the scaling problem justifies the complexity. Loops are the same.&lt;/p&gt;&lt;p&gt;Build the loop where the problem demands it. Know your harness well enough to see when a simpler solution is the right one.&lt;/p&gt;&lt;h3&gt;From verdict to production&lt;/h3&gt;&lt;p&gt;Loop engineering is new enough that there are no battle-tested playbooks yet. Most teams trying it are still figuring out where it actually helps versus where a simpler script would have been fine.&lt;/p&gt;&lt;p&gt;From a Forward Deployed Engineer’s perspective, the honest advice is: don’t start with a loop. Start by identifying one recurring workflow where the decision logic is genuinely ambiguous at runtime, something a script can’t pre-specify. Run it manually with an agent a few times. Watch where it fails, where it needs judgment, where it produces something you wouldn’t trust without review. That observation is your loop design. Also look out for token consumption, and check cost associated with it.&lt;/p&gt;&lt;p&gt;Every new industry buzzword pulls teams toward trying it before the groundwork is there. The demo stalls, or worse, it reaches production and fails.&lt;/p&gt;&lt;h3&gt;References&lt;/h3&gt;&lt;ol&gt;&lt;li&gt;Addy Osmani’s blog: &lt;a href=&quot;https://addyosmani.com/blog/loop-engineering/&quot;&gt;https://addyosmani.com/blog/loop-engineering/&lt;/a&gt;&lt;/li&gt;&lt;li&gt;&lt;a href=&quot;https://www.reddit.com/r/theprimeagen/comments/1tzrmoz/creator_of_claude_code_i_dont_write_prompts/&quot;&gt;https://www.reddit.com/r/theprimeagen/comments/1tzrmoz/creator_of_claude_code_i_dont_write_prompts/&lt;/a&gt;&lt;/li&gt;&lt;li&gt;My previous post: &lt;a href=&quot;https://medium.com/gitconnected/agent-harness-is-just-system-design-with-a-new-name-d91be4a648c5&quot;&gt;Agent harness is just system design with a new name&lt;/a&gt;&lt;/li&gt;&lt;li&gt;&lt;a href=&quot;https://docs.anthropic.com/en/docs/claude-code&quot;&gt;Claude Code documentation&lt;/a&gt;: &lt;a href=&quot;https://code.claude.com/docs/en/scheduled-tasks&quot;&gt;https://code.claude.com/docs/en/scheduled-tasks&lt;/a&gt;&lt;/li&gt;&lt;/ol&gt;&lt;img src=&quot;https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=0e764f373fb1&quot; width=&quot;1&quot; height=&quot;1&quot; alt=&quot;&quot;&gt;&lt;hr&gt;&lt;p&gt;&lt;a href=&quot;https://levelup.gitconnected.com/what-is-loop-engineering-how-it-is-different-than-harness-engineering-0e764f373fb1&quot;&gt;What is Loop Engineering? How it is different than Harness Engineering?&lt;/a&gt; was originally published in &lt;a href=&quot;https://levelup.gitconnected.com&quot;&gt;Level Up Coding&lt;/a&gt; on Medium, where people are continuing the conversation by highlighting and responding to this story.&lt;/p&gt;</content:encoded>
    </item>
  </channel>
</rss>