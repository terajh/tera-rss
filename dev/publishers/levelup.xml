<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>dev RSS - Level Up Coding</title>
    <link>https://levelup.gitconnected.com/</link>
    <description>Level Up Coding RSS 피드</description>
    <lastBuildDate>Wed, 24 Jun 2026 17:54:05 GMT</lastBuildDate>
    <item>
      <title>How to Create Loops with Claude Code: A Practical Guide to Agentic Automation</title>
      <link>https://levelup.gitconnected.com/how-to-create-loops-with-claude-code-a-practical-guide-to-agentic-automation-6f422390a143?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/how-to-create-loops-with-claude-code-a-practical-guide-to-agentic-automation-6f422390a143?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Wed, 24 Jun 2026 16:38:28 GMT</pubDate>
      <content:encoded>&lt;div class=&quot;medium-feed-item&quot;&gt;&lt;p class=&quot;medium-feed-image&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/how-to-create-loops-with-claude-code-a-practical-guide-to-agentic-automation-6f422390a143?source=rss----5517fd7b58a6---4&quot;&gt;&lt;img src=&quot;https://cdn-images-1.medium.com/max/1672/1*WbMmltJlf6xsaKxctAiUpg.png&quot; width=&quot;1672&quot;&gt;&lt;/a&gt;&lt;/p&gt;&lt;p class=&quot;medium-feed-snippet&quot;&gt;Claude Loop Engineering: How to Design Agents That Iterate, Verify, and Remember&lt;/p&gt;&lt;p class=&quot;medium-feed-link&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/how-to-create-loops-with-claude-code-a-practical-guide-to-agentic-automation-6f422390a143?source=rss----5517fd7b58a6---4&quot;&gt;Continue reading on Level Up Coding »&lt;/a&gt;&lt;/p&gt;&lt;/div&gt;</content:encoded>
    </item>
    <item>
      <title>Ghost Mode: Nmap Evasion Techniques That Actually Work</title>
      <link>https://levelup.gitconnected.com/ghost-mode-nmap-evasion-techniques-that-actually-work-0b59680b09ab?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/ghost-mode-nmap-evasion-techniques-that-actually-work-0b59680b09ab?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Wed, 24 Jun 2026 16:38:20 GMT</pubDate>
      <content:encoded>&lt;h4&gt;&lt;em&gt;Default Nmap scans are often easy to detect. Here’s what to send instead — and what each technique actually costs you.&lt;/em&gt;&lt;/h4&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*zT5Ok5TxYeHWNifZSI49GA.png&quot; /&gt;&lt;/figure&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;Series: Nmap — The Tool You Think You Know | Part 8 of 16&lt;/em&gt;&lt;/strong&gt;&lt;/blockquote&gt;&lt;p&gt;A default nmap -sS &amp;lt;TARGET&amp;gt; scan is about as subtle as a floodlight — many IDS/IPS platforms have signatures or heuristics for SYN scan patterns, most corporate firewalls log it, and some block it entirely.&lt;/p&gt;&lt;p&gt;This article covers what you’re actually evading, the techniques that address each detection method, and the honest trade-offs — because evasion always costs something, and knowing the price is part of using these flags correctly.&lt;/p&gt;&lt;h3&gt;What You’re Evading — IDS Detection Methods&lt;/h3&gt;&lt;p&gt;Before touching a single flag, understand what IDS systems actually look for:&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Port scan patterns&lt;/strong&gt; — many ports probed in rapid sequence from one IP. Statistical detection, not signature-based. Rate limiting and randomization address this.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Packet signatures&lt;/strong&gt; — Nmap’s default TTL values, TCP window sizes, IP ID patterns, and payload lengths are fingerprinted. Changing them breaks the signature match.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Timing&lt;/strong&gt; — too many probes, too fast trigger rate-based alerts. Slowing down is the simplest and most effective first step.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Flag combinations&lt;/strong&gt; — SYN floods, NULL packets, FIN packets, and Xmas tree packets all have dedicated IDS rules. Alternative scan types exploit RFC behavior instead of triggering these rules.&lt;/p&gt;&lt;p&gt;Understanding which method detects which behavior tells you which flag to reach for first.&lt;/p&gt;&lt;h3&gt;Bucket 1 — IDS Evasion&lt;/h3&gt;&lt;p&gt;Techniques that change how packets look or arrive so signature-based and statistical detection systems don’t recognize them as Nmap.&lt;/p&gt;&lt;h3&gt;Timing Evasion&lt;/h3&gt;&lt;p&gt;Timing evasion is the simplest and most effective first step. Slow down, and rate-based detection becomes irrelevant.&lt;/p&gt;&lt;pre&gt;sudo nmap -T1 --max-rate 5 -p 22,80,443 &amp;lt;TARGET_IP&amp;gt;&lt;/pre&gt;&lt;pre&gt;PORT    STATE  SERVICE&lt;br&gt;22/tcp  open   ssh&lt;br&gt;80/tcp  closed http&lt;br&gt;443/tcp closed https&lt;br&gt;Nmap done: 1 IP address (1 host up) scanned in 60.17 seconds&lt;/pre&gt;&lt;p&gt;The same three ports can take significantly longer depending on timing settings and network conditions — in this lab run, 60 seconds versus a typical sub-second default scan. That’s the cost of -T1. Statistical detection systems look for bursts — spreading probes across 60 seconds produces no burst signature.&lt;/p&gt;&lt;pre&gt;# Per-probe delay instead of rate limiting&lt;br&gt;sudo nmap --scan-delay 2s -p 22,80 &amp;lt;TARGET_IP&amp;gt;&lt;/pre&gt;&lt;pre&gt;PORT   STATE  SERVICE&lt;br&gt;22/tcp open   ssh&lt;br&gt;80/tcp closed http&lt;br&gt;Nmap done: 1 IP address (1 host up) scanned in 6.09 seconds&lt;/pre&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/839/1*k-qkvqcS61QNJYGDHMeYAQ.png&quot; /&gt;&lt;figcaption&gt;Timing evasion in practice — -T1 --max-rate 5 takes 60.17 seconds for three ports, --scan-delay 2s takes 6.09 seconds for two. Same results, drastically different timing profiles. Rate-based detection systems see no burst.&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;Two ports, 2-second delay between each probe — 6 seconds total. --scan-delay gives you precise control over the gap between individual probes rather than an overall rate cap.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Timing flags reference:&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;Flag Delay Use case -T0 (Paranoid) 5 minutes between probes. Maximum stealth is impractical for most engagements -T1 (Sneaky) 15 seconds between probes Practical stealth — slow but usable -T2 (Polite) 0.4 seconds between probes. Reduces network load, minimal stealth --scan-delay &amp;lt;time&amp;gt; Exact per-probe delay Precise control, combine with -T1 --max-rate &amp;lt;n&amp;gt; Max packets per second Rate cap — complement to timing flags&lt;/p&gt;&lt;p&gt;-T1 with --max-rate 5 is the practical stealth baseline — evades most rate-based detection while remaining usable in a real engagement.&lt;/p&gt;&lt;h3&gt;Fragmentation&lt;/h3&gt;&lt;pre&gt;sudo nmap -f -p 22 &amp;lt;TARGET_IP&amp;gt;&lt;/pre&gt;&lt;pre&gt;PORT   STATE SERVICE&lt;br&gt;22/tcp open  ssh&lt;br&gt;Nmap done: 1 IP address (1 host up) scanned in 0.14 seconds&lt;/pre&gt;&lt;pre&gt;sudo nmap --mtu 16 -p 22 &amp;lt;TARGET_IP&amp;gt;&lt;/pre&gt;&lt;pre&gt;PORT   STATE SERVICE&lt;br&gt;22/tcp open  ssh&lt;br&gt;Nmap done: 1 IP address (1 host up) scanned in 0.12 seconds&lt;/pre&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/865/1*NAEg3ex4ZrbbBQ5JV0wFFg.png&quot; /&gt;&lt;figcaption&gt;-f and --mtu 16 fragment IP packets into smaller pieces before transmission. Port 22 remains correctly identified as open — the scan works, only the packet shape changes.&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;-f fragments IP packets into small pieces, making it harder for IDS systems to reassemble and inspect the full packet. --mtu sets a custom fragment size — must be a multiple of 8. Smaller values create more fragments and increase reassembly difficulty, though there is no strict list of &amp;quot;valid&amp;quot; values beyond that constraint.&lt;/p&gt;&lt;p&gt;Some older IDS systems reassemble fragments poorly and miss signatures buried across multiple packets. Modern IDS handles fragmentation correctly — this is less effective than it was in 2005, but still worth including in a combined evasion profile. It costs nothing in scan time and may bypass older signature rules.&lt;/p&gt;&lt;h3&gt;Bucket 2 — Firewall Bypass&lt;/h3&gt;&lt;p&gt;Techniques that exploit firewall rule logic rather than IDS signatures.&lt;/p&gt;&lt;h3&gt;Source Port Spoofing&lt;/h3&gt;&lt;pre&gt;sudo nmap -g 53 -p 22,80 &amp;lt;TARGET_IP&amp;gt;&lt;/pre&gt;&lt;pre&gt;PORT   STATE  SERVICE&lt;br&gt;22/tcp open   ssh&lt;br&gt;80/tcp closed http&lt;br&gt;Nmap done: 1 IP address (1 host up) scanned in 0.14 seconds&lt;/pre&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/792/1*riypyE5dU776Z8fe7phx0Q.png&quot; /&gt;&lt;figcaption&gt;-g 53 sends all probe packets from source port 53. Where source-port allowlist firewall rules exist, this can pass traffic that a standard scan cannot.&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;-g 53 sends all scan packets from source port 53. On networks with source-port allowlist rules — a rule permitting &amp;quot;traffic from port 53&amp;quot; (DNS responses) — this may let your scan through. -g 80 works the same way for HTTP allowlists. This is not a general IDS evasion technique and won&amp;#39;t help against modern stateful inspection, but it&amp;#39;s worth trying where source-port rules exist.&lt;/p&gt;&lt;p&gt;-g and --source-port are identical flags — use whichever you remember. This is a firewall bypass technique more than an IDS evasion technique.&lt;/p&gt;&lt;h3&gt;Packet Manipulation&lt;/h3&gt;&lt;h3&gt;--data-length — Change Packet Size&lt;/h3&gt;&lt;pre&gt;sudo nmap --data-length 25 -p 22 &amp;lt;TARGET_IP&amp;gt;&lt;/pre&gt;&lt;pre&gt;PORT   STATE SERVICE&lt;br&gt;22/tcp open  ssh&lt;br&gt;Nmap done: 1 IP address (1 host up) scanned in 0.12 seconds&lt;/pre&gt;&lt;p&gt;Appends 25 random bytes to each packet. IDS signature rules often match on specific payload lengths — Nmap’s default packets have predictable sizes. Adding random padding changes the size signature without altering scan behavior.&lt;/p&gt;&lt;h3&gt;--ttl — Custom Time-To-Live&lt;/h3&gt;&lt;pre&gt;sudo nmap --ttl 64 -p 22 &amp;lt;TARGET_IP&amp;gt;&lt;/pre&gt;&lt;pre&gt;PORT   STATE SERVICE&lt;br&gt;22/tcp open  ssh&lt;br&gt;Nmap done: 1 IP address (1 host up) scanned in 0.16 seconds&lt;/pre&gt;&lt;p&gt;Nmap’s default TTL values are fingerprinted. Setting --ttl 64 makes packets look like standard Linux traffic. --ttl 128 mimics Windows. Match the TTL to the network environment you&amp;#39;re operating in.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/784/1*dtcrbqQ4rBbtnmOYA649aw.png&quot; /&gt;&lt;figcaption&gt;--data-length 25 appends random bytes to change the payload size signature. --ttl 64 sets a custom TTL that blends with standard Linux traffic. Both flags leave results unchanged.&lt;/figcaption&gt;&lt;/figure&gt;&lt;h3&gt;--spoof-mac — MAC Address Spoofing&lt;/h3&gt;&lt;pre&gt;sudo nmap --spoof-mac Cisco -p 22 &amp;lt;TARGET_IP&amp;gt;   # Cisco vendor OUI&lt;br&gt;sudo nmap --spoof-mac 0 -p 22 &amp;lt;TARGET_IP&amp;gt;        # random MAC&lt;/pre&gt;&lt;p&gt;Local network only — MAC addresses don’t traverse routers. On a network where Cisco devices are trusted or where MAC-based filtering is in use, spoofing a Cisco OUI can bypass access controls. --spoof-mac 0 generates a completely random MAC for each scan.&lt;/p&gt;&lt;h3&gt;Bucket 3 — Source Concealment&lt;/h3&gt;&lt;p&gt;Techniques that obscure or hide which IP the scan is actually coming from.&lt;/p&gt;&lt;h3&gt;Decoy Scans&lt;/h3&gt;&lt;pre&gt;sudo nmap -D RND:5 -p 22 &amp;lt;TARGET_IP&amp;gt;&lt;/pre&gt;&lt;pre&gt;PORT   STATE SERVICE&lt;br&gt;22/tcp open  ssh&lt;br&gt;Nmap done: 1 IP address (1 host up) scanned in 0.12 seconds&lt;/pre&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/972/1*-GrrM6ktpkKylB2yRemCoQ.png&quot; /&gt;&lt;figcaption&gt;-D RND:5 generates five random decoy IPs sending packets alongside the real scan. The target receives packets associated with multiple source addresses — attribution becomes the analyst&amp;#39;s problem.&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;Five random decoy IPs send spoofed packets alongside your real packets. The target sees packets associated with multiple source IPs — the analyst has to identify which one is real.&lt;/p&gt;&lt;pre&gt;# Specific decoys with your IP positioned explicitly&lt;br&gt;sudo nmap -D 10.0.0.1,10.0.0.2,ME,10.0.0.3 &amp;lt;TARGET_IP&amp;gt;&lt;/pre&gt;&lt;p&gt;ME positions your real IP in the decoy list. Without ME, your IP still sends, but isn&amp;#39;t explicitly listed. The decoy IPs must be reachable — spoofed packets from unreachable addresses don&amp;#39;t reach the target, and the trick fails.&lt;/p&gt;&lt;h3&gt;Alternative Scan Types — Exploiting RFC Behavior&lt;/h3&gt;&lt;p&gt;This is where evasion gets interesting. NULL, FIN, and Xmas scans work by exploiting how RFC 793 defines TCP behavior — not by modifying packets, but by sending packets that well-behaved firewalls and IDS systems don’t expect.&lt;/p&gt;&lt;h3&gt;-sN — NULL Scan (no flags set)&lt;/h3&gt;&lt;pre&gt;sudo nmap -sN -p 22,80 &amp;lt;TARGET_IP&amp;gt;&lt;/pre&gt;&lt;pre&gt;PORT   STATE         SERVICE&lt;br&gt;22/tcp open|filtered ssh&lt;br&gt;80/tcp closed        http&lt;/pre&gt;&lt;h3&gt;-sF — FIN Scan&lt;/h3&gt;&lt;pre&gt;sudo nmap -sF -p 22,80 &amp;lt;TARGET_IP&amp;gt;&lt;/pre&gt;&lt;pre&gt;PORT   STATE         SERVICE&lt;br&gt;22/tcp open|filtered ssh&lt;br&gt;80/tcp closed        http&lt;/pre&gt;&lt;h3&gt;-sX — Xmas Scan (FIN + PSH + URG flags)&lt;/h3&gt;&lt;pre&gt;sudo nmap -sX -p 22,80 &amp;lt;TARGET_IP&amp;gt;&lt;/pre&gt;&lt;pre&gt;PORT   STATE         SERVICE&lt;br&gt;22/tcp open|filtered ssh&lt;br&gt;80/tcp closed        http&lt;/pre&gt;&lt;p&gt;All three return identical results — and those results require explanation.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/816/1*jSyfuXKfc2VyPy6o5eSJsA.png&quot; /&gt;&lt;figcaption&gt;NULL, FIN, and Xmas scans run back to back — all three return open|filtered on port 22 and closed on port 80. Different flag combinations, identical RFC 793 behavior. No SYN flag means no SYN-based IDS rule fires.&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;&lt;strong&gt;Reading &lt;/strong&gt;&lt;strong&gt;open|filtered:&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;RFC 793 states that a closed port must respond to unexpected TCP packets with an RST. An open port should ignore them — no response required. Nmap exploits this:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;closed port → RST received → port is definitively closed&lt;/li&gt;&lt;li&gt;No response → port is either open or filtered by a firewall — Nmap can’t tell which&lt;/li&gt;&lt;li&gt;Hence: open|filtered&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;&lt;strong&gt;Why the same result on all three:&lt;/strong&gt; NULL, FIN, and Xmas send different flag combinations but rely on the same RFC behavior. The target’s response logic is identical for all three.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;The Linux vs Windows difference:&lt;/strong&gt; Linux strictly follows RFC 793 — open ports return no response, giving open|filtered. Windows typically sends RST for all of them regardless of state, making these scans less informative against Windows targets. These scans are most informative on Unix/Linux systems and some network devices, though behavior varies by OS version and configuration.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;The IDS evasion angle:&lt;/strong&gt; Many IDS rules target SYN packets specifically. NULL, FIN, and Xmas scans contain no SYN flag — they bypass rules written for SYN-based detection. This is the reason they exist.&lt;/p&gt;&lt;h3&gt;--badsum — The IDS Diagnostic&lt;/h3&gt;&lt;p&gt;--badsum is the most misunderstood flag in Nmap&amp;#39;s evasion toolkit — it&amp;#39;s not a scan technique, it&amp;#39;s a diagnostic:&lt;/p&gt;&lt;pre&gt;sudo nmap --badsum -p 22,80 &amp;lt;TARGET_IP&amp;gt;&lt;/pre&gt;&lt;pre&gt;PORT   STATE    SERVICE&lt;br&gt;22/tcp filtered ssh&lt;br&gt;80/tcp filtered http&lt;/pre&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/791/1*hVuMyqqcwiil_qpAzwtYfg.png&quot; /&gt;&lt;figcaption&gt;--badsum sends packets with deliberately corrupt checksums. Both ports return filtered — the TCP stack dropped them correctly and nothing in the path responded. Clean baseline: no IDS processing packets before checksum validation.&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;--badsum sends packets with deliberately incorrect TCP/UDP checksums.&lt;/p&gt;&lt;p&gt;A real TCP/IP stack drops bad checksum packets immediately — corrupt packet, discard, no response. Nmap reports that silence as filtered. An IDS/IPS sitting in the path may process packets before checksum validation and respond anyway, which is the tell.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Reading this output:&lt;/strong&gt; Both ports returned filtered — No response received. The target&amp;#39;s TCP stack correctly dropped the bad checksum packets, and no device in the path responded in a way that indicates checksum processing before validation. This is the clean baseline.&lt;/p&gt;&lt;p&gt;If --badsum returns anything other than filtered Or silence, something in the path is processing packets before checksum validation — that&amp;#39;s your IDS signal. Adjust your evasion profile accordingly.&lt;/p&gt;&lt;h3&gt;The Idle/Zombie Scan — True Source Concealment&lt;/h3&gt;&lt;pre&gt;sudo nmap -sI &amp;lt;zombie_host&amp;gt; &amp;lt;TARGET_IP&amp;gt;&lt;/pre&gt;&lt;p&gt;Every other technique in this article still exposes your IP at some level. The idle scan doesn’t — the target never sees your IP. All packets appear to come from the zombie host.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;How it works:&lt;/strong&gt; Exploits the IP ID field. The zombie’s IP ID increments predictably. You probe the zombie’s IP ID before and after sending spoofed packets to the target — the change in IP ID tells you whether the target’s port is open or closed, without ever sending a packet from your IP.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Finding a zombie — &lt;/strong&gt;&lt;strong&gt;ipidseq:&lt;/strong&gt;&lt;/p&gt;&lt;pre&gt;sudo nmap --script ipidseq &amp;lt;TARGET_IP&amp;gt;&lt;/pre&gt;&lt;pre&gt;|_ipidseq: All zeros&lt;/pre&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/865/1*CsY3L97LZSMnb0xmn06_rQ.png&quot; /&gt;&lt;figcaption&gt;ipidseq returns All zeros — this host randomizes its IP ID values, making it unsuitable as an idle scan zombie. Modern Linux kernels do this by default, which is why usable zombies are rare on current networks.&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;All zeros — This host uses randomized IP IDs. Modern Linux kernels randomize IP IDs for external probes, returning all zeros or random values. Not a usable zombie.&lt;/p&gt;&lt;p&gt;A usable zombie returns:&lt;/p&gt;&lt;pre&gt;|_ipidseq: Incremental!&lt;/pre&gt;&lt;p&gt;Windows XP, some printers, and some older network devices still use sequential IP IDs. On modern networks, suitable zombies are rare — this is the most powerful technique available when you find one, and practically unavailable on hardened modern networks.&lt;/p&gt;&lt;h3&gt;The Ghost Profile — Everything Combined&lt;/h3&gt;&lt;pre&gt;sudo nmap -sS -T1 \&lt;br&gt;  -f --mtu 16 \&lt;br&gt;  -D RND:5 \&lt;br&gt;  -g 53 \&lt;br&gt;  --data-length 15 \&lt;br&gt;  --ttl 64 \&lt;br&gt;  --scan-delay 2s \&lt;br&gt;  -p 22,80 &amp;lt;TARGET_IP&amp;gt;&lt;/pre&gt;&lt;pre&gt;PORT   STATE    SERVICE&lt;br&gt;22/tcp open     ssh&lt;br&gt;80/tcp filtered http&lt;br&gt;Nmap done: 1 IP address (1 host up) scanned in 61.17 seconds&lt;/pre&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*1Hw6-gfmx68_G0AkRnU1Zg.png&quot; /&gt;&lt;figcaption&gt;The full ghost profile — fragmentation, five decoys, source port 53, random padding, custom TTL, 2-second probe delay. Two ports. 61.17 seconds. That is the real cost of layered evasion.&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;Fragmented packets with custom MTU, five random decoys, source port 53, 15 bytes of random padding, TTL set to 64, and a 2-second delay between probes — two ports took 61 seconds.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;What each layer addresses:&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;Flag Evades -T1 --scan-delay 2s Rate-based and statistical detection -f --mtu 16 Signature-based reassembly detection -D RND:5 Source IP attribution -g 53 Firewall source-port rules --data-length 15 Payload length signatures --ttl 64 OS fingerprint signatures&lt;/p&gt;&lt;p&gt;&lt;strong&gt;The honest caveat:&lt;/strong&gt; 61 seconds for two ports. A full /24 Subnet scan at this profile takes hours. Modern enterprise EDR with behavioral analysis may still flag slow, methodical scanning. &amp;quot;Not detected by Snort&amp;quot; and &amp;quot;not detected by everything&amp;quot; are not the same thing. Use this profile when stealth matters more than speed — not as a default.&lt;/p&gt;&lt;h3&gt;Evasion Technique Summary&lt;/h3&gt;&lt;p&gt;&lt;strong&gt;Bucket 1 — IDS Evasion&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;Technique What It Addresses Cost -T1 / --scan-delay Rate-based and statistical detection Speed — significantly slower -f / --mtu Fragmentation-blind IDS Minimal — slight overhead --data-length Payload size signatures None --ttl OS fingerprint signatures None -sN / -sF / -sX SYN-based IDS rules: Ambiguous results — varies by OS --badsum IDS diagnostic (not a scan). Not a scan technique&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Bucket 2 — Firewall Bypass&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;Technique What It Addresses Cost -g &amp;lt;port&amp;gt; Source-port allowlist rules None — limited to rule-based firewalls --spoof-mac MAC-based filtering, Local network only&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Bucket 3 — Source Concealment&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;Technique What It Addresses Cost -D RND:n Source IP attribution Minimal — slight overhead -sI &amp;lt;zombie&amp;gt; Full source concealment, rare usable zombies on modern networks&lt;/p&gt;&lt;h3&gt;What’s Next&lt;/h3&gt;&lt;p&gt;&lt;em&gt;You now have a toolkit for scanning quietly — timing, fragmentation, decoys, alternative scan types, and the &lt;/em&gt;&lt;em&gt;--badsum diagnostic to know whether any of it matters on a given network. In &lt;/em&gt;&lt;strong&gt;&lt;em&gt;Part 9&lt;/em&gt;&lt;/strong&gt;&lt;em&gt;, we go further — scanning networks you can&amp;#39;t directly reach, through a pivot, using proxychains. The flags are different, the limitations are real, and the technique is something most eJPT learners don&amp;#39;t encounter until mid-engagement.&lt;/em&gt;&lt;/p&gt;&lt;p&gt;&lt;em&gt;Part 9← Link will be live once published&lt;/em&gt;&lt;/p&gt;&lt;blockquote&gt;&lt;em&gt;🔒 &lt;/em&gt;&lt;strong&gt;&lt;em&gt;Legal reminder:&lt;/em&gt;&lt;/strong&gt;&lt;em&gt; Evasion techniques are for authorized engagements only. Using them against systems you don’t own is illegal regardless of whether detection is evaded — and evasion itself may constitute an aggravating factor in prosecution.&lt;/em&gt;&lt;/blockquote&gt;&lt;p&gt;&lt;em&gt;Part of the series: &lt;/em&gt;&lt;strong&gt;&lt;em&gt;Nmap — The Tool You Think You Know&lt;/em&gt;&lt;/strong&gt;&lt;/p&gt;&lt;img src=&quot;https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=0b59680b09ab&quot; width=&quot;1&quot; height=&quot;1&quot; alt=&quot;&quot;&gt;&lt;hr&gt;&lt;p&gt;&lt;a href=&quot;https://levelup.gitconnected.com/ghost-mode-nmap-evasion-techniques-that-actually-work-0b59680b09ab&quot;&gt;Ghost Mode: Nmap Evasion Techniques That Actually Work&lt;/a&gt; was originally published in &lt;a href=&quot;https://levelup.gitconnected.com&quot;&gt;Level Up Coding&lt;/a&gt; on Medium, where people are continuing the conversation by highlighting and responding to this story.&lt;/p&gt;</content:encoded>
    </item>
    <item>
      <title>Building My Own Local “Claude Code”: What I Learned Demystifying Agentic Coding under the Hood</title>
      <link>https://levelup.gitconnected.com/building-my-own-local-claude-code-what-i-learned-demystifying-agentic-coding-under-the-hood-8772874b91b8?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/building-my-own-local-claude-code-what-i-learned-demystifying-agentic-coding-under-the-hood-8772874b91b8?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Wed, 24 Jun 2026 16:38:11 GMT</pubDate>
      <content:encoded>&lt;p&gt;When tools like &lt;strong&gt;Claude Code&lt;/strong&gt;, &lt;strong&gt;Aider&lt;/strong&gt;, &lt;strong&gt;Codex&lt;/strong&gt;, &lt;strong&gt;Antigravity&lt;/strong&gt;, &lt;strong&gt;Devin&lt;/strong&gt; and more started popping up, my initial reaction as an AI engineer wasn’t just &lt;em&gt;“Wow, this is cool”&lt;/em&gt; (which it is)&lt;em&gt;,&lt;/em&gt; it was&lt;em&gt; “How does this actually work under the hood, and where does it fall apart?”&lt;/em&gt;&lt;/p&gt;&lt;p&gt;If you’ve run agentic coding loops on cloud models, you know they are incredibly powerful. You also know they are token-eating monsters. A simple multi-file refactor can easily burn through your daily Anthropic API limits and cost you a premium in credit.&lt;/p&gt;&lt;p&gt;The obvious holy grail is to run these loops locally using free, open-weight 7B or 8B parameter models. &lt;em&gt;But how do you bridge the gap between a massive &lt;/em&gt;&lt;strong&gt;&lt;em&gt;frontier model&lt;/em&gt;&lt;/strong&gt;&lt;em&gt; like Claude Opus 4.8 and a &lt;/em&gt;&lt;strong&gt;&lt;em&gt;compact local 4B or 8B model&lt;/em&gt;&lt;/strong&gt;&lt;em&gt; running on your laptop?&lt;/em&gt;&lt;/p&gt;&lt;p&gt;Instead of just reading system prompts, I decided the best way to learn was hands-on: by building my own terminal-based coding assistant.&lt;/p&gt;&lt;p&gt;Enter &lt;strong&gt;ProtoAgent&lt;/strong&gt;.&lt;/p&gt;&lt;p&gt;To be absolutely honest: &lt;strong&gt;&lt;em&gt;ProtoAgent is not yet a polished, industry-grade product ready to replace your daily coding CLI tool or editor extensions&lt;/em&gt;&lt;/strong&gt;. It is an active, raw, curiosity-driven project I am building to explore the limits of local models.&lt;/p&gt;&lt;p&gt;&lt;em&gt;ProtoAgent is a laboratory for exploring what local coding models can and cannot do.&lt;/em&gt;&lt;/p&gt;&lt;p&gt;It is powered by &lt;strong&gt;ProtoLink, &lt;/strong&gt;a robust, production-oriented Python framework for &lt;strong&gt;&lt;em&gt;Agent-to-Agent (A2A)&lt;/em&gt;&lt;/strong&gt; communication that I’ve been developing and writing about and it serves as the perfect lab environment for testing how coding agents operate.&lt;/p&gt;&lt;p&gt;ProtoLink is actually an agentic framework for python that i’ve been building for the same reason, to examine how libraries like LangChain etc. operate under the hood. If you’re interested in that, in an earlier article, I used ProtoLink to build an &lt;a href=&quot;https://levelup.gitconnected.com/your-first-autonomous-agent-mesh-easier-than-you-think-ce697b3dd87a&quot;&gt;autonomous agent mesh&lt;/a&gt;. I later showed how to prototype a small &lt;a href=&quot;https://levelup.gitconnected.com/build-easily-your-own-claude-code-with-three-agents-brain-hands-and-coordinator-5236b392ddf0&quot;&gt;“Claude Code”-style system &lt;/a&gt;using Coordinator, Brain, and Hands agents.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;ProtoAgent&lt;/strong&gt; takes those experiments further.&lt;/p&gt;&lt;p&gt;The central lesson has been surprisingly consistent:&lt;/p&gt;&lt;blockquote&gt;Making small models useful is less about giving them more power and more about removing unnecessary choices.&lt;/blockquote&gt;&lt;p&gt;Here is what building ProtoAgent taught me about context overload, deterministic repository retrieval, Rust-to-Python communication, and human-in-the-loop execution.&lt;/p&gt;&lt;h4&gt;The Stack: Decoupling the Brain from the Face&lt;/h4&gt;&lt;p&gt;I wanted a snappy terminal UI, but I also wanted the flexibility to iterate on the AI logic without recompiling my code every five minutes. I ended up with a hybrid architecture:&lt;/p&gt;&lt;p&gt;The ProtoAgent &lt;strong&gt;monorepo&lt;/strong&gt;.&lt;br&gt;📂 protoagent/&lt;br&gt; ┣ 📂 &lt;strong&gt;core&lt;/strong&gt;/ ← The Brain: ProtoLink multi-agent orchestration (🐍 Python)&lt;br&gt; ┣ 📂 &lt;strong&gt;cli&lt;/strong&gt;/ ← The Terminal Face: Snappy TUI &amp;amp; approvals wrapper (🦀 Rust)&lt;br&gt; ┗ 📂 acp/ ← The Editor Face: ACP server for IDE integration&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Rust&lt;/strong&gt; owns the &lt;strong&gt;terminal&lt;/strong&gt; experience: rendering, keyboard input, project selection, progress indicators, context meters, and approval dialogs.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Python&lt;/strong&gt; owns the &lt;strong&gt;agent logic&lt;/strong&gt;: model configuration, prompts, repository context, conversation state, agent routing, and tool policies.&lt;/p&gt;&lt;p&gt;The two are connected through &lt;strong&gt;PyO3&lt;/strong&gt;, allowing the Rust binary to invoke the Python core directly. Long-running Python calls execute on a blocking worker so that the asynchronous Rust interface remains responsive.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;ProtoLink&lt;/strong&gt; then runs the local agent mesh, using a registry and local task transports to connect the individual agents.&lt;/p&gt;&lt;p&gt;This separation gives me a fast interface without sacrificing python’s flexibility for experimenting with models and orchestration.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/800/1*g__ri3AVwTEI3Y0MEiwyZg.gif&quot; /&gt;&lt;figcaption&gt;The animation showcases a simple demo of the ProtoLink CLI handling a simple task. In the beginning I show how using the &lt;em&gt;/model&lt;/em&gt; command you can freely choose one of the supported models. There the Ollama Gemma e4b model was already loaded and I changed it to gpt-4o just to showcase that Model APIs are also supported. A simple &lt;strong&gt;Task&lt;/strong&gt; to&lt;em&gt; add docstrings to a specific tagged file (file &lt;/em&gt;referenced with an @ tag&lt;em&gt;)&lt;/em&gt; was sent to ProtoAgent. The first step is that the &lt;strong&gt;Context Loom&lt;/strong&gt; builds a focused evidence pack from the repository and then the &lt;strong&gt;task&lt;/strong&gt; and that &lt;strong&gt;context&lt;/strong&gt; are passed to the &lt;strong&gt;Architect agent&lt;/strong&gt;. The Architect knows which agents are available through &lt;strong&gt;ProtoLink’s A2A Registry &lt;/strong&gt;and it can ask the &lt;strong&gt;Explorer&lt;/strong&gt; for additional evidence when needed and &lt;strong&gt;delegate&lt;/strong&gt; file modifications to Coder. The Coder agent does not write immediately. Its tools construct a typed write action containing a unified-diff preview. ProtoLink’s capability policy pauses the action, and the Rust CLI asks the user to approve or deny the change before anything touches the filesystem. Source: Author&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;Now let’s go to the purpose of this article, the lessons learned…&lt;/p&gt;&lt;h4&gt;Lesson 1: The “God Prompt” is a Trap for Small Models&lt;/h4&gt;&lt;p&gt;When you build a coding assistant for a frontier model, the temptation is to write a massive system prompt, throw 20 tools at it (file reading, writing, grep, terminal execution), and let the model figure out the flow.&lt;/p&gt;&lt;p&gt;Compact local models often degrade quickly under that load.&lt;/p&gt;&lt;p&gt;If you try this with a local 7B or 8B model (like Qwen 3 Coder, Gemma 4 e4b, Llama 3), it crashes instantly. Small models suffer from context collapse when overwhelmed by complex XML tags, tool schemas, and multi-step instructions. They will hallucinate tool arguments, enter infinite exploration loops, or simply forget what you asked them to do.&lt;/p&gt;&lt;p&gt;To solve this, I relied on &lt;em&gt;ProtoLink’s Agent-to-Agent (A2A) &lt;/em&gt;specification. Instead of a single &lt;em&gt;“God Agent”&lt;/em&gt;, I broke the workflow down into a strict three-node topology:&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*vuCklFzQcidYpVo2mK0Lgg.png&quot; /&gt;&lt;figcaption&gt;Basic Overview of Agent Topology. Source: Author&lt;/figcaption&gt;&lt;/figure&gt;&lt;ol&gt;&lt;li&gt;&lt;strong&gt;The Architect (Orchestrator)&lt;/strong&gt;: The user-facing &lt;em&gt;coordinator&lt;/em&gt;. It parses user intent, breaks down the task and &lt;strong&gt;delegates&lt;/strong&gt; tasks to the other agents. It has no tools to touch the filesystem.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;The Explorer (Cartographer):&lt;/strong&gt; A &lt;em&gt;read-only&lt;/em&gt; agent. It has access to search, regex matching, directory listing, and file-reading tools. Its only job is to scan the project and compile a dense Markdown &lt;strong&gt;Context Map&lt;/strong&gt;.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;The Coder (Synthesis Engine)&lt;/strong&gt;: A hyper-focused model and deliberately narrow. It doesn’t know how to search or explore. It is handed the Explorer’s Context Map and the target task, and its only tool is to output clean file modifications. The Coder’s context and attention is focused on synthesis rather than navigation and tool selection.&lt;/li&gt;&lt;/ol&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;What I learned: &lt;/em&gt;&lt;/strong&gt;By stripping file-searching complexity from the Coder, 100% of its parameter weight is dedicated to writing code. If you want small models to succeed, you must strip away their choices.&lt;/blockquote&gt;&lt;blockquote&gt;&lt;strong&gt;Small models succeed through constraint, not maximal autonomy. &lt;/strong&gt;&lt;em&gt;If you want a small model to behave reliably, reduce the number of decisions it must make at each step.&lt;/em&gt;&lt;/blockquote&gt;&lt;h4&gt;Lesson 2: Deterministic Context beats LLM Search&lt;/h4&gt;&lt;p&gt;When I started, I thought, &lt;em&gt;”I’ll let the Explorer agent query the directory structure to find where the auth logic lives”, so t&lt;/em&gt;he first version of the architecture relied heavily on Explorer.&lt;/p&gt;&lt;p&gt;When a user asked about authentication, Explorer would search the repository, inspect directories, read candidate files, revise its search, and eventually produce a Context Map.&lt;/p&gt;&lt;p&gt;That worked, but it was slow.&lt;/p&gt;&lt;p&gt;Therefore this was a mistake. Watching a local 7B model run 5 consecutive tool calls just to find a file path is painful and slow. At local speeds (e.g. 20–30 tokens/sec), 5 tool roundtrips can take 40 seconds of pure waiting before the model even starts writing code! By the time it begins solving the task, much of the latency and context budget has already been spent on repository navigation.&lt;/p&gt;&lt;p&gt;I realized that the model shouldn’t be searching the filesystem at all. A database should. So I built &lt;strong&gt;Context Loom&lt;/strong&gt;, a deterministic SQLite-backed &lt;strong&gt;indexer&lt;/strong&gt;.&lt;/p&gt;&lt;p&gt;Before the agents run, &lt;strong&gt;Context Loom&lt;/strong&gt; indexes your project’s files, imports, symbols, and Markdown headings. The Context Loom, a local SQLite-backed repository smart indexer.&lt;/p&gt;&lt;p&gt;Before the agents run, Context Loom indexes project files and records information such as:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;File paths and language hints&lt;/li&gt;&lt;li&gt;Symbols&lt;/li&gt;&lt;li&gt;Imports&lt;/li&gt;&lt;li&gt;Markdown headings&lt;/li&gt;&lt;li&gt;Content fingerprints&lt;/li&gt;&lt;li&gt;Git working-tree state&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;When you type a query, it uses a simple token-matching scoring algorithm to score files, so context Loom extracts query terms and scores indexed files using transparent rules.:&lt;/p&gt;&lt;p&gt;For example:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;Explicitly tagged path: +40&lt;/li&gt;&lt;li&gt;Filename or path mentioned in the request: +24&lt;/li&gt;&lt;li&gt;File currently modified in Git: +8&lt;/li&gt;&lt;li&gt;Query term found in the path: +8&lt;/li&gt;&lt;li&gt;Symbol match: +7&lt;/li&gt;&lt;li&gt;Markdown heading match: +5&lt;/li&gt;&lt;li&gt;Import match: +4&lt;/li&gt;&lt;li&gt;Content match: +2&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;It packages the top results into a compact &lt;strong&gt;Context Pack &lt;/strong&gt;containing relevant code snippets and a human-readable &lt;strong&gt;Evidence Ledger&lt;/strong&gt; explaining why they were selected.&lt;/p&gt;&lt;blockquote&gt;User request&lt;br&gt; ↓&lt;br&gt;Context Loom builds the initial evidence pack&lt;br&gt; ↓&lt;br&gt;Architect plans the task&lt;br&gt; ↓&lt;br&gt;Explorer verifies or expands evidence when needed&lt;br&gt; ↓&lt;br&gt;Coder produces an approval-gated modification&lt;/blockquote&gt;&lt;p&gt;This resolves an important tension in the three-agent design.&lt;/p&gt;&lt;p&gt;Explorer still has search tools, but it is no longer expected to rediscover the repository from scratch on every run. Context Loom handles the predictable first pass; Explorer handles ambiguity.&lt;/p&gt;&lt;p&gt;In my testing, this has been substantially faster and more reliable than allowing a compact model to wander through the filesystem. More importantly, the process is inspectable. The user can see which files were selected and why.&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;What I learned:&lt;/em&gt;&lt;/strong&gt; Pre-weaving context &lt;strong&gt;deterministically&lt;/strong&gt; is a lot faster and more reliable in my testing than asking a local model to find its own way around a repository.&lt;/blockquote&gt;&lt;blockquote&gt;&lt;em&gt;For small models, context selection should be deterministic by default and agent-driven only when uncertainty remains.&lt;/em&gt;&lt;/blockquote&gt;&lt;h4&gt;Lesson 3: File-Based IPC is Surprisingly Awesome&lt;/h4&gt;&lt;p&gt;One of the trickiest parts of embedding Python in Rust via PyO3 is that Python runs synchronously inside a spawned thread. If the Python thread is busy waiting for a local LLM response, it blocks.&lt;/p&gt;&lt;p&gt;That call may remain busy for seconds or minutes while a local model generates a response or several agents coordinate. Meanwhile, the terminal must continue to:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;Display live progress&lt;/li&gt;&lt;li&gt;Update context-usage metrics&lt;/li&gt;&lt;li&gt;Render approval requests&lt;/li&gt;&lt;li&gt;Accept cancellation&lt;/li&gt;&lt;li&gt;Remain responsive to keyboard input&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;How do you show real-time progress events or support cancelling a run if the user hits &lt;strong&gt;&lt;em&gt;Esc&lt;/em&gt;&lt;/strong&gt;?&lt;/p&gt;&lt;p&gt;I considered sockets and more elaborate RPC layers for this boundary, but the simplest solution worked remarkably well: short-lived local files.&lt;/p&gt;&lt;p&gt;I avoided complex socket protocols or gRPC layers by implementing a simple, file-based IPC:&lt;/p&gt;&lt;ol&gt;&lt;li&gt;Rust creates a temporary JSONL progress path such as /tmp/protoagent-progress-&amp;lt;pid&amp;gt;-run.jsonl.&lt;/li&gt;&lt;li&gt;The Python core appends normalized ProtoLink run events to that file.&lt;/li&gt;&lt;li&gt;Rust polls for new events every 140 milliseconds while Python runs on a blocking worker.&lt;/li&gt;&lt;li&gt;Approval requests and cancellation signals use separate JSON control files.&lt;/li&gt;&lt;li&gt;Control-file writes are atomic, preventing Rust from reading partially written JSON.&lt;/li&gt;&lt;li&gt;When the run finishes, the temporary files are removed.&lt;/li&gt;&lt;/ol&gt;&lt;p&gt;When Coder requests a write, its action contains a unified diff. ProtoLink’s policy pauses execution and invokes the application approval handler.&lt;/p&gt;&lt;p&gt;Rust detects the request, suspends the spinner, renders the action and diff, and asks:&lt;/p&gt;&lt;pre&gt;Authorize this ProtoLink action? [y/N]&lt;/pre&gt;&lt;p&gt;The default is intentionally “no.”&lt;/p&gt;&lt;p&gt;Rust writes a correlated approval decision, Python receives it, and ProtoLink either executes or rejects the action.&lt;/p&gt;&lt;p&gt;A simplified version of the Rust loop looks like this:&lt;/p&gt;&lt;pre&gt;let mut task = tokio::task::spawn_blocking(move || {&lt;br&gt;    call_process_prompt_with_progress(&lt;br&gt;        prompt,&lt;br&gt;        workspace,&lt;br&gt;        session_id,&lt;br&gt;        progress_path,&lt;br&gt;    )&lt;br&gt;});&lt;br&gt;&lt;br&gt;let json_result = loop {&lt;br&gt;    tokio::select! {&lt;br&gt;        result = &amp;amp;mut task =&amp;gt; {&lt;br&gt;            progress_events.extend(progress_file.read_new());&lt;br&gt;            break result??;&lt;br&gt;        }&lt;br&gt;&lt;br&gt;        _ = tokio::time::sleep(Duration::from_millis(140)) =&amp;gt; {&lt;br&gt;            progress_events.extend(progress_file.read_new());&lt;br&gt;&lt;br&gt;            if let Some(approval) =&lt;br&gt;                progress_file.take_approval_request()&lt;br&gt;            {&lt;br&gt;                let approved = pb.suspend(|| {&lt;br&gt;                    render_runtime_approval(&amp;amp;approval);&lt;br&gt;                    render_diff(&amp;amp;approval.diff);&lt;br&gt;&lt;br&gt;                    Confirm::new(&lt;br&gt;                        &amp;quot;Authorize this ProtoLink action?&amp;quot;&lt;br&gt;                    )&lt;br&gt;                    .with_default(false)&lt;br&gt;                    .prompt()&lt;br&gt;                })?;&lt;br&gt;&lt;br&gt;                progress_file.decide(&amp;amp;approval, approved)?;&lt;br&gt;            }&lt;br&gt;&lt;br&gt;            pb.set_message(&lt;br&gt;                latest_progress_message(&amp;amp;progress_events)&lt;br&gt;            );&lt;br&gt;        }&lt;br&gt;    }&lt;br&gt;};&lt;/pre&gt;&lt;p&gt;This file channel is specifically the Rust-to-Python progress and control bridge. ProtoLink’s agents still communicate through their own local transports (SSE by default) in the current implementation.&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;What I learned:&lt;/em&gt;&lt;/strong&gt; You don’t need complex communication architectures for local developer tooling. Simple, file-based synchronization is incredibly robust and easy to debug. Therefore, l&lt;em&gt;ocal files are an excellent control surface when the messages are small, temporary, inspectable, and written atomically. &lt;/em&gt;They are not the answer to every distributed-systems problem. For one CLI coordinating with one embedded runtime, however, they are pleasantly boring, and boring infrastructure is often good infrastructure.&lt;/blockquote&gt;&lt;h3&gt;The Hard Engineering Problems You Only Discover by Building It&lt;/h3&gt;&lt;p&gt;When you are designing these systems, you read papers about agent topologies and context architectures. But when you start coding, the real showstoppers are much more down-to-earth:&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;Architecture diagrams&lt;/strong&gt; make agent systems look elegant. The &lt;strong&gt;real problems&lt;/strong&gt; are often much less glamorous.&lt;/blockquote&gt;&lt;h4&gt;1. The VRAM vs. Context Size Memory Sloshing&lt;/h4&gt;&lt;p&gt;When using local LLM backends like Ollama or LM Studio, you have to configure the context length (num_ctx).&lt;/p&gt;&lt;p&gt;If the window is too small, the backend may reject large prompts, truncate useful information, or leave too little room for conversation history and generated code. If you leave it at the default (often 2,048 tokens), your agent will run out of space and crash or drop context immediately.&lt;/p&gt;&lt;p&gt;If it is too large, the KV cache consumes substantially more memory. Depending on the model, quantization, backend, and hardware, this can cause memory pressure or CPU offloading that dramatically reduces generation speed. Therefore If you set it to a massive value (like 32,000 tokens) on a standard 16GB RAM laptop, the Key-Value (KV) cache will balloon, overflow your GPU’s VRAM, and silently drop back to CPU offloading. Suddenly, your generation speed tanks from a snappy 40 tokens/sec to an agonizing 1.5 tokens/sec. In my experiments, an overly ambitious context setting could turn interactive generation into something painfully slow. The exact threshold is hardware-specific, but the underlying trade-off is universal: a model supporting a large theoretical context window does not mean your machine can run that window efficiently.&lt;/p&gt;&lt;p&gt;Finding that balance by capping your context packs to 8k or 16k to keep inference running purely in GPU VRAM is critical.&lt;/p&gt;&lt;p&gt;ProtoAgent therefore treats context as an explicit budget.&lt;/p&gt;&lt;p&gt;For Ollama, the configured window is passed as num_ctx. The same value is recorded in the model’s metrics profile, allowing the terminal to display how much of the available context is being used. &lt;strong&gt;Context Loom&lt;/strong&gt; also bounds the number and size of included snippets rather than filling every available token.&lt;/p&gt;&lt;p&gt;&lt;em&gt;The best local context window is not the largest one the model advertises. It is the largest one your hardware can sustain without destroying interactivity.&lt;/em&gt;&lt;/p&gt;&lt;h4&gt;2. The Pydantic Shield (Dealing with Bad JSON formatting)&lt;/h4&gt;&lt;p&gt;Compact models frequently produce almost-correct structured output.&lt;/p&gt;&lt;p&gt;They may wrap JSON in Markdown fences, add a conversational preamble, use an older action shape, omit a required field, or provide an argument with the wrong type.&lt;/p&gt;&lt;p&gt;Without a defensive runtime, one malformed response can derail the entire loop. ProtoLink handles this in layers:&lt;/p&gt;&lt;ol&gt;&lt;li&gt;It attempts to parse the response as JSON.&lt;/li&gt;&lt;li&gt;If the model added surrounding text, it extracts the enclosed JSON object.&lt;/li&gt;&lt;li&gt;It applies a small number of unambiguous action-shape repairs.&lt;/li&gt;&lt;li&gt;It validates the result against typed Pydantic action models.&lt;/li&gt;&lt;li&gt;If validation fails, it returns concise field-level diagnostics so the model can correct the specific mistake.&lt;/li&gt;&lt;/ol&gt;&lt;p&gt;The goal is not to guess what arbitrary broken JSON was supposed to mean. Overly aggressive repair would be dangerous. The goal is to tolerate harmless formatting noise while preserving a strict runtime contract.&lt;/p&gt;&lt;blockquote&gt;&lt;em&gt;Be lenient about presentation, but strict about meaning.&lt;/em&gt;&lt;/blockquote&gt;&lt;h4&gt;3. Session State: Keeping the Conversation Alive&lt;/h4&gt;&lt;p&gt;Coding conversations are rarely single-turn. A user may first request a refactor, then say:&lt;/p&gt;&lt;blockquote&gt;&lt;em&gt;Now add a unit test for that module.&lt;/em&gt;&lt;/blockquote&gt;&lt;p&gt;That follow-up only works if the model receives the relevant conversation state.&lt;/p&gt;&lt;p&gt;ProtoAgent separates two kinds of history.&lt;/p&gt;&lt;p&gt;The Rust CLI stores a project-level session ledger in:&lt;/p&gt;&lt;pre&gt;~/.protoagent/sessions.json&lt;/pre&gt;&lt;p&gt;This is the human-facing timeline. It records prompts, answer previews, models, timings, approvals, and run events for the terminal UI.&lt;/p&gt;&lt;p&gt;The actual model-facing conversation state lives in ProtoLink’s per-agent SQLite storage:&lt;/p&gt;&lt;pre&gt;~/.protoagent/conversations.sqlite&lt;/pre&gt;&lt;p&gt;Architect, Explorer, and Coder have isolated conversation namespaces, but they share the same stable project session identifier. When a project session resumes, ProtoAgent checks each agent’s history against the configured model context window. If the stored history exceeds its budget, the Python core uses ProtoLink’s history compaction before the next inference.&lt;/p&gt;&lt;p&gt;This distinction matters:&lt;/p&gt;&lt;blockquote&gt;&lt;em&gt;A timeline displayed by the UI is not automatically memory available to the model.&lt;/em&gt;&lt;/blockquote&gt;&lt;p&gt;Keeping those responsibilities explicit prevents the interface’s session history and the model’s conversation history from silently drifting apart.&lt;/p&gt;&lt;h3&gt;The Honest Verdict&lt;/h3&gt;&lt;p&gt;Building ProtoAgent has been one of the most useful hands-on lessons I have had in agent engineering.&lt;/p&gt;&lt;p&gt;We are not at the point where a local 7B model can build an entire application from scratch while you go watch a movie. They still hallucinate imports, struggle with complex cross-file abstractions, misunderstand tool schemas and require careful hand-holding.&lt;/p&gt;&lt;p&gt;But local agentic coding is not science fiction either. It is an engineering problem involving:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;Clear role boundaries&lt;/li&gt;&lt;li&gt;Small, explicit tool surfaces&lt;/li&gt;&lt;li&gt;Deterministic context selection&lt;/li&gt;&lt;li&gt;Typed runtime contracts&lt;/li&gt;&lt;li&gt;Context-budget management&lt;/li&gt;&lt;li&gt;Persistent conversation state&lt;/li&gt;&lt;li&gt;Human approval before side effects&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;The model is only one component.&lt;/p&gt;&lt;p&gt;Much of the quality comes from the system surrounding it and what &lt;strong&gt;context&lt;/strong&gt; the model sees, which &lt;strong&gt;choices&lt;/strong&gt; it is &lt;strong&gt;allowed&lt;/strong&gt; to make, how its &lt;strong&gt;output&lt;/strong&gt; is &lt;strong&gt;validated&lt;/strong&gt;, and where a &lt;strong&gt;human can interrupt the process&lt;/strong&gt; before it does something silly.&lt;/p&gt;&lt;p&gt;That is the most important thing ProtoAgent has taught me so far.&lt;/p&gt;&lt;p&gt;If you want to look at the code, play with the prompt topologies, contribute or check out the underlying ProtoLink core, everything is open source:&lt;/p&gt;&lt;p&gt;👉 protoagent repo: &lt;a href=&quot;https://github.com/nMaroulis/protoagent&quot;&gt;https://github.com/nMaroulis/protoagent&lt;/a&gt;&lt;/p&gt;&lt;p&gt;👉 protolink repo: &lt;a href=&quot;https://github.com/nMaroulis/protolink&quot;&gt;https://github.com/nMaroulis/protolink&lt;/a&gt;&lt;/p&gt;&lt;p&gt;Feel free to spin it up, break it and let me know what you find!&lt;/p&gt;&lt;img src=&quot;https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=8772874b91b8&quot; width=&quot;1&quot; height=&quot;1&quot; alt=&quot;&quot;&gt;&lt;hr&gt;&lt;p&gt;&lt;a href=&quot;https://levelup.gitconnected.com/building-my-own-local-claude-code-what-i-learned-demystifying-agentic-coding-under-the-hood-8772874b91b8&quot;&gt;Building My Own Local “Claude Code”: What I Learned Demystifying Agentic Coding under the Hood&lt;/a&gt; was originally published in &lt;a href=&quot;https://levelup.gitconnected.com&quot;&gt;Level Up Coding&lt;/a&gt; on Medium, where people are continuing the conversation by highlighting and responding to this story.&lt;/p&gt;</content:encoded>
    </item>
    <item>
      <title>A Single MCP Call Returned 278,649 Tokens. Here’s the Proxy I Built to Stop It.</title>
      <link>https://levelup.gitconnected.com/a-single-mcp-call-returned-278-649-tokens-heres-the-proxy-i-built-to-stop-it-dfaa9b5282e0?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/a-single-mcp-call-returned-278-649-tokens-heres-the-proxy-i-built-to-stop-it-dfaa9b5282e0?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Wed, 24 Jun 2026 16:38:02 GMT</pubDate>
      <content:encoded>&lt;h4&gt;How to measure per-tool MCP token cost, cut the tools/list &amp;quot;menu tax,&amp;quot; and enforce a hard budget on any MCP server — with under 2% added latency.&lt;/h4&gt;&lt;figure&gt;&lt;img alt=&quot;cover image for article with the same title and subtitle&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*i8g_uJQ3FKnKP2oImR3XsA.jpeg&quot; /&gt;&lt;/figure&gt;&lt;p&gt;&lt;strong&gt;&lt;em&gt;TL;DR:&lt;/em&gt;&lt;/strong&gt;&lt;em&gt; A single MCP call can return hundreds of thousands of tokens and get rejected outright by your harness. This post shows how to measure token costs, use native token limiters where they exist, and — when they don’t — drop a small token-budgeting proxy in front of any MCP server so your agent stops choking on its own tools.&lt;/em&gt;&lt;/p&gt;&lt;p&gt;A while back I added &lt;a href=&quot;https://get.brightdata.com/scraping-browser7181?utm_content=reducing_mcp_response_size_for_llm_context_limits&quot;&gt;Bright Data’s MCP&lt;/a&gt; to Claude Code, asked it to fetch a docs page, and watched it crash and burn spectacularly:&lt;/p&gt;&lt;blockquote&gt;&lt;em&gt;Error: MCP tool “scrape_as_markdown” response (278649 tokens) exceeds maximum allowed tokens (25000). Please use pagination, filtering, or limit parameters to reduce the response size.&lt;/em&gt;&lt;/blockquote&gt;&lt;p&gt;&lt;strong&gt;278k tokens&lt;/strong&gt; consumed before the LLM itself had read a single line, formed a thought, or done any of the work I actually asked for. That’s more than most LLMs’ context windows! Claude Code actually rejects everything above 25k tokens (&lt;a href=&quot;https://code.claude.com/docs/en/mcp&quot;&gt;configurable via &lt;/a&gt;&lt;a href=&quot;https://code.claude.com/docs/en/mcp&quot;&gt;MAX_MCP_OUTPUT_TOKENS&lt;/a&gt;), &lt;strong&gt;but some other clients have no explicit cap at all&lt;/strong&gt; — they just let the context fill until quality degrades (which is arguably worse.)&lt;/p&gt;&lt;p&gt;So let’s actually fix this. I’ll show you how to count which MCP server is costing you how many tokens, then some zero-effort fixes, and finally, a tiny client-agnostic proxy you can put in front of &lt;em&gt;any&lt;/em&gt; MCP server to enforce a hard token budget — shrink the MCP payload &lt;em&gt;before&lt;/em&gt; your LLM sees it.&lt;/p&gt;&lt;h3&gt;Why Are MCP Responses So Large?&lt;/h3&gt;&lt;p&gt;MCP responses are large because servers optimize for &lt;strong&gt;fidelity, not frugality&lt;/strong&gt; — they return full pages, files, or JSON blobs because they cannot know which slice you need. A second cost, the tools/list “menu tax,” injects every mounted tool’s schema into context each turn.&lt;/p&gt;&lt;blockquote&gt;💡 MCP servers are essentially APIs for LLMs, but the response still has to fit in a buffer with a hard size limit (the context window). Our token-budgeting proxy sits between those two worlds as an impedance-matcher — forwarding calls upstream and trimming responses before they reach the model.&lt;/blockquote&gt;&lt;p&gt;This compounds across three dimensions:&lt;/p&gt;&lt;ol&gt;&lt;li&gt;&lt;strong&gt;The payload itself.&lt;/strong&gt; A single result can be enormous regardless of domain. Bright Data MCP’s scrape_as_markdown on a long wiki page or API doc can hit 100k–150k+ tokens, GitHub MCP&amp;#39;s get_file_contents/get_pull_request_diff return a whole file or diff, and Playwright MCP&amp;#39;s browser_snapshot dumps the entire accessibility tree.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;The number of results.&lt;/strong&gt; Calls that enumerate a collection multiply per-item cost with no ceiling — e.g. GitHub’s search_code or list_* endpoints returning 100 hydrated items per page.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;The &lt;/strong&gt;&lt;strong&gt;tools/list overhead &lt;/strong&gt;— Most people don’t know that using MCP, &lt;em&gt;every &lt;/em&gt;tool’s schema from &lt;em&gt;every &lt;/em&gt;connected server gets injected into context before a single tool runs — this “menu tax” varies wildly by server.&lt;/li&gt;&lt;/ol&gt;&lt;figure&gt;&lt;img alt=&quot;Horizontal bar chart of tools/list schema token cost per MCP server (cl100k_base): Bright Data rapid 1,161, Playwright 4,986, Bright Data PRO 13,851, GitHub official 56,333. GitHub’s menu tax is largest — paid every turn before any tool runs.&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*TE9NRu7eQCMwFEH9dzyLxg.png&quot; /&gt;&lt;figcaption&gt;The official Github MCP is particularly bad at this - its schema tax is nearly &lt;strong&gt;3x &lt;/strong&gt;the size of an actual tool response.&lt;/figcaption&gt;&lt;/figure&gt;&lt;h3&gt;What Do Large MCP Responses Actually Cost You?&lt;/h3&gt;&lt;p&gt;They cost you &lt;strong&gt;twice&lt;/strong&gt;: in dollars per API call &lt;em&gt;and &lt;/em&gt;in context window capacity, before the LLM even sees any of the data.&lt;/p&gt;&lt;p&gt;To put this in perspective, here are my results from the Bright Data MCP’s scrape_as_markdown tool fetching different pages, converted from tokens to dollar cost (assuming Sonnet pricing):&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;Bar chart of raw scrape_as_markdown token cost per URL (log scale). Anthropic blog 5,624 tokens fits under Claude Code’s 25,000-token MCP cap (dashed line); Amazon product 25,449, Node.js docs 110,570, Wikipedia 161,780, and Amazon SERP 278,649 all exceed it.&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*WZ6OAgXbMmScV1Fs8Yn52w.png&quot; /&gt;&lt;/figure&gt;&lt;p&gt;Notice the color split. Blue (the blog post) is the only bar &lt;em&gt;under&lt;/em&gt; the Claude Code 25k cap. Everything else — even the “small” product page fetch — is already over budget.&lt;/p&gt;&lt;h3&gt;How To Measure MCP Token Cost&lt;/h3&gt;&lt;p&gt;Measure MCP token cost with a harness that calls tools/list and one representative tool per server, then counts tokens with tiktoken (cl100k_base). Run it per server to see both the per-turn schema tax and per-call payload size — the two separate sinks that drive total MCP cost.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;measure.py&lt;/strong&gt;&lt;/p&gt;&lt;pre&gt;&lt;br&gt;# Measures 1) tools/list schema tax &lt;br&gt;# and 2) token count of one representative call per server&lt;br&gt;import asyncio, json, os&lt;br&gt;import tiktoken&lt;br&gt;from mcp import ClientSession, StdioServerParameters, types&lt;br&gt;from mcp.client.stdio import stdio_client&lt;br&gt;&lt;br&gt;ENC = tiktoken.get_encoding(&amp;quot;cl100k_base&amp;quot;)  # close enough for budgeting&lt;br&gt;&lt;br&gt;def ntokens(text: str) -&amp;gt; int:&lt;br&gt;    return len(ENC.encode(text))&lt;br&gt;&lt;br&gt;def all_text(res: types.CallToolResult) -&amp;gt; str:&lt;br&gt;    &amp;quot;&amp;quot;&amp;quot;Count every text block — TextContent *and* EmbeddedResource (GitHub file reads).&amp;quot;&amp;quot;&amp;quot;&lt;br&gt;    parts = []&lt;br&gt;    for c in res.content:&lt;br&gt;        if isinstance(c, types.TextContent):&lt;br&gt;            parts.append(c.text or &amp;quot;&amp;quot;)&lt;br&gt;        elif isinstance(c, types.EmbeddedResource):&lt;br&gt;            t = getattr(c.resource, &amp;quot;text&amp;quot;, None)&lt;br&gt;            if t:&lt;br&gt;                parts.append(t)&lt;br&gt;    return &amp;quot;&amp;quot;.join(parts)&lt;br&gt;&lt;br&gt;AMAZON_SERP = &amp;quot;https://www.amazon.com/s?k=wireless+earbuds&amp;quot;&lt;br&gt;&lt;br&gt;SERVERS = [&lt;br&gt;    # Add your own here&lt;br&gt;    (&amp;quot;Bright Data rapid&amp;quot;, StdioServerParameters(&lt;br&gt;        command=&amp;quot;npx&amp;quot;, args=[&amp;quot;-y&amp;quot;, &amp;quot;@brightdata/mcp&amp;quot;],&lt;br&gt;        env={**os.environ, &amp;quot;API_TOKEN&amp;quot;: &amp;quot;&amp;lt;your-bright-data-token&amp;gt;&amp;quot;},&lt;br&gt;    )),&lt;br&gt;    (&amp;quot;Bright Data PRO&amp;quot;, StdioServerParameters(&lt;br&gt;        command=&amp;quot;npx&amp;quot;, args=[&amp;quot;-y&amp;quot;, &amp;quot;@brightdata/mcp&amp;quot;],&lt;br&gt;        env={**os.environ, &amp;quot;API_TOKEN&amp;quot;: &amp;quot;&amp;lt;your-bright-data-token&amp;gt;&amp;quot;, &amp;quot;PRO_MODE&amp;quot;: &amp;quot;true&amp;quot;},&lt;br&gt;    )),&lt;br&gt;    (&amp;quot;Playwright&amp;quot;, StdioServerParameters(&lt;br&gt;        command=&amp;quot;npx&amp;quot;, args=[&amp;quot;-y&amp;quot;, &amp;quot;@playwright/mcp@latest&amp;quot;],&lt;br&gt;    )),&lt;br&gt;    (&amp;quot;GitHub official&amp;quot;, StdioServerParameters(&lt;br&gt;        command=&amp;quot;docker&amp;quot;,&lt;br&gt;        args=[&amp;quot;run&amp;quot;, &amp;quot;-i&amp;quot;, &amp;quot;--rm&amp;quot;, &amp;quot;-e&amp;quot;, &amp;quot;GITHUB_PERSONAL_ACCESS_TOKEN&amp;quot;,&lt;br&gt;              &amp;quot;ghcr.io/github/github-mcp-server&amp;quot;],&lt;br&gt;        env={**os.environ, &amp;quot;GITHUB_PERSONAL_ACCESS_TOKEN&amp;quot;: &amp;quot;&amp;lt;your-github-token&amp;gt;&amp;quot;},&lt;br&gt;    )),&lt;br&gt;]&lt;br&gt;&lt;br&gt;PROBES = {&lt;br&gt;    &amp;quot;Bright Data rapid&amp;quot;: (&amp;quot;scrape_as_markdown&amp;quot;, {&amp;quot;url&amp;quot;: AMAZON_SERP}),&lt;br&gt;    &amp;quot;Bright Data PRO&amp;quot;:   (&amp;quot;scrape_as_markdown&amp;quot;, {&amp;quot;url&amp;quot;: AMAZON_SERP}),&lt;br&gt;    &amp;quot;GitHub official&amp;quot;:   (&amp;quot;get_file_contents&amp;quot;, {&lt;br&gt;        &amp;quot;owner&amp;quot;: &amp;quot;modelcontextprotocol&amp;quot;, &amp;quot;repo&amp;quot;: &amp;quot;python-sdk&amp;quot;, &amp;quot;path&amp;quot;: &amp;quot;README.md&amp;quot;,&lt;br&gt;    }),&lt;br&gt;}&lt;br&gt;&lt;br&gt;async def measure(label: str, server: StdioServerParameters):&lt;br&gt;    async with stdio_client(server) as (read, write):&lt;br&gt;        async with ClientSession(read, write) as session:&lt;br&gt;            await session.initialize()&lt;br&gt;&lt;br&gt;            # 1) How much does tools/list alone cost?&lt;br&gt;            tools = await session.list_tools()&lt;br&gt;            schema_tokens = ntokens(json.dumps([t.model_dump() for t in tools.tools]))&lt;br&gt;            print(f&amp;quot;tools/list ({label}): {len(tools.tools)} tools, {schema_tokens:,} tokens&amp;quot;)&lt;br&gt;&lt;br&gt;            # 2) How much does one real call cost?&lt;br&gt;            if label == &amp;quot;Playwright&amp;quot;:&lt;br&gt;                await session.call_tool(&lt;br&gt;                    &amp;quot;browser_navigate&amp;quot;,&lt;br&gt;                    arguments={&amp;quot;url&amp;quot;: &amp;quot;https://en.wikipedia.org/wiki/World_War_II&amp;quot;},&lt;br&gt;                )&lt;br&gt;                tool, args = &amp;quot;browser_snapshot&amp;quot;, {}&lt;br&gt;            else:&lt;br&gt;                tool, args = PROBES[label]&lt;br&gt;            res = await session.call_tool(tool, arguments=args)&lt;br&gt;            body = all_text(res)&lt;br&gt;            tag = &amp;quot;error message&amp;quot; if res.isError else &amp;quot;response&amp;quot;&lt;br&gt;            print(f&amp;quot;{tool} ({label}): {ntokens(body):,} tokens in {tag}&amp;quot;)&lt;br&gt;            print()&lt;br&gt;&lt;br&gt;async def main():&lt;br&gt;    for label, server in SERVERS:&lt;br&gt;        await measure(label, server)&lt;br&gt;&lt;br&gt;asyncio.run(main())&lt;/pre&gt;&lt;p&gt;To follow along, the Bright Data MCP needs you to &lt;a href=&quot;https://get.brightdata.com/bd7914?utm_content=reducing_mcp_response_size_for_llm_context_limits&quot;&gt;sign up here&lt;/a&gt; and get your API_TOKEN from the Control Panel set as an env var. Run the server with npx -y @brightdata/mcp. Do NOT setPRO_MODE=true for now. That adds browser tools backed by their remote &lt;a href=&quot;https://get.brightdata.com/bd-scraping-browser?utm_content=reducing_mcp_response_size_for_llm_context_limits&quot;&gt;Scraping Browser&lt;/a&gt;, which can dump snapshots just as fat as Playwright.&lt;/p&gt;&lt;p&gt;GitHub needs your GITHUB_PERSONAL_ACCESS_TOKEN from &lt;a href=&quot;https://github.com/settings/tokens&quot;&gt;Settings → Tokens&lt;/a&gt;, and also Docker. Playwright needs nothing extra.&lt;/p&gt;&lt;p&gt;Run the harness with:&lt;/p&gt;&lt;pre&gt;pip install mcp tiktoken&lt;br&gt;python measure.py&lt;/pre&gt;&lt;p&gt;Output of running this against all four MCP servers I had:&lt;/p&gt;&lt;pre&gt;tools/list (Bright Data rapid): 5 tools, 1,161 tokens&lt;br&gt;scrape_as_markdown (Bright Data rapid): 278,649 tokens in response   # Amazon SERP&lt;br&gt;&lt;br&gt;tools/list (Bright Data PRO):   74 tools, 13,851 tokens&lt;br&gt;scrape_as_markdown (Bright Data PRO): 278,649 tokens in response   # same, only adds tool cost &lt;br&gt;&lt;br&gt;tools/list (Playwright):        23 tools, 4,986 tokens&lt;br&gt;browser_snapshot (Playwright):  293,164 tokens in response            # Wikipedia WWII&lt;br&gt;&lt;br&gt;# this one is hilarious, the schema cost is ~3X more than the actual tool call.&lt;br&gt;tools/list (GitHub official):   43 tools, 56,333 tokens&lt;br&gt;get_file_contents (GitHub official): 19,406 tokens in response      # python-sdk/README.md&lt;/pre&gt;&lt;p&gt;The tools/list cost is the one that absolutely &lt;em&gt;horrified&lt;/em&gt; me — this schema or “menu” tax is something you pay every turn, on every conversation, forever — &lt;strong&gt;and with the GitHub MCP it is almost 3X the size of an actual response from a tool call!&lt;/strong&gt;&lt;/p&gt;&lt;h3&gt;Four Ways To Reduce MCP Response Size&lt;/h3&gt;&lt;p&gt;&lt;strong&gt;First, understand that there are &lt;em&gt;two &lt;/em&gt;different token sinks.&lt;/strong&gt; Every turn you pay &lt;em&gt;twice&lt;/em&gt;:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;Once for the tools/list &lt;strong&gt;schema &amp;quot;menu&amp;quot; tax&lt;/strong&gt; (the description of every tool, injected before anything runs), and&lt;/li&gt;&lt;li&gt;Once for the &lt;strong&gt;response payload&lt;/strong&gt; each tool call returns.&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;You can reduce these using four approaches: &lt;strong&gt;(1)&lt;/strong&gt; mount fewer tools to cut the tools/list schema tax, &lt;strong&gt;(2)&lt;/strong&gt; use native server limiting parameters, &lt;strong&gt;(3)&lt;/strong&gt; deploy a token-budgeting proxy for the response payload, and &lt;strong&gt;(4)&lt;/strong&gt; spill oversized response payloads to disk. So methods 1–2 target schema overhead; methods 3–4 target response payloads.&lt;/p&gt;&lt;h3&gt;Method #1: Stop Loading Tools You Don’t Use&lt;/h3&gt;&lt;p&gt;The only surefire way to cut the per-turn cost of announcing tools to the LLM is to &lt;strong&gt;shorten the menu&lt;/strong&gt; — unmount servers you don’t need, hide individual tools in your client config, or enable lean tool modes on the server itself.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;(a) Don’t mount that server&lt;/strong&gt; &lt;strong&gt;in the first place&lt;/strong&gt;. If a task never touches GitHub, leave the Github MCP disabled i.e. out of your client’s config entirely. A server that isn’t connected can’t put anything in tools/list and can&amp;#39;t incur the token cost.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;screenshot of an MCP client’s server list&quot; src=&quot;https://cdn-images-1.medium.com/max/798/1*e-SxjQzh1fg7Eagkw0KR4g.png&quot; /&gt;&lt;figcaption&gt;All MCP clients let you do this in the UI, but modifying the JSON config works too.&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;&lt;strong&gt;(b) Hide extraneous tools on a server you do mount&lt;/strong&gt;. Most clients let you pick and choose individual tools within enabled servers (Cursor does this in the UI, Claude Code has permission &lt;strong&gt;deny&lt;/strong&gt; rules like mcp__github__*; and others have allowedTools / excludeTools). Tools denied in such a fine-grained way leave context &lt;em&gt;entirely; &lt;/em&gt;they’re not simply blocked at call time.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;another screenshot of the same MCP client, zooming in on a single server’s tools, showing they can be enabled or disabled in a fine grained manner&quot; src=&quot;https://cdn-images-1.medium.com/max/808/1*CnwmsEgKcGx2oIGxYtWDIQ.png&quot; /&gt;&lt;figcaption&gt;Fine grained control over an MCP server’s tools in Cursor. A dimmed tool means it was hidden.&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;&lt;strong&gt;(c) Let the server trim itself&lt;/strong&gt;. Rare, but some MCP servers use a lean selection of tools by default while letting you opt-in to more if you need them. For example, Bright Data defaults to &lt;strong&gt;5 tools / 1,161 tokens&lt;/strong&gt; and only exposes its full catalog at PRO_MODE=true (&lt;strong&gt;74 tools / 13,851&lt;/strong&gt;). GitHub&amp;#39;s --toolsets/--tools and Playwright&amp;#39;s --caps options do the same thing.&lt;/p&gt;&lt;p&gt;Basically, shorten the “menu” to what the agent &lt;em&gt;actually &lt;/em&gt;needs for your use-case.&lt;/p&gt;&lt;h3&gt;Method #2: Ask For a Smaller Response, Natively.&lt;/h3&gt;&lt;p&gt;Check whether the server already lets you ask for a smaller response payload — it’s strictly better to limit &lt;em&gt;at the source&lt;/em&gt; than to download 300k tokens and throw most away.&lt;/p&gt;&lt;p&gt;This “asking for less” can take different forms. Some MCP servers let you paginate (max_results, per_page), ask for fewer fields (fields=[...]), or adjust format/verbosity (format: &amp;quot;compact&amp;quot;).&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;Heatmap: can each MCP server natively return a smaller response? Bright Data, GitHub, and Playwright rated Yes, Partial, or No across fetch fewer items, field filtering, thinner format, and spill to disk. Only Playwright has built-in spill; most heavy payloads still need a proxy.&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*DzLevnSXW11mhxsHojNx_A.png&quot; /&gt;&lt;figcaption&gt;Can you get a smaller response from this server natively? Three popular MCP servers compared. “Spill to disk” only counts native methods here.&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;The catch is that &lt;em&gt;most &lt;/em&gt;MCP servers have no such setting, and per-call params depend on the &lt;em&gt;model&lt;/em&gt; setting them every time; &lt;a href=&quot;https://github.com/brightdata/brightdata-mcp/issues/40&quot;&gt;server authors are wary of open-ended field selection because models hallucinate field names&lt;/a&gt;, preferring to enforce a sane default.&lt;/p&gt;&lt;p&gt;When native limits are missing or unreliable, you need a &lt;strong&gt;token-budgeting proxy&lt;/strong&gt; that enforces a hard ceiling on every response regardless of what the model requests.&lt;/p&gt;&lt;h3&gt;Method #3: Build a Token-Budgeting Proxy&lt;/h3&gt;&lt;p&gt;A token-budgeting proxy is a thin MCP server you write that sits between your client and the real server, forwards every call upstream, and post-processes each response down to a hard token budget (e.g. 8,000 tokens) before the model sees it — using strip, JSON projection, or disk spill depending on payload shape.&lt;/p&gt;&lt;p&gt;The agent talks to this proxy. Your proxy talks to the actual MCP server. Neither one knows the difference.&lt;/p&gt;&lt;pre&gt;┌──────────┐   stdio    ┌────────────────┐   stdio    ┌───────────────────┐&lt;br&gt;│  Client  │ ─────────► │  budget-proxy  │ ─────────► │ Bright Data MCP   │&lt;br&gt;│ (Claude) │ ◄───────── │  (your code)   │ ◄───────── │ (@brightdata/mcp) │&lt;br&gt;└──────────┘  trimmed   └────────────────┘   raw      └───────────────────┘&lt;br&gt;              ≤ budget                     300k tokens&lt;/pre&gt;&lt;p&gt;I’ll walk you through it. We’ll use &lt;a href=&quot;https://github.com/PrefectHQ/fastmcp&quot;&gt;FastMCP&lt;/a&gt; for the server half and the official &lt;a href=&quot;https://pypi.org/project/mcp/&quot;&gt;Python MCP client SDK&lt;/a&gt; (again) for the upstream half.&lt;/p&gt;&lt;blockquote&gt;&lt;em&gt;The examples below spawn an MCP server locally over stdio; swap &lt;/em&gt;&lt;em&gt;stdio_client for &lt;/em&gt;&lt;em&gt;streamablehttp_client if your upstream is a remote HTTP MCP server instead.&lt;/em&gt;&lt;/blockquote&gt;&lt;p&gt;This proxy isn’t a compressor that magically shrinks &lt;em&gt;any &lt;/em&gt;response. It’s a &lt;strong&gt;circuit breaker with shape-aware routing.&lt;/strong&gt; Its job is to guarantee the client never chokes on an oversized payload, and it does so by sending each response down the &lt;em&gt;least destructive&lt;/em&gt; path for its shape — projecting JSON, stripping noise from prose, and spilling to disk whatever is still too big to return inline.&lt;/p&gt;&lt;p&gt;When a payload spills beyond a token budget you set (I picked 8k) the model still receives a preview plus a file handle, and can &lt;a href=&quot;https://man7.org/linux/man-pages/man1/grep.1.html&quot;&gt;grep&lt;/a&gt; or read the rest on demand — pretty much any LLM these days is smart enough to do this.&lt;/p&gt;&lt;p&gt;We’ll build the router up in stages. Start with the path every prose/text payload takes — two passes that already cover most of the work.&lt;/p&gt;&lt;h4&gt;Step 1: Strip Known Garbage + Spill To Disk If Needed&lt;/h4&gt;&lt;p&gt;First, we’ll strip the obvious noise — tracking URLs, base64 inlined images, etc. Then, if the result is &lt;em&gt;still&lt;/em&gt; over budget, we’ll write the full payload to disk and return a preview, and a file path.&lt;/p&gt;&lt;blockquote&gt;Full code for&lt;strong&gt; compact.py &lt;/strong&gt;here&lt;strong&gt;: &lt;/strong&gt;&lt;a href=&quot;https://gist.github.com/sixthextinction/7ba52d1cc9f8f7b0b688de129c26a2c9&quot;&gt;https://gist.github.com/sixthextinction/7ba52d1cc9f8f7b0b688de129c26a2c9&lt;/a&gt;&lt;/blockquote&gt;&lt;blockquote&gt;Full code for &lt;strong&gt;spill.py &lt;/strong&gt;here&lt;strong&gt;: &lt;/strong&gt;&lt;a href=&quot;https://gist.github.com/sixthextinction/b5e322f7ca9b97d7b0a6c1dc8a272baf&quot;&gt;https://gist.github.com/sixthextinction/b5e322f7ca9b97d7b0a6c1dc8a272baf&lt;/a&gt;&lt;/blockquote&gt;&lt;pre&gt;# compact.py + spill.py&lt;br&gt;# strip noise, then spill to disk if still over budget&lt;br&gt;import re&lt;br&gt;import tiktoken&lt;br&gt;import pathlib&lt;br&gt;&lt;br&gt;ENC = tiktoken.get_encoding(&amp;quot;cl100k_base&amp;quot;)&lt;br&gt;&lt;br&gt;def ntokens(s: str) -&amp;gt; int:&lt;br&gt;    return len(ENC.encode(s))&lt;br&gt;&lt;br&gt;# Pass 1: strip the obvious garbage. On e-commerce scrapes, long tracking-param&lt;br&gt;# URLs in markdown link parens are often the biggest win.&lt;br&gt;_DATA_URI = re.compile(r&amp;quot;!\[[^\]]*\]\(data:image/[^)]+\)&amp;quot;)&lt;br&gt;_LONG_URL = re.compile(r&amp;quot;\(https?://[^)]{200,}\)&amp;quot;)&lt;br&gt;&lt;br&gt;def strip_noise(text: str) -&amp;gt; str:&lt;br&gt;    text = _DATA_URI.sub(&amp;quot;[image removed]&amp;quot;, text)&lt;br&gt;    text = _LONG_URL.sub(&amp;quot;(url removed)&amp;quot;, text)&lt;br&gt;    text = re.sub(r&amp;quot;\n{3,}&amp;quot;, &amp;quot;\n\n&amp;quot;, text)&lt;br&gt;    return text.strip()&lt;br&gt;&lt;br&gt;# Pass 2: if still over budget, spill full text to disk; preview fills inline budget.&lt;br&gt;def prose_spill_or_pass(text, tool, arguments, budget, spill_dir):&lt;br&gt;    cleaned = strip_noise(text)&lt;br&gt;    if ntokens(cleaned) &amp;lt;= budget:&lt;br&gt;        return cleaned, &amp;quot;pass_through&amp;quot;, None&lt;br&gt;    path = write_spill(spill_dir, tool, arguments, cleaned)&lt;br&gt;    header = f&amp;quot;[budget-proxy] Full result ({ntokens(text):,} tokens) saved to:\n  {path}\n\nPreview:\n\n&amp;quot;&lt;br&gt;    footer = &amp;quot;\n\nUse grep or a file-reading tool to pull specific sections on demand.&amp;quot;&lt;br&gt;    preview_budget = budget - ntokens(header) - ntokens(footer)&lt;br&gt;    preview = truncate_to_budget(cleaned, preview_budget)[0]&lt;br&gt;    body = header + preview + footer  # total ≤ budget&lt;br&gt;    return body, &amp;quot;spill&amp;quot;, path&lt;/pre&gt;&lt;p&gt;Here’s what this actually did across a spread of real URLs, budget set to 8k:&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;Grouped bar chart: can the proxy fit each scrape under 8,000 tokens? Per URL, bars show raw upstream tokens, after strip_noise, and inline returned. Green dashed line = 8k budget; red = 25k client cap. Heavy pages spill to disk but inline preview stays under budget; Amazon SERP goes 278,649 → 127,131 → 7,700.&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*Iqz3IZB_QCYDvD2BuyICzg.png&quot; /&gt;&lt;figcaption&gt;Green bar = yes, it fits the 8k inline budget. Dashed green line is the budget target. Dashed red line is Claude Code’s 25k MCP cap.&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;So the two passes divide labor, essentially. &lt;strong&gt;The&lt;/strong&gt;&lt;strong&gt;strip_noise deletes genuine garbage, and spill offloads the bulk without losing a byte.&lt;/strong&gt; When a payload spills, the inline response fills the budget — header plus as much preview as fits, not a token teaser — while the complete payload waits on disk for the moment a grep or a deeper read needs it.&lt;/p&gt;&lt;p&gt;A nice little bonus of this strategy is that re-running the same call is free — because spill.py keys files by {tool, arguments} hash.&lt;/p&gt;&lt;blockquote&gt;💡 The mature servers are starting to ship this too. &lt;a href=&quot;https://github.com/microsoft/playwright-mcp#configuration&quot;&gt;Playwright MCP&lt;/a&gt; has exactly this pattern as a native flag: --output-mode file writes snapshots, console messages, and network logs to disk (in --output-dir, with --output-max-size evicting old files) instead of streaming them into context.&lt;/blockquote&gt;&lt;p&gt;Before going further, wire the code above into a small router.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;budget.py&lt;/strong&gt;&lt;/p&gt;&lt;pre&gt;# budget.py (we&amp;#39;ll add more to this soon!)  &lt;br&gt;from dataclasses import dataclass  &lt;br&gt;import pathlib  &lt;br&gt;  &lt;br&gt;from lib.compact import strip_noise  &lt;br&gt;from lib.spill import prose_spill_inline  &lt;br&gt;from lib.tokens import ntokens  &lt;br&gt;  &lt;br&gt;  &lt;br&gt;@dataclass  &lt;br&gt;class BudgetResult:  &lt;br&gt;    text: str  &lt;br&gt;    strategy: str  # pass_through | spill  &lt;br&gt;  &lt;br&gt;  &lt;br&gt;def budget_text(text, tool, arguments, budget, spill_dir):  &lt;br&gt;    if not text.strip():  &lt;br&gt;        return BudgetResult(text=&amp;quot;&amp;quot;, strategy=&amp;quot;pass_through&amp;quot;)  &lt;br&gt;    cleaned = strip_noise(text)  &lt;br&gt;    if ntokens(cleaned) &amp;lt;= budget:  &lt;br&gt;        return BudgetResult(text=cleaned, strategy=&amp;quot;pass_through&amp;quot;)  &lt;br&gt;    _path, body, _, _ = prose_spill_inline(  &lt;br&gt;        tool, arguments, text, spill_dir, budget  &lt;br&gt;    )  &lt;br&gt;    return BudgetResult(text=body, strategy=&amp;quot;spill&amp;quot;)  &lt;br&gt;  &lt;br&gt;  &lt;br&gt;def budget_from_extracted(extracted_text, tool, arguments, budget, spill_dir, *, preview_tokens=1500):  &lt;br&gt;    return budget_text(extracted_text, tool, arguments, budget, spill_dir)  &lt;/pre&gt;&lt;h4&gt;Step 2: Setting Up The Proxy Server&lt;/h4&gt;&lt;p&gt;Now the wrapper — i.e. a runnable MCP server that wraps the upstream server. This opens one upstream connection at startup (via FastMCP’s lifespan), exposes a single call tool that forwards to any upstream tool by name, and runs every response through our budgeting router above (budget.py):&lt;/p&gt;&lt;blockquote&gt;Full code for &lt;strong&gt;budget_proxy.py &lt;/strong&gt;here&lt;strong&gt;: &lt;/strong&gt;&lt;a href=&quot;https://gist.github.com/sixthextinction/9a9b3b9a51bfebce401133d164a71f5f&quot;&gt;https://gist.github.com/sixthextinction/9a9b3b9a51bfebce401133d164a71f5f&lt;/a&gt;&lt;/blockquote&gt;&lt;pre&gt;# budget_proxy.py — token-budgeting MCP proxy with a single `call` wrapper  &lt;br&gt;import os  &lt;br&gt;import pathlib  &lt;br&gt;from collections.abc import AsyncIterator  &lt;br&gt;from contextlib import asynccontextmanager  &lt;br&gt;  &lt;br&gt;from mcp import ClientSession, StdioServerParameters, types  &lt;br&gt;from mcp.client.stdio import stdio_client  &lt;br&gt;from mcp.server.fastmcp import Context, FastMCP  &lt;br&gt;  &lt;br&gt;from lib.budget import budget_from_extracted  &lt;br&gt;  &lt;br&gt;# Inline ceiling: pass through if under this; else spill + preview filling remaining budget.  &lt;br&gt;TOKEN_BUDGET = int(os.getenv(&amp;quot;MCP_TOKEN_BUDGET&amp;quot;, &amp;quot;8000&amp;quot;))  &lt;br&gt;PREVIEW_TOKENS = int(os.getenv(&amp;quot;MCP_PREVIEW_TOKENS&amp;quot;, &amp;quot;1500&amp;quot;))  # JSON spill short preview only  &lt;br&gt;SPILL_DIR = pathlib.Path(os.getenv(&amp;quot;MCP_SPILL_DIR&amp;quot;, &amp;quot;./mcp_spill&amp;quot;))  &lt;br&gt;  &lt;br&gt;UPSTREAM = StdioServerParameters(  &lt;br&gt;    command=&amp;quot;npx&amp;quot;,  &lt;br&gt;    args=[&amp;quot;-y&amp;quot;, &amp;quot;@brightdata/mcp&amp;quot;],  &lt;br&gt;    env={&amp;quot;API_TOKEN&amp;quot;: os.environ[&amp;quot;API_TOKEN&amp;quot;]},  &lt;br&gt;)  &lt;br&gt;  &lt;br&gt;  &lt;br&gt;def text_from_result(res: types.CallToolResult) -&amp;gt; str:  &lt;br&gt;    return &amp;quot;&amp;quot;.join(c.text or &amp;quot;&amp;quot; for c in res.content if isinstance(c, types.TextContent))  &lt;br&gt;  &lt;br&gt;  &lt;br&gt;@asynccontextmanager  &lt;br&gt;async def upstream_lifespan(_server: FastMCP) -&amp;gt; AsyncIterator[ClientSession]:  &lt;br&gt;    async with stdio_client(UPSTREAM) as (read, write):  &lt;br&gt;        async with ClientSession(read, write) as session:  &lt;br&gt;            await session.initialize()  &lt;br&gt;            yield session  &lt;br&gt;  &lt;br&gt;  &lt;br&gt;mcp = FastMCP(&amp;quot;bright-data-budgeted&amp;quot;, lifespan=upstream_lifespan)  &lt;br&gt;  &lt;br&gt;  &lt;br&gt;@mcp.tool()  &lt;br&gt;async def call(tool: str, arguments: dict, ctx: Context) -&amp;gt; str:  &lt;br&gt;    &amp;quot;&amp;quot;&amp;quot;Proxy any upstream Bright Data tool, budgeted via strip + spill-or-pass.&amp;quot;&amp;quot;&amp;quot;  &lt;br&gt;    session: ClientSession = ctx.request_context.lifespan_context  &lt;br&gt;    res = await session.call_tool(tool, arguments=arguments)  &lt;br&gt;    text = text_from_result(res)  &lt;br&gt;  &lt;br&gt;    result = budget_from_extracted(  &lt;br&gt;        text, tool, arguments, TOKEN_BUDGET, SPILL_DIR,  &lt;br&gt;        preview_tokens=PREVIEW_TOKENS,  &lt;br&gt;    )  &lt;br&gt;    return result.text  &lt;br&gt;  &lt;br&gt;  &lt;br&gt;if __name__ == &amp;quot;__main__&amp;quot;:  &lt;br&gt;    mcp.run()&lt;/pre&gt;&lt;p&gt;Then point your client at the proxy instead of the actual MCP:&lt;/p&gt;&lt;pre&gt;{&lt;br&gt;  &amp;quot;mcpServers&amp;quot;: {&lt;br&gt;    &amp;quot;Bright Data (budgeted)&amp;quot;: {&lt;br&gt;      &amp;quot;command&amp;quot;: &amp;quot;python&amp;quot;,&lt;br&gt;      &amp;quot;args&amp;quot;: [&amp;quot;budget_proxy.py&amp;quot;],&lt;br&gt;      &amp;quot;env&amp;quot;: {&lt;br&gt;        &amp;quot;API_TOKEN&amp;quot;: &amp;quot;&amp;lt;your-token&amp;gt;&amp;quot;,&lt;br&gt;        &amp;quot;MCP_TOKEN_BUDGET&amp;quot;: &amp;quot;8000&amp;quot;&lt;br&gt;      }&lt;br&gt;    }&lt;br&gt;  }&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;The agent only ever sees one tool — call — and passes the real upstream name and args inside it: call(tool=&amp;quot;scrape_as_markdown&amp;quot;, arguments={&amp;quot;url&amp;quot;: &amp;quot;...&amp;quot;}).&lt;/p&gt;&lt;p&gt;Behind that, the proxy fetches the full 278k-token response, strips the noise, spills the remainder to disk if it&amp;#39;s still over budget, and returns a budget-filling preview plus a path. The client accepts it happily, and nothing has been discarded.&lt;/p&gt;&lt;blockquote&gt;💡 &lt;strong&gt;Why does saving to disk beat truncation for text?&lt;/strong&gt; Because a truncated response is worse than an error: the model treats a partial result as the whole truth and confidently fills in the rest. Spill makes the contract explicit — the model sees a preview, the full bytes are on disk, and the path tells it where to grep or read for more.&lt;/blockquote&gt;&lt;p&gt;The first time I ran that Amazon SERP URL through the proxy, the inline response fell from 278,649 tokens to ~7,700 while the full 127k-token stripped scrape sat on disk. When the task only needed the top listings, that preview was enough on its own; when it needed a product buried halfway down the page, the agent simply grepped the saved file.&lt;/p&gt;&lt;p&gt;The trade-off of this approach is that the model must be smart enough to route through the call tool rather than calling the MCP’s tools directly, and — when a result spills — to follow up with a grep or a partial read rather than mistaking the preview for the whole page. Not a problem for a frontier model.&lt;/p&gt;&lt;h3&gt;How Much Latency Does the MCP Proxy Add?&lt;/h3&gt;&lt;p&gt;Measured on the same Amazon SERP scrape, the upstream fetch took &lt;strong&gt;~8.6s&lt;/strong&gt; — that’s their &lt;a href=&quot;https://get.brightdata.com/bd-web-unlocker?utm_content=reducing_mcp_response_size_for_llm_context_limits&quot;&gt;Web Unlocker infra&lt;/a&gt; loading the page, not &lt;em&gt;our&lt;/em&gt; proxy server. The strip + spill pass (write to disk + preview on a 278k-token payload) added &lt;strong&gt;~175ms&lt;/strong&gt; — about &lt;strong&gt;2%&lt;/strong&gt; of the fetch time.&lt;/p&gt;&lt;p&gt;On smaller pages, the &lt;em&gt;entire &lt;/em&gt;pass was always in the &lt;strong&gt;~10–12ms&lt;/strong&gt; range and essentially free.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;Stacked bar chart of wall-clock time on one 278,649-token Amazon SERP proxy call. Upstream Bright Data fetch ~8.6 s (~98%); strip + spill pass ~175 ms (~2%). The budget compaction pass is not the bottleneck.&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*UVAJ1pj7-T2ZbWlqeuCMcw.png&quot; /&gt;&lt;figcaption&gt;The budget pass is ~2% of fetch time on this payload.&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;Sure, you’re paying for an extra stdio hop and JSON framing on top of that, but against an 8-second network fetch it’s noise. The expensive part of the proxy isn’t the proxy — it’s still the upstream call you’re making anyway.&lt;/p&gt;&lt;h3&gt;How To Shrink MCP Responses Based on Payload Shape&lt;/h3&gt;&lt;p&gt;Our strategy above keeps any simple text payload under budget without losing data, but there are a few things that can trip us up, still:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;&lt;strong&gt;Some MCP’s like GitHub hide text &lt;/strong&gt;in an EmbeddedResource and not TextContent.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Not all text is safe to line-truncate. &lt;/strong&gt;Remember: JSON is also technically “text”. Minified JSON responses arrive as a single line of text with no boundary to cut on, so any blind trim turns it into garbage.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Saving to disk needs the agent to follow up.&lt;/strong&gt; The preview is the top of the payload, so a mid-file answer still requires a grep or a ranged read. That’s by design, but it assumes the model actually has filesystem or grep tools to reach for — and that it won’t treat the preview as the whole story. If you skip this, you inherit truncation’s hallucination risk.&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;Essentially, the proxy needs a decision tree for &lt;strong&gt;routing by shape.&lt;/strong&gt; Here is that router.&lt;/p&gt;&lt;h4&gt;&lt;strong&gt;Failure 1 — Text the proxy can’t see.&lt;/strong&gt;&lt;/h4&gt;&lt;p&gt;Our text_from_result only reads TextContent blocks. That&amp;#39;s fine for any MCP that returns everything as one block of text. But as an example, the GitHub MCP&amp;#39;sget_file_contents tool splits its response into &lt;em&gt;two&lt;/em&gt; blocks: a short TextContent status line &lt;em&gt;and&lt;/em&gt; the actual file in an EmbeddedResource.&lt;/p&gt;&lt;p&gt;Fetching python-sdk/README.md with this as an example gives us:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;&lt;strong&gt;Legacy &lt;/strong&gt;&lt;strong&gt;text_from_result (TextContent only)&lt;/strong&gt; — TextContent status line (&amp;quot;successfully downloaded text file (SHA: …)&amp;quot;). This is the only thing we receive — just &lt;strong&gt;31 tokens.&lt;/strong&gt;&lt;/li&gt;&lt;li&gt;&lt;strong&gt;The file itself (completely missed)&lt;/strong&gt; — EmbeddedResource (markdown). &lt;strong&gt;19,376 tokens.&lt;/strong&gt;&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Full response&lt;/strong&gt; — both blocks. &lt;strong&gt;19,406 tokens.&lt;/strong&gt;&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;So the proxy that was supposed to &lt;em&gt;shrink&lt;/em&gt; a 19k-token file instead cheerfully passed through 31 tokens — and the model never saw the actual file at all. &lt;strong&gt;That’s definitely not what we want.&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;The fix is to extract &lt;strong&gt;every&lt;/strong&gt; text-bearing block, not just TextContent:&lt;/p&gt;&lt;blockquote&gt;Full code for &lt;strong&gt;&lt;em&gt;extract.py&lt;/em&gt;&lt;/strong&gt; here: &lt;a href=&quot;https://gist.github.com/sixthextinction/7c412d2b8901a71f23a6395236748756&quot;&gt;https://gist.github.com/sixthextinction/7c412d2b8901a71f23a6395236748756&lt;/a&gt;&lt;/blockquote&gt;&lt;pre&gt;# extract.py — read TextContent AND EmbeddedResource.text&lt;br&gt;from mcp import types&lt;br&gt;&lt;br&gt;def all_text_from_result(res: types.CallToolResult) -&amp;gt; tuple[str, list[str]]:&lt;br&gt;    parts, kinds = [], []&lt;br&gt;    for block in res.content:&lt;br&gt;        kinds.append(type(block).__name__)&lt;br&gt;        if isinstance(block, types.TextContent):&lt;br&gt;            parts.append(block.text or &amp;quot;&amp;quot;)&lt;br&gt;        elif isinstance(block, types.EmbeddedResource):&lt;br&gt;            resource = block.resource&lt;br&gt;            text = getattr(resource, &amp;quot;text&amp;quot;, None)&lt;br&gt;            if text:                       # text resource (file, diff)&lt;br&gt;                parts.append(text)&lt;br&gt;            elif getattr(resource, &amp;quot;blob&amp;quot;, None):   # binary — don&amp;#39;t inline it&lt;br&gt;                parts.append(&lt;br&gt;                    f&amp;quot;[budget-proxy] Binary resource at {getattr(resource, &amp;#39;uri&amp;#39;, &amp;#39;?&amp;#39;)} &amp;quot;&lt;br&gt;                    f&amp;quot;({len(resource.blob)} bytes base64 — not inlined).&amp;quot;&lt;br&gt;                )&lt;br&gt;    return &amp;quot;&amp;quot;.join(parts), kinds&lt;/pre&gt;&lt;p&gt;Next, MCPs that return JSON as a response.&lt;/p&gt;&lt;h4&gt;&lt;strong&gt;Failure 2 — Truncating JSON Naively Will Produce Garbage.&lt;/strong&gt;&lt;/h4&gt;&lt;p&gt;GitHub’s list/search tools return &lt;strong&gt;minified JSON&lt;/strong&gt; in a single TextContent block. Line-boundary truncation — perfectly safe for markdown — is catastrophic here: a 25k-token JSON object on &lt;em&gt;one line&lt;/em&gt; has no line boundary to cut on, so the truncator either keeps the whole thing or (with a hard char cap) slices mid-object and hands the model invalid JSON.&lt;/p&gt;&lt;p&gt;So what do we do? Simple — don’t truncate JSON at all. &lt;strong&gt;Project the fields you need before serializing.&lt;/strong&gt; &lt;a href=&quot;https://jmespath.org/&quot;&gt;JMESPath&lt;/a&gt; is a small query language for JSON that will help us with this.&lt;/p&gt;&lt;p&gt;&lt;a href=&quot;https://github.com/jmespath/jmespath.py&quot;&gt;GitHub - jmespath/jmespath.py: JMESPath is a query language for JSON.&lt;/a&gt;&lt;/p&gt;&lt;p&gt;You write an expression that picks fields, slices arrays, and renames keys, and it returns a smaller JSON object. Think SQL SELECT for a JSON blob — items[:5].{title: title, url: html_url} keeps five list entries with just the columns you care about, and drops the rest. &lt;em&gt;Perfect &lt;/em&gt;for our needs.&lt;/p&gt;&lt;p&gt;Using jmespath in our proxy, we wire this up as a &lt;strong&gt;registry&lt;/strong&gt; — a dict keyed by upstream tool name, each value a &lt;a href=&quot;https://jmespath.org/tutorial.html&quot;&gt;jmespath expression&lt;/a&gt; tuned for that tool’s JSON shape. When a response parses as JSON, we look up the tool in JMESPATH_REGISTRY:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;If there’s a match, we run jmespath.search(expr, data) and re-serialize the result.&lt;/li&gt;&lt;li&gt;No match? Fall through to generic structural shrink.&lt;/li&gt;&lt;li&gt;Not JSON at all? Leave it for the prose/compact path.&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;You write this expression once per tool you &lt;em&gt;know for sure &lt;/em&gt;returns heavy responses, and the proxy applies it on every call — the model never has to remember to ask for fewer fields.&lt;/p&gt;&lt;blockquote&gt;Full code for&lt;strong&gt; shrink_json.py &lt;/strong&gt;here&lt;strong&gt;: &lt;/strong&gt;&lt;a href=&quot;https://gist.github.com/sixthextinction/8d313cb753e02deb15f9fc3408f0ad28&quot;&gt;https://gist.github.com/sixthextinction/8d313cb753e02deb15f9fc3408f0ad28&lt;/a&gt;&lt;/blockquote&gt;&lt;pre&gt;# Project structured tools instead of truncating them&lt;br&gt;import json, jmespath&lt;br&gt;&lt;br&gt;PREVIEW_ITEMS = 5&lt;br&gt;JMESPATH_REGISTRY = {&lt;br&gt;    &amp;quot;search_code&amp;quot;: (&lt;br&gt;        &amp;quot;{total_count: total_count, incomplete_results: incomplete_results, &amp;quot;&lt;br&gt;        f&amp;quot;items: items[:{PREVIEW_ITEMS}].{{name: name, path: path, sha: sha, &amp;quot;&lt;br&gt;        &amp;quot;repository: repository.full_name, html_url: html_url}}&amp;quot;&lt;br&gt;    ),&lt;br&gt;    &amp;quot;list_pull_requests&amp;quot;: (&lt;br&gt;        f&amp;quot;[:{PREVIEW_ITEMS}].{{number: number, title: title, state: state, &amp;quot;&lt;br&gt;        &amp;quot;user: user.login, html_url: html_url, created_at: created_at}&amp;quot;&lt;br&gt;    ),&lt;br&gt;    # … search_repositories, search_issues, get_commit, get_gist …&lt;br&gt;}&lt;br&gt;&lt;br&gt;def shrink_json_text(text: str, tool: str) -&amp;gt; tuple[str | None, str]:&lt;br&gt;    &amp;quot;&amp;quot;&amp;quot;Return (shrunk_json, method) where method is jmespath|generic|none.&amp;quot;&amp;quot;&amp;quot;&lt;br&gt;    try:&lt;br&gt;        data = json.loads(text)&lt;br&gt;    except json.JSONDecodeError:&lt;br&gt;        return None, &amp;quot;none&amp;quot;                      # not JSON — leave it for the prose path&lt;br&gt;    expr = JMESPATH_REGISTRY.get(tool)&lt;br&gt;    if expr:&lt;br&gt;        shrunk = jmespath.search(expr, data)&lt;br&gt;        if shrunk is not None:&lt;br&gt;            return json.dumps(shrunk, indent=2), &amp;quot;jmespath&amp;quot;&lt;br&gt;    return json.dumps(_generic_shrink(data), indent=2), &amp;quot;generic&amp;quot;  # cap arrays, trim strings&lt;/pre&gt;&lt;p&gt;Valid tokens measured across GitHub’s heavy hitters at an 8k budget, with jmespath and without:&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;Bar chart of GitHub JSON tools: raw upstream vs JMESPath-projected tokens (log scale, 8k budget line). list_pull_requests 25,378 → 473; search_code 7,960 → 358. Blind line-truncation on list_pull_requests yields 0 valid JSON tokens. get_file_contents legacy TextContent-only extraction returned 31 tokens and missed the 19k-token EmbeddedResource file.&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*jEkdeoUkyfvWsG8o-XpG6Q.png&quot; /&gt;&lt;figcaption&gt;Raw upstream vs JMESPath-projected tokens (log scale, 8k budget)&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;Look at list_pull_requests: blind compact returns &lt;strong&gt;0 valid tokens&lt;/strong&gt; — it cut the single minified line and produced unparseable JSON. But the jmespath strategy worked correctly -- while still shrinking properly -- turning 25,378 tokens into &lt;strong&gt;473&lt;/strong&gt;, still valid JSON the model can parse.&lt;/p&gt;&lt;p&gt;For tools without a registered projection, a &lt;strong&gt;generic structural shrink&lt;/strong&gt; (cap arrays to &lt;em&gt;N&lt;/em&gt; items, truncate long string values) is the fallback — less surgical than jmespath, but still valid JSON.&lt;/p&gt;&lt;h3&gt;Putting It All Together&lt;/h3&gt;&lt;p&gt;Extend the budget.py from earlier like this. The proxy then becomes a small decision tree keyed on &lt;em&gt;what the payload actually is&lt;/em&gt;:&lt;/p&gt;&lt;blockquote&gt;Full code for the new &lt;strong&gt;budget.py &lt;/strong&gt;here&lt;strong&gt;: &lt;/strong&gt;&lt;a href=&quot;https://gist.github.com/sixthextinction/c40cfe9552466612868ffe22e5d5e7c8&quot;&gt;https://gist.github.com/sixthextinction/c40cfe9552466612868ffe22e5d5e7c8&lt;/a&gt;&lt;/blockquote&gt;&lt;pre&gt;# new budget.py&lt;br&gt;# replace budget_text(), but keep BudgetResult + budget_from_extracted() from Step 1  &lt;br&gt;def budget_text(text, tool, arguments, budget, spill_dir, *, preview_tokens=1500):  &lt;br&gt;    if not text.strip():  &lt;br&gt;        return BudgetResult(text=&amp;quot;&amp;quot;, strategy=&amp;quot;pass_through&amp;quot;)  &lt;br&gt;  &lt;br&gt;    # 1) JSON → project (jmespath) or structurally shrink; spill if still huge.  &lt;br&gt;    shrunk, method = shrink_json_text(text, tool)  &lt;br&gt;    if method != &amp;quot;none&amp;quot; and shrunk is not None:  &lt;br&gt;        if ntokens(shrunk) &amp;lt;= budget:  &lt;br&gt;            return BudgetResult(text=shrunk, strategy=f&amp;quot;json_{method}&amp;quot;)  &lt;br&gt;        path, preview, _ = spill_response(  &lt;br&gt;            tool, arguments, text, spill_dir, preview_tokens=preview_tokens, suffix=&amp;quot;.json&amp;quot;  &lt;br&gt;        )  &lt;br&gt;        body = f&amp;quot;[budget-proxy] Full JSON saved to:\n  {path}\n\nPreview:\n\n{preview}&amp;quot;  &lt;br&gt;        return BudgetResult(text=body, strategy=&amp;quot;spill&amp;quot;)  &lt;br&gt;  &lt;br&gt;    # 2) Prose — same path as Step 1 (unchanged).  &lt;br&gt;    cleaned = strip_noise(text)  &lt;br&gt;    if ntokens(cleaned) &amp;lt;= budget:  &lt;br&gt;        return BudgetResult(text=cleaned, strategy=&amp;quot;pass_through&amp;quot;)  &lt;br&gt;    _path, body, _, _ = prose_spill_inline(tool, arguments, text, spill_dir, budget)  &lt;br&gt;    return BudgetResult(text=body, strategy=&amp;quot;spill&amp;quot;)  &lt;br&gt;  &lt;br&gt;  &lt;br&gt;def budget_from_extracted(extracted_text, tool, arguments, budget, spill_dir, *, preview_tokens=1500):  &lt;br&gt;    return budget_text(extracted_text, tool, arguments, budget, spill_dir, preview_tokens=preview_tokens) &lt;/pre&gt;&lt;p&gt;This is a &lt;em&gt;least-destructive-first&lt;/em&gt; strategy. Structured data gets projected — the smallest output, exact, and still valid JSON — while prose/text gets stripped and, if it’s still too big, saved to disk. &lt;strong&gt;Nothing is ever line-truncated and thrown away.&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;Update the Step 2 call tool in budget_proxy.py to use extract.py and the expanded router:&lt;/p&gt;&lt;pre&gt;# In budget_proxy.py&lt;br&gt;# replace the call tool from Step 2  &lt;br&gt;from lib.extract import all_text_from_result  &lt;br&gt;  &lt;br&gt;@mcp.tool()  &lt;br&gt;async def call(tool: str, arguments: dict, ctx: Context) -&amp;gt; str:  &lt;br&gt;    &amp;quot;&amp;quot;&amp;quot;Proxy any upstream MCP tool with tiered token budgeting.&amp;quot;&amp;quot;&amp;quot;  &lt;br&gt;    session: ClientSession = ctx.request_context.lifespan_context  &lt;br&gt;    res = await session.call_tool(tool, arguments=arguments)  &lt;br&gt;    text, _ = all_text_from_result(res)        # Failure 1 fix — merges EmbeddedResource.text  &lt;br&gt;    result = budget_from_extracted(  &lt;br&gt;        text, tool, arguments, TOKEN_BUDGET, SPILL_DIR,  &lt;br&gt;        preview_tokens=PREVIEW_TOKENS,  &lt;br&gt;    )  &lt;br&gt;    return result.text  &lt;/pre&gt;&lt;p&gt;Same proxy, same 8k budget, run live against each upstream — each server naturally exercises a different branch, and the agent never sees a payload over ~8k:&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;Bar chart of six live proxy calls: light bar = raw upstream tokens, solid overlay = returned to agent (log scale, 8k budget line). GitHub JSON tools use field trim (e.g. list_pull_requests 25,378 → 493); large file reads and Wikipedia snapshots spill to disk (~7.6k inline + file); small payloads pass through. Every call stays under 8,000 inline tokens.&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*4N5_Zj82GwAAa1F3-0fCNw.png&quot; /&gt;&lt;figcaption&gt;Only one “call” tool with four strategies, picked by payload shape rather than by which server you happened to mount.&lt;/figcaption&gt;&lt;/figure&gt;&lt;h3&gt;Frequently Asked Questions&lt;/h3&gt;&lt;h4&gt;What does “MCP tool response exceeds maximum allowed tokens” mean in Claude, and how do I fix it?&lt;/h4&gt;&lt;p&gt;It means a single MCP tool returned more tokens than your client allows in one response — for example, Claude Code rejects tool results above &lt;strong&gt;25,000 tokens&lt;/strong&gt; by default. The fix, in order of effort: (1) expose fewer tools to cut tools/list overhead, (2) use native limiting parameters like max_results or per_page, (3) put a token-budgeting proxy in front of the server that trims every response to a hard ceiling (we use &lt;strong&gt;8,000 tokens&lt;/strong&gt;), and (4) spill oversized payloads to disk and return a file handle instead.&lt;/p&gt;&lt;h4&gt;&lt;strong&gt;What is the MCP output token limit in Claude Code, Claude Desktop, and Cursor?&lt;/strong&gt;&lt;/h4&gt;&lt;p&gt;There is no single MCP standard — every client sets its own limit. &lt;strong&gt;Claude Code&lt;/strong&gt; rejects tool results above &lt;strong&gt;25,000 tokens&lt;/strong&gt; (&lt;a href=&quot;https://code.claude.com/docs/en/mcp#mcp-output-limits-and-warnings&quot;&gt;configurable via&lt;/a&gt; &lt;a href=&quot;https://code.claude.com/docs/en/mcp#mcp-output-limits-and-warnings&quot;&gt;MAX_MCP_OUTPUT_TOKENS&lt;/a&gt;). &lt;strong&gt;Claude Desktop&lt;/strong&gt; uses roughly a &lt;strong&gt;150,000-character&lt;/strong&gt; cap (&lt;a href=&quot;https://claude.com/docs/connectors/building&quot;&gt;per the connector docs&lt;/a&gt;). &lt;strong&gt;Cursor and VS Code Copilot&lt;/strong&gt; don’t document a clean number and instead truncate or degrade. Some clients have no explicit cap at all and simply let the context fill until quality drops.&lt;/p&gt;&lt;h4&gt;&lt;strong&gt;Why are MCP responses so large?&lt;/strong&gt;&lt;/h4&gt;&lt;p&gt;MCP servers return a whole page, file, or JSON blob because it can’t know which slice you actually need. &lt;strong&gt;The menu tax&lt;/strong&gt; is the other half of the problem. Before any tool runs, the client injects every mounted tool’s name, description, and parameter schema from tools/list into context, and you pay that on every turn whether you call those tools or not. Mount many servers with dozens of tools, and that alone can dwarf a single call&amp;#39;s output.&lt;/p&gt;&lt;h4&gt;&lt;strong&gt;How do I reduce MCP token usage?&lt;/strong&gt;&lt;/h4&gt;&lt;p&gt;Build a small &lt;strong&gt;token-budgeting proxy&lt;/strong&gt; — a thin MCP server of your own that forwards each call to the real server and post-processes the response down to a fixed inline budget before it reaches the model. It routes each payload to the cheapest correct strategy: &lt;strong&gt;strip + spill-to-disk&lt;/strong&gt; for text (full payload on disk, preview filling the ~8k inline budget), &lt;strong&gt;JMESPath field projection&lt;/strong&gt; for JSON (so you never hand the model invalid truncated JSON).&lt;/p&gt;&lt;h4&gt;&lt;strong&gt;Why not just raise the client’s token limit instead?&lt;/strong&gt;&lt;/h4&gt;&lt;p&gt;Because raising the limit treats the symptom, not the cause. A 300k-token tool result &lt;em&gt;would&lt;/em&gt; fit in a 1M-token context window, but it evicts everything else the agent needs to remember and costs far more per turn. The limit is really a feature. Also, not every client exposes that setting.&lt;/p&gt;&lt;h4&gt;&lt;strong&gt;Does trimming MCP responses hurt answer quality?&lt;/strong&gt;&lt;/h4&gt;&lt;p&gt;Stripping known garbage like tracking URLs, base64-images, and markdown noise is usually a pure win. Spilling preserves full fidelity on disk — task quality depends on the agent following up with grep or partial reads, not on silently losing the tail. The only failure, really, is treating the &lt;strong&gt;preview&lt;/strong&gt; as the whole payload.&lt;/p&gt;&lt;h4&gt;&lt;strong&gt;How much latency does an MCP proxy add?&lt;/strong&gt;&lt;/h4&gt;&lt;p&gt;Very little. On a 278k-token Amazon SERP scrape, the upstream fetch took &lt;strong&gt;~8.6 seconds&lt;/strong&gt; while the compaction pass (strip + truncate) added only &lt;strong&gt;~175ms — about 2%&lt;/strong&gt;. On a small 5.6k-token page, compaction is ~12ms and effectively a no-op. The expensive part is the upstream network call you were making anyway; the proxy’s extra stdio hop and token counting are noise against it.&lt;/p&gt;&lt;h3&gt;Summary: How to Fix MCP Token Bloat&lt;/h3&gt;&lt;p&gt;MCP token bloat comes from two sinks — the tools/list schema “menu tax” and oversized tool response payloads. Pick the fix based on which sink is hurting you:&lt;/p&gt;&lt;ol&gt;&lt;li&gt;&lt;strong&gt;Schema tax too high?&lt;/strong&gt; Mount fewer servers and tools (Method #1). GitHub’s official MCP costs &lt;strong&gt;56,333 tokens&lt;/strong&gt; in tools/list alone — nearly 3x a typical tool response.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Response too big, but the MCP server supports limits?&lt;/strong&gt; Use native pagination and field filtering first (Method #2), then add a proxy for anything still oversized.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Response huge but you may need &lt;em&gt;any &lt;/em&gt;part of it?&lt;/strong&gt; Deploy a token-budgeting proxy (Method #3) that spills to disk and returns a preview + file path — full fidelity on disk, ~8k inline. MCP’s native &lt;a href=&quot;https://modelcontextprotocol.io/specification/2025-03-26/server/utilities/pagination&quot;&gt;pagination&lt;/a&gt; only covers list operations (tools/list, resources/list), not tool-call results — so for oversized payloads you invent your own handle-and-fetch scheme.&lt;/li&gt;&lt;/ol&gt;&lt;p&gt;Measure first with a tiktoken harness, fix the menu tax, limit at the source where possible, and enforce a hard inline budget everywhere else.&lt;/p&gt;&lt;p&gt;Be adaptable! The mistake is using &lt;em&gt;one &lt;/em&gt;hammer for all three — mounting every tool on every server when the agent only scrapes, or pulling a megabyte page inline when max_results=3 at the source would have been enough.&lt;/p&gt;&lt;p&gt;&lt;em&gt;Some links in this article are tracking links used for analytics purposes only. I do not receive any commission or compensation from them.&lt;/em&gt;&lt;/p&gt;&lt;img src=&quot;https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=dfaa9b5282e0&quot; width=&quot;1&quot; height=&quot;1&quot; alt=&quot;&quot;&gt;&lt;hr&gt;&lt;p&gt;&lt;a href=&quot;https://levelup.gitconnected.com/a-single-mcp-call-returned-278-649-tokens-heres-the-proxy-i-built-to-stop-it-dfaa9b5282e0&quot;&gt;A Single MCP Call Returned 278,649 Tokens. Here’s the Proxy I Built to Stop It.&lt;/a&gt; was originally published in &lt;a href=&quot;https://levelup.gitconnected.com&quot;&gt;Level Up Coding&lt;/a&gt; on Medium, where people are continuing the conversation by highlighting and responding to this story.&lt;/p&gt;</content:encoded>
    </item>
    <item>
      <title>The V Language: The Best Parts of Go, Rust, and Python — All in One</title>
      <link>https://levelup.gitconnected.com/the-v-language-the-best-parts-of-go-rust-and-python-all-in-one-4c2864671301?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/the-v-language-the-best-parts-of-go-rust-and-python-all-in-one-4c2864671301?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Wed, 24 Jun 2026 16:37:52 GMT</pubDate>
      <content:encoded>&lt;div class=&quot;medium-feed-item&quot;&gt;&lt;p class=&quot;medium-feed-image&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/the-v-language-the-best-parts-of-go-rust-and-python-all-in-one-4c2864671301?source=rss----5517fd7b58a6---4&quot;&gt;&lt;img src=&quot;https://cdn-images-1.medium.com/max/1600/1*w2g8_DB5QZTFUmvOMgWoXw.png&quot; width=&quot;1600&quot;&gt;&lt;/a&gt;&lt;/p&gt;&lt;p class=&quot;medium-feed-snippet&quot;&gt;A Go-like simple, Rust-like safe, and Python-like productive language that transpiles to C&lt;/p&gt;&lt;p class=&quot;medium-feed-link&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/the-v-language-the-best-parts-of-go-rust-and-python-all-in-one-4c2864671301?source=rss----5517fd7b58a6---4&quot;&gt;Continue reading on Level Up Coding »&lt;/a&gt;&lt;/p&gt;&lt;/div&gt;</content:encoded>
    </item>
    <item>
      <title>Claude Can See My Screen. Here’s What It Gets Wrong.</title>
      <link>https://levelup.gitconnected.com/claude-can-see-my-screen-heres-what-it-gets-wrong-624bdb78b3c3?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/claude-can-see-my-screen-heres-what-it-gets-wrong-624bdb78b3c3?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Wed, 24 Jun 2026 16:37:43 GMT</pubDate>
      <content:encoded>&lt;p&gt;&lt;em&gt;A technical deep-dive into the AI vision layer behind Ezzi — how it reads a coding problem off a screenshot, where it quietly breaks, and which failures I actually engineered around versus the ones I just live with.&lt;/em&gt;&lt;/p&gt;&lt;p&gt;&lt;strong&gt;TL;DR&lt;/strong&gt; — Ezzi captures a screenshot of your interview problem and an AI reads it off the pixels. The surprising part is how well that works now. The useful part is where it doesn’t. I’ll walk through the real pipeline (a cheap Sonnet model reads the screen, an expensive Opus model solves from the transcript and never sees the image), the failure modes I hit in production, and an honest split between the ones I fixed and the ones that are still just limitations.&lt;/p&gt;&lt;h3&gt;Here’s the Situation&lt;/h3&gt;&lt;p&gt;Ezzi is an open-source interview assistant. You hit a shortcut, it captures the coding problem on your screen, an AI reads it, and you get a solution rendered in an overlay that stays invisible to screen sharing.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;Screen recording of Ezzi solving a Longest Palindromic Substring problem: the coding problem on screen, a generating-solution overlay, then the finished solution code and a time and space complexity panel.&quot; src=&quot;https://cdn-images-1.medium.com/max/800/1*OA0e2xT1zwnca760LJhsdg.gif&quot; /&gt;&lt;figcaption&gt;Ezzi end to end: it reads the problem off the screen, generates a solution and a complexity analysis, and renders them in an overlay that stays invisible to screen sharing.&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;Vision is the front door to the whole thing. Everything else, the solution, the thought process, the complexity analysis, depends on one assumption: that the model correctly read what was on the screen. If it misreads, it doesn’t fail loudly. It confidently solves the wrong problem and hands you a clean, well-commented answer to a question nobody asked.&lt;/p&gt;&lt;p&gt;I wrote a piece a while back on the OS-level tricks that make the window invisible. This is the other half. Making the window disappear was a hardware-and-compositor problem with mostly deterministic answers. Reading the screen is an AI problem, which means the answers are probabilistic and the failures are subtle. Subtle is worse.&lt;/p&gt;&lt;p&gt;So let me show you what’s actually behind “Claude can see my screen”, and then let me be honest about where it gets things wrong.&lt;/p&gt;&lt;h3&gt;What “Claude Can See My Screen” Actually Means&lt;/h3&gt;&lt;p&gt;The first thing worth clearing up: there isn’t one model doing this. There are two, and they do very different jobs.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;Ezzi overlay showing a My Thoughts panel with the solution approach, a Solution panel with the generated code, and a code editor pane on the right.&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*yFPekh_2-lHwPqcuZxMiVQ.png&quot; /&gt;&lt;figcaption&gt;What Ezzi renders over your screen: the transcribed approach in My Thoughts, the generated Solution, and the code, all in the overlay that stays invisible to screen sharing.&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;When you capture a problem in Live Interview mode, the screenshot goes to a cheap, deterministic model whose only job is to read the screen and classify what it sees. Here’s that stage:&lt;/p&gt;&lt;pre&gt;// src/integrations/prompts/catalog/extract-problem/extract-problem.prompt.ts&lt;br&gt;private readonly modelParams: ClaudeModelParams = {&lt;br&gt;  model: &amp;#39;claude-sonnet-4-6&amp;#39;,&lt;br&gt;  max_tokens: 10_000,&lt;br&gt;  temperature: 0,&lt;br&gt;};&lt;/pre&gt;&lt;p&gt;Temperature zero is deliberate. This stage isn’t supposed to be creative. It’s supposed to transcribe the problem statement from the image and label it, the same way every time, given the same pixels.&lt;/p&gt;&lt;p&gt;The output of that stage is text. That text, the transcribed problem, is what gets handed to the second model, the one that actually solves:&lt;/p&gt;&lt;pre&gt;// src/integrations/prompts/catalog/solve/solve.prompt.ts&lt;br&gt;private readonly modelParams: ClaudeModelParams = {&lt;br&gt;  model: &amp;#39;claude-opus-4-8&amp;#39;,&lt;br&gt;  max_tokens: 20_000,&lt;br&gt;  effort: &amp;#39;high&amp;#39;,&lt;br&gt;};&lt;/pre&gt;&lt;p&gt;Now here’s the part that matters more than anything else in this article. Look at how the solve stage calls the model:&lt;/p&gt;&lt;pre&gt;// src/integrations/prompts/catalog/solve/solve.prompt.ts&lt;br&gt;const response = await this.claudeService.callModel({&lt;br&gt;  systemPrompt: SYSTEM_PROMPT,&lt;br&gt;  userPrompt: promptText,&lt;br&gt;  images: [], // &amp;lt;- the solver never sees the screenshot&lt;br&gt;  modelParams: this.modelParams,&lt;br&gt;  jsonSchema: { /* thoughts, code, complexity, problem_statement */ },&lt;br&gt;});&lt;/pre&gt;&lt;p&gt;images: []. The expensive reasoning model, Opus, never sees your screen. It solves entirely from the transcript that Sonnet produced. The model that reads the pixels and the model that does the thinking are two different models, and the only thing passing between them is a string.&lt;/p&gt;&lt;p&gt;This is a reasonable architecture. Sonnet is cheaper and faster, and reading text off a clean IDE screenshot is not a job that needs the most powerful model available. Splitting it also lets me gate bad screenshots before paying for an expensive solve (more on that shortly).&lt;/p&gt;&lt;p&gt;But it has a consequence that defines the entire failure surface: if the transcription is wrong, nothing downstream can catch it. Opus has no pixels to fall back on. It trusts the string.&lt;/p&gt;&lt;p&gt;Here’s the full shape of it.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;Table comparing the three Claude calls behind one solve action: extract and classify with Sonnet 4.6, solve with Opus 4.8, and debug with Opus 4.8.&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*Pn-mm2pYSXFh4ZSO8qYltQ.png&quot; /&gt;&lt;figcaption&gt;The three Claude calls behind a single “solve this problem” action. Note that only two of the three ever look at pixels.&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;The whole thing runs against the Anthropic SDK with a tight budget:&lt;/p&gt;&lt;pre&gt;// src/integrations/ai-models/claude/claude.service.ts&lt;br&gt;this.anthropic = new Anthropic({&lt;br&gt;  timeout: 60_000,&lt;br&gt;  maxRetries: 2,&lt;br&gt;});&lt;/pre&gt;&lt;p&gt;Sixty seconds, two retries. An interview doesn’t give you the luxury of a slow answer.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;Diagram of the Ezzi vision pipeline: Claude Sonnet 4.6 reads the screenshot and classifies it, a gate rejects non-coding screenshots, and Claude Opus 4.8 solves from the text transcript only, with no image attached.&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*aDwfFISSDHWsJbx0NCVWuA.png&quot; /&gt;&lt;figcaption&gt;The vision model and the reasoning model are separate. The transcript is the only thing that crosses the gap, which is exactly where the interesting failures live.&lt;/figcaption&gt;&lt;/figure&gt;&lt;h3&gt;The Capture Layer: What the Model Actually Receives&lt;/h3&gt;&lt;p&gt;Before any model sees anything, the desktop app has to grab the screen and turn it into something the API will accept. There’s no fancy capture pipeline here, just the OS doing the work:&lt;/p&gt;&lt;pre&gt;// electron/screenshot.helper.ts&lt;br&gt;private async captureScreenshotMac(): Promise&amp;lt;Buffer&amp;gt; {&lt;br&gt;  const tmpPath = path.join(app.getPath(&amp;#39;temp&amp;#39;), `${uuidv4()}.png`);&lt;br&gt;  await execFileAsync(&amp;#39;screencapture&amp;#39;, [&amp;#39;-x&amp;#39;, tmpPath]);&lt;br&gt;  const buffer = await fs.promises.readFile(tmpPath);&lt;br&gt;  await fs.promises.unlink(tmpPath);&lt;br&gt;  return buffer;&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;macOS gets screencapture, Windows gets a PowerShell CopyFromScreen call, Linux gets gnome-screenshot. Native PNG, full resolution, straight from the OS.&lt;/p&gt;&lt;p&gt;That PNG is then base64-encoded and sent up. And this is where the first quiet trade-off lives. Claude’s vision API has a size limit, and a full-resolution screenshot of a 4K or multi-monitor setup can blow right past it. So the app compresses, but only when it has to:&lt;/p&gt;&lt;pre&gt;// electron/screenshot.helper.ts&lt;br&gt;private static readonly MAX_IMAGE_SIZE = 3932160; // ~3.75 MB&lt;br&gt;private static readonly QUALITY_STEPS = [90, 85, 80, 70];&lt;br&gt; &lt;br&gt;private compressIfNeeded(buffer: Buffer): { data: Buffer; ext: string } {&lt;br&gt;  if (buffer.length &amp;lt;= ScreenshotHelper.MAX_IMAGE_SIZE) {&lt;br&gt;    return { data: buffer, ext: &amp;#39;.png&amp;#39; }; // small enough, stays lossless&lt;br&gt;  }&lt;br&gt;  const image = nativeImage.createFromBuffer(buffer);&lt;br&gt;  for (const quality of ScreenshotHelper.QUALITY_STEPS) {&lt;br&gt;    const jpegBuffer = image.toJPEG(quality);&lt;br&gt;    if (jpegBuffer.length &amp;lt;= ScreenshotHelper.MAX_IMAGE_SIZE) {&lt;br&gt;      return { data: jpegBuffer, ext: &amp;#39;.jpg&amp;#39; };&lt;br&gt;    }&lt;br&gt;  }&lt;br&gt;  // last resort: ship the most-compressed version even if still over&lt;br&gt;  const last = ScreenshotHelper.QUALITY_STEPS.at(-1);&lt;br&gt;  return { data: image.toJPEG(last), ext: &amp;#39;.jpg&amp;#39; };&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;If your screenshot is under the limit, it stays a lossless PNG and the model gets crisp text. If it’s over, it gets walked down a JPEG quality ladder until it fits. JPEG is lossy, and the thing it’s worst at is exactly what we care about: sharp edges on small text. Compression artifacts smear the boundaries between characters, and small monospace code is nothing but sharp edges on small characters.&lt;/p&gt;&lt;p&gt;Here’s the honest part. There is no legibility check anywhere in that function. Nothing measures whether the compressed text is still readable. If you’re on a giant display and the screenshot gets pushed down to quality 70, the model is reading slightly mushy characters, and nobody told it (or you) that the input quality dropped. It just does its best with what it got.&lt;/p&gt;&lt;p&gt;For most people on a normal laptop this never triggers, the PNG is well under 3.75 MB and stays lossless. But it’s a real edge, and pretending it isn’t would be dishonest.&lt;/p&gt;&lt;h3&gt;Where the Vision Actually Breaks&lt;/h3&gt;&lt;p&gt;Now the part you came for. These are the failure modes I’ve actually seen, sorted by how much I was able to do about them.&lt;/p&gt;&lt;h4&gt;1. The split brain: a misread the solver can’t catch&lt;/h4&gt;&lt;p&gt;Go back to images: []. Because Opus solves from text and never sees the screen, any character the Sonnet stage gets wrong is baked in permanently.&lt;/p&gt;&lt;p&gt;Vision models misread the things you’d expect a human to squint at. rn rendered in a thin font reads as m. A lowercase l reads as a 1. A capital O reads as a 0. In a problem statement, &amp;quot;the array nums&amp;quot; surviving as &amp;quot;the array nuns&amp;quot; is harmless, the solver figures out the intent. But a misread in a constraint (n &amp;lt;= 10^5 becoming n &amp;lt;= 10^6) or in a sample input quietly changes the problem, and Opus solves the changed version with total confidence.&lt;/p&gt;&lt;p&gt;Temperature zero makes this deterministic, which is a double-edged thing. The same screenshot misreads the same way every time. Stable, but stably wrong. Re-shooting the screenshot (slightly different framing, a cleaner crop) is the only real recovery, because it changes the pixels going into the one stage that looks at pixels.&lt;/p&gt;&lt;p&gt;People sometimes assume the “readable variable names” toggle is the fix here. It isn’t, and I want to be precise about that. Here’s what that toggle actually does:&lt;/p&gt;&lt;pre&gt;// src/integrations/prompts/catalog/shared-snippets.ts&lt;br&gt;export const READABLE_VAR_NAMES_INSTRUCTION =&lt;br&gt;  &amp;#39;\n\nUse descriptive, human-readable variable names that convey &amp;#39; +&lt;br&gt;  &amp;#39;meaning (e.g. `leftIndex` instead of `l`, `currentSum` instead &amp;#39; +&lt;br&gt;  &amp;#39;of `s`, `nodeCount` instead of `n`). ...&amp;#39;;&lt;/pre&gt;&lt;p&gt;That’s an instruction about the code Opus writes, not about the text Sonnet reads. It makes the output easier to explain out loud in an interview. It does nothing to recover a misread on the way in. Two different problems that happen to both involve variable names, and conflating them would be selling you a fix that doesn’t exist.&lt;/p&gt;&lt;h4&gt;2. The wrong screenshot entirely (the fix I’m proudest of)&lt;/h4&gt;&lt;p&gt;The most common failure in practice isn’t a subtle misread. It’s that you captured the wrong thing. You meant to grab the problem, but the screenshot caught your inbox, a billing page, the login screen, or a Slack window.&lt;/p&gt;&lt;p&gt;If every capture went straight to Opus, every one of those would burn an expensive solve and hand you a “solution” to your inbox. So the cheap Sonnet stage doesn’t just transcribe, it classifies. This is the classification gate, and it’s the piece of this system I’m happiest with.&lt;/p&gt;&lt;p&gt;The prompt forces the screen into one of six buckets:&lt;/p&gt;&lt;pre&gt;// src/integrations/prompts/catalog/extract-problem/extract-problem.constants.ts&lt;br&gt;&amp;#39;- DSA: Data structures &amp;amp; algorithms problem (LeetCode, HackerRank...)\n&amp;#39; +&lt;br&gt;&amp;#39;- SQL: Database query problem (SQL challenges, query writing tasks)\n&amp;#39; +&lt;br&gt;&amp;#39;- SYSTEM_DESIGN: System design question (architecture, scalability...)\n&amp;#39; +&lt;br&gt;&amp;#39;- BEHAVIORAL: Behavioral interview question (non-technical, soft skills)\n&amp;#39; +&lt;br&gt;&amp;#39;- NOT_A_PROBLEM: Screenshot does not contain any programming problem &amp;#39; +&lt;br&gt;&amp;#39;  (chat apps, billing pages, login screens, browsers, calendars...)\n&amp;#39; +&lt;br&gt;&amp;#39;- OTHER: Contains a programming task that does not fit the above\n&amp;#39;&lt;/pre&gt;&lt;p&gt;That classification comes back as a structured field, and the command layer decides whether to proceed:&lt;/p&gt;&lt;pre&gt;// src/solutions/app-modes/live-interview/live-interview-solve.command.ts&lt;br&gt;if (&lt;br&gt;  !extractedProblem.is_valid_problem ||&lt;br&gt;  !ALLOWED_PROBLEM_TYPES[AppMode.LIVE_INTERVIEW].includes(&lt;br&gt;    extractedProblem.problem_type,&lt;br&gt;  )&lt;br&gt;) {&lt;br&gt;  return {&lt;br&gt;    thoughts: [&lt;br&gt;      `The screenshot does not contain a supported problem type. ` +&lt;br&gt;      `Detected: ${extractedProblem.problem_type}`,&lt;br&gt;    ],&lt;br&gt;    // ...solve prompt is never called&lt;br&gt;  };&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;Only DSA and SQL make it through. A billing page comes back NOT_A_PROBLEM and is bounced before the expensive model ever runs. A system design question is recognized for what it is and turned away with a message that tells you why, instead of confidently faking an answer it has no business giving.&lt;/p&gt;&lt;p&gt;I want to be careful not to oversell this. I don’t have a metric for how often it fires, there’s no counter or analytics in the code measuring a rejection rate, so I’m not going to quote you a percentage I can’t back up. What I can tell you is what it does: it’s a cheap, deterministic filter that catches the dumbest and most common failure (wrong screenshot) before it costs anything, and it gives the model an honest way to say “that’s not something I solve” instead of hallucinating confidence.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;Funnel diagram of the classification gate: of six categories (DSA, SQL, system design, behavioral, not-a-problem, other) only DSA and SQL pass through to the Opus solver, while the other four are rejected with a reason.&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*pCbPXvvTYp0shGSUEWqALw.png&quot; /&gt;&lt;figcaption&gt;The classification gate. Four of the six categories never reach the solver, which is the whole point.&lt;/figcaption&gt;&lt;/figure&gt;&lt;h4&gt;3. Dark mode, light mode, and the thing I didn’t build&lt;/h4&gt;&lt;p&gt;I expected theme to matter. Light text on a dark IDE versus dark text on a white page felt like the kind of thing that would trip up OCR and force me to write preprocessing, contrast normalization, maybe a grayscale pass.&lt;/p&gt;&lt;p&gt;I never wrote any of it. There is no theme detection, no contrast handling, no theme-aware preprocessing anywhere in the codebase. Every screenshot is treated identically. And in practice, modern vision models read both dark and light themes fine, so it simply hasn’t been a problem worth solving.&lt;/p&gt;&lt;p&gt;I’m including this not as a feature but as an honest non-event. The flip side of “I didn’t need to build it” is “there’s no safety net if it ever regresses.” If a future model update started struggling with low-contrast themes, nothing in the pipeline would catch it or compensate. It works today because the underlying model is good, not because Ezzi does anything clever.&lt;/p&gt;&lt;h4&gt;4. Handwriting and whiteboard photos&lt;/h4&gt;&lt;p&gt;This one is just a limitation, and I’d rather say so than dress it up.&lt;/p&gt;&lt;p&gt;The entire pipeline assumes a rendered screenshot, clean glyphs from an IDE or a coding platform, pixels generated by software. A phone photo of a whiteboard, or handwritten notes, is a completely different vision problem: variable lighting, perspective skew, handwriting, glare. The prompts don’t expect it and there’s no detection that would flag it.&lt;/p&gt;&lt;p&gt;Could the model sometimes read a tidy whiteboard? Maybe. But it isn’t designed for it, I haven’t tuned for it, and I wouldn’t trust it in the one moment that counts. If the problem is on a whiteboard, type it out and screenshot that. The tool is built for screens, and I’d rather be clear about the boundary than imply a capability I haven’t earned.&lt;/p&gt;&lt;h4&gt;5. The debug loop: when the model has to actually look again&lt;/h4&gt;&lt;p&gt;The solve stage throws the image away. The debug stage does the opposite, and it’s the one place where the expensive model reads pixels directly.&lt;/p&gt;&lt;p&gt;When you’ve got a solution and it’s failing tests, you screenshot the error and ask for a fix. That flow runs the error screenshot through a dedicated classifier that sorts what it’s looking at:&lt;/p&gt;&lt;pre&gt;// src/solutions/types.ts&lt;br&gt;export enum IssueType {&lt;br&gt;  FAILING_TEST_CASES = &amp;#39;FAILING_TEST_CASES&amp;#39;,&lt;br&gt;  RUNTIME_ERROR = &amp;#39;RUNTIME_ERROR&amp;#39;,&lt;br&gt;  NOTHING_FOUND = &amp;#39;NOTHING_FOUND&amp;#39;,&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;A failing assertion is a different kind of problem from a stack trace, and NOTHING_FOUND exists precisely so the model can admit it doesn&amp;#39;t see an actual error rather than inventing one. Then the debug prompt is told, plainly, what to do with it:&lt;/p&gt;&lt;pre&gt;Consider errors provided in screenshot 3 and avoid writing code&lt;br&gt;that repeats those errors.&lt;/pre&gt;&lt;p&gt;The reason debug can do this is that the whole conversation is rebuilt and replayed. Each solve-then-debug chain shares a conversation id, and prior steps are reconstructed into the message history so the model remembers the original problem and its own previous attempt:&lt;/p&gt;&lt;pre&gt;// src/integrations/prompts/core/utils/prompt.utils.ts&lt;br&gt;buildConversationHistory(conversation: ConversationContext): MessageParam[] {&lt;br&gt;  const messages: MessageParam[] = [];&lt;br&gt;  for (const step of conversation.steps) {&lt;br&gt;    messages.push({ role: &amp;#39;user&amp;#39;, content: [{ type: &amp;#39;text&amp;#39;, text: step.request }] });&lt;br&gt;    messages.push({ role: &amp;#39;assistant&amp;#39;, content: [{ type: &amp;#39;text&amp;#39;, text: step.response }] });&lt;br&gt;  }&lt;br&gt;  return messages;&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;This is the engineered fix for “the model forgot what we were doing.” Without it, every debug would be a cold read of an error with no idea what problem it belonged to. With it, the model sees the arc: here was the problem, here’s what you wrote, here’s what broke, don’t do that again. Debug also runs at a higher effort budget with adaptive thinking turned on, because fixing a wrong answer is genuinely harder than producing the first one.&lt;/p&gt;&lt;h3&gt;The Failure the Model Never Warns You About&lt;/h3&gt;&lt;p&gt;Here’s the one that still bothers me, and it ties everything above together.&lt;/p&gt;&lt;p&gt;There is no confidence signal anywhere in this pipeline. The classification gate is binary, valid or not. When something throws, the user gets a generic “Error occurred.” What’s missing is any sense of doubt: the read stage can’t tell you it’s only 60% sure it parsed a constraint, and nothing flags a transcript that came off a low-quality image. The pipeline reports success and failure, but never uncertainty.&lt;/p&gt;&lt;p&gt;Stack that on top of the split-brain architecture and you get the genuinely dangerous case: Sonnet misreads a constraint, returns it at temperature zero with no hedging, Opus solves the misread version at high effort, and you receive a confident, well-commented, correctly-formatted solution to a problem that’s subtly not yours. Every layer did its job. The output looks exactly as trustworthy as a correct one.&lt;/p&gt;&lt;p&gt;A wrong answer that looks uncertain is easy to catch. A wrong answer that looks polished is not. If I were ranking what to build next, it wouldn’t be theme handling or whiteboard support. It would be a confidence signal, some way for the read stage to tell you when it isn’t sure, so you know to re-shoot before you trust the output. That’s the real gap, and I’d rather name it than hide it.&lt;/p&gt;&lt;h3&gt;What I Learned&lt;/h3&gt;&lt;p&gt;A few things that might transfer to anyone building on top of a vision model.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;The failures get subtler as the models get better.&lt;/strong&gt; A model that can’t read your screen at all is easy to deal with, you see garbage and try again. A model that reads it 98% right is harder, because the 2% is plausible and confident. Better vision moved my problem from “does it work” to “how do I know when it didn’t.”&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Split the cheap reader from the expensive thinker, but respect the seam.&lt;/strong&gt; Using Sonnet to read and classify before paying for an Opus solve is good engineering. It’s cheaper, faster, and it lets me gate junk early. But the seam between them is a string, and a string can’t carry uncertainty. The architecture that saves you money is the same one that hides the misread.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Most of the real mitigations are prompt-level and boring.&lt;/strong&gt; The classification gate is a prompt and an if statement. The error-avoidance in debug is one sentence. The readable-names option is a string you append. There’s no computer vision wizardry in here. The leverage was in deciding what to ask the model and when to not call it at all.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Be honest about what you didn’t build.&lt;/strong&gt; Theme handling, whiteboard support, a legibility check on compressed images, a confidence score, none of those exist. Some I skipped because the model made them unnecessary, one I skipped and shouldn’t have. Knowing which is which is the actual engineering.&lt;/p&gt;&lt;h3&gt;The Bigger Picture&lt;/h3&gt;&lt;p&gt;If you’re putting a vision model anywhere near the front of your product, the lesson isn’t “vision works now,” even though it mostly does. It’s that the interesting work starts after it works. Where does a confident misread go? What can’t catch it? What does the user see when the model is wrong but doesn’t know it?&lt;/p&gt;&lt;p&gt;For an interview assistant the stakes are small and the moment is yours to redo. For a medical scan, a legal document, a financial statement, the same architecture with the same blind seam is a much scarier thing. The pattern is identical: a cheap model reads, an expensive model reasons, and nothing in between admits doubt.&lt;/p&gt;&lt;p&gt;If you want to see exactly how this is wired, all of it is open source: &lt;a href=&quot;https://github.com/GetEzzi/ezzi-app&quot;&gt;github.com/GetEzzi/ezzi-app&lt;/a&gt;. The prompts, the gate, the capture layer, it’s all in there to read and pick apart.&lt;/p&gt;&lt;p&gt;&lt;em&gt;If you’ve shipped a vision model into production, I’d like to hear how you handle the confident-but-wrong case. That’s the part nobody seems to have a clean answer for, and the war stories are where the real knowledge is.&lt;/em&gt;&lt;/p&gt;&lt;p&gt;I hope this was helpful. Good luck, and happy engineering!&lt;/p&gt;&lt;img src=&quot;https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=624bdb78b3c3&quot; width=&quot;1&quot; height=&quot;1&quot; alt=&quot;&quot;&gt;&lt;hr&gt;&lt;p&gt;&lt;a href=&quot;https://levelup.gitconnected.com/claude-can-see-my-screen-heres-what-it-gets-wrong-624bdb78b3c3&quot;&gt;Claude Can See My Screen. Here’s What It Gets Wrong.&lt;/a&gt; was originally published in &lt;a href=&quot;https://levelup.gitconnected.com&quot;&gt;Level Up Coding&lt;/a&gt; on Medium, where people are continuing the conversation by highlighting and responding to this story.&lt;/p&gt;</content:encoded>
    </item>
    <item>
      <title>Plant, Plant, Plant: What Word Representation Gets Wrong</title>
      <link>https://levelup.gitconnected.com/plant-plant-plant-what-word-representation-gets-wrong-6b942de6a4a6?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/plant-plant-plant-what-word-representation-gets-wrong-6b942de6a4a6?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Wed, 24 Jun 2026 16:37:35 GMT</pubDate>
      <content:encoded>&lt;div class=&quot;medium-feed-item&quot;&gt;&lt;p class=&quot;medium-feed-image&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/plant-plant-plant-what-word-representation-gets-wrong-6b942de6a4a6?source=rss----5517fd7b58a6---4&quot;&gt;&lt;img src=&quot;https://cdn-images-1.medium.com/max/1536/1*Amdttv3PdQSi0l7xOcJuog.png&quot; width=&quot;1536&quot;&gt;&lt;/a&gt;&lt;/p&gt;&lt;p class=&quot;medium-feed-snippet&quot;&gt;Why early language systems treated every &amp;#x201C;plant&amp;#x201D; as the same thing &amp;#x2014; and how that single design choice created years of hidden mistakes.&lt;/p&gt;&lt;p class=&quot;medium-feed-link&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/plant-plant-plant-what-word-representation-gets-wrong-6b942de6a4a6?source=rss----5517fd7b58a6---4&quot;&gt;Continue reading on Level Up Coding »&lt;/a&gt;&lt;/p&gt;&lt;/div&gt;</content:encoded>
    </item>
    <item>
      <title>Building a Local Multimodal Search Engine with Gemma 4 and Qdrant: A Step-by-Step Build Guide</title>
      <link>https://levelup.gitconnected.com/building-a-local-multimodal-search-engine-with-gemma-4-and-qdrant-a-step-by-step-build-guide-45447aca65ab?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/building-a-local-multimodal-search-engine-with-gemma-4-and-qdrant-a-step-by-step-build-guide-45447aca65ab?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Wed, 24 Jun 2026 16:37:28 GMT</pubDate>
      <content:encoded>&lt;h4&gt;&lt;em&gt;How I made hours of video, audio, and text searchable by plain language, and how you can rebuild it from scratch, entirely on your own machine&lt;/em&gt;&lt;/h4&gt;&lt;h3&gt;What we’re building (and why)&lt;/h3&gt;&lt;p&gt;That single constraint, local first, shaped every decision in this build. It is the reason the architecture looks the way it does, and it’s also why this is a tutorial about two tools doing a lot of work locally: &lt;strong&gt;Gemma 4&lt;/strong&gt; (served by Ollama) for understanding pixels and audio, and &lt;strong&gt;Qdrant&lt;/strong&gt; for storing and searching vectors.&lt;/p&gt;&lt;p&gt;This guide walks the whole thing from an empty folder to a working search engine, in the order I actually built it:&lt;/p&gt;&lt;ol&gt;&lt;li&gt;Set up the environment (uv, FFmpeg, Ollama, models)&lt;/li&gt;&lt;li&gt;Understand the one design decision that makes multimodal search work: &lt;strong&gt;describe-then-embed&lt;/strong&gt;&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Install and choose Qdrant&lt;/strong&gt;, and why I picked it&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Design the Qdrant schema&lt;/strong&gt; for moments, not files (the heart of this tutorial)&lt;/li&gt;&lt;li&gt;Build the describe-then-embed bridge with Gemma 4&lt;/li&gt;&lt;li&gt;Build the ingestion pipeline (how to actually get data in)&lt;/li&gt;&lt;li&gt;Query: per-space search, fusion, MaxSim, rerank&lt;/li&gt;&lt;li&gt;Route the questions retrieval shouldn’t answer (counting, inventory)&lt;/li&gt;&lt;li&gt;Stream the answers, add guardrails, and learn from the performance traps&lt;/li&gt;&lt;/ol&gt;&lt;p&gt;Let’s go.&lt;/p&gt;&lt;h3&gt;Step 0: Prerequisites and environment&lt;/h3&gt;&lt;p&gt;You need three things on the machine before running Python:&lt;/p&gt;&lt;ol&gt;&lt;li&gt;&lt;strong&gt;macOS (Apple Silicon recommended) or Linux&lt;/strong&gt;&lt;/li&gt;&lt;li&gt;&lt;a href=&quot;https://docs.astral.sh/uv/&quot;&gt;`uv`&lt;/a&gt;, the Python package/venv manager this project uses&lt;/li&gt;&lt;li&gt;&lt;a href=&quot;https://ffmpeg.org/&quot;&gt;`ffmpeg`&lt;/a&gt;, every video/audio extraction step shells out to it&lt;/li&gt;&lt;li&gt;&lt;a href=&quot;https://ollama.com&quot;&gt;Ollama&lt;/a&gt; ≥ 0.30, the local runtime that serves Gemma 4&lt;/li&gt;&lt;/ol&gt;&lt;p&gt;&lt;strong&gt;On macOS:&lt;/strong&gt;&lt;/p&gt;&lt;pre&gt;# Tooling&lt;br&gt;brew install ffmpeg&lt;br&gt;curl -LsSf https://astral.sh/uv/install.sh | sh&lt;br&gt;# Ollama: download from https://ollama.com and launch it (it runs a local server on :11434)&lt;br&gt;&lt;br&gt;# 1. venv + editable install (+ dev tools)&lt;br&gt;uv venv --python 3.12&lt;br&gt;uv pip install -e &amp;quot;.[dev]&amp;quot;&lt;br&gt; &lt;br&gt;# 2. Pull the models (≈10 GB for gemma4:e4b)&lt;br&gt;ollama pull gemma4:e4b&lt;br&gt;ollama pull nomic-embed-text&lt;/pre&gt;&lt;p&gt;&lt;strong&gt;On Windows:&lt;/strong&gt;&lt;/p&gt;&lt;pre&gt;1. Install UV&lt;br&gt;Open PowerShell as Administrator:&lt;br&gt;powershell -ExecutionPolicy ByPass -c &amp;quot;irm https://astral.sh/uv/install.ps1 | iex&amp;quot;&lt;br&gt;uv --version&lt;br&gt;&lt;br&gt;&lt;br&gt;2. Install FFmpeg - powershell&lt;br&gt;winget install Gyan.FFmpeg&lt;br&gt;ffmpeg -version&lt;/pre&gt;&lt;p&gt;Restart PowerShell if FFmpeg is not found.&lt;/p&gt;&lt;h4&gt;Two models, two jobs:&lt;/h4&gt;&lt;ul&gt;&lt;li&gt;&lt;strong&gt;`gemma4:e4b`&lt;/strong&gt;, the multimodal brain. It reads images and listens to audio natively, and it also drives query-intent analysis, reranking, and the RAG chat. The E in E4B stands for effective parameters: an efficient design that keeps the memory footprint friendly on Apple Silicon while keeping vision and audio in the same model.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;`nomic-embed-text`&lt;/strong&gt;, a dedicated text embedder that maps everything into one unified &lt;strong&gt;768-dimensional&lt;/strong&gt; space.&lt;/li&gt;&lt;/ul&gt;&lt;blockquote&gt;&lt;strong&gt;Don’t have the weights yet?&lt;/strong&gt; The whole pipeline, ingest, index, search, RAG runs against a deterministic hash-based stub so you can develop offline and in CI:&lt;/blockquote&gt;&lt;blockquote&gt;DEEPSEARCH_USE_STUB=true uv run uvicorn deepsearch.api.main:app — port 8000 ``&lt;/blockquote&gt;&lt;h3&gt;The “two-model trap” I avoided&lt;/h3&gt;&lt;p&gt;The obvious first instinct is to reach for a pipeline with three or four specialist models: one for speech-to-text, one for OCR on the frames, one vision-language model for the screen, and glue to stitch the outputs together. That works, and it’s a maintenance tax you pay forever. Four downloads, four sets of version drift, four failure modes.&lt;/p&gt;&lt;p&gt;Gemma 4 changed that: one model reads images and listens to audio. So the model question was settled early, and the interesting problem moved to &lt;strong&gt;retrieval&lt;/strong&gt;, which is where Qdrant comes in.&lt;/p&gt;&lt;h3&gt;Step 1: The one idea that makes multimodal search work: describe, then embed&lt;/h3&gt;&lt;p&gt;Before any schema, you have to internalise the single most important design decision in the system. Get this wrong and nothing downstream matters.&lt;/p&gt;&lt;p&gt;Here is the trap, and it’s a good one because it sounds correct:&lt;/p&gt;&lt;blockquote&gt;“Gemma is multimodal, so I’ll embed the video frames into vectors, embed the audio into vectors, embed the text into vectors, store all of them in Qdrant, and at query time embed the user’s question and search across all three spaces. Cross-modal search, solved.”&lt;/blockquote&gt;&lt;p&gt;&lt;strong&gt;It does not work.&lt;/strong&gt; Gemma 4 is a generative decoder. It is &lt;strong&gt;not&lt;/strong&gt; a contrastive dual-encoder like CLIP or ImageBind. Models like CLIP are trained with one specific objective: to push an image and its caption close together in a shared space. Gemma has no such objective. Its internal image activations are optimised for predicting the next token, not for sitting near the text that describes them.&lt;/p&gt;&lt;p&gt;If you index raw frame activations and search them with a text query vector, you’re comparing two things in different coordinate systems. The cosine similarity is close to noise. You’ll get results, and they’ll be confidently wrong.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;The fix:&lt;/strong&gt; stop treating Gemma as an embedder and start treating it as a translator.&lt;/p&gt;&lt;ol&gt;&lt;li&gt;Gemma &lt;strong&gt;describes&lt;/strong&gt; each keyframe in a sentence or two.&lt;/li&gt;&lt;li&gt;Gemma (or Whisper, more on that later) &lt;strong&gt;transcribes&lt;/strong&gt; each audio chunk into text.&lt;/li&gt;&lt;li&gt;Captions are already text.&lt;/li&gt;&lt;/ol&gt;&lt;p&gt;Now every modality is text, and a single dedicated embedder (nomic-embed-text) maps all of it into &lt;strong&gt;one unified 768-dimensional space&lt;/strong&gt;. The user query gets embedded by the same model into the same space. A natural-language query now genuinely retrieves across video, audio, and text, because everything, including the question, lives in one coordinate system.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/936/1*WMQE8duhde0034svrvAQzg.png&quot; /&gt;&lt;figcaption&gt;Image created by Author&lt;/figcaption&gt;&lt;/figure&gt;&lt;h3&gt;Step 2: Installing Qdrant (and why I chose it)&lt;/h3&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*8eu7FsXzkkb-xPtG9zqcsA.png&quot; /&gt;&lt;/figure&gt;&lt;h3&gt;Why Qdrant&lt;/h3&gt;&lt;p&gt;I evaluated several vector databases for this architecture, and Qdrant emerged as the optimal choice due to its alignment with local-first, multimodal constraints:&lt;/p&gt;&lt;p&gt;Qdrant Documentation Link: &lt;a href=&quot;https://qdrant.tech/documentation/&quot;&gt;https://qdrant.tech/documentation/&lt;/a&gt;&lt;/p&gt;&lt;ol&gt;&lt;li&gt;&lt;strong&gt;Embedded, Zero-Infrastructure Deployment:&lt;/strong&gt; The primary constraint is a local-first environment without cloud dependencies. Qdrant’s Python client provides an in-process, disk-backed database — QdrantClient(path=”./qdrant_storage”) — that requires no Docker or daemons. This turns the project into a portable laptop tool.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Seamless Scalability:&lt;/strong&gt; The application code is agnostic to the database topology. By toggling the DEEPSEARCH_QDRANT_URL environment variable, the same codebase transitions from embedded local storage to a remote Qdrant server, enabling fleet-wide scaling without rewriting the storage logic.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;First-Class Named Vectors:&lt;/strong&gt; Qdrant supports multiple independently searchable vector fields per point. This maps perfectly to our requirement to manage distinct text, video, and audio embedding spaces within a single collection, eliminating the need to synchronise separate indexes.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Native ColBERT-style MaxSim:&lt;/strong&gt; Late-interaction retrieval is a native concept rather than an implementation challenge, providing efficient multi-vector support out of the box.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Integrated Quantisation and Filtering:&lt;/strong&gt; Features like INT8 scalar quantisation (to optimise index size in unified memory) and metadata pre-filtering are simple configurations rather than engineering hurdles.&lt;/li&gt;&lt;/ol&gt;&lt;p&gt;These are simple configurations, not heavy engineering tasks.&lt;/p&gt;&lt;h3&gt;Option A — Embedded local mode (the default, recommended to start)&lt;/h3&gt;&lt;p&gt;There is nothing to install beyond the Python client, which uv pip install -e. already pulled (qdrant-client&amp;gt;=1.12). The store simply opens a folder:&lt;/p&gt;&lt;pre&gt;# src/deepsearch/vectorstore/store.py&lt;br&gt;def _connect(self) -&amp;gt; QdrantClient:&lt;br&gt;    url = self.settings.qdrant.url.strip()&lt;br&gt;    if url:                                   # server mode&lt;br&gt;        log.info(f&amp;quot;Connecting to Qdrant server at {url}&amp;quot;)&lt;br&gt;        return QdrantClient(url=url)&lt;br&gt;    path = self.settings.qdrant_storage_path  # embedded local mode&lt;br&gt;    path.mkdir(parents=True, exist_ok=True)&lt;br&gt;    log.info(f&amp;quot;Using embedded Qdrant (local mode) at {path}&amp;quot;)&lt;br&gt;    return QdrantClient(path=str(path))&lt;/pre&gt;&lt;p&gt;With &lt;em&gt;DEEPSEARCH_QDRANT_URL&lt;/em&gt; empty (the default), the index lives in ./qdrant_storage. Done. No server.&lt;/p&gt;&lt;h3&gt;Option B — Qdrant as a server (when you want the dashboard or scale)&lt;/h3&gt;&lt;p&gt;When you outgrow embedded mode, you want the web dashboard, or you want several processes hitting one index, flip to a server with Docker:&lt;/p&gt;&lt;pre&gt;docker compose up -d                       # starts qdrant on :6333 (REST) / :6334 (gRPC)&lt;br&gt;export DEEPSEARCH_QDRANT_URL=http://localhost:6333&lt;/pre&gt;&lt;p&gt;The docker-compose.yml is tiny:&lt;/p&gt;&lt;pre&gt;services:&lt;br&gt;  qdrant:&lt;br&gt;    image: qdrant/qdrant:latest&lt;br&gt;    container_name: deepsearch-qdrant&lt;br&gt;    ports:&lt;br&gt;      - &amp;quot;6333:6333&amp;quot;   # REST / dashboard at http://localhost:6333/dashboard&lt;br&gt;      - &amp;quot;6334:6334&amp;quot;   # gRPC&lt;br&gt;    volumes:&lt;br&gt;      - ./qdrant_storage:/qdrant/storage&lt;br&gt;    restart: unless-stopped&lt;/pre&gt;&lt;h3&gt;Step 3: Designing the Qdrant schema for moments, not files&lt;/h3&gt;&lt;p&gt;This is the heart of the build. Two requirements pull against each other here, and how you resolve the tension determines whether timestamps are free or impossible.&lt;/p&gt;&lt;h4&gt;Requirement 1: Multivectors (ColBERT-style late interaction)&lt;/h4&gt;&lt;p&gt;Qdrant supports an &lt;strong&gt;array of vectors per named field&lt;/strong&gt;, scored with &lt;strong&gt;MaxSim&lt;/strong&gt;, the ColBERT-style late-interaction idea. A single asset (a whole video) can hold, say twenty keyframe vectors, and you score the entire asset by its best-matching frame. This gives fast, high-recall asset-level retrieval: “Which files are even relevant?”&lt;/p&gt;&lt;h4&gt;Requirement 2: Accurate playback&lt;/h4&gt;&lt;p&gt;You don’t want to know that a file matches. You want the &lt;strong&gt;exact offset&lt;/strong&gt; so the player can seek to the second.&lt;/p&gt;&lt;h3&gt;Why they fight&lt;/h3&gt;&lt;p&gt;Asset-level MaxSim gives you &lt;strong&gt;one score per asset&lt;/strong&gt; and throws away the sub-vector matched. You lose the timestamp. So you cannot make the multivector point your primary retrieval unit, not if pinpoint playback is the whole point.&lt;/p&gt;&lt;h4&gt;The resolution: the fragment is the primary retrieval unit&lt;/h4&gt;&lt;p&gt;So I index at two granularities in &lt;strong&gt;one collection&lt;/strong&gt;:&lt;/p&gt;&lt;ol&gt;&lt;li&gt;&lt;strong&gt;Fragment points (primary).&lt;/strong&gt; One Qdrant point per keyframe, per audio chunk, per caption line. Each fragment populates &lt;strong&gt;exactly one&lt;/strong&gt; of three named single-vector spaces and carries a payload with the precise offsets. These are what make timestamps free.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Asset-level MaxSim points (secondary).&lt;/strong&gt; One point per asset holding the array of that asset’s frame/audio vectors, in two multivector spaces, for fast recall.&lt;/li&gt;&lt;/ol&gt;&lt;p&gt;The three named single-vector spaces and the two multivector spaces:&lt;/p&gt;&lt;pre&gt;# src/deepsearch/vectorstore/schema.py&lt;br&gt;# Fragment-level, temporal targeting, one populated per fragment point.&lt;br&gt;NAMED_VECTORS = (&amp;quot;text_descriptions&amp;quot;, &amp;quot;video_frames&amp;quot;, &amp;quot;audio_chunks&amp;quot;)&lt;br&gt; &lt;br&gt;# Asset-level MaxSim multivector spaces (ColBERT-style arrays per asset).&lt;br&gt;MULTIVECTOR_VECTORS = (&amp;quot;frames_maxsim&amp;quot;, &amp;quot;audio_maxsim&amp;quot;)&lt;/pre&gt;&lt;p&gt;And the fragment data model — note that the payload carries start_s / end_s, which is where second-accurate playback comes from:&lt;/p&gt;&lt;pre&gt;@dataclass&lt;br&gt;class Fragment:&lt;br&gt;    asset_id: str               # stable id shared by all fragments of one file&lt;br&gt;    asset_name: str&lt;br&gt;    file_path: str              # absolute path to the source media on disk&lt;br&gt;    modality: Modality          # TEXT | VIDEO | AUDIO&lt;br&gt;    text: str                   # description / transcript / caption (this is what gets embedded)&lt;br&gt;    start_s: float = 0.0        # fragment start offset within the asset&lt;br&gt;    end_s: float = 0.0          # fragment end offset within the asset&lt;br&gt;    duration_s: float = 0.0&lt;br&gt;    category: str = &amp;quot;uncategorized&amp;quot;&lt;br&gt;    thumbnail_path: str | None = None&lt;br&gt;    ingested_at: int = ...&lt;br&gt;    point_id: str = ...&lt;br&gt; &lt;br&gt;# Payload fields indexed for fast pre-filtering:&lt;br&gt;KEYWORD_INDEXES = (&amp;quot;asset_id&amp;quot;, &amp;quot;modality&amp;quot;, &amp;quot;category&amp;quot;)&lt;br&gt;INTEGER_INDEXES = (&amp;quot;ingested_at&amp;quot;,)&lt;/pre&gt;&lt;h4&gt;Creating the collection (the multivector setup, line by line)&lt;/h4&gt;&lt;p&gt;Here is ensure_collection, the one function that declares the whole schema:&lt;/p&gt;&lt;pre&gt;# src/deepsearch/vectorstore/store.py&lt;br&gt;def ensure_collection(self, recreate: bool = False) -&amp;gt; None:&lt;br&gt;    dim = self._embedding_dim()      # 768 for nomic-embed-text (probed live at startup)&lt;br&gt;    distance = self._distance()      # Cosine&lt;br&gt; &lt;br&gt;    # 1) Three single-vector named spaces  , one per modality (fragment level).&lt;br&gt;    vectors = {&lt;br&gt;        name: models.VectorParams(size=dim, distance=distance)&lt;br&gt;        for name in NAMED_VECTORS    # text_descriptions, video_frames, audio_chunks&lt;br&gt;    }&lt;br&gt; &lt;br&gt;    # 2) Two MaxSim multivector spaces (ColBERT-style late interaction, asset level).&lt;br&gt;    for name in MULTIVECTOR_VECTORS:  # frames_maxsim, audio_maxsim&lt;br&gt;        vectors[name] = models.VectorParams(&lt;br&gt;            size=dim,&lt;br&gt;            distance=distance,&lt;br&gt;            multivector_config=models.MultiVectorConfig(&lt;br&gt;                comparator=models.MultiVectorComparator.MAX_SIM&lt;br&gt;            ),&lt;br&gt;        )&lt;br&gt; &lt;br&gt;    # 3) Create the collection with INT8 scalar quantization (always in RAM).&lt;br&gt;    self.client.create_collection(&lt;br&gt;        collection_name=self.collection,&lt;br&gt;        vectors_config=vectors,&lt;br&gt;        quantization_config=self._quantization(),   # scalar INT8, always_ram&lt;br&gt;    )&lt;br&gt; &lt;br&gt;    # 4) Payload indexes so Qdrant can PRE-FILTER before the vector search.&lt;br&gt;    for field in KEYWORD_INDEXES:    # asset_id, modality, category&lt;br&gt;        self.client.create_payload_index(&lt;br&gt;            self.collection, field, models.PayloadSchemaType.KEYWORD&lt;br&gt;        )&lt;br&gt;    for field in INTEGER_INDEXES:    # ingested_at&lt;br&gt;        self.client.create_payload_index(&lt;br&gt;            self.collection, field, models.PayloadSchemaType.INTEGER&lt;br&gt;        )&lt;/pre&gt;&lt;p&gt;Four things are happening, and each earns its place:&lt;/p&gt;&lt;ol&gt;&lt;li&gt;&lt;strong&gt;Named single-vector spaces&lt;/strong&gt;: the fragment-level spaces a text query searches directly.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;MaxSim multivector spaces&lt;/strong&gt;: declared with MultiVectorConfig(comparator=MAX_SIM). This is the entire multivector setup — same dimension, same distance, plus the MaxSim comparator. That one line is what turns a named field into a ColBERT-style array.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Scalar quantisation&lt;/strong&gt;: compresses the vectors to INT8 and pins them in RAM (always_ram). This is what lets the database coexist with a multi-gigabyte model inside the same unified memory on a laptop.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Payload indexes&lt;/strong&gt;: they let Qdrant pre-filter before the vector search runs, so a query like “only videos added in the last thirty days” becomes a cheap keyword + integer filter instead of a full scan.&lt;/li&gt;&lt;/ol&gt;&lt;h4&gt;Writing a fragment point&lt;/h4&gt;&lt;p&gt;Each fragment populates &lt;strong&gt;exactly one&lt;/strong&gt; named vector, the one matching its modality:&lt;/p&gt;&lt;pre&gt;def upsert_fragments(self, fragments, vectors) -&amp;gt; int:&lt;br&gt;    points = []&lt;br&gt;    for frag, vec in zip(fragments, vectors, strict=True):&lt;br&gt;        points.append(&lt;br&gt;            models.PointStruct(&lt;br&gt;                id=frag.point_id,&lt;br&gt;                vector={frag.modality.value: vec},  # populate ONE named space&lt;br&gt;                payload=frag.payload(),             # asset_id, file_path, start_s, end_s, …&lt;br&gt;            )&lt;br&gt;        )&lt;br&gt;    if points:&lt;br&gt;        self.client.upsert(self.collection, points=points, wait=True)&lt;br&gt;    return len(points)&lt;/pre&gt;&lt;h4&gt;Writing the asset-level MaxSim point&lt;/h4&gt;&lt;p&gt;One point per asset, holding the arrays of frame and audio vectors. The point ID is namespaced so it never collides with a fragment ID:&lt;/p&gt;&lt;pre&gt;def upsert_asset_multivectors(self, asset_id, payload, frame_vectors, audio_vectors):&lt;br&gt;    vec: dict[str, list[list[float]]] = {}&lt;br&gt;    if frame_vectors:&lt;br&gt;        vec[&amp;quot;frames_maxsim&amp;quot;] = frame_vectors   # array of N frame vectors&lt;br&gt;    if audio_vectors:&lt;br&gt;        vec[&amp;quot;audio_maxsim&amp;quot;] = audio_vectors    # array of M audio vectors&lt;br&gt;    if not vec:&lt;br&gt;        return&lt;br&gt;    point_id = str(uuid.uuid5(uuid.NAMESPACE_OID, f&amp;quot;asset:{asset_id}&amp;quot;))&lt;br&gt;    self.client.upsert(&lt;br&gt;        self.collection,&lt;br&gt;        points=[models.PointStruct(id=point_id, vector=vec, payload=payload)],&lt;br&gt;        wait=True,&lt;br&gt;    )&lt;/pre&gt;&lt;p&gt;A sharp edge worth flagging: compute MaxSim client-side in embedded mode&lt;/p&gt;&lt;p&gt;Here’s a gotcha that cost me time. The &lt;strong&gt;embedded&lt;/strong&gt; Qdrant client (the on-disk, no-server mode) does &lt;strong&gt;not&lt;/strong&gt; reliably run native multivector queries. I tried both the 1-D and 2-D query shapes and got internal errors. The portable fix is to compute MaxSim &lt;strong&gt;on the client&lt;/strong&gt;: scroll the asset-level points, take the maximum dot product between the query vector and each stored sub-vector, and rank. Because all vectors are L2-normalised, the dot product is cosine similarity. This runs identically in embedded and server mode:&lt;/p&gt;&lt;pre&gt;def search_maxsim(self, vector_name, query_vector, limit, query_filter=None):&lt;br&gt;    records, _ = self.client.scroll(&lt;br&gt;        self.collection, scroll_filter=query_filter,&lt;br&gt;        limit=10_000, with_payload=True, with_vectors=[vector_name],&lt;br&gt;    )&lt;br&gt;    scored = []&lt;br&gt;    for r in records:&lt;br&gt;        vecs = (r.vector or {}).get(vector_name) if isinstance(r.vector, dict) else None&lt;br&gt;        if not vecs:&lt;br&gt;            continue&lt;br&gt;        best = max(_dot(query_vector, sub) for sub in vecs)   # MaxSim&lt;br&gt;        scored.append(models.ScoredPoint(id=r.id, version=0, score=best, payload=r.payload))&lt;br&gt;    scored.sort(key=lambda p: p.score, reverse=True)&lt;br&gt;    return scored[:limit]&lt;/pre&gt;&lt;blockquote&gt;&lt;strong&gt;The takeaway for this section:&lt;/strong&gt; pick your retrieval unit before anything else. Fragments-versus-assets is not a detail; it decides whether timestamps are free or impossible. I get both: fragments for the exact second, an asset-level MaxSim point for fast recall, all in one Qdrant collection.&lt;/blockquote&gt;&lt;h3&gt;Step 4: The describe-then-embed bridge (Gemma 4 client)&lt;/h3&gt;&lt;p&gt;Now make Step 1 real. The model client never pretends to produce retrieval embeddings from pixels; it produces words, which the embedder then turns into vectors.&lt;/p&gt;&lt;pre&gt;# src/deepsearch/models/gemma.py&lt;br&gt;def describe_image(self, image_path) -&amp;gt; str:&lt;br&gt;    if self.is_stub:&lt;br&gt;        return f&amp;quot;[stub frame description for {Path(image_path).name}]&amp;quot;&lt;br&gt;    b64 = _b64_file(image_path)&lt;br&gt;    resp = self._invoke(&lt;br&gt;        self._client.generate,&lt;br&gt;        model=self.settings.models.gemma_model,&lt;br&gt;        prompt=_FRAME_PROMPT,&lt;br&gt;        images=[b64],&lt;br&gt;        options=self._desc_opts(),&lt;br&gt;        keep_alive=self.settings.models.keep_alive,&lt;br&gt;        think=False,        # see note below&lt;br&gt;    )&lt;br&gt;    return resp[&amp;quot;response&amp;quot;].strip()&lt;/pre&gt;&lt;p&gt;Two details earn their place:&lt;/p&gt;&lt;ol&gt;&lt;li&gt;&lt;strong&gt;`think=False`.&lt;/strong&gt; Gemma 4 is a thinking model. If you let it think while capping the output budget (num_predict), the reasoning tokens eat the budget, and you get an empty answer. I learned that the hard way: ingestion produced zero fragments and crashed on an empty vector. Turning thinking off for description/transcription fixed it.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;`keep_alive` (30m).&lt;/strong&gt; Keeps the weights resident in VRAM between calls so the next one doesn’t reload the model, a big latency win on local Apple Silicon.&lt;/li&gt;&lt;/ol&gt;&lt;p&gt;The embedding side is just as plain, empty input is guarded, and the dimension is checked, so a misconfigured model fails instead of silently corrupting the index:&lt;/p&gt;&lt;pre&gt;def embed_text(self, texts: list[str]) -&amp;gt; list[list[float]]:&lt;br&gt;    if not texts:&lt;br&gt;        return []&lt;br&gt;    out = []&lt;br&gt;    ka = self.settings.models.keep_alive&lt;br&gt;    for text in texts:&lt;br&gt;        prompt = text if text and text.strip() else &amp;quot; &amp;quot;   # never send an empty prompt&lt;br&gt;        resp = self._invoke(self._client.embeddings,&lt;br&gt;                            model=self.embed_model_name, prompt=prompt, keep_alive=ka)&lt;br&gt;        vec = list(resp.get(&amp;quot;embedding&amp;quot;) or [])&lt;br&gt;        if len(vec) != self.dim:&lt;br&gt;            raise ValueError(f&amp;quot;Embedding model returned {len(vec)} dims (expected {self.dim}).&amp;quot;)&lt;br&gt;        out.append(_normalize(vec))   # L2-normalise so dot product == cosine&lt;br&gt;    return out&lt;/pre&gt;&lt;p&gt;Note the _normalise at the end, that’s the invariant the client-side MaxSim relies on.&lt;/p&gt;&lt;h3&gt;Step 5: The ingestion pipeline (how to actually ingest)&lt;/h3&gt;&lt;p&gt;With the schema and the bridge in place, ingestion is the orchestration that fills the index. For each file:&lt;/p&gt;&lt;pre&gt;video  → keyframes (described) + audio chunks (transcribed) + sidecar captions&lt;br&gt;audio  → audio chunks (transcribed)&lt;br&gt;image  → single description&lt;br&gt;text   → timed/untimed segments&lt;/pre&gt;&lt;h3&gt;Run it&lt;/h3&gt;&lt;p&gt;From the CLI:&lt;/p&gt;&lt;pre&gt;# Index a single file or a whole folder&lt;br&gt;uv run deepsearch ingest ./data/media --category meetings&lt;br&gt; &lt;br&gt;# Or via the Make target&lt;br&gt;make ingest P=./data/media C=meetings&lt;/pre&gt;&lt;p&gt;Or from the UI: open the &lt;strong&gt;Ingest&lt;/strong&gt; tab, drop in video/audio/.srt/.vtt/text, pick a category, click &lt;strong&gt;Ingest&lt;/strong&gt;. The backend streams live progress over Server-Sent Events.&lt;/p&gt;&lt;h3&gt;What happens inside ingest_file&lt;/h3&gt;&lt;pre&gt;self.store.ensure_collection()      # create the schema if needed (idempotent)&lt;br&gt;asset_id = _asset_id(path)          # stable sha1 of the resolved path&lt;br&gt;self.store.delete_asset(asset_id)   # re-ingest REPLACES, never duplicates&lt;br&gt;# ... build fragments per modality ...&lt;br&gt;fragments = [f for f in fragments if (f.text or &amp;quot;&amp;quot;).strip()]  # drop empties (zero-vectors break upsert)&lt;br&gt;self._embed_and_upsert(fragments, progress)&lt;/pre&gt;&lt;p&gt;&lt;strong&gt;Keyframes&lt;/strong&gt; come from scene-boundary detection (PySceneDetect’s content detector), falling back to uniform sampling, capped at max_keyframes (12 by default, fewer Gemma vision calls = much faster ingest). Each keyframe is written as a JPEG (used both as the Gemma input and the UI thumbnail) and tagged with its timestamp.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Audio&lt;/strong&gt; is normalised to 16 kHz mono WAV via FFmpeg (with optional afftdn denoise), then transcribed — see the performance note below for which transcriber.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Embedding and the dual write&lt;/strong&gt; happen together in _embed_and_upsert: fragments are embedded in batches, each batch is upserted as fragment points, and the per-modality vectors are accumulated so that, at the end, a single asset-level MaxSim point is written:&lt;/p&gt;&lt;pre&gt;def _embed_and_upsert(self, fragments, progress):&lt;br&gt;    frame_vecs, audio_vecs, asset_payload = [], [], None&lt;br&gt;    for group in _batched(fragments, self.settings.ingestion.batch_size):&lt;br&gt;        vectors = self.model.embed_text([f.text for f in group])&lt;br&gt;        self.store.upsert_fragments(group, vectors)          # fragment points&lt;br&gt;        for frag, vec in zip(group, vectors, strict=True):&lt;br&gt;            asset_payload = asset_payload or {... &amp;quot;kind&amp;quot;: &amp;quot;asset&amp;quot;}&lt;br&gt;            if frag.modality is Modality.VIDEO:   frame_vecs.append(vec)&lt;br&gt;            elif frag.modality is Modality.AUDIO: audio_vecs.append(vec)&lt;br&gt;    if asset_payload and (frame_vecs or audio_vecs):&lt;br&gt;        self.store.upsert_asset_multivectors(           # ONE asset-level MaxSim point&lt;br&gt;            asset_payload[&amp;quot;asset_id&amp;quot;], asset_payload, frame_vecs, audio_vecs&lt;br&gt;        )&lt;/pre&gt;&lt;h3&gt;Verify it landed&lt;/h3&gt;&lt;pre&gt;uv run deepsearch stats&lt;br&gt;# assets=1 fragments=37 by_modality={&amp;#39;video_frames&amp;#39;: 12, &amp;#39;audio_chunks&amp;#39;: 24, &amp;#39;text_descriptions&amp;#39;: 1}&lt;/pre&gt;&lt;h3&gt;Step 6: Querying: per-space search, fusion, MaxSim, rerank&lt;/h3&gt;&lt;p&gt;A search runs: &lt;strong&gt;safety filter → intent → embed → per-space prefetch → fuse → (optional) rerank → timestamped hits.&lt;/strong&gt;&lt;/p&gt;&lt;pre&gt;# src/deepsearch/query/search.py&lt;br&gt;qvec = self.model.embed_query(query)&lt;br&gt;flt = build_filter(categories, modalities, ingested_after)   # payload pre-filter&lt;br&gt; &lt;br&gt;# Only query spaces with non-trivial weight (saves work for focused intents).&lt;br&gt;spaces = [s for s in NAMED_VECTORS if weights.get(s, 0.0) &amp;gt; 0.01] or list(NAMED_VECTORS)&lt;br&gt;per_space = {&lt;br&gt;    space: self.store.search_named(space, qvec, cfg.prefetch_limit, flt)&lt;br&gt;    for space in spaces&lt;br&gt;}&lt;br&gt; &lt;br&gt;strategy = &amp;quot;weighted&amp;quot; if weights_override is not None else cfg.fusion&lt;br&gt;fused = fusion_mod.fuse(per_space, strategy, weights)&lt;br&gt; &lt;br&gt;top = fused[: cfg.rerank_top_n] if cfg.rerank else fused[: cfg.top_k]&lt;br&gt;if cfg.rerank and len(top) &amp;gt; 1:&lt;br&gt;    top = rerank(query, top, cfg.rerank_top_n)&lt;br&gt;hits = [self._to_hit(h) for h in top][: cfg.top_k]&lt;/pre&gt;&lt;h3&gt;Fusion is a client-side decision, not a Qdrant feature&lt;/h3&gt;&lt;p&gt;A lot of write-ups imply you can hand Qdrant a weight per named vector and let it blend the spaces. You can’t. Qdrant fuses candidate lists you define (via its Query API, prefetch, and RRF/DBSF); the weighting across named spaces happens in your code. So the engine searches each weighted space, then fuses:&lt;/p&gt;&lt;ol&gt;&lt;li&gt;&lt;strong&gt;RRF (Reciprocal Rank Fusion)&lt;/strong&gt; is the default, rank-based and scale-free, so it doesn’t care that the three spaces produce scores on different scales.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Weighted&lt;/strong&gt; kicks in when the user drags the weight sliders. That was a real bug at first: the weights existed, but the default RRF ignored them, so the sliders did nothing until the engine learned to honour an explicit override (strategy = “weighted” if weights_override is None).&lt;/li&gt;&lt;/ol&gt;&lt;h4&gt;One honest tradeoff: rerank is slow&lt;/h4&gt;&lt;p&gt;The Gemma rerank is a listwise prompt, and on a local 4-billion-effective-parameter model, a single rerank call can run for tens of seconds. So rerank &lt;strong&gt;and&lt;/strong&gt; the LLM intent step are &lt;strong&gt;off by default&lt;/strong&gt; for fast local search; you flip them on per request when quality matters more than latency. RRF on its own is already strong.&lt;/p&gt;&lt;h3&gt;Step 7: Route the questions retrieval shouldn’t answer&lt;/h3&gt;&lt;p&gt;Some questions are not retrieval questions, and knowing which to route away from the model is half of making a grounded assistant feel trustworthy.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Counting.&lt;/strong&gt; “How many times is finance mentioned?” must never be a model guess; it will round, hedge, and miss occurrences outside the top results. So counting is &lt;strong&gt;deterministic&lt;/strong&gt;: scan every fragment, count exact substring matches, and report the number with timestamps. No model in the loop.&lt;/p&gt;&lt;pre&gt;def _count(self, term: str) -&amp;gt; dict:&lt;br&gt;    needle = term.lower()&lt;br&gt;    total, sources, per_asset = 0, [], {}&lt;br&gt;    for p in self.store.all_fragments():&lt;br&gt;        n = (p.get(&amp;quot;text&amp;quot;) or &amp;quot;&amp;quot;).lower().count(needle)&lt;br&gt;        if n:&lt;br&gt;            total += n&lt;br&gt;            per_asset[p[&amp;quot;asset_name&amp;quot;]] = per_asset.get(p[&amp;quot;asset_name&amp;quot;], 0) + n&lt;br&gt;            sources.append(Source(...))&lt;br&gt;    # ... report total + timestamps ...&lt;/pre&gt;&lt;p&gt;&lt;strong&gt;Inventory.&lt;/strong&gt; “What types of files do I have?” is answered straight from the asset list, counted once per asset, with no sources, because no single fragment answers a library-wide question.&lt;/p&gt;&lt;h3&gt;Step 8: Stream the answer, then lock it down&lt;/h3&gt;&lt;h4&gt;Streaming, because waiting in silence feels broken&lt;/h4&gt;&lt;p&gt;The interface is a chat: FastAPI backend, Next.js frontend, answer streamed token-by-token. A chat that freezes for forty seconds and dumps a wall of text feels broken even when it’s working; a chat that types feels alive. Retrieval and deterministic routing happen first, then the LLM tokens flow as Server-Sent Events, then a final event carries the sources:&lt;/p&gt;&lt;pre&gt;@app.post(&amp;quot;/api/ask/stream&amp;quot;)&lt;br&gt;def ask_stream(req: schemas.AskRequest) -&amp;gt; StreamingResponse:&lt;br&gt;    _require_live_backend()&lt;br&gt;    history = [{&amp;quot;role&amp;quot;: m.role, &amp;quot;content&amp;quot;: m.content} for m in req.history]&lt;br&gt; &lt;br&gt;    def sse():&lt;br&gt;        for kind, data in get_rag().stream(req.question, history):&lt;br&gt;            if kind == &amp;quot;token&amp;quot;:&lt;br&gt;                yield f&amp;quot;data: {json.dumps({&amp;#39;type&amp;#39;: &amp;#39;token&amp;#39;, &amp;#39;data&amp;#39;: data})}\n\n&amp;quot;&lt;br&gt;            else:&lt;br&gt;                yield f&amp;quot;data: {json.dumps({&amp;#39;type&amp;#39;: &amp;#39;sources&amp;#39;, &amp;#39;data&amp;#39;: serialize(data)})}\n\n&amp;quot;&lt;br&gt; &lt;br&gt;    return StreamingResponse(sse(), media_type=&amp;quot;text/event-stream&amp;quot;,&lt;br&gt;                             headers={&amp;quot;Cache-Control&amp;quot;: &amp;quot;no-cache&amp;quot;, &amp;quot;X-Accel-Buffering&amp;quot;: &amp;quot;no&amp;quot;})&lt;/pre&gt;&lt;p&gt;&lt;strong&gt;One subtle production note.&lt;/strong&gt; The Next.js dev proxy must point at 127.0.0.1, not localhost. Node resolves localhost to IPv6 first, the Python server binds IPv4 only, and your logs fill with connection refused, while the browser looks fine.&lt;/p&gt;&lt;h4&gt;Guardrails: the system would be unsafe without them&lt;/h4&gt;&lt;p&gt;Three guardrails earn their keep:&lt;/p&gt;&lt;ol&gt;&lt;li&gt;&lt;strong&gt;A prompt-injection pre-filter&lt;/strong&gt; runs on the query before any vector lookup, so a query trying to hijack the instructions never reaches the model.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;A description sanitiser&lt;/strong&gt; scrubs what Gemma generates, because a video can contain adversarial on-screen text that the model would otherwise transcribe and later obey.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;An ingest semaphore&lt;/strong&gt; serialises heavy jobs, so uploading three long videos queues them instead of thrashing the GPU.&lt;/li&gt;&lt;/ol&gt;&lt;h3&gt;Step 9: The long-video problem, and what actually fixed it&lt;/h3&gt;&lt;p&gt;A fifty-seven-minute lecture took more than ten minutes to index. Unacceptable, and the profiler told a clear story in ten seconds. Keyframes were capped at twelve, so vision was cheap. &lt;strong&gt;Audio was the killer:&lt;/strong&gt; thirty-second chunks over fifty-seven minutes is ~137 chunks, each a separate Gemma audio call, run one after another.&lt;/p&gt;&lt;p&gt;Two changes fixed it:&lt;/p&gt;&lt;ol&gt;&lt;li&gt;&lt;strong&gt;Concurrency.&lt;/strong&gt; Describe/transcribe calls now run through a thread pool; Ollama serves parallel requests for roughly &lt;strong&gt;3× throughput&lt;/strong&gt;.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;A dedicated speech model.&lt;/strong&gt; Audio transcription has moved to &lt;strong&gt;faster-whisper&lt;/strong&gt;, which transcribes in a single fast pass rather than one model call per chunk. In a real lecture, it ran at ~&lt;strong&gt;14× real time&lt;/strong&gt;, so 57 minutes of audio finished in a few minutes. Because Whisper runs on the CPU, it overlaps with Gemma vision on the GPU.&lt;/li&gt;&lt;/ol&gt;&lt;p&gt;The purity of “Gemma does everything” bent a little here, and that’s fine: a dedicated speech model is the right tool for an hour of audio. The Gemma audio path stays available behind a config flag (audio_backend: gemma) for anyone who wants it.&lt;/p&gt;&lt;h3&gt;What I’d tell another engineer before they start&lt;/h3&gt;&lt;ul&gt;&lt;li&gt;&lt;strong&gt;Pick your retrieval unit before anything else.&lt;/strong&gt; Fragments versus assets is not a detail; it decides whether timestamps are free or impossible. In Qdrant, I got both in one collection.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Don’t assume a generative model gives you a metric space.&lt;/strong&gt; It doesn’t. Describe, then embed the bridge.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Route the questions that have exact answers away from the LLM.&lt;/strong&gt; Counting and inventory belong to plain code.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Measure before you optimize.&lt;/strong&gt; I was certain the bottleneck was scene detection. It was audio, and the profiler said so in ten seconds.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Let your storage layer be the easy part.&lt;/strong&gt; Qdrant’s embedded-to-server continuum, named vectors, native MaxSim, payload filtering, and quantisation meant the database was the one thing I never had to fight. Choose tools that let you keep your attention on the hard problem.&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;The whole thing runs on a laptop, indexes an hour of footage in a few minutes, answers in plain language, and lands you on the exact second. No cloud, no keys, no data leaving the machine. That was the entire point.&lt;/p&gt;&lt;p&gt;Clone it, point it at your own messy folder, and ask it something only you can ask.&lt;/p&gt;&lt;h3&gt;GitHub&lt;/h3&gt;&lt;p&gt;&lt;a href=&quot;https://github.com/sarveshtalele/deep-search-in-media-library-using-gemma-4-and-qdrant-multivectors&quot;&gt;GitHub - sarveshtalele/deep-search-in-media-library-using-gemma-4-and-qdrant-multivectors: Cross-modal natural-language search over local video, audio &amp;amp; text - powered by Gemma 4 and Qdrant multivectors.&lt;/a&gt;&lt;/p&gt;&lt;p&gt;&lt;em&gt;Follow &lt;/em&gt;&lt;strong&gt;&lt;em&gt;Sarvesh Talele&lt;/em&gt;&lt;/strong&gt;&lt;em&gt; here on Medium for practical, step-by-step AI guides that actually make a difference in your workflow.&lt;/em&gt;&lt;/p&gt;&lt;img src=&quot;https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=45447aca65ab&quot; width=&quot;1&quot; height=&quot;1&quot; alt=&quot;&quot;&gt;&lt;hr&gt;&lt;p&gt;&lt;a href=&quot;https://levelup.gitconnected.com/building-a-local-multimodal-search-engine-with-gemma-4-and-qdrant-a-step-by-step-build-guide-45447aca65ab&quot;&gt;Building a Local Multimodal Search Engine with Gemma 4 and Qdrant: A Step-by-Step Build Guide&lt;/a&gt; was originally published in &lt;a href=&quot;https://levelup.gitconnected.com&quot;&gt;Level Up Coding&lt;/a&gt; on Medium, where people are continuing the conversation by highlighting and responding to this story.&lt;/p&gt;</content:encoded>
    </item>
    <item>
      <title>Japan Just Entered the AI Race with Sakana, Claiming to Beat Mythos with a Router</title>
      <link>https://levelup.gitconnected.com/japan-just-entered-the-ai-race-with-sakana-claiming-to-beat-mythos-with-a-router-691b9b56ccbe?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/japan-just-entered-the-ai-race-with-sakana-claiming-to-beat-mythos-with-a-router-691b9b56ccbe?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Wed, 24 Jun 2026 16:37:19 GMT</pubDate>
      <content:encoded>&lt;div class=&quot;medium-feed-item&quot;&gt;&lt;p class=&quot;medium-feed-image&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/japan-just-entered-the-ai-race-with-sakana-claiming-to-beat-mythos-with-a-router-691b9b56ccbe?source=rss----5517fd7b58a6---4&quot;&gt;&lt;img src=&quot;https://cdn-images-1.medium.com/max/1376/1*sTSeQ2ofq2oq6zGHDIkJvg.png&quot; width=&quot;1376&quot;&gt;&lt;/a&gt;&lt;/p&gt;&lt;p class=&quot;medium-feed-snippet&quot;&gt;At 5:21 p.m. Eastern on Friday, June 12, 2026, Anthropic received a letter from the US Department of Commerce and, by its own account, had&amp;#x2026;&lt;/p&gt;&lt;p class=&quot;medium-feed-link&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/japan-just-entered-the-ai-race-with-sakana-claiming-to-beat-mythos-with-a-router-691b9b56ccbe?source=rss----5517fd7b58a6---4&quot;&gt;Continue reading on Level Up Coding »&lt;/a&gt;&lt;/p&gt;&lt;/div&gt;</content:encoded>
    </item>
    <item>
      <title>What Outlives the Plan</title>
      <link>https://levelup.gitconnected.com/what-outlives-the-plan-10c14f67839e?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/what-outlives-the-plan-10c14f67839e?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Wed, 24 Jun 2026 16:37:12 GMT</pubDate>
      <content:encoded>&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*ps49lmibGMyUMgeJzSu4LA.png&quot; /&gt;&lt;figcaption&gt;Development Plan exploding (AI-generated)&lt;/figcaption&gt;&lt;/figure&gt;&lt;h4&gt;&lt;em&gt;This was supposed to be the article about plans going stale. Then its own plan went stale. The four day-one decisions that outlived the plan all live somewhere the code can’t reach.&lt;/em&gt;&lt;/h4&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;Part 4 of “Lessons from Building UpAllNight”&lt;/em&gt;&lt;/strong&gt; &lt;em&gt;— a .NET 10 multiplayer card game.&lt;/em&gt;&lt;/blockquote&gt;&lt;blockquote&gt;&lt;a href=&quot;https://levelup.gitconnected.com/dapper-at-scale-the-stored-procedure-lessons-that-cost-me-an-afternoon-e3b1bc9cfa25&quot;&gt;Dapper at Scale: The Stored-Procedure Lessons That Cost Me an Afternoon&lt;/a&gt;&lt;/blockquote&gt;&lt;blockquote&gt;&lt;a href=&quot;https://levelup.gitconnected.com/real-time-multiplayer-is-a-ux-problem-not-a-transport-problem-1f6d9aa87237&quot;&gt;Real-Time Multiplayer Is a UX Problem, Not a Transport Problem&lt;/a&gt;&lt;/blockquote&gt;&lt;blockquote&gt;&lt;a href=&quot;https://levelup.gitconnected.com/money-is-a-ledger-not-a-balance-a5e4eb221d90&quot;&gt;Money Is a Ledger, Not a Balance&lt;/a&gt;&lt;/blockquote&gt;&lt;blockquote&gt;&lt;strong&gt;What Outlives the Plan&lt;/strong&gt; — &lt;em&gt;you are here&lt;/em&gt;&lt;/blockquote&gt;&lt;blockquote&gt;&lt;em&gt;Related reading: &lt;/em&gt;&lt;a href=&quot;https://medium.com/@tnjturner/dapper-llms-generating-stored-procedures-youd-actually-ship-e0f557a885e1&quot;&gt;&lt;em&gt;Dapper + LLMs: Generating Stored Procedures You’d Actually Ship →&lt;/em&gt;&lt;/a&gt;&lt;/blockquote&gt;&lt;p&gt;The series I planned called this part &lt;em&gt;“The plan is stale the moment you ship it.”&lt;/em&gt; Then I sat down to write it, and the plan was stale.&lt;/p&gt;&lt;p&gt;One of the lessons I’d planned to write up — a small pattern for piping transient state through a domain aggregate — didn’t fit the shape of an article. The plan-going-stale lesson alone wouldn’t carry one. The four lessons that &lt;em&gt;did&lt;/em&gt; hang together share a different thread, and it isn’t “plans go stale.” Plans going stale is just one of them.&lt;/p&gt;&lt;p&gt;What they share is this: &lt;strong&gt;the plan went stale in the spots where it tried to hold the current state. Four day-one decisions outlived it because they live somewhere the code can’t silently drift away from.&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;This article is the thing it describes. Here’s what stayed.&lt;/p&gt;&lt;h3&gt;The plan, and the part of it that didn’t rot&lt;/h3&gt;&lt;p&gt;Before a single C# file existed, the project had a 1,200-line docs/plan.md covering the solution layout, the project-reference rules, the database schema, and the phased build order. It was the document that paid back the hardest. Every time I asked, &amp;quot;Where does this live?&amp;quot; the answer was on the page. Every time I asked &amp;quot;what&amp;#39;s next,&amp;quot; I already had a phase to point at.&lt;/p&gt;&lt;p&gt;It also went stale.&lt;/p&gt;&lt;p&gt;The lobby section is in plan.md describes a flow that no longer exists. GameLobby.razor was rebuilt twice as I learned what joining a game actually feels like for a tester, and the plan wasn’t updated either time. By the time I noticed, the doc was lying — confidently — about a feature shipped three months earlier. I deleted the section because rewriting it would have meant rewriting it again next month.&lt;/p&gt;&lt;p&gt;Other parts of the same document are still load-bearing. The project-reference rule — &lt;em&gt;Web and &lt;/em&gt;&lt;em&gt;Mobile may not reference &lt;/em&gt;&lt;em&gt;Api, &lt;/em&gt;&lt;em&gt;Services, or &lt;/em&gt;&lt;em&gt;Infrastructure; they talk over HTTP and SignalR only&lt;/em&gt; — is on the page in the plan, in CLAUDE.md, and enforced by the .csproj graph. It hasn&amp;#39;t moved. Three feature reorganizations didn&amp;#39;t touch it, because the rule lives somewhere the features can&amp;#39;t reach.&lt;/p&gt;&lt;p&gt;That’s the difference, and it’s what this article is about. The lobby section tried to hold both &lt;em&gt;the decision&lt;/em&gt; (“here’s how lobby works”) and &lt;em&gt;the current state&lt;/em&gt; (“here’s what GameLobby.razor does today&amp;quot;). The current state always wins. The state drifted with the code, and the section underneath rotted silently. The project-reference rule only ever held the decision (there is no &amp;quot;current state&amp;quot; of &lt;em&gt;may not reference&lt;/em&gt;, there&amp;#39;s just the rule), so there was nothing for the code to drift away from.&lt;/p&gt;&lt;p&gt;The plan was useful for exactly the parts where the decision was the whole content. The plan was the receipt for those decisions. The decisions outlived the plan because they sat on shelves the code couldn’t reach — and the rest of this article is about three of those shelves, plus the counterexample I’m paying for in real time.&lt;/p&gt;&lt;h3&gt;The layers — and the two shelves the rules sit on&lt;/h3&gt;&lt;p&gt;The UpAllNight solution is split into seven projects: Api, Services, Infrastructure, Contracts, App, Web, and Mobile, plus a separate SSDT Database project. A new feature touches six of them — a stored procedure, a repository, a service, a controller, a contract DTO, and an SDK client call. The temptation to skip the layers and write Controller -&amp;gt; Dapper directly was, and still is, real.&lt;/p&gt;&lt;p&gt;I didn’t. The layers paid back in two specific ways.&lt;/p&gt;&lt;p&gt;The Blazor App project is shared by both Web (Blazor WebAssembly host) and Mobile (.NET MAUI Blazor Hybrid). Pages live in App; the hosts are thin shells. Mobile shipped without a parallel UI codebase, and it stayed that way through the rebuilds, because Web couldn&amp;#39;t touch mobile-specific concerns and Mobile couldn&amp;#39;t touch browser-specific ones.&lt;/p&gt;&lt;p&gt;The Contracts assembly is the only thing both clients and the server reference. Changing a request shape produces compile errors at every consumer — browser, mobile, SDK — at build time. Not at runtime, not in a Postman test, at build time. That&amp;#39;s the rule earning its keep, every PR.&lt;/p&gt;&lt;p&gt;What kept those rules from drifting was that they didn’t live in the C# files. They lived in two places the C# files can’t reach.&lt;/p&gt;&lt;p&gt;CLAUDE.md holds the &lt;em&gt;why&lt;/em&gt;. &lt;em&gt;&amp;quot;&lt;/em&gt;&lt;em&gt;Web and &lt;/em&gt;&lt;em&gt;Mobile may not reference &lt;/em&gt;&lt;em&gt;Api, &lt;/em&gt;&lt;em&gt;Services, or &lt;/em&gt;&lt;em&gt;Infrastructure — they communicate over HTTP and SignalR only.&amp;quot;&lt;/em&gt; That sentence is one line. It&amp;#39;s been one line for a year. No PR has touched it. The C# can rearrange itself however a feature needs it to, and the line sits there unmoved.&lt;/p&gt;&lt;p&gt;The .csproj graph holds the &lt;em&gt;what&lt;/em&gt;. The Web.csproj doesn&amp;#39;t list Api.csproj as a project reference. If a contributor types using UpAllNight.Api.Whatever; inside Web, they get a build error before they get a code review. The rule is mechanical. The .csproj files are the &lt;em&gt;enforcement&lt;/em&gt;; CLAUDE.md is the &lt;em&gt;explanation&lt;/em&gt;.&lt;/p&gt;&lt;p&gt;The combination is what held. Either alone wouldn’t have. A documented rule without the build graph is aspirational: easy to forget, easy to violate quietly. A build graph without the documented rule is mysterious — the next contributor adds a reference, the compiler complains, they don’t know &lt;em&gt;why&lt;/em&gt;, so they restructure the wrong thing to make the error go away.&lt;/p&gt;&lt;p&gt;The layer rules outlived three feature reorganizations because they were pinned to two shelves the features didn’t get to touch.&lt;/p&gt;&lt;h3&gt;Memory, or: corrections that stop costing twice&lt;/h3&gt;&lt;p&gt;The third shelf is project-level memory in Claude Code. The corrections I’ve made once become memories the next session opens with.&lt;/p&gt;&lt;p&gt;Two have paid back across more sessions than I can count.&lt;/p&gt;&lt;p&gt;The first is a feedback memory captured after the seed-script incident from Part 1: &lt;em&gt;seed scripts must be idempotent — guard with &lt;/em&gt;&lt;em&gt;IF NOT EXISTS per row or per block; never raw &lt;/em&gt;&lt;em&gt;INSERT.&lt;/em&gt; Every subsequent generated-asset seed file inherits it without me asking. The avatar generator, the deck generator, the lobby background generator — none have produced a seed script that failed on second deploy. Not because I told them to, but because the rule loads before the work starts.&lt;/p&gt;&lt;p&gt;The second is a project memory documenting an &lt;em&gt;intentional&lt;/em&gt; deferred decision: &lt;em&gt;book-strip symmetry follow-up — container-relative sizing is deliberately deferred, here’s why.&lt;/em&gt; That note exists because I caught myself, three sessions in a row, re-litigating the same “shouldn’t the book strips be proportional?” conversation. The memory is short. It says: yes, eventually; no, not yet; here’s the reason. The next session opens with the decision settled. Three sessions of re-arguing collapsed to zero.&lt;/p&gt;&lt;p&gt;The pattern is small and load-bearing: &lt;strong&gt;every correction goes into memory before it goes back into the code.&lt;/strong&gt; When I notice myself fixing the same thing twice, the third occurrence isn’t fixing the code first; it’s writing the rule into memory and &lt;em&gt;then&lt;/em&gt; fixing the code. Otherwise the next session has nothing to anchor on, and the correction has to be re-derived by whoever shows up.&lt;/p&gt;&lt;p&gt;This is the same shape as the layer rules. The memory lives somewhere the code can’t reach. The code keeps iterating; the rule keeps loading. The two don’t drift apart because the code never had a way to touch the rule.&lt;/p&gt;&lt;h3&gt;Provisioning, or: the lesson I’m living in real time&lt;/h3&gt;&lt;p&gt;The fourth lesson is the one I’m paying the cost of right now. It’s a proof by counterexample.&lt;/p&gt;&lt;p&gt;The payment seam from Part 3 — the IPaymentProvider interface, the Stripe + Apple + Google wiring, the idempotent grant keyed on (Provider, ProviderTransactionId) — is built. The unit tests pass. The MAUI host picks the right native provider via conditional compilation. The webhook controller handles the Stripe shape and the receipt-validation controller handles the Apple/Google shape. The seam works.&lt;/p&gt;&lt;p&gt;None of it has run a real sandbox purchase end-to-end.&lt;/p&gt;&lt;p&gt;It can’t, because three external accounts the code depends on don’t exist yet. The Apple Developer account isn’t approved. The Mac required for the Apple build (MacInCloud, in my case) isn’t provisioned. The Play Console account is in review. The Google Cloud Pub/Sub topic Play Billing needs for server notifications isn’t created. Every one of those is a multi-day process, and some of them are serial: the Pub/Sub topic can’t be created until the Play Console is approved. I am writing this with the entire economy stack built and unit-tested, and zero ability to prove any of it works in front of a real store.&lt;/p&gt;&lt;p&gt;The provisioning lesson is a &lt;em&gt;sentence&lt;/em&gt; in plan.md. That&amp;#39;s exactly the problem. A sentence in the plan is not a date on a calendar. A sentence goes stale when the feature it gates gets reshuffled. A date on a calendar — or better, a multi-week task on whatever you actually look at every Monday — does not. Calendars don&amp;#39;t drift to fit the code.&lt;/p&gt;&lt;p&gt;The retroactive rule: &lt;strong&gt;paid external accounts with multi-day approvals belong on the critical path, not in the doc.&lt;/strong&gt; Day-one ask on the next project, in writing, before any code that depends on the answer: &lt;em&gt;what paid external thing does this feature need, and what is its approval clock?&lt;/em&gt; That answer goes on a calendar. The plan can describe it, but the calendar is what makes it survive.&lt;/p&gt;&lt;p&gt;I am writing this section because I am paying the cost of not having written it earlier.&lt;/p&gt;&lt;h3&gt;What outlives the plan&lt;/h3&gt;&lt;p&gt;Four lessons, one shelf each. Project-reference rules on CLAUDE.md and the .csproj graph. Past corrections in project memory. External-account approvals on the calendar. None of them live in the C# files, and that is exactly why they outlived the plan. The C# iterated. The shelves didn&amp;#39;t.&lt;/p&gt;&lt;p&gt;What didn’t outlive the plan was every section that tried to be both the rule and the current state. The lobby notes. The provisioning sentence. The transient-flag pattern I cut from this article — I’d told myself it would still feel sharp in Part 4, and it had already gone stale by the time I sat down to type it.&lt;/p&gt;&lt;p&gt;The plan is the receipt. The rules outlive it because they live where the code can’t reach.&lt;/p&gt;&lt;p&gt;This was supposed to be the article about plans going stale. It still is. It just has a different name on it.&lt;/p&gt;&lt;p&gt;&lt;em&gt;Part 4 of “Lessons from Building UpAllNight.” Earlier parts:&lt;/em&gt; &lt;a href=&quot;https://levelup.gitconnected.com/dapper-at-scale-the-stored-procedure-lessons-that-cost-me-an-afternoon-e3b1bc9cfa25&quot;&gt;&lt;em&gt;Dapper at Scale&lt;/em&gt;&lt;/a&gt; &lt;em&gt;·&lt;/em&gt; &lt;a href=&quot;https://levelup.gitconnected.com/real-time-multiplayer-is-a-ux-problem-not-a-transport-problem-1f6d9aa87237&quot;&gt;&lt;em&gt;Real-Time Multiplayer Is a UX Problem&lt;/em&gt;&lt;/a&gt; &lt;em&gt;·&lt;/em&gt; &lt;a href=&quot;https://levelup.gitconnected.com/money-is-a-ledger-not-a-balance-a5e4eb221d90&quot;&gt;&lt;em&gt;Money Is a Ledger, Not a Balance&lt;/em&gt;&lt;/a&gt;&lt;em&gt;. Related reading:&lt;/em&gt; &lt;a href=&quot;https://medium.com/@tnjturner/dapper-llms-generating-stored-procedures-youd-actually-ship-e0f557a885e1&quot;&gt;&lt;em&gt;Dapper + LLMs&lt;/em&gt;&lt;/a&gt;&lt;em&gt;.&lt;/em&gt;&lt;/p&gt;&lt;img src=&quot;https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=10c14f67839e&quot; width=&quot;1&quot; height=&quot;1&quot; alt=&quot;&quot;&gt;&lt;hr&gt;&lt;p&gt;&lt;a href=&quot;https://levelup.gitconnected.com/what-outlives-the-plan-10c14f67839e&quot;&gt;What Outlives the Plan&lt;/a&gt; was originally published in &lt;a href=&quot;https://levelup.gitconnected.com&quot;&gt;Level Up Coding&lt;/a&gt; on Medium, where people are continuing the conversation by highlighting and responding to this story.&lt;/p&gt;</content:encoded>
    </item>
  </channel>
</rss>