<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>dev RSS - Level Up Coding</title>
    <link>https://levelup.gitconnected.com/</link>
    <description>Level Up Coding RSS 피드</description>
    <lastBuildDate>Sat, 05 Sep 2026 17:31:33 GMT</lastBuildDate>
    <item>
      <title>Agent Loop Runs One Thing at a Time. A Graph Loop Doesn’t.</title>
      <link>https://levelup.gitconnected.com/agent-loop-runs-one-thing-at-a-time-a-graph-loop-doesnt-bebda1759715?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/agent-loop-runs-one-thing-at-a-time-a-graph-loop-doesnt-bebda1759715?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Fri, 04 Sep 2026 15:04:23 GMT</pubDate>
      <content:encoded>&lt;div class=&quot;medium-feed-item&quot;&gt;&lt;p class=&quot;medium-feed-image&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/agent-loop-runs-one-thing-at-a-time-a-graph-loop-doesnt-bebda1759715?source=rss----5517fd7b58a6---4&quot;&gt;&lt;img src=&quot;https://cdn-images-1.medium.com/max/1536/1*qSnplIUT6SI57v62vNV7Pw.png&quot; width=&quot;1536&quot;&gt;&lt;/a&gt;&lt;/p&gt;&lt;p class=&quot;medium-feed-snippet&quot;&gt;Graph engineering without the hype. What actually changes in your code, where to cut, and the 45% rule that says don&amp;#x2019;t bother.&lt;/p&gt;&lt;p class=&quot;medium-feed-link&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/agent-loop-runs-one-thing-at-a-time-a-graph-loop-doesnt-bebda1759715?source=rss----5517fd7b58a6---4&quot;&gt;Continue reading on Level Up Coding »&lt;/a&gt;&lt;/p&gt;&lt;/div&gt;</content:encoded>
    </item>
    <item>
      <title>The Earth Is Running an Algorithm</title>
      <link>https://levelup.gitconnected.com/the-earth-is-running-an-algorithm-0c75964b8286?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/the-earth-is-running-an-algorithm-0c75964b8286?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Fri, 04 Sep 2026 15:04:12 GMT</pubDate>
      <content:encoded>&lt;div class=&quot;medium-feed-item&quot;&gt;&lt;p class=&quot;medium-feed-image&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/the-earth-is-running-an-algorithm-0c75964b8286?source=rss----5517fd7b58a6---4&quot;&gt;&lt;img src=&quot;https://cdn-images-1.medium.com/max/1400/0*HZ0onZ9ugtwr9AYq.jpg&quot; width=&quot;1400&quot;&gt;&lt;/a&gt;&lt;/p&gt;&lt;p class=&quot;medium-feed-snippet&quot;&gt;Rivers don&amp;#x2019;t draw lines. They search for the cheapest path.&lt;/p&gt;&lt;p class=&quot;medium-feed-link&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/the-earth-is-running-an-algorithm-0c75964b8286?source=rss----5517fd7b58a6---4&quot;&gt;Continue reading on Level Up Coding »&lt;/a&gt;&lt;/p&gt;&lt;/div&gt;</content:encoded>
    </item>
    <item>
      <title>Reviewed Plugin Code Shouldn’t Ask Permission</title>
      <link>https://levelup.gitconnected.com/reviewed-plugin-code-shouldnt-ask-permission-aad049906e14?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/reviewed-plugin-code-shouldnt-ask-permission-aad049906e14?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Fri, 04 Sep 2026 15:03:52 GMT</pubDate>
      <content:encoded>&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*Lqa9dSSeCUVrWmwtkkw_Tw.png&quot; /&gt;&lt;figcaption&gt;Illustration generated specifically for this blog post.&lt;/figcaption&gt;&lt;/figure&gt;&lt;h4&gt;What building a blueprint-manifest auto-allow taught me — the wall that reviewed code keeps hitting, the provenance fix that clears it, and the measurement that took my design apart three times and left it better.&lt;/h4&gt;&lt;p&gt;Every skill in my Claude Code setup opens the same way. It writes a little state file — a run directory, a PR number, a report path — into $TMPDIR, then reads it back a step later, because the Bash tool hands you a fresh shell on every call and forgets everything in between. The read looks like this:&lt;/p&gt;&lt;pre&gt;VAR=$(cat &amp;quot;${TMPDIR:-/tmp}/run-dir-${CSID}&amp;quot;)&lt;/pre&gt;&lt;p&gt;I wrote that line. I reviewed it. It lives in a versioned plugin file I ship to a marketplace. And Claude Code stops to ask my permission before running it — every skill, every session, dozens of times a day. Approving it teaches the tool nothing, because the string is never the same twice: the temp path and the session ID rotate on every run, so there’s no prefix to remember, and no box to tick that makes it stop.&lt;/p&gt;&lt;p&gt;The paradigm I actually want is one sentence. Code written verbatim in reviewed, versioned plugin files should run without asking; anything the model adjusts on the fly stays gated. Pre-canned runs, custom prompts. That’s the whole policy.&lt;/p&gt;&lt;p&gt;This post is about why that one sentence turned out to be hard to express to a permission system, the fix that finally expressed it — trusting &lt;em&gt;where a command came from&lt;/em&gt; instead of &lt;em&gt;what it looks like&lt;/em&gt; — and the part I didn’t plan for, where I instrumented the whole thing to check my work, and it contradicted three of my confident conclusions in a row.&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;A note on scope.&lt;/strong&gt; Everything here is a snapshot of one plugin suite — foundry, oss, develop, research, codemap, about 890 executable bash blocks — against Claude Code’s permission model as of August 2026. The design, its generator, its runtime hook, and a two-pass adversarial review have shipped, and the live pilot that counts prompts eliminated in real sessions is running as I write this. I’ll be explicit about which numbers came from measuring the code on disk and which are early signal from a pilot still in flight, because the gap between those two is where most of the honesty in this post lives.&lt;/blockquote&gt;&lt;h3&gt;Why reviewed code looks suspicious&lt;/h3&gt;&lt;p&gt;The prompts aren’t a nuisance bolted onto an otherwise clean setup. They’re structural, and they trace straight back to one harness fact: the Bash tool doesn’t keep state across calls. Each call is a fresh shell. So any skill that needs to carry a value from one step to the next has to round-trip it through a file — write echo &amp;quot;$X&amp;quot; &amp;gt; &amp;quot;${TMPDIR:-/tmp}/name-${CSID}&amp;quot;, read it back later. That single read is the most repeated line in the whole suite.&lt;/p&gt;&lt;p&gt;Once state lives in files, the rest follows. Run directories get timestamped names, so $(date -u +%Y-%m-%dT%H-%M-%SZ)shows up everywhere. Rule stubs resolve their versioned cache path at read time through $(ls -td ~/.claude/plugins/cache/…), because the path changes every release and can&amp;#39;t be hardcoded. Report filenames embed $(git branch --show-current). The blueprint code is full of command substitution because the harness leaves it no other way to work.&lt;/p&gt;&lt;p&gt;And command substitution is exactly what Claude Code’s permission matcher refuses to allow-list. Two blunt rules stand in the way:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;&lt;strong&gt;“Contains expansion.”&lt;/strong&gt; Any command containing $(...) makes prefix allow-rules fail closed. The prompt fires no matter what the allow list says. No entry can ever cover VAR=$(cat file).&lt;/li&gt;&lt;li&gt;&lt;strong&gt;First-token prefix matching.&lt;/strong&gt; The matcher inspects only the command’s first token. A leading assignment — IFS= read -r VAR &amp;lt; file — presents IFS= as the first token, which matches no allow entry, ever.&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;My first instinct was the obvious one: rewrite $(cat …) into the substitution-free IFS= read -r VAR &amp;lt; …, and dodge the &amp;quot;Contains expansion&amp;quot; rule entirely. It half-worked, and then it landed squarely on the &lt;em&gt;other&lt;/em&gt; rule — the IFS= first token that matches nothing. You can&amp;#39;t out-idiom a first-token matcher. The two rules cover each other&amp;#39;s blind spots, and reviewed code falls into the seam.&lt;/p&gt;&lt;h3&gt;The first fix, and the ceiling it hit&lt;/h3&gt;&lt;p&gt;The first working mitigation was a PreToolUse hook that thinks in shapes. It auto-allows a command only when every segment starts with a read-only whitelisted token, and its only substitutions are known-safe idioms — the sentinel read, the timestamp stamp. Anything else drops silently through to the normal prompt.&lt;/p&gt;&lt;p&gt;I did not trust it on my own say-so. I ran it through a two-pass adversarial review with Codex as the attacker, which found four distinct bypass classes across nineteen proofs of concept. Every PoC became a regression test. The hardening stripped writer tokens like mkdir and find from the safe set, rejected path traversal and write-redirects, and blocked loader-poisoning assignments. What came out the far side has a property worth stating plainly: its allows are provably non-mutating. By construction, it can only ever green-light a read.&lt;/p&gt;&lt;p&gt;That’s a good hook. It also has a ceiling, and the ceiling is the point. Shape analysis works when a command’s safety is visible in its structure. It has nothing to say about $(python some_script.py) — the largest single class, 189 of them — because &amp;quot;capture whatever this program prints to stdout&amp;quot; has no safe shape. No pattern proves it. Same story for eval &amp;quot;$(…)&amp;quot;, for mkdir compounds, for the git-branch capture in every report name. The commands I most needed to stop prompting on were precisely the ones living above the shape ceiling.&lt;/p&gt;&lt;h3&gt;Provenance, not shape&lt;/h3&gt;&lt;p&gt;So I stopped asking the question the hook was built to answer. Not &lt;em&gt;does this command look safe?&lt;/em&gt; — but &lt;em&gt;is this the exact text of a command I already reviewed and committed?&lt;/em&gt;&lt;/p&gt;&lt;p&gt;That reframe is the whole design. Trust attaches to provenance, not appearance. A command is allowed because its bytes exist, verbatim, in a reviewed plugin file — not because its structure passed a heuristic.&lt;/p&gt;&lt;p&gt;The mechanism is deliberately dull. A generator walks each plugin’s skills, agents, and rules, pulls every fenced bash block, normalizes it conservatively, and hashes each whole block and each whole logical command into a per-plugin manifest:&lt;/p&gt;&lt;pre&gt;{&lt;br&gt;  &amp;quot;schema&amp;quot;: 1,&lt;br&gt;  &amp;quot;plugin&amp;quot;: &amp;quot;cc_foundry@0.4.0&amp;quot;,&lt;br&gt;  &amp;quot;entries&amp;quot;: {&lt;br&gt;    &amp;quot;&amp;lt;sha256&amp;gt;&amp;quot;: { &amp;quot;src&amp;quot;: &amp;quot;skills/review/SKILL.md:L120&amp;quot;, &amp;quot;kind&amp;quot;: &amp;quot;block&amp;quot; }&lt;br&gt;  }&lt;br&gt;}&lt;/pre&gt;&lt;p&gt;At runtime, a second hook normalizes the incoming command with the &lt;em&gt;same&lt;/em&gt; steps, hashes it, and looks for an exact match. Match → allow, with a reason that names the source file and line. Miss anywhere → silent passthrough to the normal prompt. A pre-commit drift gate regenerates every manifest and fails the commit if one is stale, so editing a skill’s bash without rebuilding the manifest can’t slip through.&lt;/p&gt;&lt;p&gt;That last piece is what makes it a standing guarantee rather than a patch for today’s idioms. The classes shape analysis could never touch — $(python …) output capture, eval &amp;quot;$(…)&amp;quot;, $(git branch …), mkdir run-dir compounds — are all covered the moment they&amp;#39;re committed, because the match is on text, not shape. And it doesn&amp;#39;t stop at the blocks that exist now: the drift gate enrolls every &lt;em&gt;future&lt;/em&gt; blueprint block at commit time, so from here on, any expansion the harness forces on me is covered as blueprint automatically, with no per-idiom hook work ever again. I fixed a category, not a list.&lt;/p&gt;&lt;p&gt;The elegant part is what falls out for free. An &lt;em&gt;adjusted&lt;/em&gt; command — the model interpolated a real PR number, resolved a concrete path, spliced in an argument — no longer matches the committed text, so it prompts. The “custom code stays gated” half of my one-sentence policy isn’t a feature I built. It’s the default behavior of exact match. I got it by not doing anything.&lt;/p&gt;&lt;p&gt;And it’s easier to trust than the shape hook, which is the counter-intuitive bit. The shape hook took two adversarial rounds to reach a state I’d trust — the second came back clean — but it parses attacker-influenceable structure, so every future extension would reopen that review from scratch. The manifest parses nothing untrusted. Its trust statement fits in a sentence you can audit in one read: &lt;em&gt;only text from reviewed, versioned files ever auto-runs.&lt;/em&gt; There’s nothing to bypass. There’s only something to replay.&lt;/p&gt;&lt;h3&gt;The danger filter&lt;/h3&gt;&lt;p&gt;Which is the real residual, and worth naming. Provenance proves where a command came from, not whether it’s the right moment to run it. A blueprinted cleanup step — git worktree remove --force … — is real, reviewed text. A prompt-injected agent could replay it out of order and do damage with a command that legitimately lives in the manifest.&lt;/p&gt;&lt;p&gt;So a manifest match is necessary but not sufficient. Any command carrying a danger token — rm, git push, git commit, git reset, a --force on a git subcommand, dd, chmod — keeps its prompt regardless of provenance. Provenance handles the boring, high-frequency 97%. A human still stands in front of the sharp edges. The deny rules and the commit guard sit behind that as the outer backstop, unchanged.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*SnLU3J3GsJbN_Wio2S2SGQ.png&quot; /&gt;&lt;figcaption&gt;Illustration generated specifically for this blog post.&lt;/figcaption&gt;&lt;/figure&gt;&lt;h3&gt;Then I measured it, and lost three arguments&lt;/h3&gt;&lt;p&gt;Everything above was designed by reasoning, and reasoning feels more reliable than it is. So before I trusted it in the wild, I instrumented a copy of the shape hook to report exactly &lt;em&gt;why&lt;/em&gt; it rejected each command, ran both hooks against every bash block on disk, and started building the extensions the data seemed to be asking for. Three arguments I was sure of lost to that measurement — and the design came out smaller and better for each loss. None of what follows is the system failing. It’s my confidence failing, which is the cheap kind to catch, and each finding below is a belief I held, then what the data did to it.&lt;/p&gt;&lt;h4&gt;Belief: the rejection histogram is a prioritized backlog&lt;/h4&gt;&lt;p&gt;&lt;strong&gt;Breaks — and it’s good news.&lt;/strong&gt; Instrumenting the hook produced a clean ranked list of rejection reasons — comments 107, write-redirect 46, path-traversal 27 — and I read it the way anyone would: the top of the list is where the wins are. A first-reason histogram counts what fails &lt;em&gt;first&lt;/em&gt;, though, and blockers stack, so fixing the top entry only exposes the next one down. Building each fix and re-running told the real story: “skip whole-line comments” was billed at 107 and added 28; “permit the sentinel-write redirect” was billed at 46 and added 1. Small gains — and that’s the good part, not a weak fix. They’re small because the manifest had already swept up nearly everything worth catching, so the histogram was ranking a backlog that mostly no longer existed.&lt;/p&gt;&lt;p&gt;The sharpest version was the token everyone reaches for first. sed and awk had been pulled from the safe set during hardening, at a coverage cost that felt real at the time. Add them back, and they unblock &lt;em&gt;nothing&lt;/em&gt; — every block that would reach them is already stopped upstream. The safety trade I&amp;#39;d braced to pay for turned out to be free, and I only knew that because I built the variant and ran it.&lt;/p&gt;&lt;h4&gt;Belief: the low coverage number means expand the shape hook&lt;/h4&gt;&lt;p&gt;&lt;strong&gt;Breaks — and it’s good for us.&lt;/strong&gt; Provenance came in at 34.4%, which read as one instruction: the shape hook is carrying real unique load, so grow it. Wrong. Every plugin’s blocks had been routed through &lt;em&gt;one&lt;/em&gt; plugin’s hook, and since each plugin ships its own manifest, cross-plugin blocks could never match. Routed per plugin, coverage jumped to 92.9%, and the shape hook’s unique contribution fell to zero. The recommendation flipped from “expand it” to “leave it alone” — the manifest had been doing far more all along than the broken number let it show, which is the better world to be in.&lt;/p&gt;&lt;p&gt;The part I’m keeping in the post on purpose: the number that falsified my headline was already in my own output. A control sample had shown one plugin’s hook allowing 143 of its own 166 blocks — impossible to square with 32% across the corpus. I’d run the control and read straight past it. A control you already ran can kill your conclusion while you’re busy admiring the conclusion.&lt;/p&gt;&lt;h4&gt;Belief: zero unique coverage makes the shape hook dead weight&lt;/h4&gt;&lt;p&gt;&lt;strong&gt;Breaks — and it’s good I checked, because deleting it would have hurt.&lt;/strong&gt; On verbatim text, the shape hook catches nothing the manifest misses, so the tidy move is to drop it. The mutation test said keep it. Take a blueprinted command and vary it the way a &lt;em&gt;different run legitimately would&lt;/em&gt; — rename the sentinel file, rename the session variable, leave the structure intact. Exact-match provenance misses every one, 0 of 57. The shape hook catches all 17 of 17. Neither works on the other’s ground: the manifest owns committed text, the shape hook owns the model’s honest variations, and the design needs both.&lt;/p&gt;&lt;p&gt;One caveat cost me a whole pass, and it’s the useful kind. “The way a different run legitimately would” is load-bearing. My first mutation swapped ${TMPDIR:-/tmp} for a resolved literal path, which deletes the very anchor the hook keys on, scored both hooks at zero, and proved nothing except that I&amp;#39;d written a bad test. When a test scores every arm zero, suspect the test before the arms.&lt;/p&gt;&lt;h4&gt;Two smaller beliefs, checked the same way&lt;/h4&gt;&lt;p&gt;&lt;strong&gt;The 7% neither hook covers is a coverage hole — breaks, good for us.&lt;/strong&gt; Fifty-six of the ~790 blocks are allowed by neither hook, which looks like a hole until you see 51 of them carry a danger token and are gated &lt;em&gt;on purpose&lt;/em&gt;. The real residual is 5 blocks, 0.6%. Most of a gap can be the policy doing its job; check before you optimize it.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;The on-disk corpus can measure real coverage —breaks— and it’s what sent me to the pilot.&lt;/strong&gt; A sample of real session transcripts scored almost nothing, and meant almost nothing: those were plugin-development sessions, all ad-hoc git and grep and pytest, not skill runs. The population these hooks exist to serve is &lt;em&gt;deviation traffic&lt;/em&gt; — the commands the model assembles when it steps off the blueprint — and no file on disk holds it. Only live sessions do, which is exactly what the pilot now exercises, and so far it runs clean.&lt;/p&gt;&lt;h3&gt;What I would take from it&lt;/h3&gt;&lt;p&gt;Beyond the specific mechanism, five things I now build by. Each cost me real time.&lt;/p&gt;&lt;ol&gt;&lt;li&gt;&lt;strong&gt;A ranked list from static analysis is an upper bound, not a backlog.&lt;/strong&gt; Measure marginal gain by building each fix and re-running — or you’ll spend a day earning the 1 block you were promised 46 of.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Instrument the thing, then prove the instrument agrees with the real decision before you trust one number it reports.&lt;/strong&gt; A drifted copy produces a confident, wrong histogram, and it looks exactly like a right one.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;The scariest-looking safety trade can cost nothing.&lt;/strong&gt; Before you pay for hardening in coverage, build the unsafe variant and measure what admitting it would actually have bought. Sometimes it’s zero.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Two mechanisms can be fully redundant on the corpus you can measure and non-substitutable in production.&lt;/strong&gt; Pick the test that separates them — here, fair mutation — or you’ll delete the one you needed.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Self-correction is the story, not the part you quietly delete.&lt;/strong&gt; The routing bug, the unfair mutation, and the 34.4% headline all shipped as findings. They’re the most useful thing in the writeup.&lt;/li&gt;&lt;/ol&gt;&lt;h3&gt;Where it stands&lt;/h3&gt;&lt;p&gt;What shipped is the whole spine — generator, per-plugin manifest, runtime hook, drift gate, docs — and it earns its place by what it takes out of the day: the reviewed idioms that used to fire a permission prompt on every skill run now execute silently, and the drift gate means I never hand-maintain that coverage as the plugins grow. The two-pass adversarial review matters more than a changelog line makes it sound. A hook that auto-allows anything is a trust surface, and the review — clean on the second pass — is what earns the right to run reviewed code without asking; without it, the whole idea is just a convenient way to get burned. The only two shape-hook extensions the data justified — skipping whole-line comments, matching .. as a real path component rather than a bare substring — shipped with a negative-test suite that still leaks nothing, test count 44 to 80. Everything the data didn&amp;#39;t justify stayed out, which is the restraint the measurement was for in the first place.&lt;/p&gt;&lt;p&gt;The pilot is rolled out now, and in live use it holds. The reviewed idioms that used to interrupt every skill run go through quietly, and I haven’t seen a surprise auto-allow. The manifest covers 92.9% of the blueprint text on disk, and the live behavior matches what that predicted — the daily friction is simply gone. One caveat I’ll state plainly: the pilot numbers were collected right at the edge of the rollout, before it had fully settled, so treat them as noisy rather than final — a first read, not a booked result. What I can say without a caveat is that real usage feels clean. I haven’t frozen a single before/after percentage yet, because I’d rather let it run wider than freeze a noisy one, so take the target — ninety-plus percent of blueprint prompts gone, zero unexpected allows — as the target the rollout is bearing out. If you run it, I’d like your before/after: repost your numbers and I’ll fold the field data in.&lt;/p&gt;&lt;p&gt;The design that survived is smaller than the one I started with. Provenance owns the committed text. Shape owns the honest variations. A danger filter owns the sharp edges. A human owns anything none of them can vouch for. I trust it more than the version I was certain about before I measured it — and the reason I trust it more is that I stopped being certain.&lt;/p&gt;&lt;p&gt;The through-line isn’t really Claude Code permissions. It’s that a ranked list of reasons, generated by a tool I wrote myself, talked me out of the right answer three times — and each time the thing that caught it was building the fix and running it against the real corpus. The system that came out the other side is smaller than the one I planned, running live now, and I trust it more precisely because it survived being measured. So the question I now ask before trusting any backlog a static analyzer hands me, and the one I’ll leave you with: what’s the cheapest experiment that would tell me this ordering is wrong?&lt;/p&gt;&lt;p&gt;Better yet, run the rig against your own setup and see where it bends. If you’ve got a permission model that doesn’t punish reviewed code, or before/after numbers that beat or dent mine, repost them — the issues tab is open: &lt;a href=&quot;https://github.com/Borda/AI-Rig&quot;&gt;github.com/Borda/AI-Rig&lt;/a&gt;.&lt;/p&gt;&lt;img src=&quot;https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=aad049906e14&quot; width=&quot;1&quot; height=&quot;1&quot; alt=&quot;&quot;&gt;&lt;hr&gt;&lt;p&gt;&lt;a href=&quot;https://levelup.gitconnected.com/reviewed-plugin-code-shouldnt-ask-permission-aad049906e14&quot;&gt;Reviewed Plugin Code Shouldn’t Ask Permission&lt;/a&gt; was originally published in &lt;a href=&quot;https://levelup.gitconnected.com&quot;&gt;Level Up Coding&lt;/a&gt; on Medium, where people are continuing the conversation by highlighting and responding to this story.&lt;/p&gt;</content:encoded>
    </item>
    <item>
      <title>Building KodaDB: From a Class Project to a Database Engine</title>
      <link>https://levelup.gitconnected.com/building-kodadb-from-a-class-project-to-a-database-engine-15d0393dc9ca?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/building-kodadb-from-a-class-project-to-a-database-engine-15d0393dc9ca?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Fri, 04 Sep 2026 15:03:41 GMT</pubDate>
      <content:encoded>&lt;p&gt;I took CS645 Database Systems at UMass Amherst with &lt;a href=&quot;https://www.linkedin.com/in/marco-serafini-99163a2/&quot;&gt;Professor Marco Serafini&lt;/a&gt;. In that course, my team built a small toy database. That was the whole semester long project, and it was already a lot of work.&lt;/p&gt;&lt;p&gt;After the course ended, I kept thinking about that tiny database. It felt like the first ten percent of something much bigger. So I decided to take it apart, keep the ideas that mattered, and rebuild it into something that actually behaves like a database. I called it &lt;a href=&quot;https://github.com/shubhamshettyy/KodaDB&quot;&gt;&lt;strong&gt;&lt;em&gt;KodaDB&lt;/em&gt;&lt;/strong&gt;&lt;/a&gt;.&lt;/p&gt;&lt;p&gt;&lt;a href=&quot;https://github.com/shubhamshettyy/KodaDB&quot;&gt;KodaDB &lt;/a&gt;is a relational database engine written from scratch in C++17. It stores tables on disk, it has a real buffer pool, it has B+ tree indexes, it runs SQL through a planner and a set of query operators, and it has an interactive shell you can type into. It does not have transactions or concurrency yet, and there are plenty of other things I want to improve. But the core pieces I wanted to learn about like storage, indexing, catalogs, query execution, and a SQL front end are there and working.&lt;/p&gt;&lt;p&gt;If you are trying to learn how databases work internally, I hope this is useful to you, since I built this specifically to understand that.&lt;/p&gt;&lt;h3&gt;The shape of the system&lt;/h3&gt;&lt;p&gt;Before going component by component, here is the high level path a query takes through &lt;a href=&quot;https://github.com/shubhamshettyy/KodaDB&quot;&gt;KodaDB&lt;/a&gt;:&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*pb2enrrPSCI4tps5vPmXDw.png&quot; /&gt;&lt;figcaption&gt;&lt;em&gt;How one query travels through KodaDB, from the shell down to bytes on disk.&lt;/em&gt;&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;I built the system bottom up, roughly in this order: storage first, then the buffer pool, then indexing, then the catalog, then query execution, and finally the parser, planner, and shell on top. That is also the order I will walk through below.&lt;/p&gt;&lt;h3&gt;Storage: how a row actually sits on disk&lt;/h3&gt;&lt;p&gt;The first real decision I had to make was how to store rows inside a page. A page in &lt;a href=&quot;https://github.com/shubhamshettyy/KodaDB&quot;&gt;KodaDB &lt;/a&gt;is 4096 bytes, which is a normal disk page size.&lt;/p&gt;&lt;p&gt;I had two options. I could use slotted pages, where each row can be a different length and a small directory at the start of the page tracks where each row begins. This is what most real databases use, and it is also what the original roadmap for this project called for. Or I could use fixed width records, where every row in a table takes exactly the same number of bytes, and the position of row number five on a page can be calculated with simple math instead of looked up in a directory.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*aBA0zjCGnK4KZF5xs7FhqA.png&quot; /&gt;&lt;figcaption&gt;&lt;em&gt;Slotted pages (left) versus KodaDB’s fixed-width packed pages (right).&lt;/em&gt;&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;I chose fixed width records. The reason was simple. I was still learning how buffer management and indexing worked, and I did not want to debug offset math and fragmentation on top of that. With fixed width rows, if I know the schema, I know exactly where every row lives. A row’s position, called a &lt;em&gt;Rid &lt;/em&gt;(page id plus slot id), stays stable for as long as that slot exists. That stability turned out to matter a lot later, because indexes store &lt;em&gt;(key, Rid)&lt;/em&gt; pairs, and if a row’s position could shift around, every index pointing at it would break.&lt;/p&gt;&lt;p&gt;The downside is that every column has a fixed maximum width chosen when the table is created. A string longer than that width gets truncated. There is no overflow page for long values. I accepted that cost on purpose, and I wrote that decision down at the time so I would not forget why I made that call.&lt;/p&gt;&lt;p&gt;Each page starts with a four byte record count, and then rows are packed back to back after that. Each row also gets a one byte flag in front of it, which is either “alive” or “deleted.” This flag is how I implemented &lt;strong&gt;&lt;em&gt;DELETE&lt;/em&gt;&lt;/strong&gt;.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*xUfZPmjxGdO-Vwweo_WI7w.png&quot; /&gt;&lt;figcaption&gt;&lt;em&gt;A heap file is just a sequence of fixed 4KB pages, each holding a record count and packed rows.&lt;/em&gt;&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;Here is why the flag matters. When I added &lt;em&gt;DELETE&lt;/em&gt;, I had to decide what happens to a row’s slot when it is removed. The straightforward approach is to physically remove the row, shift every row after it down by one slot, and reduce the row count. But if I do that, every row after the deleted one changes its slot id, which means every index entry pointing at those rows becomes wrong. I would have to walk every index and fix every pointer, every single time anyone deletes a row.&lt;/p&gt;&lt;p&gt;Instead I used tombstones. Deleting a row just flips its flag to “deleted.” The slot stays exactly where it was. Every index pointer still points to the right place. A scan or an index lookup checks the flag and skips the row if it is marked deleted. The cost is wasted space, since a deleted slot still takes up a full row’s worth of bytes until I add space reuse or compaction. For a project at this scale, that was a cost worth paying to keep everything else simple.&lt;/p&gt;&lt;p&gt;Sitting on top of the raw page bytes is a &lt;em&gt;Schema&lt;/em&gt;, which is just an ordered list of fields with names, byte widths, and a type label like &lt;em&gt;INT&lt;/em&gt;, &lt;em&gt;BOOL&lt;/em&gt;, or &lt;em&gt;TEXT&lt;/em&gt;. A &lt;em&gt;GenericRecord &lt;/em&gt;is a byte buffer exactly as wide as the schema and knows how to read and write individual fields into that buffer. Values are stored as fixed width byte strings, not as native typed values in memory. I kept it that way on purpose, because a dumb and predictable on disk format was easier to reason about while I was still learning this layer.&lt;/p&gt;&lt;h3&gt;The buffer pool: the part that decides what stays in memory&lt;/h3&gt;&lt;p&gt;Once rows are laid out on disk, the next question is who actually reads and writes those bytes, and how much of the database lives in memory at once.&lt;/p&gt;&lt;p&gt;I split this into two pieces, and I think keeping them separate was one of the better decisions I made early on. A &lt;em&gt;DiskManager&lt;/em&gt; only knows about files and page sized reads and writes. It has no idea what a row or an index is. Page number five in a file always starts at byte (5 * 4096), and that is the entire idea.&lt;/p&gt;&lt;p&gt;Above that sits the &lt;em&gt;BufferManager&lt;/em&gt;. It owns a fixed number of frames in memory, and each frame holds one page along with which file and page number it came from, whether it has been modified, and when it was last used. Every part of the engine above storage, heap pages, B+ tree pages, everything, goes through the buffer pool. Nothing reads a file directly.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*j_7_Rjhd1HECGUeX9eckQg.png&quot; /&gt;&lt;figcaption&gt;&lt;em&gt;Anatomy of a buffer pool frame: page id, dirty bit, pin count, and last-used time.&lt;/em&gt;&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;When something asks for a page, the buffer pool checks if it is already sitting in memory. If it is, it hands back a pointer. If it is not, it picks a frame to reuse. If every frame is currently pinned, meaning something is actively using it, it cannot evict anything and the request has to fail. Otherwise it picks the frame that has gone the longest without being used and evicts it, writing it back to disk first if it was modified.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*wSv7_R36xV0_UzPt0Rn1dQ.png&quot; /&gt;&lt;figcaption&gt;&lt;em&gt;What happens when someone asks the buffer pool for a page.&lt;/em&gt;&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;The pool size is fixed when the database starts, twenty frames by default. There is no growing the pool later. I liked this because it made eviction behavior easy to test and reason about. A production database would size this based on available memory and expose real statistics about hits and misses. I have neither of those things today, and that is one of the gaps in the project.&lt;/p&gt;&lt;p&gt;The other honest gap here is durability. There is no write ahead log. Writes go straight to the data files through the buffer pool. If the process crashes in the middle of a write, there is nothing to replay on restart. I decided that recovery is its own topic that deserves its own focused effort later, once transactions exist, rather than something to bolt on halfway through building the storage layer.&lt;/p&gt;&lt;h3&gt;Indexes: B+ trees for fast lookups&lt;/h3&gt;&lt;p&gt;With storage and the buffer pool working, I wanted to add indexing so queries didn’t always have to scan the whole table.&lt;/p&gt;&lt;p&gt;I built a B+ tree, and I stored it in its own file per index. Every page of the tree, whether it is a leaf or an internal node, is fetched through the same buffer pool as heap pages. That reuse felt satisfying, since it meant I did not have to write a second caching layer just for indexes.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*aXxdl8TaOme1arBe80KuCw.png&quot; /&gt;&lt;figcaption&gt;&lt;em&gt;The B+ tree index: metadata page, root, leaves, and the sibling chain that makes range scans possible.&lt;/em&gt;&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;A leaf page stores entries as a &lt;em&gt;key &lt;/em&gt;followed by a &lt;em&gt;Rid&lt;/em&gt;, which is the same page id and slot id used by heap pages. An internal page stores child pointers and separator keys, and the tree finds the right child using binary search. Leaf pages are also chained together through a “next page” pointer, which lets range searches walk sideways across the leaf level without going back up to the root.&lt;/p&gt;&lt;p&gt;Insert is the most complete part of the tree. It walks down to the right leaf, inserts the new key, and if the page is now too full, it splits the page and pushes a new separator up into the parent. This can cascade all the way up and even create a new root if the old root splits.&lt;/p&gt;&lt;p&gt;Delete is where I made another deliberate tradeoff. A textbook B+ tree delete rebalances the tree after removing an entry. If a leaf becomes too empty, it borrows an entry from a neighboring leaf or merges with it, and that change can ripple up through the parent separators. I did not build that. My delete finds the entry, matching both the key and the specific &lt;em&gt;Rid &lt;/em&gt;since duplicate keys can point at different rows, removes it, and shifts the remaining entries left. Nothing merges, nothing rebalances, and the tree never shrinks back down in height.&lt;/p&gt;&lt;p&gt;I made this choice because insert, search, and range search were the parts that actually mattered for running indexed queries and CRUD demos correctly. The cost is that after a lot of deletes, leaves can become sparse and internal separators can become a little stale in edge cases. For a single user learning database with modest amounts of churn, queries still behave correctly.&lt;/p&gt;&lt;p&gt;When someone runs &lt;strong&gt;&lt;em&gt;CREATE INDEX&lt;/em&gt;&lt;/strong&gt; on a table that already has data, I did not want the new index to only cover future rows. So creating an index scans the entire heap, skips any tombstoned rows, and inserts a &lt;em&gt;(key, Rid)&lt;/em&gt; pair for every live row it finds. This uses the exact same insert path that a normal INSERT statement uses, so there was no second code path to keep correct.&lt;/p&gt;&lt;h3&gt;The catalog and the Database facade&lt;/h3&gt;&lt;p&gt;At this point I had storage, a buffer pool, and indexes, but nothing tying table names to files, or files to schemas. That is what the catalog does.&lt;/p&gt;&lt;p&gt;The &lt;strong&gt;&lt;em&gt;SystemCatalog &lt;/em&gt;&lt;/strong&gt;is an in-memory map from table name to a &lt;em&gt;TableEntry&lt;/em&gt;, which holds the heap file, a copy of the schema, and a map of every index defined on that table. This lives entirely in memory while the database is running, but it also has to survive a restart, so I wrote a save and load routine that reads and writes a plain text file called &lt;em&gt;catalog.meta&lt;/em&gt;.&lt;/p&gt;&lt;p&gt;I chose plain text over a binary format on purpose. I wanted to be able to open the catalog file in a normal text editor while I was debugging and actually understand what it said, instead of running it through a hex dump. The tradeoff is that the text format has to stay backward compatible as I add new fields, which is a small parsing headache, but for a project this size that was an easy trade to make.&lt;/p&gt;&lt;p&gt;Sitting above the catalog is the &lt;em&gt;Database&lt;/em&gt; class, which is the one object that owns the disk manager, the buffer pool, and the catalog together for one database directory. When it starts up, it creates the directory if needed, loads the catalog file if one exists, and opens every heap and index file the catalog knows about. When it shuts down, it flushes every dirty page in the buffer pool, rewrites the catalog file, and closes everything. This shutdown call also happens automatically if the &lt;em&gt;Database&lt;/em&gt; object is destroyed, so a normal program exit does not leave dirty pages unflushed.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;&lt;em&gt;CREATE TABLE&lt;/em&gt;&lt;/strong&gt; and &lt;strong&gt;&lt;em&gt;CREATE INDEX&lt;/em&gt;&lt;/strong&gt; are both handled through this &lt;em&gt;Database&lt;/em&gt; object rather than scattered across the query execution code. That was intentional. I wanted one narrow place where “does this table exist,” “does this index exist,” and “what does this schema look like” get decided.&lt;/p&gt;&lt;h3&gt;Running queries: the operator model&lt;/h3&gt;&lt;p&gt;With storage, indexing, and a catalog in place, I could finally build the part that actually runs a query.&lt;/p&gt;&lt;p&gt;I used what is called the &lt;a href=&quot;https://15445.courses.cs.cmu.edu/spring2025/notes/13-queryexecution1.pdf&quot;&gt;Volcano model&lt;/a&gt;. Every operator implements three methods, &lt;em&gt;open&lt;/em&gt;, &lt;em&gt;next&lt;/em&gt;, and &lt;em&gt;close&lt;/em&gt;. Calling &lt;em&gt;next&lt;/em&gt; on an operator pulls one row at a time, either from disk directly or from a child operator underneath it. This means you can build a query as a small tree of operators, and running the query is just calling &lt;em&gt;next &lt;/em&gt;on the operator at the top of the tree until it runs out of rows.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*UZ138zuTA-2n1VCsmqIEAA.png&quot; /&gt;&lt;figcaption&gt;&lt;em&gt;The Volcano pull model: &lt;/em&gt;next() &lt;em&gt;calls flow down the operator tree, rows flow back up.&lt;/em&gt;&lt;/figcaption&gt;&lt;/figure&gt;&lt;p&gt;The operators I ended up with are a &lt;strong&gt;sequential scan&lt;/strong&gt; that walks every page of a table and skips tombstoned rows, an &lt;strong&gt;index scan&lt;/strong&gt; that looks up candidate rows in a B+ tree and then fetches the actual row by &lt;em&gt;Rid&lt;/em&gt;, a &lt;strong&gt;selection operator &lt;/strong&gt;that filters rows against a predicate, a &lt;strong&gt;projection operator&lt;/strong&gt; that keeps only the requested columns, and separate operators for insert, update, delete, and join.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Insert &lt;/strong&gt;takes a set of values, finds the last page of the heap file, and either appends into free space on that page or allocates a brand new page if the current one is full. After the row lands on the heap, the same operator inserts a matching entry into every index defined on that table.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Update &lt;/strong&gt;finds every matching row through a scan or an index scan, applies the &lt;em&gt;SET&lt;/em&gt; changes in memory, writes the updated bytes back into the same slot, and then checks whether any of the columns it changed are actually indexed. If they are, it removes the old key and inserts the new one. If an update does not touch an indexed column, the index is left alone completely, since there is nothing to fix.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Delete &lt;/strong&gt;is where the tombstone decision from the storage section and the indexing decision meet each other. For every row being deleted, the delete operator first flips the tombstone flag on the heap page, and only after that does it remove the row’s entry from every index. I chose that order on purpose. If the process crashed after removing the index entries but before setting the tombstone, a sequential scan would still show the row while an indexed lookup would not, and those two answers disagreeing is a worse bug than a small amount of wasted space. If the process crashes after the tombstone but before the index cleanup finishes, the row is already invisible everywhere that matters, and the only cost is a stale index entry that never resurrects a deleted row.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Join &lt;/strong&gt;is implemented as a &lt;strong&gt;&lt;em&gt;block nested loop join&lt;/em&gt;&lt;/strong&gt; between two table scans. It is not the fastest join strategy, but it is one of the easiest to understand and get correct, and it does not use an index on either side yet. I added it because a two table query makes for a much more realistic database than single table queries alone.&lt;/p&gt;&lt;h3&gt;The parser, the planner, and the shell&lt;/h3&gt;&lt;p&gt;The last layer is the one people actually see and type into. A hand rolled parser reads a small subset of SQL, things like &lt;em&gt;CREATE TABLE&lt;/em&gt;, &lt;em&gt;INSERT&lt;/em&gt;, &lt;em&gt;SELECT&lt;/em&gt; with an optional &lt;em&gt;WHERE&lt;/em&gt;, &lt;em&gt;UPDATE&lt;/em&gt;, &lt;em&gt;DELETE&lt;/em&gt;, and a single table &lt;em&gt;JOIN&lt;/em&gt;, and turns each line into a typed &lt;em&gt;Statement &lt;/em&gt;value.&lt;/p&gt;&lt;p&gt;The planner takes that &lt;em&gt;Statement&lt;/em&gt; and decides which operators to build. This is also where the index versus scan decision actually happens. If a query has exactly one equality condition in its &lt;em&gt;WHERE&lt;/em&gt; clause, and that column has an index, the planner builds an index scan. Otherwise it falls back to a full sequential scan with a filter on top. There is no cost based optimizer choosing between plans, but this is something I want to build later to dive deeper.&lt;/p&gt;&lt;p&gt;On top of the parser and planner sits an interactive shell. It has a proper prompt, colored output for results and errors, an ASCII table format for query results, and a handful of meta commands like &lt;em&gt;.tables&lt;/em&gt;, &lt;em&gt;.schema&lt;/em&gt;, and &lt;em&gt;.indexes&lt;/em&gt; to inspect what exists in the current database. It can also run in batch mode, reading an entire SQL script from a file, which is what the demo script uses to show create, insert, select, join, update, delete, and a restart all in one run.&lt;/p&gt;&lt;h3&gt;Things on the roadmap&lt;/h3&gt;&lt;ul&gt;&lt;li&gt;&lt;strong&gt;Cost based query optimizer&lt;/strong&gt;: Maybe some join reordering, cardinality/selectivity estimation from histograms, physical plan selection etc.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;MVCC + WAL&lt;/strong&gt;: I want to build something similar to &lt;a href=&quot;https://en.wikipedia.org/wiki/Algorithms_for_Recovery_and_Isolation_Exploiting_Semantics&quot;&gt;ARIES&lt;/a&gt;. Proving that killing my process mid transaction still recovers the database correctly.&lt;/li&gt;&lt;/ul&gt;&lt;h3&gt;What this project taught me&lt;/h3&gt;&lt;p&gt;Building KodaDB changed how I think about databases. Things that feel simple when you’re using a database become much less simple when you have to implement them yourself. DELETE was a good example of that. A single SQL statement ended up raising questions about row locations, indexes, and what should happen if the process crashes halfway through.&lt;/p&gt;&lt;p&gt;I am grateful to &lt;a href=&quot;https://www.linkedin.com/in/marco-serafini-99163a2/&quot;&gt;Professor Marco Serafini&lt;/a&gt; for giving me the seed of this project in the first place. The database we built in that class is where KodaDB started.&lt;/p&gt;&lt;p&gt;&lt;a href=&quot;https://github.com/shubhamshettyy/KodaDB&quot;&gt;KodaDB &lt;/a&gt;is open source, and the full write up for each subsystem lives in the &lt;em&gt;docs/&lt;/em&gt; folder of the repository, including the design decision notes I linked to throughout this post.&lt;/p&gt;&lt;p&gt;Thanks for reading! If you enjoyed this project, let’s connect. You can find more of my work or reach out for collaborations below&lt;/p&gt;&lt;ul&gt;&lt;li&gt;&lt;a href=&quot;https://www.linkedin.com/in/shubhamshettyy/&quot;&gt;Shubham Shetty - C3 AI | LinkedIn&lt;/a&gt;&lt;/li&gt;&lt;li&gt;&lt;a href=&quot;https://github.com/shubhamshettyy&quot;&gt;shubhamshettyy - Overview&lt;/a&gt;&lt;/li&gt;&lt;/ul&gt;&lt;img src=&quot;https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=15d0393dc9ca&quot; width=&quot;1&quot; height=&quot;1&quot; alt=&quot;&quot;&gt;&lt;hr&gt;&lt;p&gt;&lt;a href=&quot;https://levelup.gitconnected.com/building-kodadb-from-a-class-project-to-a-database-engine-15d0393dc9ca&quot;&gt;Building KodaDB: From a Class Project to a Database Engine&lt;/a&gt; was originally published in &lt;a href=&quot;https://levelup.gitconnected.com&quot;&gt;Level Up Coding&lt;/a&gt; on Medium, where people are continuing the conversation by highlighting and responding to this story.&lt;/p&gt;</content:encoded>
    </item>
    <item>
      <title>Peers, Not Rivals: A Two-Way Street Between Claude Code and Codex</title>
      <link>https://levelup.gitconnected.com/peers-not-rivals-a-two-way-street-between-claude-code-and-codex-9efcd04e13e4?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/peers-not-rivals-a-two-way-street-between-claude-code-and-codex-9efcd04e13e4?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Fri, 04 Sep 2026 15:03:29 GMT</pubDate>
      <content:encoded>&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*rTybIfsQIHP_a_7YVojvyg.png&quot; /&gt;&lt;figcaption&gt;Illustration generated specifically for this blog post.&lt;/figcaption&gt;&lt;/figure&gt;&lt;h4&gt;What building a bridge between the two biggest coding harnesses on my machine taught me — the transport that couldn’t authenticate, the resume that cost 2.46M tokens, and why the official plugin’s marketing outran its maintenance.&lt;/h4&gt;&lt;p&gt;For months, both Claude Code and Codex ran on the same machine, both fully capable, and the traffic between them went one way and badly. Claude could hand work to Codex through OpenAI’s official codex@openai-codex &lt;a href=&quot;https://github.com/openai/codex-plugin-cc&quot;&gt;plugin&lt;/a&gt;, but that path never picked a model or an effort tier for the task, never reused a session, dropped the raw transcript straight back into the calling context, and had no timeout, watchdog, or retry anywhere. A hung job just sat there until I noticed. Codex, meanwhile, couldn&amp;#39;t hand anything back at all — there was no reverse artifact in that plugin. The second-strongest reviewer on the box couldn&amp;#39;t ask the strongest one a question.&lt;/p&gt;&lt;p&gt;So the plan was obvious, and I shipped the adjacent version: turn the one-way pipe into a two-way street. One repository package, installed into &lt;strong&gt;two hosts&lt;/strong&gt; — Claude Code so it can reach Codex, Codex so it can reach Claude — giving both the same three operations, with the caller choosing model and effort on &lt;em&gt;every&lt;/em&gt; call in both directions, a compact result instead of a transcript dump, a hard cutoff on hangs, and a guard so the two agents can’t summon each other in a loop.&lt;/p&gt;&lt;p&gt;Then I hit the wall I couldn’t argue my way past: &lt;strong&gt;&lt;em&gt;How does Codex reach Claude at all?&lt;/em&gt;&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;This post is about the bridge that came out the other side. More usefully, it’s about the times a probe walked in and rewrote a decision I’d have sworn was safe. If you take one thing from it, take the pattern, not the plugin.&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;A note on scope.&lt;/strong&gt; Everything here reflects Codex CLI 0.146–0.147 and Claude Code 2.1.x on &lt;strong&gt;macOS&lt;/strong&gt;, August 2026. The auth mechanism I lean on most is a macOS Seatbelt/keychain detail; sandbox and credential specifics differ on Linux and Windows, and I flag that wherever it matters. I&amp;#39;ll be explicit about which claims I verified against a real run and which are still design-time — the whole reason this post exists is that the gap between the two kept biting me.&lt;/blockquote&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*QAZhAHyzqIqHkmcyNlrfAA.png&quot; /&gt;&lt;figcaption&gt;Illustration generated specifically for this blog post.&lt;/figcaption&gt;&lt;/figure&gt;&lt;h3&gt;Why not just use the official plugin?&lt;/h3&gt;&lt;p&gt;Fair question — and for a long time I didn’t ask it, because the official codex@openai-codex plugin genuinely earned its place. It&amp;#39;s OpenAI&amp;#39;s own repo, north of 30k stars, Apache-2.0, the thing everyone points to, and it did real work for me for months. I learned a lot about delegating to Codex from Claude through it. This isn&amp;#39;t a teardown of something I never liked; it&amp;#39;s the opposite.&lt;/p&gt;&lt;p&gt;The limitations arrived slowly, the way they usually do. A new Codex release would land, and the plugin would crash until things settled — it’s pinned to protocol details that keep moving underneath it, and it’s owned by the same team that owns those details, so drift is structural. Handovers that should have been cheap weren’t: I watched Claude burn about &lt;strong&gt;100k tokens on a single simple handoff&lt;/strong&gt;, because the path loads its skill context fresh on every call and drops the entire raw transcript straight back into the caller. Annoying, but survivable — I was still using it.&lt;/p&gt;&lt;p&gt;The breaking point came when I finally went upstream for a fix. &lt;strong&gt;“Actively maintained” turned out to be generous.&lt;/strong&gt; The default branch carries 29 commits, ever — 26 in the launch burst over about three weeks, and 3 in the four months since, against roughly 425 open issues and 2,100 forks: about 0.75 commits a month against a very large backlog. And the exact gaps I was hitting aren’t my misreading — they’re open issues with the lights off: background tasks that hang indefinitely, no turn-level timeout, rescue tasks that hang forever, --effort silently dropped on the review paths. Four months open, no ETA. It wasn&amp;#39;t any one crash that pushed me off; it was realizing the fixes I needed weren&amp;#39;t coming.&lt;/p&gt;&lt;p&gt;None of this makes it a bad plugin. It’s a &lt;em&gt;thin, human-facing forwarder&lt;/em&gt;, and for that job it’s correct — it hands your phrasing to Codex and returns the transcript verbatim, exactly as a person typing /codex would want. The friction is entirely about calling it as an &lt;em&gt;internal step&lt;/em&gt;, where &amp;quot;return the transcript verbatim&amp;quot; (three upstream instructions forbid compressing it) becomes that 100k-token dump in the orchestrator&amp;#39;s context, where no model or effort is ever chosen for the task, and where there&amp;#39;s no timeout and no retry. And it ships nothing on the Codex side at all: the reverse direction isn&amp;#39;t underdeveloped, it&amp;#39;s absent.&lt;/p&gt;&lt;p&gt;The case for depending on it was always “you inherit upstream fixes for free.” Measured against three commits in four months, none touching the hang bugs, that’s worth close to nothing. So this isn’t invented-here — it’s a tool I used, leaned on, and outgrew. I needed two things it couldn’t do and wasn’t moving toward: let the caller direct the call, and let the work go both ways.&lt;/p&gt;&lt;h3&gt;The shape: two goals&lt;/h3&gt;&lt;p&gt;Strip away the internals and the bridge chases two goals. Everything else is downstream of them.&lt;/p&gt;&lt;ul&gt;&lt;li&gt;&lt;strong&gt;Minimize the caller’s overhead.&lt;/strong&gt; Asking the other agent a question shouldn’t cost a transcript dump, a reloaded skill stack, or four copies of the same dispatch logic.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Keep the caller directing the process.&lt;/strong&gt; Model, effort, timeout, and the shape of the answer are the caller’s decision on every call — not a default it inherits and can’t see.&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;From those two, the design falls out. One install, two hosts, three symmetric verbs — implement for bounded write work, advise for a read-only question, review for a read-only adversarial pass — running in both directions. Each half works standalone; install only the Claude side, only the Codex side, or both.&lt;/p&gt;&lt;h4&gt;On overhead, four concrete levers&lt;/h4&gt;&lt;ul&gt;&lt;li&gt;&lt;strong&gt;A compact envelope, not a transcript.&lt;/strong&gt; Results come back as verdict | findings[] | files_touched[] | remaining[] | usage | duration, plus a pointer to a contained transcript on disk. The caller&amp;#39;s context stays clean; the decision-critical parts are inline and machine-readable.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Direct, not wrapped.&lt;/strong&gt; Going straight to the codex CLI drops the ~476-line per-call skill tax the official subagent pays &lt;em&gt;fresh on every invocation&lt;/em&gt; — Claude Code doesn&amp;#39;t cache subagent skill context across calls, so that tax compounds linearly with call count.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;One owner, not four.&lt;/strong&gt; Twenty-five dispatch sites across cc_foundry / cc_oss / cc_develop / cc_research collapse onto one versioned contract — one bump per change, not four byte-identical edits kept in sync by discipline.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;A trimmed reverse call.&lt;/strong&gt; On the Codex→Claude side, trimming the config sources makes a trivial round-trip &lt;strong&gt;4.4× cheaper&lt;/strong&gt; — the un-trimmed worker was loading the full CLAUDE.md, every plugin, every MCP server, and every skill just to reply &amp;quot;OK&amp;quot;.&lt;/li&gt;&lt;/ul&gt;&lt;h4&gt;On directing the process&lt;/h4&gt;&lt;ul&gt;&lt;li&gt;&lt;strong&gt;The caller picks model and effort every call — as a real flag.&lt;/strong&gt; Not the Effort level: medium. Prose my own resolve skill currently writes into the prompt and hops on. When the caller omits effort, the bridge classifies the task and picks the tier, so a three-line lint fix stops costing the same as a full-diff review.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;The caller sets the timeout budget&lt;/strong&gt; (advise 2 min, review 5 min, implement 10 min, all overridable) — and the budget is &lt;em&gt;announced to the callee&lt;/em&gt;, which turns a hard kill into a self-scoped run that returns partial results and names its blockers instead of losing everything.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;A recursion guard&lt;/strong&gt; keeps the two agents from summoning each other in a loop — the failure mode a two-way bridge otherwise ships with by default.&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;That’s the pitch. The interesting part is everything the pitch didn’t survive contact with.&lt;/p&gt;&lt;h3&gt;The wall, examined: why MCP and not a plain call&lt;/h3&gt;&lt;p&gt;The Codex→Claude design you reach for first is a subprocess: Codex spawns claude -p &amp;quot;…&amp;quot;. True model-to-model, per-call model and effort, no extra moving parts. I was ready to make it the primary path. It doesn&amp;#39;t work, and it&amp;#39;s worth walking the elimination, because the reason isn&amp;#39;t the one you&amp;#39;d guess.&lt;/p&gt;&lt;p&gt;There are three ways a call from inside Codex’s sandbox could fail, and only one of them is the real one:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;&lt;strong&gt;Network?&lt;/strong&gt; Open. curl to api.anthropic.com returned a real HTTP status — server reached — under both read-only and workspace-write sandbox modes. Not the problem. (Caveat: this machine doesn&amp;#39;t set network_access = false; one that did would behave differently.)&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Auth?&lt;/strong&gt; Blocked. The identical claude -p that succeeds unsandboxed comes back, inside the sandbox, with Not logged in · Please run /login. Claude Code&amp;#39;s credentials live in the macOS &lt;strong&gt;login keychain&lt;/strong&gt;, not a dotfile, and Seatbelt hides the keychain from the sandboxed process — so Claude correctly reports it has no login. &lt;strong&gt;The subprocess can reach the entire internet and still can&amp;#39;t read its own credentials.&lt;/strong&gt;&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Escalate the sandbox to expose the keychain?&lt;/strong&gt; danger-full-access does make the call work — but it drops the sandbox for the &lt;em&gt;whole&lt;/em&gt; Codex turn, not just the credential read. You&amp;#39;d trade a bounded credential lookup for an unsandboxed model turn. No.&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;So the real question is narrower than “subprocess or MCP”: &lt;em&gt;is there a way to keep the model turn sandboxed and still let the Claude call reach the keychain?&lt;/em&gt; There is — put the Claude call in a process that was never inside the sandbox to begin with. &lt;strong&gt;MCP servers declared to Codex launch outside Seatbelt.&lt;/strong&gt; The model turn stays sandboxed; the bridge’s own stdio MCP server runs beside it with the host’s auth context. I probed exactly that: with the model turn pinned at read-only, the MCP server read the keychain, wrote a file, and completed a full claude -p call cleanly.&lt;/p&gt;&lt;p&gt;That’s the examined reason for MCP — not fashion. &lt;strong&gt;It’s the only transport that satisfies both constraints at once: a sandboxed model turn &lt;em&gt;and&lt;/em&gt; an authenticated Claude call.&lt;/strong&gt; A plain subprocess forces you to give up one of the two. Which also means MCP is &lt;em&gt;directional&lt;/em&gt; — mandatory for Codex→Claude and therefore for a complete two-way install, and entirely unnecessary for Claude→Codex, which just calls the codex CLI directly:&lt;/p&gt;&lt;pre&gt;Claude Code ──► codex exec ─────────────────► Codex     (direct CLI; no MCP)&lt;br&gt;Codex       ──► bridge MCP server ──► claude -p ──► Claude&lt;br&gt;                 └─ runs OUTSIDE the sandbox, so auth resolves&lt;/pre&gt;&lt;p&gt;One honesty caveat I’m carrying into the next phase: my probe declared the MCP server via config, not via a literal plugin manifest. Same loader, but I haven’t separately exercised the plugin-declared path, so it stays on the verify list, not the done list.&lt;/p&gt;&lt;h3&gt;The optimization that was a trap: session resume&lt;/h3&gt;&lt;p&gt;“Reuse the session” sounds like free money. Every cc_ delegation starts a fresh Codex thread, so surely resuming an existing one saves the setup cost. I measured it before wiring it in, and I&amp;#39;m glad I did.&lt;/p&gt;&lt;p&gt;The first number was almost comically bad. A fresh trivial call cost &lt;strong&gt;~15.6k tokens&lt;/strong&gt;; the “resumed” version of the same trivial call cost &lt;strong&gt;~2.46M&lt;/strong&gt;. That’s 158× — and it’s a lie about resume and a truth about a footgun. Codex’s --last doesn&amp;#39;t mean &amp;quot;the session I just created.&amp;quot; It means &amp;quot;the newest recorded session in this working directory,&amp;quot; and after months of use there are &lt;em&gt;thousands&lt;/em&gt; of candidates — I had roughly 2,000 rollout files sitting under ~/.codex/sessions. With that many sessions, &amp;quot;newest in this directory&amp;quot; isn&amp;#39;t an address, it&amp;#39;s a guess: it had latched onto a large, unrelated thread and even warned it was resuming one recorded under a different model. Under concurrency, two delegations race for &amp;quot;newest&amp;quot;; fired inside a user&amp;#39;s own repo, the bridge call &lt;em&gt;becomes&lt;/em&gt; the newest session there and shadows their work. So --last is banned in bridge code outright — resume only ever happens by the explicit session_id captured from the run that created it.&lt;/p&gt;&lt;p&gt;Then I measured resume &lt;em&gt;properly&lt;/em&gt;: same directory, same flags, a three-turn thread carrying about five tokens of real content. Turn 1 billed &lt;strong&gt;~26k&lt;/strong&gt; input; turn 2, &lt;strong&gt;~52k&lt;/strong&gt;; turn 3, &lt;strong&gt;~79k&lt;/strong&gt;. &lt;strong&gt;Resume replays the entire transcript every turn&lt;/strong&gt;, so cost grows linearly with turn count on a conversation that contains almost nothing. Most of the replay lands as discounted cached input, but the uncached portion climbs too. The verdict reversed cleanly: &lt;strong&gt;resume is not the cheap path, it’s the expensive one&lt;/strong&gt; for the short bounded tasks this bridge exists to run. So advise and review default to ephemeral — no session written, nothing to resume, nothing to leak or shadow — and the rare legitimate resume is just a fresh call with the leftover work pasted into the prompt.&lt;/p&gt;&lt;p&gt;There’s a nastier tail. Reading the sandbox policy back out of a resumed thread across three turns: turn 1 recorded read-only, turn 2 with an explicit override recorded read-only, turn 3 with a plain resume recorded &lt;strong&gt;workspace-write&lt;/strong&gt;. A resumed call silently fell back to the config default and &lt;em&gt;gained write access&lt;/em&gt; — quietly breaking the &amp;quot;no writes&amp;quot; guarantee an advise is supposed to hold. So the bridge builds its resume command from its own template that always re-pins the sandbox, never inherits it. None of that appeared in any --help output. It only showed up in a real run.&lt;/p&gt;&lt;h3&gt;The hang you can’t see&lt;/h3&gt;&lt;p&gt;This is the failure mode I’d most want you to internalize, because it’s invisible from the outside and it burns your whole timeout budget at zero cost.&lt;/p&gt;&lt;p&gt;codex exec with a prompt argument still &lt;strong&gt;hangs&lt;/strong&gt; if its stdin is an inherited pipe that never sends EOF. Measured: 180 seconds to a forced kill, zero bytes out, zero session files, zero tokens spent — stderr showing only &amp;quot;Reading additional input from stdin…&amp;quot;. The identical command with stdin pointed at /dev/null finished in 13 seconds.&lt;/p&gt;&lt;p&gt;From outside the process this is indistinguishable from a model hang. It spends nothing, so no cost signal catches it. No fault string ever returns, so a self-healing playbook has nothing to match against — there’s no error, just silence for the full ten-minute budget. &lt;strong&gt;A hang with no error message is worse than a crash&lt;/strong&gt;, because a crash at least tells you it happened. You can’t detect this one after the fact; you have to prevent it at the source. The whole fix is one line: spawn every child with stdin: &amp;#39;ignore&amp;#39;. No watchdog required once it&amp;#39;s set.&lt;/p&gt;&lt;p&gt;The general lesson outlived the bug. The compact envelope, the per-operation timeouts, and the bounded one-shot retry are all built on the same principle: &lt;strong&gt;fail loudly and on my own timeline&lt;/strong&gt;, rather than inherit a path that hangs indefinitely and waits for a human to notice.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*wfzSL0AAl8XyAs62wNC7Mg.png&quot; /&gt;&lt;figcaption&gt;Illustration generated specifically for this blog post.&lt;/figcaption&gt;&lt;/figure&gt;&lt;h3&gt;Lessons, in plain terms&lt;/h3&gt;&lt;p&gt;Strip the specifics and here’s what I’ll carry into the next build. Each one cost real time.&lt;/p&gt;&lt;h4&gt;1. An open network is not an open door.&lt;/h4&gt;&lt;p&gt;I assumed a sandbox failure would be a network failure. It was an auth failure — the box could reach the whole internet and still couldn’t read its own keychain. When something fails inside a sandbox, don’t reason about the failure you’d &lt;em&gt;expect&lt;/em&gt;; run the trivial probe and read the actual error. The plausible cause and the real cause pointed at different fixes entirely, and only one of them led to MCP.&lt;/p&gt;&lt;h4&gt;2. Measure the optimization before you trust it.&lt;/h4&gt;&lt;p&gt;“Reuse the session” was self-evidently cheaper right up until it was 158× more expensive, and even the honest measurement left it the costly path. The intuitive saving and the measured cost pointed opposite directions. Anything you’d wire in &lt;em&gt;because it obviously helps&lt;/em&gt; is exactly the thing to put on a scale first — especially when the mechanism (here, transcript replay) hides behind a friendly flag name.&lt;/p&gt;&lt;h4&gt;3. A hang with no error is the worst thing you can ship.&lt;/h4&gt;&lt;p&gt;A crash announces itself. A silent stall looks identical to real work, spends nothing to hide, and defeats every after-the-fact recovery you build. Prevent that class at the spawn point; don’t try to detect it downstream. And give every path a bounded cutoff, so the worst case is a clean partial result instead of an open-ended wait.&lt;/p&gt;&lt;h4&gt;4. A green test that shares your bug is worse than no test.&lt;/h4&gt;&lt;p&gt;My first installed-plugin detector and its test both used an invented flat registry fixture. Claude’s real registry nests those entries under a plugins key — but because the fake repeated the same wrong shape, the focused tests passed while production detection would have failed on the first real call. A fixture written from your own assumption can&amp;#39;t falsify that assumption. Tests have to meet the real schema, and a migration regression has to name &lt;em&gt;every&lt;/em&gt; retired path, not just the obvious one, or you get a false green.&lt;/p&gt;&lt;h4&gt;5. A delegated task is an API call, not a conversation.&lt;/h4&gt;&lt;p&gt;When you hand work to another agent, it doesn’t have your plan, your scrollback, or your shorthand — it has only the bytes you send and the files it can reach. Every cc_ consumer I migrated had to stop sending things like &amp;quot;do step 3 the way we discussed&amp;quot; or a bare &amp;lt;self-contained task&amp;gt; placeholder, because the peer can&amp;#39;t ask what you meant. It just guesses — and a confident guess reads exactly like understanding, which is how you end up debugging a &amp;quot;wrong answer&amp;quot; that was really a vague question. A brief has to stand on its own: the concrete objective, the target paths or supplied evidence, the permission boundary, the expected result, and the stopping condition, coherent with none of your context attached. When a peer &amp;quot;misunderstands your instructions,&amp;quot; the fix is almost never a bigger model. It&amp;#39;s a request that didn&amp;#39;t need the conversation to make sense.&lt;/p&gt;&lt;h3&gt;The process lesson&lt;/h3&gt;&lt;p&gt;Last time I wrote about a Codex plugin, the story was that I’d written a careful plan and never tested the one claim it stood on — “a plugin can bundle agents the same way it bundles skills” — because it didn’t look like a claim, it looked like background. Half a day of throwaway probing would have caught it. I found out with the package already built around it.&lt;/p&gt;&lt;p&gt;This time I did the opposite, and it’s the reason the build went where it did. &lt;strong&gt;The single genuinely hard question — can Codex reach Claude at all — got probed before the plan hardened around an answer.&lt;/strong&gt; That one probe is what killed the subprocess design and surfaced MCP. Two more adversarial review rounds, run against the plan before any real code existed, retired the rest while they were still cheap to be wrong about — the stdin hang, the resume cost, the silent sandbox escalation.&lt;/p&gt;&lt;p&gt;There’s a second-order payoff I only understood after the last build: &lt;strong&gt;a foundation you’ve tested is one you can afford to hold the line on.&lt;/strong&gt; Once the transport question was settled by measurement, every later irritation was just an irritation — not fresh evidence the whole design was rotten. I could fix problems &lt;em&gt;inside&lt;/em&gt; the plan instead of redesigning around each one. Untested foundations don’t only make you rebuild; they make you &lt;em&gt;doubt&lt;/em&gt;, and doubt turns every obstacle into a costume for “start over.”&lt;/p&gt;&lt;p&gt;The staging followed from the same instinct. &lt;strong&gt;Standalone bridge first, consumer migration second — two separate commits.&lt;/strong&gt; Stage 1 delivered the complete dual-host bridge in isolation, touching no existing plugin. Only after it stood on its own did Stage 2 migrate the four cc_ consumers onto it, retire the old integration, and bump each affected plugin exactly once. Those twenty-five dispatch sites collapsed onto one contract only after the thing they were collapsing onto had earned it.&lt;/p&gt;&lt;h3&gt;Where it stands&lt;/h3&gt;&lt;p&gt;The bridge is committed in two stages: the standalone dual-host plugin at 0.1.0, the consumer migration and integration at 0.2.0. The consolidated Stage 2 gate recorded 3,964 tests passing, five capability-gated skips, one pre-existing SciPy warning; strict manifest validation, package validation, lint, formatting, and documentation build all green. Every active cc_foundry, cc_oss, cc_develop, and cc_research call now routes through implement, advise, or review, with explicit available/ disabled / absent detection so an optional route skips cleanly when the bridge isn&amp;#39;t installed and a required one stops with a named missing prerequisite. Nothing falls back to the retired path.&lt;/p&gt;&lt;p&gt;What I have &lt;em&gt;not&lt;/em&gt; done, and won’t claim: no provider-backed live bridge call was rerun in Stage 2, and native Windows execution wasn’t re-exercised — the lifecycle safety leans on POSIX primitives, and on platforms that don’t provide them the manager should decline to mutate rather than half-do it. Live synchronization is deliberately gated on the pushed commit, because the sync script installs the &lt;em&gt;pushed&lt;/em&gt; remote snapshot; running it early would reinstall the previous one. That last step is human-owned by design.&lt;/p&gt;&lt;p&gt;Once it’s live, installing is the same shape in either host:&lt;/p&gt;&lt;pre&gt;# Claude Code&lt;br&gt;claude plugin marketplace add Borda/AI-Rig&lt;br&gt;claude plugin add bridge-cc-codex@borda-ai-rig&lt;br&gt;&lt;br&gt;# Codex&lt;br&gt;codex plugin marketplace add Borda/AI-Rig&lt;br&gt;codex plugin add bridge-cc-codex@borda-ai-rig&lt;/pre&gt;&lt;p&gt;Two of the strongest coding harnesses on your machine, finally able to hand each other work — a task, a question, or a review — with you choosing the model and the effort every time, minimal overhead landing back in your context, and neither one able to drag the other into a loop.&lt;/p&gt;&lt;p&gt;The honesty about what each direction needs, and about what I haven’t verified yet, is the part I trust most. If you install it and find a wall I didn’t cover — especially on a platform that isn’t macOS — that’s exactly the report I want.&lt;/p&gt;&lt;p&gt;Try it, break it, tell me how: &lt;a href=&quot;https://borda.github.io/AI-Rig/bridge_cc-codex/&quot;&gt;https://borda.github.io/AI-Rig/bridge_cc-codex&lt;/a&gt;&lt;/p&gt;&lt;h3&gt;References&lt;/h3&gt;&lt;ul&gt;&lt;li&gt;AI-Rig, the full maintenance rig: &lt;a href=&quot;https://github.com/Borda/AI-Rig&quot;&gt;github.com/Borda/AI-Rig&lt;/a&gt;&lt;/li&gt;&lt;li&gt;The sibling build this one learned from — &lt;a href=&quot;https://medium.com/codex/role-cards-not-agents-a-three-tier-fallback-for-native-codex-plugins-8f647344cefc&quot;&gt;Role Cards, Not Agents: A Three-Tier Fallback for Native Codex Plugins&lt;/a&gt;&lt;/li&gt;&lt;li&gt;Codex CLI and Claude Code docs — treat them as versioned evidence and check them against the CLI version you actually have installed. Several contracts moved under me during this build.&lt;/li&gt;&lt;/ul&gt;&lt;img src=&quot;https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=9efcd04e13e4&quot; width=&quot;1&quot; height=&quot;1&quot; alt=&quot;&quot;&gt;&lt;hr&gt;&lt;p&gt;&lt;a href=&quot;https://levelup.gitconnected.com/peers-not-rivals-a-two-way-street-between-claude-code-and-codex-9efcd04e13e4&quot;&gt;Peers, Not Rivals: A Two-Way Street Between Claude Code and Codex&lt;/a&gt; was originally published in &lt;a href=&quot;https://levelup.gitconnected.com&quot;&gt;Level Up Coding&lt;/a&gt; on Medium, where people are continuing the conversation by highlighting and responding to this story.&lt;/p&gt;</content:encoded>
    </item>
    <item>
      <title>Using OpenCode with Local Models on an Old Intel Mac</title>
      <link>https://levelup.gitconnected.com/using-opencode-with-local-models-on-an-old-intel-mac-59dabdd1cfb1?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/using-opencode-with-local-models-on-an-old-intel-mac-59dabdd1cfb1?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Fri, 04 Sep 2026 15:03:18 GMT</pubDate>
      <content:encoded>&lt;div class=&quot;medium-feed-item&quot;&gt;&lt;p class=&quot;medium-feed-image&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/using-opencode-with-local-models-on-an-old-intel-mac-59dabdd1cfb1?source=rss----5517fd7b58a6---4&quot;&gt;&lt;img src=&quot;https://cdn-images-1.medium.com/max/1200/0*sbKADsdB1hjQUw_i.png&quot; width=&quot;1200&quot;&gt;&lt;/a&gt;&lt;/p&gt;&lt;p class=&quot;medium-feed-snippet&quot;&gt;A practical guide to running an isolated autonomous coding agent with native Ollama inference on an old Intel MacBook Pro.&lt;/p&gt;&lt;p class=&quot;medium-feed-link&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/using-opencode-with-local-models-on-an-old-intel-mac-59dabdd1cfb1?source=rss----5517fd7b58a6---4&quot;&gt;Continue reading on Level Up Coding »&lt;/a&gt;&lt;/p&gt;&lt;/div&gt;</content:encoded>
    </item>
    <item>
      <title>I Didn’t Leave IntelliJ. Claude Code Moved My Desk.</title>
      <link>https://levelup.gitconnected.com/i-didnt-leave-intellij-claude-code-moved-my-desk-51fa4beca61b?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/i-didnt-leave-intellij-claude-code-moved-my-desk-51fa4beca61b?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Fri, 04 Sep 2026 15:03:06 GMT</pubDate>
      <content:encoded>&lt;div class=&quot;medium-feed-item&quot;&gt;&lt;p class=&quot;medium-feed-image&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/i-didnt-leave-intellij-claude-code-moved-my-desk-51fa4beca61b?source=rss----5517fd7b58a6---4&quot;&gt;&lt;img src=&quot;https://cdn-images-1.medium.com/max/1408/1*RBeyXjzRNV3UFWp5NWNVgQ.jpeg&quot; width=&quot;1408&quot;&gt;&lt;/a&gt;&lt;/p&gt;&lt;p class=&quot;medium-feed-snippet&quot;&gt;Why an AI coding agent, not a better terminal, is what finally pulled me out of the IDE&lt;/p&gt;&lt;p class=&quot;medium-feed-link&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/i-didnt-leave-intellij-claude-code-moved-my-desk-51fa4beca61b?source=rss----5517fd7b58a6---4&quot;&gt;Continue reading on Level Up Coding »&lt;/a&gt;&lt;/p&gt;&lt;/div&gt;</content:encoded>
    </item>
    <item>
      <title>Senior Developers Know Every Function Has a Contract</title>
      <link>https://levelup.gitconnected.com/senior-developers-know-every-function-has-a-contract-291bc28fbbd2?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/senior-developers-know-every-function-has-a-contract-291bc28fbbd2?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Fri, 04 Sep 2026 15:02:54 GMT</pubDate>
      <content:encoded>&lt;div class=&quot;medium-feed-item&quot;&gt;&lt;p class=&quot;medium-feed-image&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/senior-developers-know-every-function-has-a-contract-291bc28fbbd2?source=rss----5517fd7b58a6---4&quot;&gt;&lt;img src=&quot;https://cdn-images-1.medium.com/max/2600/1*icfXxhsPVCVz9c5Se4D1Zg.jpeg&quot; width=&quot;6016&quot;&gt;&lt;/a&gt;&lt;/p&gt;&lt;p class=&quot;medium-feed-snippet&quot;&gt;Understanding the contracts behind functions: what they require, what they guarantee, and what they must preserve.&lt;/p&gt;&lt;p class=&quot;medium-feed-link&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/senior-developers-know-every-function-has-a-contract-291bc28fbbd2?source=rss----5517fd7b58a6---4&quot;&gt;Continue reading on Level Up Coding »&lt;/a&gt;&lt;/p&gt;&lt;/div&gt;</content:encoded>
    </item>
    <item>
      <title>Connection Pooling: What Actually Happens Between Your App and Database</title>
      <link>https://levelup.gitconnected.com/connection-pooling-what-actually-happens-between-your-app-and-database-5aef7b543f51?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/connection-pooling-what-actually-happens-between-your-app-and-database-5aef7b543f51?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Fri, 04 Sep 2026 15:02:44 GMT</pubDate>
      <content:encoded>&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/1*AfdjR-XVASMC71664smI9w.png&quot; /&gt;&lt;/figure&gt;&lt;p&gt;At first, a database connection feels almost invisible.&lt;/p&gt;&lt;p&gt;Your backend receives a request, runs a query, gets the result, and sends the response.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/650/0*DdyRj9oZYn4OY6hH.png&quot; /&gt;&lt;/figure&gt;&lt;p&gt;With one user, there is nothing interesting to see.&lt;/p&gt;&lt;p&gt;But production systems rarely stay at one user.&lt;/p&gt;&lt;p&gt;So let’s slow down and ask a more useful question:&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;What is actually happening inside the computer when that request reaches the database?&lt;/em&gt;&lt;/strong&gt;&lt;/blockquote&gt;&lt;p&gt;Because before we can understand connection pooling, we need to understand &lt;strong&gt;what a connection is consuming.&lt;/strong&gt;&lt;/p&gt;&lt;h3&gt;One request is already using resources&lt;/h3&gt;&lt;p&gt;Your backend is not floating somewhere outside the machine.&lt;/p&gt;&lt;p&gt;It is running as a &lt;strong&gt;process&lt;/strong&gt; managed by the operating system.&lt;/p&gt;&lt;p&gt;When the request arrives, the application needs CPU time to execute instructions and memory to keep its current state, objects, buffers, and other data.&lt;/p&gt;&lt;p&gt;Then the application needs to communicate with PostgreSQL.&lt;/p&gt;&lt;p&gt;That communication happens through a network connection, which involves a socket and operating-system networking state.&lt;/p&gt;&lt;p&gt;A simplified picture looks like this:&lt;/p&gt;&lt;pre&gt;                         One Request&lt;br&gt;                              │&lt;br&gt;                              ▼&lt;br&gt;                     ┌─────────────────┐&lt;br&gt;                     │ Backend Process │&lt;br&gt;                     └────────┬────────┘&lt;br&gt;                              │&lt;br&gt;                    ┌─────────┼─────────┐&lt;br&gt;                    │         │         │&lt;br&gt;                   CPU       RAM      Socket&lt;br&gt;                                      │&lt;br&gt;                                      ▼&lt;br&gt;                                   Network&lt;br&gt;                                      │&lt;br&gt;                                      ▼&lt;br&gt;                               PostgreSQL&lt;/pre&gt;&lt;p&gt;There are several layers here, and they matter.&lt;/p&gt;&lt;p&gt;The request uses &lt;strong&gt;CPU&lt;/strong&gt; to do work.&lt;/p&gt;&lt;p&gt;It uses &lt;strong&gt;memory&lt;/strong&gt; to hold state and data.&lt;/p&gt;&lt;p&gt;The connection uses &lt;strong&gt;networking resources&lt;/strong&gt; to move bytes between the application and database.&lt;/p&gt;&lt;p&gt;And PostgreSQL has its own resources and internal state for handling that client and executing its query.&lt;/p&gt;&lt;p&gt;So a database request is not free.&lt;/p&gt;&lt;h3&gt;But what exactly is a connection?&lt;/h3&gt;&lt;p&gt;Think of a database connection as an established communication channel between your application and PostgreSQL.&lt;/p&gt;&lt;pre&gt;Application&lt;br&gt;    │&lt;br&gt;    │  database connection&lt;br&gt;    ▼&lt;br&gt;PostgreSQL&lt;/pre&gt;&lt;p&gt;Under the hood, this communication uses networking primitives such as a &lt;strong&gt;socket&lt;/strong&gt;.&lt;/p&gt;&lt;p&gt;For a typical PostgreSQL deployment, the application communicates with PostgreSQL over TCP/IP when the database is reached over the network.&lt;/p&gt;&lt;p&gt;So the path is closer to:&lt;/p&gt;&lt;p&gt;This is why the line:&lt;/p&gt;&lt;pre&gt;await db.query(...)&lt;/pre&gt;&lt;p&gt;can hide a surprising amount of machinery.&lt;/p&gt;&lt;p&gt;Your application code looks simple.&lt;/p&gt;&lt;p&gt;The system underneath isn’t.&lt;/p&gt;&lt;h3&gt;Now let’s add more requests&lt;/h3&gt;&lt;p&gt;One request is easy.&lt;/p&gt;&lt;p&gt;Let’s say ten requests arrive concurrently.&lt;/p&gt;&lt;p&gt;If we use the naive approach of creating a separate connection for each request, we get something like:&lt;/p&gt;&lt;pre&gt;Request 1  ──► Connection 1 ──┐&lt;br&gt;Request 2  ──► Connection 2 ──┤&lt;br&gt;Request 3  ──► Connection 3 ──┤&lt;br&gt;Request 4  ──► Connection 4 ──┤&lt;br&gt;Request 5  ──► Connection 5 ──┤&lt;br&gt;Request 6  ──► Connection 6 ──┤&lt;br&gt;Request 7  ──► Connection 7 ──┤&lt;br&gt;Request 8  ──► Connection 8 ──┤&lt;br&gt;Request 9  ──► Connection 9 ──┤&lt;br&gt;Request 10 ──► Connection 10 ─┘&lt;br&gt;                              │&lt;br&gt;                              ▼&lt;br&gt;                         PostgreSQL&lt;/pre&gt;&lt;p&gt;Ten concurrent requests.&lt;/p&gt;&lt;p&gt;Ten connections.&lt;/p&gt;&lt;p&gt;Still probably manageable.&lt;/p&gt;&lt;p&gt;But let’s keep increasing the load.&lt;/p&gt;&lt;pre&gt;1 concurrent request&lt;br&gt;        ↓&lt;br&gt;10 concurrent requests&lt;br&gt;        ↓&lt;br&gt;100 concurrent requests&lt;br&gt;        ↓&lt;br&gt;1,000 concurrent requests&lt;br&gt;        ↓&lt;br&gt;10,000 concurrent requests&lt;/pre&gt;&lt;p&gt;If every request creates its own connection, the number of connections grows with the traffic.&lt;/p&gt;&lt;p&gt;And that’s where the architecture starts becoming uncomfortable.&lt;/p&gt;&lt;h3&gt;The database has a finite amount of resources&lt;/h3&gt;&lt;p&gt;Imagine our PostgreSQL server has:&lt;/p&gt;&lt;pre&gt;CPU:  4 cores&lt;br&gt;RAM:  4 GB&lt;/pre&gt;&lt;p&gt;Those resources don’t magically increase when another thousand requests arrive.&lt;/p&gt;&lt;p&gt;The machine still has the same four CPU cores.&lt;/p&gt;&lt;p&gt;It still has the same four gigabytes of memory.&lt;/p&gt;&lt;p&gt;It still has finite network, storage, and other operating-system resources.&lt;/p&gt;&lt;p&gt;We can visualize the database server like this:&lt;/p&gt;&lt;pre&gt;              PostgreSQL Server&lt;br&gt;        ┌──────────────────────────────┐&lt;br&gt;        │                              │&lt;br&gt;        │     CPU   CPU   CPU   CPU    │&lt;br&gt;        │                              │&lt;br&gt;        │          4 GB RAM            │&lt;br&gt;        │                              │&lt;br&gt;        │       Network / Sockets      │&lt;br&gt;        │                              │&lt;br&gt;        │          Storage             │&lt;br&gt;        │                              │&lt;br&gt;        └──────────────────────────────┘&lt;/pre&gt;&lt;p&gt;Now imagine hundreds or thousands of clients trying to use those same resources.&lt;/p&gt;&lt;p&gt;The problem is not simply:&lt;/p&gt;&lt;blockquote&gt;&lt;em&gt;“The database has many connections.”&lt;/em&gt;&lt;/blockquote&gt;&lt;p&gt;The deeper problem is:&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;Many pieces of work are competing for a finite set of resources.&lt;/em&gt;&lt;/strong&gt;&lt;/blockquote&gt;&lt;p&gt;That’s the real systems problem.&lt;/p&gt;&lt;h3&gt;More connections don’t create more CPU&lt;/h3&gt;&lt;p&gt;This is an important distinction.&lt;/p&gt;&lt;p&gt;Suppose our database has four CPU cores.&lt;/p&gt;&lt;p&gt;Adding 100 connections does &lt;strong&gt;not&lt;/strong&gt; turn it into a 100-core machine.&lt;/p&gt;&lt;pre&gt;4 CPU cores&lt;br&gt;     │&lt;br&gt;     │   100 connections&lt;br&gt;     ▼&lt;br&gt;Many pieces of work competing&lt;br&gt;for the same CPU resources&lt;/pre&gt;&lt;p&gt;The operating system and PostgreSQL have to manage all that concurrent work.&lt;/p&gt;&lt;p&gt;Some work may be waiting for CPU.&lt;/p&gt;&lt;p&gt;Some may be waiting for I/O.&lt;/p&gt;&lt;p&gt;Some may be waiting for locks.&lt;/p&gt;&lt;p&gt;Some may be waiting on the network.&lt;/p&gt;&lt;p&gt;And some connections may simply be sitting idle.&lt;/p&gt;&lt;p&gt;So &lt;strong&gt;connection count and useful parallelism are not the same thing&lt;/strong&gt;.&lt;/p&gt;&lt;p&gt;That’s going to become very important later.&lt;/p&gt;&lt;h3&gt;And memory has a limit too&lt;/h3&gt;&lt;p&gt;Connections also have associated state.&lt;/p&gt;&lt;p&gt;The application maintains connection-related state.&lt;/p&gt;&lt;p&gt;The operating system maintains networking state.&lt;/p&gt;&lt;p&gt;PostgreSQL maintains state associated with connected clients and their work.&lt;/p&gt;&lt;p&gt;So if connection count keeps growing, resource usage can grow too.&lt;/p&gt;&lt;p&gt;A useful mental model is:&lt;/p&gt;&lt;pre&gt;More connections&lt;br&gt;       │&lt;br&gt;       ├──► More connection state&lt;br&gt;       │&lt;br&gt;       ├──► More memory pressure&lt;br&gt;       │&lt;br&gt;       ├──► More work to manage&lt;br&gt;       │&lt;br&gt;       └──► More concurrent database work&lt;br&gt;                    │&lt;br&gt;                    ▼&lt;br&gt;             Resource contention&lt;/pre&gt;&lt;p&gt;The exact memory cost is &lt;strong&gt;not a universal fixed number&lt;/strong&gt;.&lt;/p&gt;&lt;p&gt;It depends on PostgreSQL configuration, workload, connection state, driver behavior, operating system, and other factors.&lt;/p&gt;&lt;p&gt;So we shouldn’t teach:&lt;/p&gt;&lt;blockquote&gt;&lt;em&gt;“One PostgreSQL connection always costs exactly X MB.”&lt;/em&gt;&lt;/blockquote&gt;&lt;p&gt;That sounds precise but gives the reader the wrong mental model.&lt;/p&gt;&lt;p&gt;The useful lesson is simpler:&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;Connections consume resources, and those resources are finite.&lt;/em&gt;&lt;/strong&gt;&lt;/blockquote&gt;&lt;h3&gt;What Happens When Traffic Keeps Growing?&lt;/h3&gt;&lt;p&gt;Let’s use a deliberately simplified example.&lt;/p&gt;&lt;p&gt;Suppose our application receives &lt;strong&gt;1,000 concurrent requests&lt;/strong&gt;.&lt;/p&gt;&lt;p&gt;And suppose, for the moment, that every request creates its own database connection.&lt;/p&gt;&lt;p&gt;That gives us:&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;1,000 requests → 1,000 database connections&lt;/em&gt;&lt;/strong&gt;&lt;/blockquote&gt;&lt;p&gt;Now imagine the traffic grows to &lt;strong&gt;10,000 concurrent requests&lt;/strong&gt;.&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;10,000 requests → 10,000 database connections&lt;/em&gt;&lt;/strong&gt;&lt;/blockquote&gt;&lt;p&gt;The problem is now obvious.&lt;/p&gt;&lt;p&gt;The application’s traffic is directly controlling the number of database connections.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;More traffic means more connections.&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;And that means more connection state, more memory pressure, and more work for the database to manage.&lt;/p&gt;&lt;p&gt;That’s exactly what we don’t want.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/276/0*d-BklOMDsuRdTZZt.png&quot; /&gt;&lt;/figure&gt;&lt;p&gt;Eventually, some resource becomes the bottleneck.&lt;/p&gt;&lt;p&gt;Maybe memory.&lt;/p&gt;&lt;p&gt;Maybe CPU.&lt;/p&gt;&lt;p&gt;Maybe I/O.&lt;/p&gt;&lt;p&gt;Maybe connection limits.&lt;/p&gt;&lt;p&gt;Maybe query contention.&lt;/p&gt;&lt;p&gt;The exact failure depends on the workload.&lt;/p&gt;&lt;p&gt;But the architectural problem is already clear:&lt;/p&gt;&lt;p&gt;&lt;strong&gt;we allowed request volume to directly determine database connection count.&lt;/strong&gt;&lt;/p&gt;&lt;h3&gt;We need a boundary&lt;/h3&gt;&lt;p&gt;Instead of saying:&lt;/p&gt;&lt;blockquote&gt;&lt;em&gt;“Every request gets a connection.”&lt;/em&gt;&lt;/blockquote&gt;&lt;p&gt;we want something more controlled.&lt;/p&gt;&lt;p&gt;Imagine we decide:&lt;/p&gt;&lt;blockquote&gt;&lt;em&gt;“This application is allowed to maintain only 10 database connections.”&lt;/em&gt;&lt;/blockquote&gt;&lt;p&gt;Now the relationship changes.&lt;/p&gt;&lt;pre&gt;                Many Requests&lt;br&gt;                       │&lt;br&gt;                       ▼&lt;br&gt;                ┌─────────────┐&lt;br&gt;                │   10        │&lt;br&gt;                │ Connections │&lt;br&gt;                └──────┬──────┘&lt;br&gt;                       │&lt;br&gt;                       ▼&lt;br&gt;                  PostgreSQL&lt;/pre&gt;&lt;p&gt;We have separated:&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Requests ≠ Database Connections&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;And that is the fundamental idea behind &lt;strong&gt;connection pooling&lt;/strong&gt;.&lt;/p&gt;&lt;p&gt;A connection pool keeps a bounded set of reusable database connections.&lt;/p&gt;&lt;p&gt;A request gets an available connection, uses it, and gives it back.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/900/0*oxu0ZEqAUiw3vPr7.png&quot; /&gt;&lt;/figure&gt;&lt;p&gt;The connection doesn’t disappear after every request.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;It goes back into the pool and can be reused by the next request.&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;That simple change gives us a much stronger control point.&lt;/p&gt;&lt;p&gt;Instead of allowing traffic to create unlimited database connections, we put a boundary between &lt;strong&gt;incoming work&lt;/strong&gt; and &lt;strong&gt;database concurrency&lt;/strong&gt;.&lt;/p&gt;&lt;p&gt;But we’ve just created a new question:&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;If 1,000 requests arrive and the pool contains only 10 connections, what happens to the other 990?&lt;/em&gt;&lt;/strong&gt;&lt;/blockquote&gt;&lt;h3&gt;When the Pool Is Full&lt;/h3&gt;&lt;p&gt;We now have a boundary.&lt;/p&gt;&lt;p&gt;Instead of allowing every request to create another database connection, our application might keep a pool of &lt;strong&gt;10 connections&lt;/strong&gt;.&lt;/p&gt;&lt;pre&gt;                Connection Pool&lt;br&gt;        ┌─────────────────────────────┐&lt;br&gt;        │ C1 C2 C3 C4 C5 C6 C7 C8 C9 C10 │&lt;br&gt;        └──────────────┬──────────────┘&lt;br&gt;                       │&lt;br&gt;                       ▼&lt;br&gt;                  PostgreSQL&lt;/pre&gt;&lt;p&gt;But requests don’t arrive one at a time.&lt;/p&gt;&lt;p&gt;Suppose 10 requests arrive together.&lt;/p&gt;&lt;pre&gt;Request 1  ──► C1&lt;br&gt;Request 2  ──► C2&lt;br&gt;Request 3  ──► C3&lt;br&gt;Request 4  ──► C4&lt;br&gt;Request 5  ──► C5&lt;br&gt;Request 6  ──► C6&lt;br&gt;Request 7  ──► C7&lt;br&gt;Request 8  ──► C8&lt;br&gt;Request 9  ──► C9&lt;br&gt;Request 10 ──► C10&lt;/pre&gt;&lt;p&gt;Every connection is busy.&lt;/p&gt;&lt;p&gt;Then Request 11 arrives.&lt;/p&gt;&lt;p&gt;There is no free connection.&lt;/p&gt;&lt;p&gt;So what happens?&lt;/p&gt;&lt;p&gt;It &lt;strong&gt;waits&lt;/strong&gt;.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1011/0*SeUI7IU2hoUK2t9G.png&quot; /&gt;&lt;/figure&gt;&lt;p&gt;The pool doesn’t need to create Connection 11.&lt;/p&gt;&lt;p&gt;That’s the whole point.&lt;/p&gt;&lt;p&gt;Instead, the request waits until one of the existing connections finishes its work.&lt;/p&gt;&lt;h3&gt;A connection has a life&lt;/h3&gt;&lt;p&gt;Let’s follow one connection.&lt;/p&gt;&lt;p&gt;Suppose Request 1 gets C1.&lt;/p&gt;&lt;pre&gt;          Request 1&lt;br&gt;              │&lt;br&gt;              ▼&lt;br&gt;          Acquire C1&lt;br&gt;              │&lt;br&gt;              ▼&lt;br&gt;          Execute query&lt;br&gt;              │&lt;br&gt;              ▼&lt;br&gt;          Receive result&lt;br&gt;              │&lt;br&gt;              ▼&lt;br&gt;          Release C1&lt;br&gt;              │&lt;br&gt;              ▼&lt;br&gt;          C1 becomes available&lt;/pre&gt;&lt;p&gt;The connection stays alive.&lt;/p&gt;&lt;p&gt;Only the &lt;strong&gt;work using the connection&lt;/strong&gt; finishes.&lt;/p&gt;&lt;p&gt;That’s an important distinction.&lt;/p&gt;&lt;pre&gt;Connection lifecycle&lt;br&gt;&lt;br&gt;&lt;br&gt;Create ────────────────► Reuse ───────────────► Close&lt;br&gt;                           ▲&lt;br&gt;                           │&lt;br&gt;                    Request 1&lt;br&gt;                    Request 2&lt;br&gt;                    Request 3&lt;br&gt;                    Request 4&lt;br&gt;                       ...&lt;/pre&gt;&lt;p&gt;A pool can therefore reuse the same connection for many requests over its lifetime.&lt;/p&gt;&lt;p&gt;That’s where the efficiency comes from.&lt;/p&gt;&lt;p&gt;We’re not repeatedly paying the cost of creating a new connection just because another request arrived.&lt;/p&gt;&lt;h3&gt;But now we have a queue&lt;/h3&gt;&lt;p&gt;This is the first trade-off of pooling.&lt;/p&gt;&lt;p&gt;Before the pool, our naive system looked like:&lt;/p&gt;&lt;pre&gt;More requests&lt;br&gt;      ↓&lt;br&gt;More connections&lt;br&gt;      ↓&lt;br&gt;More database work/resources&lt;/pre&gt;&lt;p&gt;With a pool:&lt;/p&gt;&lt;pre&gt;More requests&lt;br&gt;      ↓&lt;br&gt;More competition for pool slots&lt;br&gt;      ↓&lt;br&gt;Some requests may wait&lt;/pre&gt;&lt;p&gt;So we’ve exchanged &lt;strong&gt;unbounded connection growth&lt;/strong&gt; for &lt;strong&gt;bounded concurrency plus waiting&lt;/strong&gt;.&lt;/p&gt;&lt;p&gt;That’s a much healthier design — but waiting has a cost.&lt;/p&gt;&lt;h3&gt;Where does the extra time come from?&lt;/h3&gt;&lt;p&gt;Imagine a request takes 20 ms to execute its database query.&lt;/p&gt;&lt;p&gt;If a connection is immediately available:&lt;/p&gt;&lt;pre&gt;Request&lt;br&gt;  │&lt;br&gt;  ├── Acquire connection&lt;br&gt;  │&lt;br&gt;  ├── 20 ms query&lt;br&gt;  │&lt;br&gt;  └── Release connection&lt;/pre&gt;&lt;p&gt;The request spends roughly the query duration doing database work.&lt;/p&gt;&lt;p&gt;But if every connection is busy:&lt;/p&gt;&lt;pre&gt;Request&lt;br&gt;  │&lt;br&gt;  ├── Wait for connection   ← queue time&lt;br&gt;  │&lt;br&gt;  ├── 20 ms query           ← service time&lt;br&gt;  │&lt;br&gt;  └── Release connection&lt;/pre&gt;&lt;p&gt;Now the request’s latency includes &lt;strong&gt;both&lt;/strong&gt; waiting time and actual database execution time.&lt;/p&gt;&lt;p&gt;We can think of it as:&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/212/1*edyenACQ5CFB5PA6a3QcDw.png&quot; /&gt;&lt;/figure&gt;&lt;p&gt;For example:&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/311/1*ieeRZqNMxzQzYfOynzQJ-g.png&quot; /&gt;&lt;/figure&gt;&lt;p&gt;The database query itself didn’t become slower.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;The request became slower because it had to wait for a connection.&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;That’s a crucial performance insight.&lt;/p&gt;&lt;h3&gt;And this is where pool size gets interesting&lt;/h3&gt;&lt;p&gt;Suppose we have:&lt;/p&gt;&lt;pre&gt;Pool size = 2&lt;/pre&gt;&lt;p&gt;Only two database operations can use those two connections at the same time.&lt;/p&gt;&lt;p&gt;If requests arrive faster than those connections can finish work, a queue starts forming.&lt;/p&gt;&lt;p&gt;Increase the pool:&lt;/p&gt;&lt;pre&gt;2 connections&lt;br&gt;       ↓&lt;br&gt;5 connections&lt;br&gt;       ↓&lt;br&gt;10 connections&lt;/pre&gt;&lt;p&gt;Now more operations can execute concurrently.&lt;/p&gt;&lt;p&gt;That can improve throughput.&lt;/p&gt;&lt;p&gt;But there is a catch.&lt;/p&gt;&lt;p&gt;The database itself still has finite resources.&lt;/p&gt;&lt;pre&gt;            PostgreSQL&lt;br&gt;          ┌─────────────────┐&lt;br&gt;          │ CPU             │&lt;br&gt;          │ RAM             │&lt;br&gt;          │ Disk / I/O      │&lt;br&gt;          │ Locks           │&lt;br&gt;          │ Network         │&lt;br&gt;          └─────────────────┘&lt;/pre&gt;&lt;p&gt;Increasing the pool doesn’t create more CPU cores or make the storage system infinitely fast.&lt;/p&gt;&lt;p&gt;It simply allows more work to reach the database concurrently.&lt;/p&gt;&lt;p&gt;And this is where a common assumption starts to break:&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;More connections ≠ automatically more performance.&lt;/em&gt;&lt;/strong&gt;&lt;/blockquote&gt;&lt;h3&gt;Let’s Make the Numbers Concrete&lt;/h3&gt;&lt;p&gt;Imagine a database query takes an average of &lt;strong&gt;20 ms&lt;/strong&gt;.&lt;/p&gt;&lt;p&gt;That means:&lt;/p&gt;&lt;p&gt;&lt;strong&gt;20 ms = 0.020 seconds&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;Suppose we have &lt;strong&gt;10 connections&lt;/strong&gt;, and for this simplified example, each connection is continuously busy handling queries that take roughly 20 ms.&lt;/p&gt;&lt;p&gt;A single connection can theoretically complete:&lt;/p&gt;&lt;p&gt;&lt;strong&gt;1 ÷ 0.020 = 50 operations per second&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;With 10 connections:&lt;/p&gt;&lt;p&gt;&lt;strong&gt;10 × 50 = 500 operations per second&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;So our simplified model gives us:&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;≈ 500 operations per second&lt;/em&gt;&lt;/strong&gt;&lt;/blockquote&gt;&lt;p&gt;But there’s an important assumption here.&lt;/p&gt;&lt;p&gt;We’re assuming PostgreSQL can actually sustain that workload.&lt;/p&gt;&lt;p&gt;If the database becomes limited by &lt;strong&gt;CPU, storage, memory, locks, or another resource&lt;/strong&gt;, the real throughput can be much lower.&lt;/p&gt;&lt;p&gt;So treat this calculation as a &lt;strong&gt;capacity model, not a performance guarantee&lt;/strong&gt;.&lt;/p&gt;&lt;h3&gt;This gives us three numbers to watch&lt;/h3&gt;&lt;p&gt;When thinking about a connection pool, three concepts become important:&lt;/p&gt;&lt;pre&gt;              Connection Pool&lt;br&gt;                      │&lt;br&gt;          ┌───────────┼───────────┐&lt;br&gt;          ▼           ▼           ▼&lt;br&gt;      Concurrency   Latency    Throughput&lt;/pre&gt;&lt;p&gt;&lt;strong&gt;Concurrency&lt;/strong&gt; — how many operations are happening at the same time.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Latency&lt;/strong&gt; — how long one request takes, including time spent waiting.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Throughput&lt;/strong&gt; — how much work the system completes per second.&lt;/p&gt;&lt;p&gt;They are related, but they are &lt;strong&gt;not the same thing&lt;/strong&gt;.&lt;/p&gt;&lt;p&gt;And this is why simply saying:&lt;/p&gt;&lt;blockquote&gt;&lt;em&gt;“My server has 8 CPU cores, so I’ll create 16 connections.”&lt;/em&gt;&lt;/blockquote&gt;&lt;p&gt;is not enough to understand the problem.&lt;/p&gt;&lt;p&gt;We need to know what the database work is actually doing, how long it takes, and which resource becomes the bottleneck.&lt;/p&gt;&lt;p&gt;That gives us a much better question:&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;How do we determine whether 5, 10, 20, or 100 connections is actually a good pool size?&lt;/em&gt;&lt;/strong&gt;&lt;/blockquote&gt;&lt;p&gt;To answer that, we need some queueing math — and one very useful equation called &lt;strong&gt;Little’s Law&lt;/strong&gt;.&lt;/p&gt;&lt;h3&gt;How Do We Choose the Pool Size?&lt;/h3&gt;&lt;p&gt;We now have a better picture of the problem.&lt;/p&gt;&lt;p&gt;A small pool can make requests wait.&lt;/p&gt;&lt;p&gt;A larger pool allows more database work to happen at the same time.&lt;/p&gt;&lt;p&gt;But making the pool larger doesn’t mean the database becomes faster.&lt;/p&gt;&lt;p&gt;So we’re looking for a balance.&lt;/p&gt;&lt;pre&gt;Too small&lt;br&gt;   │&lt;br&gt;   ▼&lt;br&gt;More waiting&lt;br&gt;   │&lt;br&gt;   ▼&lt;br&gt;Increase pool&lt;br&gt;   │&lt;br&gt;   ▼&lt;br&gt;More useful concurrency&lt;br&gt;   │&lt;br&gt;   ▼&lt;br&gt;Sweet spot&lt;br&gt;   │&lt;br&gt;   ▼&lt;br&gt;Too large&lt;br&gt;   │&lt;br&gt;   ▼&lt;br&gt;More contention&lt;br&gt;   │&lt;br&gt;   ▼&lt;br&gt;Latency can increase&lt;/pre&gt;&lt;p&gt;The interesting question is:&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;Can we put some numbers around this instead of guessing?&lt;/em&gt;&lt;/strong&gt;&lt;/blockquote&gt;&lt;p&gt;Yes.&lt;/p&gt;&lt;p&gt;One of the simplest tools for reasoning about this is &lt;strong&gt;Little’s Law&lt;/strong&gt;.&lt;/p&gt;&lt;h3&gt;Little’s Law&lt;/h3&gt;&lt;p&gt;Little’s Law connects three things:&lt;/p&gt;&lt;p&gt;L = λW&lt;/p&gt;&lt;p&gt;Don’t let the equation scare you.&lt;/p&gt;&lt;p&gt;Each letter represents something simple:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;(L) = average number of items in the system&lt;/li&gt;&lt;li&gt;(λ) = arrival rate&lt;/li&gt;&lt;li&gt;(W) = average time an item spends in the system&lt;/li&gt;&lt;li&gt;For our database example, we can think of them as:&lt;/li&gt;&lt;/ul&gt;&lt;pre&gt;L        → concurrent database work&lt;br&gt;&lt;br&gt;λ        → database operations per second&lt;br&gt;W        → average time each operation takes&lt;/pre&gt;&lt;p&gt;So:&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Concurrency = Throughput × Latency&lt;/strong&gt;&lt;/p&gt;&lt;h3&gt;Let’s Use a Real Number&lt;/h3&gt;&lt;p&gt;Suppose our application is successfully completing &lt;strong&gt;500 operations per second&lt;/strong&gt;.&lt;/p&gt;&lt;p&gt;And each database operation takes an average of &lt;strong&gt;20 ms&lt;/strong&gt;.&lt;/p&gt;&lt;p&gt;First, convert 20 milliseconds into seconds:&lt;/p&gt;&lt;p&gt;&lt;strong&gt;20 ms = 0.020 seconds&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;Now apply Little’s Law:&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;L = λW&lt;/em&gt;&lt;/strong&gt;&lt;/blockquote&gt;&lt;p&gt;Where:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;&lt;strong&gt;L&lt;/strong&gt; = average number of operations in the system&lt;/li&gt;&lt;li&gt;&lt;strong&gt;λ&lt;/strong&gt; = throughput&lt;/li&gt;&lt;li&gt;&lt;strong&gt;W&lt;/strong&gt; = average time each operation spends in the system&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;So:&lt;/p&gt;&lt;p&gt;&lt;strong&gt;L = 500 × 0.020&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;&lt;strong&gt;L = 10&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;That means we have approximately:&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;10 concurrent database operations&lt;/em&gt;&lt;/strong&gt;&lt;/blockquote&gt;&lt;p&gt;Here’s the important part:&lt;/p&gt;&lt;p&gt;&lt;strong&gt;500 operations per second does not mean we need 500 connections.&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;If each operation takes only 20 ms on average, roughly &lt;strong&gt;10 operations can be in flight at the same time&lt;/strong&gt; to sustain 500 operations per second, assuming the system is operating steadily and the database can actually handle that workload.&lt;/p&gt;&lt;p&gt;That’s the mental model Little’s Law gives us:&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;High throughput doesn’t necessarily require a huge number of connections. It depends heavily on how long each operation takes.&lt;/em&gt;&lt;/strong&gt;&lt;/blockquote&gt;&lt;h3&gt;What Happens If Queries Become Slower?&lt;/h3&gt;&lt;p&gt;Suppose the database is still completing:&lt;/p&gt;&lt;p&gt;&lt;strong&gt;500 operations per second&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;But the average operation now takes:&lt;/p&gt;&lt;p&gt;&lt;strong&gt;100 ms&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;That’s:&lt;/p&gt;&lt;p&gt;&lt;strong&gt;0.100 seconds&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;Apply Little’s Law again:&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;L = λW&lt;/em&gt;&lt;/strong&gt;&lt;/blockquote&gt;&lt;p&gt;So:&lt;/p&gt;&lt;p&gt;&lt;strong&gt;L = 500 × 0.100&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;&lt;strong&gt;L = 50&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;Now we have approximately:&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;50 concurrent operations&lt;/em&gt;&lt;/strong&gt;&lt;/blockquote&gt;&lt;p&gt;Same throughput.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Different latency.&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;And therefore much more concurrent work is sitting inside the system.&lt;/p&gt;&lt;pre&gt;20 ms query&lt;br&gt;500 ops/sec&lt;br&gt;      ↓&lt;br&gt;~10 concurrent operations&lt;br&gt;&lt;br&gt;100 ms query&lt;br&gt;500 ops/sec&lt;br&gt;      ↓&lt;br&gt;~50 concurrent operations&lt;/pre&gt;&lt;p&gt;This is why latency matters so much when thinking about connection pools.&lt;/p&gt;&lt;h3&gt;But there’s an important catch&lt;/h3&gt;&lt;p&gt;Little’s Law doesn’t tell us:&lt;/p&gt;&lt;blockquote&gt;&lt;em&gt;“Your pool must be exactly 10.”&lt;/em&gt;&lt;/blockquote&gt;&lt;p&gt;That’s not what the equation means.&lt;/p&gt;&lt;p&gt;It describes a relationship between &lt;strong&gt;average concurrency, throughput, and time in the system&lt;/strong&gt; under steady-state conditions.&lt;/p&gt;&lt;p&gt;Your pool also needs to account for things such as:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;different query durations&lt;/li&gt;&lt;li&gt;bursts of traffic&lt;/li&gt;&lt;li&gt;transactions&lt;/li&gt;&lt;li&gt;slow queries&lt;/li&gt;&lt;li&gt;connection wait time&lt;/li&gt;&lt;li&gt;CPU limits&lt;/li&gt;&lt;li&gt;I/O limits&lt;/li&gt;&lt;li&gt;locks&lt;/li&gt;&lt;li&gt;database configuration&lt;/li&gt;&lt;li&gt;application behavior&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;So we should treat the calculation as a &lt;strong&gt;model for reasoning&lt;/strong&gt;, not a magic configuration formula.&lt;/p&gt;&lt;h3&gt;Now imagine increasing the pool&lt;/h3&gt;&lt;p&gt;Suppose our pool has only 2 connections.&lt;/p&gt;&lt;p&gt;But our workload needs around 10 concurrent database operations to sustain the target throughput.&lt;/p&gt;&lt;pre&gt;Needed concurrency ≈ 10&lt;br&gt;&lt;br&gt;Pool:&lt;br&gt;C1 C2&lt;/pre&gt;&lt;p&gt;We’re limiting the database work too aggressively.&lt;/p&gt;&lt;p&gt;Requests may spend more time waiting for a connection.&lt;/p&gt;&lt;p&gt;Increase the pool:&lt;/p&gt;&lt;pre&gt;C1 C2 C3 C4 C5 C6 C7 C8 C9 C10&lt;/pre&gt;&lt;p&gt;Now there is enough connection capacity for more concurrent work.&lt;/p&gt;&lt;p&gt;That can reduce pool waiting and improve throughput.&lt;/p&gt;&lt;p&gt;But what happens if we keep going?&lt;/p&gt;&lt;pre&gt;10 connections&lt;br&gt;      ↓&lt;br&gt;20 connections&lt;br&gt;      ↓&lt;br&gt;50 connections&lt;br&gt;      ↓&lt;br&gt;100 connections&lt;br&gt;      ↓&lt;br&gt;500 connections&lt;/pre&gt;&lt;p&gt;The database hasn’t gained any CPU cores.&lt;/p&gt;&lt;p&gt;Its storage hasn’t magically become faster.&lt;/p&gt;&lt;p&gt;Its memory hasn’t become infinite.&lt;/p&gt;&lt;p&gt;We’re simply allowing more work to compete for the same underlying resources.&lt;/p&gt;&lt;h3&gt;The Sweet Spot&lt;/h3&gt;&lt;p&gt;This is why pool sizing often looks like a curve rather than:&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;More connections = more RPS&lt;/em&gt;&lt;/strong&gt;&lt;/blockquote&gt;&lt;p&gt;Conceptually:&lt;/p&gt;&lt;pre&gt;Throughput&lt;br&gt;   │&lt;br&gt;   │                         ______&lt;br&gt;   │                    ____/&lt;br&gt;   │                ___/&lt;br&gt;   │            ___/&lt;br&gt;   │        ___/&lt;br&gt;   │____ ___/&lt;br&gt;   └──────────────────────────────►&lt;br&gt;             Pool Size&lt;/pre&gt;&lt;p&gt;At first, increasing the pool can help because we’re removing unnecessary waiting.&lt;/p&gt;&lt;p&gt;Eventually, we reach a point where the database is already using its resources effectively.&lt;/p&gt;&lt;p&gt;Beyond that point, additional concurrency may provide little benefit.&lt;/p&gt;&lt;p&gt;And if we push much further, contention can make things worse.&lt;/p&gt;&lt;pre&gt;Sweet spot&lt;br&gt;                  ↓&lt;br&gt;                  ●&lt;br&gt;                /   \&lt;br&gt;              /       \&lt;br&gt;            /           \&lt;br&gt;───────────/─────────────\────────&lt;br&gt;       too small       too large&lt;/pre&gt;&lt;p&gt;The exact shape isn’t universal. Different workloads behave differently.&lt;/p&gt;&lt;p&gt;A CPU-heavy query, an I/O-heavy query, a query waiting on locks, and a very fast indexed lookup can all behave differently.&lt;/p&gt;&lt;p&gt;That’s why &lt;strong&gt;“CPU cores × 2” is a rule of thumb, not a law of physics.&lt;/strong&gt;&lt;/p&gt;&lt;h3&gt;The CPU Is Part of the Story&lt;/h3&gt;&lt;p&gt;Imagine a database server with:&lt;/p&gt;&lt;pre&gt;10 CPU cores&lt;/pre&gt;&lt;p&gt;and a pool with:&lt;/p&gt;&lt;pre&gt;20 connections&lt;/pre&gt;&lt;p&gt;That does &lt;strong&gt;not&lt;/strong&gt; mean:&lt;/p&gt;&lt;blockquote&gt;&lt;em&gt;“Each CPU core gets exactly two connections.”&lt;/em&gt;&lt;/blockquote&gt;&lt;p&gt;Connections aren’t permanently assigned to CPU cores.&lt;/p&gt;&lt;p&gt;Instead, many pieces of work become runnable, and the operating system and database coordinate their execution.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/886/0*TDax8hZpouX9quZ3.png&quot; /&gt;&lt;/figure&gt;&lt;p&gt;If there is more runnable work than the CPU can execute at once, the system has to share CPU time among that work.&lt;/p&gt;&lt;p&gt;That can introduce scheduling overhead and contention.&lt;/p&gt;&lt;p&gt;But remember something else:&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Database work is not always CPU-bound.&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;A query might spend much of its time waiting for:&lt;/p&gt;&lt;pre&gt;Disk / storage&lt;br&gt;Network&lt;br&gt;Locks&lt;br&gt;Other database work&lt;/pre&gt;&lt;p&gt;So CPU cores alone cannot tell us the perfect pool size.&lt;/p&gt;&lt;p&gt;This is why connection-pool tuning is really a &lt;strong&gt;measurement problem&lt;/strong&gt;.&lt;/p&gt;&lt;p&gt;We need to observe what the database is doing.&lt;/p&gt;&lt;h3&gt;And there’s another scaling problem&lt;/h3&gt;&lt;p&gt;So far, we’ve been talking about &lt;strong&gt;one backend instance&lt;/strong&gt;.&lt;/p&gt;&lt;p&gt;Imagine this:&lt;/p&gt;&lt;pre&gt;Backend&lt;br&gt;   │&lt;br&gt;Pool = 10&lt;br&gt;   │&lt;br&gt;PostgreSQL&lt;/pre&gt;&lt;p&gt;It looks perfectly reasonable.&lt;/p&gt;&lt;p&gt;But modern applications often run multiple backend instances.&lt;/p&gt;&lt;p&gt;Maybe:&lt;/p&gt;&lt;pre&gt;1 instance&lt;/pre&gt;&lt;p&gt;becomes:&lt;/p&gt;&lt;pre&gt;10 instances&lt;/pre&gt;&lt;p&gt;and eventually:&lt;/p&gt;&lt;pre&gt;50 instances&lt;/pre&gt;&lt;p&gt;Each instance has its &lt;strong&gt;own connection pool&lt;/strong&gt;.&lt;/p&gt;&lt;p&gt;Now the number of connections isn’t just:&lt;/p&gt;&lt;p&gt;PoolSize&lt;/p&gt;&lt;p&gt;It’s:&lt;/p&gt;&lt;p&gt;Instances \times PoolSize&lt;/p&gt;&lt;p&gt;And suddenly our nice little pool becomes a much bigger database-level problem.&lt;/p&gt;&lt;p&gt;That is where connection pooling stops being just an application configuration detail and becomes an &lt;strong&gt;architecture problem&lt;/strong&gt;.&lt;/p&gt;&lt;h3&gt;When One Backend Becomes Twenty&lt;/h3&gt;&lt;p&gt;So far, we’ve been looking at one application instance:&lt;/p&gt;&lt;pre&gt;Backend&lt;br&gt;   │&lt;br&gt;   │ Pool = 10&lt;br&gt;   ▼&lt;br&gt;PostgreSQL&lt;/pre&gt;&lt;p&gt;That seems simple.&lt;/p&gt;&lt;p&gt;But production systems often run multiple instances of the same backend. We might put them behind a load balancer so traffic can be spread across several machines.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/668/0*QcgOojd8jXWyiygV.png&quot; /&gt;&lt;/figure&gt;&lt;p&gt;Each backend instance is a separate running process.&lt;/p&gt;&lt;p&gt;And each instance normally has its &lt;strong&gt;own connection pool&lt;/strong&gt;.&lt;/p&gt;&lt;p&gt;So we actually have this:&lt;/p&gt;&lt;pre&gt;Backend 1 ── Pool: 10 ──┐&lt;br&gt;Backend 2 ── Pool: 10 ──┤&lt;br&gt;Backend 3 ── Pool: 10 ──┤──► PostgreSQL&lt;br&gt;Backend 4 ── Pool: 10 ──┤&lt;br&gt;                         │&lt;br&gt;                         ▼&lt;br&gt;                    40 possible&lt;br&gt;                    connections&lt;/pre&gt;&lt;p&gt;The important part is the multiplication.&lt;/p&gt;&lt;p&gt;If:&lt;/p&gt;&lt;p&gt;&lt;strong&gt;N&lt;/strong&gt; = number of application instances&lt;/p&gt;&lt;p&gt;and&lt;/p&gt;&lt;p&gt;&lt;strong&gt;P&lt;/strong&gt; = maximum connections per pool&lt;/p&gt;&lt;p&gt;Then a simple upper bound is:&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Cₘₐₓ = N × P&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;For four instances with a pool size of 10:&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Cₘₐₓ = 4 × 10 = 40 connections&lt;/strong&gt;&lt;/p&gt;&lt;h3&gt;Scaling the backend can scale database connections too&lt;/h3&gt;&lt;p&gt;Suppose our application grows.&lt;/p&gt;&lt;pre&gt;4 instances&lt;br&gt;× 10 connections&lt;br&gt;= 40 connections&lt;/pre&gt;&lt;p&gt;Then we deploy 20 instances:&lt;/p&gt;&lt;p&gt;&lt;strong&gt;20 × 10 = 200 connections&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;Now PostgreSQL may have to deal with up to &lt;strong&gt;200 application connections&lt;/strong&gt;.&lt;/p&gt;&lt;p&gt;Notice what happened.&lt;/p&gt;&lt;p&gt;We didn’t change the database.&lt;/p&gt;&lt;p&gt;We didn’t increase the pool size.&lt;/p&gt;&lt;p&gt;We simply increased the number of backend instances.&lt;/p&gt;&lt;p&gt;Yet the database connection count increased.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/276/0*rc6Sa0JTOYb6e3KQ.png&quot; /&gt;&lt;/figure&gt;&lt;p&gt;This is one of the easiest things to miss when scaling horizontally.&lt;/p&gt;&lt;h3&gt;Why can’t every request just get a connection?&lt;/h3&gt;&lt;p&gt;Because we still have a finite database.&lt;/p&gt;&lt;p&gt;Imagine PostgreSQL is configured to allow a certain maximum number of client connections.&lt;/p&gt;&lt;p&gt;Our application might have:&lt;/p&gt;&lt;pre&gt;20 instances&lt;br&gt;×&lt;br&gt;20 connections&lt;br&gt;=&lt;br&gt;400 possible connections&lt;/pre&gt;&lt;p&gt;But the database may not have enough resources for 400 simultaneously active sessions to be useful.&lt;/p&gt;&lt;p&gt;And there’s another subtle problem:&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Maximum connections and useful concurrency are different concepts.&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;A database being able to accept 400 connections doesn’t mean 400 queries can execute efficiently at the same time.&lt;/p&gt;&lt;p&gt;Some connections might be idle.&lt;/p&gt;&lt;p&gt;Some might be waiting.&lt;/p&gt;&lt;p&gt;Some might be running queries.&lt;/p&gt;&lt;p&gt;Some might be waiting on locks or I/O.&lt;/p&gt;&lt;p&gt;So this:&lt;/p&gt;&lt;pre&gt;400 connections&lt;/pre&gt;&lt;p&gt;doesn’t mean:&lt;/p&gt;&lt;pre&gt;400 × database performance&lt;/pre&gt;&lt;h3&gt;The database is now a shared resource&lt;/h3&gt;&lt;p&gt;Our architecture has changed.&lt;/p&gt;&lt;p&gt;At first:&lt;/p&gt;&lt;pre&gt;1 Application&lt;br&gt;     │&lt;br&gt;     ▼&lt;br&gt;1 Pool&lt;br&gt;     │&lt;br&gt;     ▼&lt;br&gt;Database&lt;/pre&gt;&lt;p&gt;Now:&lt;/p&gt;&lt;pre&gt;                    ┌── Pool ──┐&lt;br&gt;                    │          │&lt;br&gt;Backend 1 ──────────┤          │&lt;br&gt;Backend 2 ──────────┤          │&lt;br&gt;Backend 3 ──────────┤          │&lt;br&gt;Backend 4 ──────────┤          │&lt;br&gt;                    │          │&lt;br&gt;                    └──────────┘&lt;br&gt;                         │&lt;br&gt;                         ▼&lt;br&gt;                    PostgreSQL&lt;/pre&gt;&lt;p&gt;PostgreSQL is shared by all those application instances.&lt;/p&gt;&lt;p&gt;Every application pool is making its own decision about how many connections it wants.&lt;/p&gt;&lt;p&gt;But PostgreSQL sees the &lt;strong&gt;combined result&lt;/strong&gt;.&lt;/p&gt;&lt;p&gt;That’s the architectural boundary we need to understand.&lt;/p&gt;&lt;h3&gt;So where does PgBouncer enter?&lt;/h3&gt;&lt;p&gt;This is where a connection pooler such as &lt;strong&gt;PgBouncer&lt;/strong&gt; becomes useful.&lt;/p&gt;&lt;p&gt;Instead of having every application instance maintain a large number of direct PostgreSQL connections, we can put a connection pooler between the applications and PostgreSQL.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/532/0*406Dv7dEfLrIKinv.png&quot; /&gt;&lt;/figure&gt;&lt;p&gt;Now there are &lt;strong&gt;two different pooling layers&lt;/strong&gt;.&lt;/p&gt;&lt;pre&gt;Application&lt;br&gt;     │&lt;br&gt;     ▼&lt;br&gt;Application-side pool&lt;br&gt;     │&lt;br&gt;     ▼&lt;br&gt;PgBouncer&lt;br&gt;     │&lt;br&gt;     ▼&lt;br&gt;PostgreSQL&lt;/pre&gt;&lt;p&gt;And these two layers solve related but different problems.&lt;/p&gt;&lt;h3&gt;Application-side pooling&lt;/h3&gt;&lt;p&gt;Your database driver or ORM may maintain a pool.&lt;/p&gt;&lt;p&gt;For example, conceptually:&lt;/p&gt;&lt;pre&gt;Request&lt;br&gt;   │&lt;br&gt;   ▼&lt;br&gt;Application Pool&lt;br&gt;   │&lt;br&gt;   ├── Connection&lt;br&gt;   ├── Connection&lt;br&gt;   ├── Connection&lt;br&gt;   └── Connection&lt;br&gt;          │&lt;br&gt;          ▼&lt;br&gt;     PostgreSQL&lt;/pre&gt;&lt;p&gt;The application asks the pool:&lt;/p&gt;&lt;blockquote&gt;&lt;em&gt;“Give me a connection.”&lt;/em&gt;&lt;/blockquote&gt;&lt;p&gt;When the query finishes:&lt;/p&gt;&lt;blockquote&gt;&lt;em&gt;“I’m done. Put it back.”&lt;/em&gt;&lt;/blockquote&gt;&lt;p&gt;The next request can reuse it.&lt;/p&gt;&lt;h3&gt;PgBouncer is another layer&lt;/h3&gt;&lt;p&gt;PgBouncer sits between clients and PostgreSQL.&lt;/p&gt;&lt;pre&gt;Application instances&lt;br&gt;        │&lt;br&gt;        │ client connections&lt;br&gt;        ▼&lt;br&gt;   ┌───────────┐&lt;br&gt;   │ PgBouncer │&lt;br&gt;   └─────┬─────┘&lt;br&gt;         │&lt;br&gt;         │ PostgreSQL connections&lt;br&gt;         ▼&lt;br&gt;    PostgreSQL&lt;/pre&gt;&lt;p&gt;This gives us an important separation:&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;The number of application-side connections does not have to equal the number of PostgreSQL connections actually maintained behind the pooler.&lt;/em&gt;&lt;/strong&gt;&lt;/blockquote&gt;&lt;p&gt;That’s the powerful part.&lt;/p&gt;&lt;p&gt;But we need to be precise here.&lt;/p&gt;&lt;p&gt;PgBouncer isn’t simply a magical “load balancer for databases.”&lt;/p&gt;&lt;p&gt;Its main job is &lt;strong&gt;connection pooling and connection management&lt;/strong&gt;.&lt;/p&gt;&lt;p&gt;It can accept client connections and reuse a smaller set of server-side PostgreSQL connections according to its pooling mode.&lt;/p&gt;&lt;h3&gt;A concrete example&lt;/h3&gt;&lt;p&gt;Suppose we have:&lt;/p&gt;&lt;pre&gt;20 backend instances&lt;/pre&gt;&lt;p&gt;Each application instance has:&lt;/p&gt;&lt;pre&gt;Pool = 10&lt;/pre&gt;&lt;p&gt;So the application layer could potentially have:&lt;/p&gt;&lt;p&gt;&lt;strong&gt;20 × 10 = 200 connections&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;Now imagine we introduce PgBouncer and configure a smaller server-side connection capacity.&lt;/p&gt;&lt;p&gt;Conceptually:&lt;/p&gt;&lt;pre&gt;20 Backend Instances&lt;br&gt;        │&lt;br&gt;        │ many client connections&lt;br&gt;        ▼&lt;br&gt;    PgBouncer&lt;br&gt;        │&lt;br&gt;        │ bounded PostgreSQL connections&lt;br&gt;        ▼&lt;br&gt;   PostgreSQL&lt;/pre&gt;&lt;p&gt;The pooler can manage which client requests get access to available PostgreSQL server connections.&lt;/p&gt;&lt;p&gt;This gives us another control point.&lt;/p&gt;&lt;h3&gt;But we’ve created another queue&lt;/h3&gt;&lt;p&gt;And this is important.&lt;/p&gt;&lt;p&gt;Earlier, our application pool could have a queue:&lt;/p&gt;&lt;pre&gt;Requests&lt;br&gt;    │&lt;br&gt;    ▼&lt;br&gt;Application Pool&lt;br&gt;    │&lt;br&gt;    ├── busy&lt;br&gt;    ├── busy&lt;br&gt;    ├── busy&lt;br&gt;    └── waiting&lt;/pre&gt;&lt;p&gt;With PgBouncer, we can have another layer where client work may wait for an available server-side connection.&lt;/p&gt;&lt;p&gt;So the complete path can become:&lt;/p&gt;&lt;pre&gt;Request&lt;br&gt;   │&lt;br&gt;   ▼&lt;br&gt;Application Pool&lt;br&gt;   │&lt;br&gt;   ▼&lt;br&gt;PgBouncer&lt;br&gt;   │&lt;br&gt;   ▼&lt;br&gt;PostgreSQL&lt;/pre&gt;&lt;p&gt;And therefore latency can contain waiting at multiple points.&lt;/p&gt;&lt;pre&gt;Total time&lt;br&gt;   │&lt;br&gt;   ├── Application queue&lt;br&gt;   │&lt;br&gt;   ├── Network time&lt;br&gt;   │&lt;br&gt;   ├── PgBouncer waiting&lt;br&gt;   │&lt;br&gt;   ├── PostgreSQL execution&lt;br&gt;   │&lt;br&gt;   └── Response/network time&lt;/pre&gt;&lt;p&gt;This is why adding infrastructure doesn’t automatically make a system faster.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Every new layer introduces another place where work can wait.&lt;/strong&gt;&lt;/p&gt;&lt;h3&gt;The architecture is becoming a resource pipeline&lt;/h3&gt;&lt;p&gt;We can now see the bigger picture:&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/0*-NQfP9QgRcRer78N.png&quot; /&gt;&lt;/figure&gt;&lt;p&gt;A request is moving through a series of constrained resources.&lt;/p&gt;&lt;p&gt;That’s the deeper story behind connection pooling.&lt;/p&gt;&lt;p&gt;It’s not really about:&lt;/p&gt;&lt;blockquote&gt;&lt;em&gt;“How do I configure &lt;/em&gt;&lt;em&gt;max: 10?”&lt;/em&gt;&lt;/blockquote&gt;&lt;p&gt;It’s about:&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;Where should concurrency be allowed, where should it be limited, and where will work wait when capacity is exhausted?&lt;/em&gt;&lt;/strong&gt;&lt;/blockquote&gt;&lt;p&gt;And now we have enough pieces to understand why connection-pool sizing becomes a systems problem rather than an ORM setting.&lt;/p&gt;&lt;h3&gt;When Connections Stop Belonging to One Request&lt;/h3&gt;&lt;p&gt;So far, we have seen two layers of pooling:&lt;/p&gt;&lt;pre&gt;Request&lt;br&gt;   ↓&lt;br&gt;Application pool&lt;br&gt;   ↓&lt;br&gt;PgBouncer&lt;br&gt;   ↓&lt;br&gt;PostgreSQL&lt;/pre&gt;&lt;p&gt;But there is a subtle question hiding here:&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;If PgBouncer reuses a PostgreSQL connection, how can it safely give that connection to another request?&lt;/em&gt;&lt;/strong&gt;&lt;/blockquote&gt;&lt;p&gt;Because a database connection isn’t just an empty pipe.&lt;/p&gt;&lt;p&gt;It can carry &lt;strong&gt;state&lt;/strong&gt;.&lt;/p&gt;&lt;h3&gt;A connection can remember things&lt;/h3&gt;&lt;p&gt;Imagine a client connects to PostgreSQL and starts a transaction.&lt;/p&gt;&lt;p&gt;Conceptually:&lt;/p&gt;&lt;pre&gt;Client&lt;br&gt;  │&lt;br&gt;  ▼&lt;br&gt;PostgreSQL connection&lt;br&gt;  │&lt;br&gt;  ├── authenticated session&lt;br&gt;  ├── transaction state&lt;br&gt;  ├── session settings&lt;br&gt;  └── other session-related state&lt;/pre&gt;&lt;p&gt;Now suppose PgBouncer takes that PostgreSQL connection and gives it to another client too early.&lt;/p&gt;&lt;p&gt;That could be dangerous.&lt;/p&gt;&lt;p&gt;You don’t want:&lt;/p&gt;&lt;pre&gt;User A&amp;#39;s state&lt;br&gt;      ↓&lt;br&gt;PostgreSQL connection&lt;br&gt;      ↓&lt;br&gt;User B accidentally gets it&lt;/pre&gt;&lt;p&gt;So the key question becomes:&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;When exactly can a PostgreSQL connection be reused?&lt;/em&gt;&lt;/strong&gt;&lt;/blockquote&gt;&lt;p&gt;That’s what PgBouncer’s pooling modes are fundamentally about.&lt;/p&gt;&lt;h3&gt;Session Pooling&lt;/h3&gt;&lt;p&gt;The easiest model to understand is &lt;strong&gt;session pooling&lt;/strong&gt;.&lt;/p&gt;&lt;p&gt;A client gets a PostgreSQL server connection and keeps it for the lifetime of its session.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/721/0*urmh97hkQRCXn-V3.png&quot; /&gt;&lt;/figure&gt;&lt;p&gt;The server connection stays associated with that client session.&lt;/p&gt;&lt;p&gt;This makes session state easier to reason about.&lt;/p&gt;&lt;p&gt;But it also means the PostgreSQL connection isn’t available for another client while the first session is holding it.&lt;/p&gt;&lt;p&gt;So pooling efficiency is lower.&lt;/p&gt;&lt;h3&gt;Transaction Pooling&lt;/h3&gt;&lt;p&gt;Now consider a different strategy.&lt;/p&gt;&lt;p&gt;Instead of keeping a PostgreSQL connection attached to a client for the entire session, PgBouncer can associate a server connection with a client &lt;strong&gt;for the duration of a transaction&lt;/strong&gt;.&lt;/p&gt;&lt;p&gt;Conceptually:&lt;/p&gt;&lt;pre&gt;Client A&lt;br&gt;   │&lt;br&gt;   ▼&lt;br&gt;BEGIN&lt;br&gt;   │&lt;br&gt;   ▼&lt;br&gt;PgBouncer&lt;br&gt;   │&lt;br&gt;   ▼&lt;br&gt;PostgreSQL Connection 1&lt;br&gt;   │&lt;br&gt;   ▼&lt;br&gt;Queries&lt;br&gt;   │&lt;br&gt;   ▼&lt;br&gt;COMMIT&lt;br&gt;   │&lt;br&gt;   ▼&lt;br&gt;Connection becomes reusable&lt;/pre&gt;&lt;p&gt;Then another client can use that server connection:&lt;/p&gt;&lt;pre&gt;Client A ──► Transaction ──► DB Connection 1&lt;br&gt;                              │&lt;br&gt;                              ▼&lt;br&gt;                         COMMIT&lt;br&gt;                              │&lt;br&gt;                              ▼&lt;br&gt;Client B ──► Transaction ──► DB Connection 1&lt;/pre&gt;&lt;p&gt;Notice what happened.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;The PostgreSQL connection stayed alive.&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;The client using it changed.&lt;/p&gt;&lt;p&gt;That’s the important distinction.&lt;/p&gt;&lt;h3&gt;Why this can dramatically change connection usage&lt;/h3&gt;&lt;p&gt;Imagine:&lt;/p&gt;&lt;pre&gt;100 application clients&lt;/pre&gt;&lt;p&gt;but only:&lt;/p&gt;&lt;pre&gt;20 PostgreSQL server connections&lt;/pre&gt;&lt;p&gt;If clients are mostly doing short transactions, a pooler can reuse those 20 server connections across many clients.&lt;/p&gt;&lt;p&gt;Conceptually:&lt;/p&gt;&lt;pre&gt;100 Clients&lt;br&gt;    │&lt;br&gt;    ▼&lt;br&gt;┌──────────────┐&lt;br&gt;│  PgBouncer   │&lt;br&gt;└──────┬───────┘&lt;br&gt;       │&lt;br&gt;       ▼&lt;br&gt;20 PostgreSQL&lt;br&gt;connections&lt;/pre&gt;&lt;p&gt;We’re separating:&lt;/p&gt;&lt;p&gt;\text{Client connections}&lt;/p&gt;&lt;p&gt;from:&lt;/p&gt;&lt;p&gt;\text{PostgreSQL server connections}&lt;/p&gt;&lt;p&gt;That’s one of the biggest reasons an external pooler can be useful when many application instances exist.&lt;/p&gt;&lt;h3&gt;But there is a trade-off&lt;/h3&gt;&lt;p&gt;Transaction pooling gives us more reuse.&lt;/p&gt;&lt;p&gt;But session state becomes harder to rely on.&lt;/p&gt;&lt;p&gt;Suppose your application expects something to remain attached to the same PostgreSQL session.&lt;/p&gt;&lt;p&gt;With transaction pooling, the next transaction might get a &lt;strong&gt;different server connection&lt;/strong&gt;.&lt;/p&gt;&lt;pre&gt;Transaction 1&lt;br&gt;     │&lt;br&gt;     ▼&lt;br&gt;Connection A&lt;br&gt;&lt;br&gt;Transaction ends&lt;br&gt;     │&lt;br&gt;     ▼&lt;br&gt;Connection returned to pool&lt;br&gt;Transaction 2&lt;br&gt;     │&lt;br&gt;     ▼&lt;br&gt;Connection B&lt;/pre&gt;&lt;p&gt;So application features that depend on session affinity or session-level state need careful consideration.&lt;/p&gt;&lt;p&gt;This is why pooling isn’t simply:&lt;/p&gt;&lt;blockquote&gt;&lt;em&gt;“Put PgBouncer in front of PostgreSQL and everything gets faster.”&lt;/em&gt;&lt;/blockquote&gt;&lt;p&gt;The pooling mode has to match how the application uses PostgreSQL.&lt;/p&gt;&lt;h3&gt;Now let’s zoom back out&lt;/h3&gt;&lt;p&gt;We started with one request:&lt;/p&gt;&lt;pre&gt;Request&lt;br&gt;   ↓&lt;br&gt;Database&lt;/pre&gt;&lt;p&gt;Then discovered the actual path:&lt;/p&gt;&lt;pre&gt;Request&lt;br&gt;   ↓&lt;br&gt;Backend Process&lt;br&gt;   ↓&lt;br&gt;Connection Pool&lt;br&gt;   ↓&lt;br&gt;Socket / Network&lt;br&gt;   ↓&lt;br&gt;PostgreSQL&lt;/pre&gt;&lt;p&gt;Then we scaled the application:&lt;/p&gt;&lt;pre&gt;                    ┌── Backend 1 ── Pool ──┐&lt;br&gt;                    │                       │&lt;br&gt;                    ├── Backend 2 ── Pool ──┤&lt;br&gt;                    │                       │&lt;br&gt;                    ├── Backend 3 ── Pool ──┤&lt;br&gt;                    │                       │&lt;br&gt;                    └── Backend N ── Pool ──┘&lt;br&gt;                                             │&lt;br&gt;                                             ▼&lt;br&gt;                                         PgBouncer&lt;br&gt;                                             │&lt;br&gt;                                             ▼&lt;br&gt;                                        PostgreSQL&lt;/pre&gt;&lt;p&gt;And now we can see something deeper.&lt;/p&gt;&lt;p&gt;There isn’t one single “connection count.”&lt;/p&gt;&lt;p&gt;There can be several different populations of connections:&lt;/p&gt;&lt;pre&gt;Application-side client connections&lt;br&gt;              │&lt;br&gt;              ▼&lt;br&gt;           PgBouncer&lt;br&gt;              │&lt;br&gt;              ▼&lt;br&gt;PostgreSQL server-side connections&lt;/pre&gt;&lt;p&gt;Those numbers can be very different.&lt;/p&gt;&lt;h3&gt;The hidden queue&lt;/h3&gt;&lt;p&gt;There’s another piece we haven’t fully explored yet.&lt;/p&gt;&lt;p&gt;Suppose PostgreSQL can efficiently handle only a certain amount of concurrent work.&lt;/p&gt;&lt;p&gt;But our application sends more work than that.&lt;/p&gt;&lt;p&gt;Eventually, somewhere, requests must wait.&lt;/p&gt;&lt;figure&gt;&lt;img alt=&quot;&quot; src=&quot;https://cdn-images-1.medium.com/max/1024/0*Q_4YVjxcum9_S7-M.png&quot; /&gt;&lt;/figure&gt;&lt;p&gt;And this gives us a very important systems principle:&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;You cannot remove waiting from a system. You can only decide where the waiting happens and how much capacity you give each stage.&lt;/em&gt;&lt;/strong&gt;&lt;/blockquote&gt;&lt;p&gt;A pool doesn’t eliminate waiting.&lt;/p&gt;&lt;p&gt;It &lt;strong&gt;controls where concurrency is bounded&lt;/strong&gt;.&lt;/p&gt;&lt;h3&gt;Why latency suddenly becomes complicated&lt;/h3&gt;&lt;p&gt;Earlier we had:&lt;/p&gt;&lt;p&gt;Tₜₒₜₐₗ = Tᵥₐᵢₜ + Tqᵤₑᵣᵧ&lt;/p&gt;&lt;p&gt;But now there can be multiple waiting points.&lt;/p&gt;&lt;p&gt;A simplified request path might look like:&lt;/p&gt;&lt;pre&gt;Request&lt;br&gt;   │&lt;br&gt;   ├── Application pool wait&lt;br&gt;   │&lt;br&gt;   ├── Network time&lt;br&gt;   │&lt;br&gt;   ├── PgBouncer wait&lt;br&gt;   │&lt;br&gt;   ├── PostgreSQL execution&lt;br&gt;   │       │&lt;br&gt;   │       ├── CPU&lt;br&gt;   │       ├── memory/cache&lt;br&gt;   │       ├── I/O&lt;br&gt;   │       └── locks&lt;br&gt;   │&lt;br&gt;   └── Network response&lt;/pre&gt;&lt;p&gt;So a slow API response doesn’t automatically mean:&lt;/p&gt;&lt;blockquote&gt;&lt;em&gt;“The SQL query is slow.”&lt;/em&gt;&lt;/blockquote&gt;&lt;p&gt;The query itself might take 10 ms while the request spent another 90 ms waiting for a connection.&lt;/p&gt;&lt;p&gt;Or PostgreSQL might spend most of the time waiting on I/O.&lt;/p&gt;&lt;p&gt;Or the application might have a saturated pool.&lt;/p&gt;&lt;p&gt;Or the pooler might be waiting for a server connection.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Latency is the total journey, not just the query execution time.&lt;/strong&gt;&lt;/p&gt;&lt;h3&gt;And now the math gets interesting&lt;/h3&gt;&lt;p&gt;Imagine:&lt;/p&gt;&lt;pre&gt;Application pool:&lt;br&gt;10 connections&lt;br&gt;&lt;br&gt;PgBouncer:&lt;br&gt;20 server connections&lt;br&gt;Average DB execution:&lt;br&gt;20 ms&lt;br&gt;Incoming DB work:&lt;br&gt;500 operations/sec&lt;/pre&gt;&lt;p&gt;From Little’s Law:&lt;/p&gt;&lt;p&gt;&lt;strong&gt;L = λW&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;&lt;strong&gt;L = 500 × 0.020&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;&lt;strong&gt;L = 10&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;Our simplified model says around &lt;strong&gt;10 concurrent database operations&lt;/strong&gt; are needed to sustain 500 operations/sec at 20 ms average service time.&lt;/p&gt;&lt;p&gt;Now imagine the query becomes &lt;strong&gt;100 ms&lt;/strong&gt;:&lt;/p&gt;&lt;p&gt;&lt;strong&gt;L = 500 × 0.100&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;&lt;strong&gt;L = 50&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;Now we’d need around &lt;strong&gt;50 concurrent operations&lt;/strong&gt; to sustain the same throughput under the same simplified assumptions.&lt;/p&gt;&lt;p&gt;But our application pool has only &lt;strong&gt;10 connections&lt;/strong&gt;.&lt;/p&gt;&lt;p&gt;So requests start waiting.&lt;/p&gt;&lt;pre&gt;Required concurrency ≈ 50&lt;br&gt;&lt;br&gt;Application pool&lt;br&gt;┌──────────────────────┐&lt;br&gt;│ C1 C2 C3 ... C10     │&lt;br&gt;└──────────────────────┘&lt;br&gt;          │&lt;br&gt;          ▼&lt;br&gt;       BUSY&lt;br&gt;Remaining work&lt;br&gt;     ↓&lt;br&gt;   WAITING&lt;/pre&gt;&lt;p&gt;This is where our earlier discussion comes together.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Pool size, query latency, concurrency, throughput, and queueing are connected.&lt;/strong&gt;&lt;/p&gt;&lt;h3&gt;But what if we simply increase the pool to 50?&lt;/h3&gt;&lt;p&gt;That’s the trap.&lt;/p&gt;&lt;p&gt;Maybe it helps.&lt;/p&gt;&lt;p&gt;Maybe it doesn’t.&lt;/p&gt;&lt;p&gt;If PostgreSQL is already CPU-bound, adding more connections may simply create more contention.&lt;/p&gt;&lt;p&gt;If it’s waiting on I/O, more concurrency might help up to a point.&lt;/p&gt;&lt;p&gt;If queries are blocked on locks, more connections won’t magically remove the lock.&lt;/p&gt;&lt;p&gt;If the database is already saturated, a larger pool can make the queue move &lt;strong&gt;inside PostgreSQL instead of inside the application&lt;/strong&gt;.&lt;/p&gt;&lt;p&gt;And that’s an extremely important idea:&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;Moving the queue doesn’t necessarily make the system faster.&lt;/em&gt;&lt;/strong&gt;&lt;/blockquote&gt;&lt;p&gt;Sometimes it’s better to make the bottleneck explicit and controlled.&lt;/p&gt;&lt;h3&gt;So where is the “sweet spot”?&lt;/h3&gt;&lt;p&gt;We’re finally ready for the real engineering question.&lt;/p&gt;&lt;p&gt;Not:&lt;/p&gt;&lt;blockquote&gt;&lt;em&gt;“What’s the recommended pool size?”&lt;/em&gt;&lt;/blockquote&gt;&lt;p&gt;But:&lt;/p&gt;&lt;blockquote&gt;&lt;strong&gt;&lt;em&gt;How much concurrent database work can this particular workload and database handle before additional concurrency stops helping?&lt;/em&gt;&lt;/strong&gt;&lt;/blockquote&gt;&lt;p&gt;To answer that, we need to look at the database’s actual bottlenecks — &lt;strong&gt;CPU, memory, I/O, locks, query time, and throughput&lt;/strong&gt; — and then connect those measurements back to the pool.&lt;/p&gt;&lt;p&gt;That’s where connection-pool tuning stops being configuration guessing and becomes performance engineering.&lt;/p&gt;&lt;img src=&quot;https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=5aef7b543f51&quot; width=&quot;1&quot; height=&quot;1&quot; alt=&quot;&quot;&gt;&lt;hr&gt;&lt;p&gt;&lt;a href=&quot;https://levelup.gitconnected.com/connection-pooling-what-actually-happens-between-your-app-and-database-5aef7b543f51&quot;&gt;Connection Pooling: What Actually Happens Between Your App and Database&lt;/a&gt; was originally published in &lt;a href=&quot;https://levelup.gitconnected.com&quot;&gt;Level Up Coding&lt;/a&gt; on Medium, where people are continuing the conversation by highlighting and responding to this story.&lt;/p&gt;</content:encoded>
    </item>
    <item>
      <title>How Scikit-learn Pipelines Prevent Data Leakage</title>
      <link>https://levelup.gitconnected.com/how-scikit-learn-pipelines-prevent-data-leakage-e18e8efe1217?source=rss----5517fd7b58a6---4</link>
      <guid>https://levelup.gitconnected.com/how-scikit-learn-pipelines-prevent-data-leakage-e18e8efe1217?source=rss----5517fd7b58a6---4</guid>
      <pubDate>Fri, 04 Sep 2026 15:02:31 GMT</pubDate>
      <content:encoded>&lt;div class=&quot;medium-feed-item&quot;&gt;&lt;p class=&quot;medium-feed-image&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/how-scikit-learn-pipelines-prevent-data-leakage-e18e8efe1217?source=rss----5517fd7b58a6---4&quot;&gt;&lt;img src=&quot;https://cdn-images-1.medium.com/max/2400/1*Ir2CyB3vMRKk5vZu48j6oA.png&quot; width=&quot;2400&quot;&gt;&lt;/a&gt;&lt;/p&gt;&lt;p class=&quot;medium-feed-snippet&quot;&gt;I got 81% accuracy from completely random data. Here&amp;#x2019;s why.&lt;/p&gt;&lt;p class=&quot;medium-feed-link&quot;&gt;&lt;a href=&quot;https://levelup.gitconnected.com/how-scikit-learn-pipelines-prevent-data-leakage-e18e8efe1217?source=rss----5517fd7b58a6---4&quot;&gt;Continue reading on Level Up Coding »&lt;/a&gt;&lt;/p&gt;&lt;/div&gt;</content:encoded>
    </item>
  </channel>
</rss>